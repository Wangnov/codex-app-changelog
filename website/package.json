{
  "name": "codex-app-changelog-site",
  "type": "module",
  "private": true,
  "scripts": {
    "sync": "node scripts/sync-releases.mjs",
    "dev": "npm run sync && astro dev",
    "build": "npm run sync && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.11",
    "astro": "^5.6.0"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}

---
title: "Codex 桌面版 26.417.41555"
version: "26.417.41555"
build: 1858
previous_version: "26.417.40842"
previous_build: 1851
platform: "macOS arm64"
released: "2026-04-21"
compared_from: "2026-04-20"
method: "Sparkle 增量包重建 + 结构化 bundle 差异"
summary: "这是一版极小幅维护更新：版本与 build 更新，app.asar 内容哈希变化，但文件树、依赖与可见能力面没有新增证据。"
official_release_notes: false
---

# Codex 桌面版 `26.417.41555` build 1858

> 第三方变更日志 · 非官方 · 基于官方更新包逆向差异

本版从 `26.417.40842` build 1851 更新到 `26.417.41555` build 1858，产物层面呈现为极小幅维护更新。  
One-liner: a very small maintenance build with no confirmed new user-facing capability in the provided artifact facts.

## 关于这份报告

这不是官方变更日志，而是一份**基于官方分发产物的逆向差异报告**。本次事实包说明该版本为历史回填：官方 appcast 未对这一对版本提供增量包，因此改为下载两个官方全量包直接比较。

对比范围为 macOS arm64：上一版 `26.417.40842` build 1851，发布于 `2026-04-20`；最新版 `26.417.41555` build 1858，发布于 `2026-04-21`。

证据等级分两档：

- **【实证】**：来自 Info.plist、依赖清单、文件树统计、签名与公证验证等人类可读直接证据。
- **【信号】**：仅来自前端模块名、二进制体积变化等间接线索，指向某方向，但无法从产物还原到源码级意图。

本事实包没有提供内置插件清单变化、新增内置插件明细、新增可读字符串、同名前端模块改动线索或关键文件的具体改动内容；因此本文不推断新增功能。

## ✨ 重点变化

### 1. 版本号与 build 更新 【实证】

这是一次从 `26.417.40842` 到 `26.417.41555` 的版本更新，build 从 `1851` 提升到 `1858`。

可感知点主要是应用版本元数据变化；事实包没有给出对应的官方 release notes，也没有提供可确认的新功能文案或设置项。

证据：

```diff
CFBundleShortVersionString: 26.417.40842 → 26.417.41555
CFBundleVersion: 1851 → 1858
```

### 2. app.asar 内容发生变化，但体积几乎不变 【实证】

`Contents/Resources/app.asar` 的完整性哈希发生变化，说明应用层打包内容确有更新；但文件级统计显示 bundle 文件数保持 `497 → 497`，新增 `0`、移除 `0`、内容改动 `43`，主要组件体积变化为 `+0.00 MB`。

这意味着本版更像小范围维护或重新打包更新，而不是一次可从产物结构直接看出的能力扩张。app.asar 内部的 `webview/assets` hash 文件增减属于构建产物重命名噪音，不能据此判断新增或删除功能。

证据：

```diff
ElectronAsarIntegrity/Resources/app.asar/hash:
503840ccd1090c7859f7d666c2de706170823dcd8b783714522357b1a58588d3
→ eda8b73ad668dd304d3a68504c3c2882429418e6f9af71c4191bcffaf18bdbc6
```

### 3. cua_node 依赖没有新增包 【实证】

事实包显示 `cua_node` 新增依赖为 `0` 个包。

因此，至少从这组依赖清单看，本版没有证据显示新增了 Node 原生能力或新的底层运行时依赖。

证据：

```text
cua_node 新增 0 个包
```

## 📊 原始数据

### Bundle 文件级统计

| 指标 | 上一版 | 最新版 | 变化 |
| --- | ---: | ---: | ---: |
| Bundle 文件数 | 497 | 497 | 0 |
| 新增文件 | 0 | 0 | 0 |
| 移除文件 | 0 | 0 | 0 |
| 内容改动文件 |  |  | 43 |

### 关键组件体积变化

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 111.2 MB | 111.2 MB | +0.00 MB |

### 新增依赖清单

| 范围 | 新增依赖数量 | 明细 |
| --- | ---: | --- |
| `cua_node` | 0 | 无 |

## 🔁 可复现信息

| 制品 | URL | SHA-256 | 大小 | EdDSA 验签结论 | codesign/spctl 公证结论 |
| --- | --- | --- | --- | --- | --- |
| 上一版 `26.417.40842` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.417.40842.zip` | `93afc61a2fe4099a113d5c4351439231e43f6598a6d9f4942c611c44461c7629` | 事实包未提供 | 事实包未提供 | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| 最新版 `26.417.41555` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.417.41555.zip` | `5cd157c770584f743bd2855e3e4ebf5558b82a4d4130ea4928b701aa1d4513a5` | 事实包未提供 | 事实包未提供 | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ 边界与声明

本文与 OpenAI 无关，是非官方第三方逆向差异报告。

本文不重分发任何官方二进制，只发布基于官方签名分发产物的差异结论。

本文没有把 `webview/assets`、`.vite/build` 下带 hash 的文件名增减解读为功能变化；这些属于构建产物重命名噪音。

【信号】条目只表示产物线索指向某方向，不构成源码级事实。本文当前版本没有列出【信号】级功能推断。

欢迎基于同一事实包或更完整的官方材料指正。


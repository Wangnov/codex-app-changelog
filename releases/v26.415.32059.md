---
title: "Codex 桌面版 26.415.32059"
version: "26.415.32059"
build: 1789
previous_version: "26.415.30602"
previous_build: 1773
platform: "macOS arm64"
released: "2026-04-18"
compared_from: "2026-04-17"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "本版主要是版本与打包内容的小幅更新，未见新增依赖、文件树扩张或可明确归纳的新能力。"
official_release_notes: false
---

# Codex 桌面版 `26.415.32059`(build 1789)

> 第三方变更日志 · 非官方 · 基于官方更新包逆向差异

这是一个非常克制的小版本：可确认的变化集中在版本号、build(构建编号)与 `app.asar`(Electron 应用资源包)内容哈希更新，未观察到新增依赖或新增 bundle(应用包)文件。  
English one-liner: a small packaging/version update with no clearly evidenced new user-facing capability.

## 关于这份报告

这不是官方变更日志，而是一份**基于官方分发产物的逆向差异报告**。本次为历史回填：事实包说明两个版本均超出官方 Sparkle(桌面应用更新框架)增量窗口，因此不是通过增量包重建，而是下载两个官方全量包后直接做结构化 diff(差异比较)。

可信度方面，两个对比对象均通过 Apple(苹果)公证与 OpenAI Developer ID(开发者身份签名)验证：

- 上一版 `26.415.30602`：`accepted`，来源为 `Notarized Developer ID`
- 最新版 `26.415.32059`：`accepted`，来源为 `Notarized Developer ID`
- 签名来源均为 `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`

证据等级分两档：

- **【实证】**：来自人类可读的直接证据，例如 `Info.plist`(应用元信息文件)键值、依赖清单、签名与公证结果。
- **【信号】**：来自间接证据，例如前端模块名或二进制体积变化；这类证据只能说明“指向某方向，但无法从产物还原到源码级意图”。

本报告对比范围：`26.415.30602` build `1773`，发布于 `2026-04-17`；到 `26.415.32059` build `1789`，发布于 `2026-04-18`。

## ✨ 重点变化

### 1. 版本从 `26.415.30602` 更新到 `26.415.32059` 【实证】

这是本次最明确的变化：应用的短版本号与 build 均发生更新。

意味着用户看到的桌面版版本信息会从 `26.415.30602` 变为 `26.415.32059`，构建号从 `1773` 变为 `1789`。

关键证据：

```diff
/CFBundleShortVersionString: '26.415.30602' → '26.415.32059'
/CFBundleVersion: '1773' → '1789'
```

### 2. `app.asar` 内容发生变化，但体积几乎不变 【实证】

`Contents/Resources/app.asar` 的完整性哈希发生变化，说明应用层资源包内容确实被重新打包或修改；但事实包中记录的体积仍为 `111.5 MB → 111.5 MB`，变化为 `+0.00 MB`。

可感知层面看，这不像一次加入大型功能模块或资源包的更新，更接近小规模内容调整、构建输出变化或内部资源更新。这里不能进一步断言具体功能变化。

关键证据：

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
4060f6ecc7fa407ee3522af3024e504701401f6745cf5df1b523b88f0d644f97
→
7aa7db298d9b72c91594737c441e2273f6b057b2f9d15d30a6d2f6121434dd3c
```

### 3. 没有新增 bundle 文件，也没有新增 `cua_node` 依赖 【实证】

外层 bundle 文件数保持 `498 → 498`，新增文件 `0`，移除文件 `0`，内容改动 `40`。`cua_node` 依赖新增数量为 `0`。

这意味着从产物层面看，本版没有引入新的外层组件，也没有新增可归纳为“新能力依赖群”的本地依赖。事实包没有提供新的 PDF、OCR(图像文字识别)、图像处理或浏览器自动化等依赖变化，因此不能写成新增能力。

关键证据：

```text
Bundle 文件数: 498 → 498
新增 0 / 移除 0 / 内容改动 40
cua_node 新增 0 个包
```

### 4. `app.asar` 内部出现文件增减，但主要属于构建哈希噪音 【实证】

应用层内部文件数保持 `1000 → 1000`，机器差异显示新增 `66`、移除 `66`、改动 `6`。但事实包明确标注：`webview/assets` 与 `.vite/build` 下带 hash(内容指纹)的文件名增减是 Vite(前端构建工具)构建产物重命名噪音，不能据此判断新增或删除功能。

因此，这部分只说明前端构建产物发生了重新生成，不能单独写成用户可感知的新功能。

关键证据：

```text
内部文件: 1000 → 1000 (新增 66 / 移除 66 / 改动 6)
注:webview/assets 下大量增减是 Vite 构建 hash 重命名,属噪音,不要据此判断新功能。
```

## 📊 原始数据

### Bundle 文件级统计

| 指标 | 上一版 | 最新版 | 变化 |
| --- | ---: | ---: | ---: |
| 文件数 | 498 | 498 | 0 |
| 新增文件 | 0 | 0 | 0 |
| 移除文件 | 0 | 0 | 0 |
| 内容改动 | - | - | 40 |

### 关键组件体积变化

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 111.5 MB | 111.5 MB | +0.00 MB |

### 新增依赖清单

| 范围 | 新增数量 | 说明 |
| --- | ---: | --- |
| `cua_node` | 0 | 未新增依赖包 |

## 🔁 可复现信息

| 制品 | URL | SHA-256 | 验证结论 |
| --- | --- | --- | --- |
| 上一版全量包 `26.415.30602` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.415.30602.zip` | `fc9a2b142c67b44edcaf54fad15cf66d1fbfa8baa30527b0e64862baf3747a64` | `spctl`(macOS 安全评估工具) 结果为 `accepted`；来源 `Notarized Developer ID` |
| 最新版全量包 `26.415.32059` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.415.32059.zip` | `8d2cdf5a922f0820acc1903c2b4006c1cb2ffdeed0d53bdf0607ec4b3183bd97` | `spctl` 结果为 `accepted`；来源 `Notarized Developer ID` |

| 项目 | 结论 |
| --- | --- |
| EdDSA(爱德华曲线数字签名算法) 增量包验签 | 不适用；事实包说明本次为历史回填，两个版本均超出官方 Sparkle 增量窗口，改为下载两个官方全量包直接比较 |
| `codesign`(代码签名验证工具) / `spctl` 公证结论 | 两个版本均为 `accepted` |
| 签名来源 | `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ 边界与声明

这份报告与 OpenAI 无关，不是官方 release notes(发布说明)。

本文不重分发任何官方二进制，只发布基于差异事实包整理出的结论。

所有【信号】条目都不构成源码级事实；本版事实包中也没有足够的【信号】级材料可归纳为新增用户功能。

如有遗漏或事实包解读错误，欢迎基于可复现证据指正。
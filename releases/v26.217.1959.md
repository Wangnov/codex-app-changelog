---
title: "Codex 桌面版 26.217.1959"
version: "26.217.1959"
build: 669
previous_version: "26.212.1823"
previous_build: 661
platform: "macOS arm64"
released: "2026-02-18"
compared_from: "2026-02-13"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "本版主要是版本构建更新，bundle 层面文件总数不变，仅有少量内容改动；可读证据中未出现新的用户功能开关或依赖能力扩展。"
official_release_notes: false
---

# Codex 桌面版 `26.217.1959`(build 669)

> 第三方变更日志 · 非官方 · 基于官方更新包逆向差异

这一版从可读证据看，更接近一次小幅构建更新：主程序二进制略增、应用包略减，但没有可确认的新功能入口。  
English one-liner: a small build-level update with no confirmed user-facing feature additions in the readable diff.

## 关于这份报告

这不是官方变更日志，而是一份**基于官方分发产物的逆向差异报告**。本次是历史回填：两个版本均超出官方 Sparkle(应用自动更新框架)增量窗口，因此事实包采用两个官方全量包直接比较，而不是增量包重建。

对比范围是 `26.212.1823` build `661`，发布于 `2026-02-13`，到 `26.217.1959` build `669`，发布于 `2026-02-18`。

可信度方面，事实包给出了两个官方全量包的 SHA-256(文件指纹)以及公证验证结果。上一版和最新版均显示 `CSSMERR_TP_CERT_REVOKED`。事实包说明：这类早期版本的签名证书后来可能被吊销，会影响系统安全安装判断，但不影响对 bundle(应用包)内容进行差异分析。

本文使用两档证据等级：

- **【实证】**：来自人类可读的直接证据，例如 `Info.plist`、`package.json`、签名或验证结果。
- **【信号】**：来自间接证据，例如前端模块名或二进制体积变化；这类信息只指向某方向，但无法从产物还原到源码级意图。

## ✨ 重点变化

### 1. 版本与构建号推进到 `26.217.1959` build `669` 【实证】

这是最明确的变化：应用短版本号从 `26.212.1823` 更新到 `26.217.1959`，构建号从 `661` 更新到 `669`。

这意味着这是一个正式打包出的新版 macOS arm64 分发产物，而不是单纯的资源重签或文件时间戳变化。

关键证据：

```diff
-  "version": "26.212.1823",
+  "version": "26.217.1959",
```

```diff
-  "codexBuildNumber": "661",
+  "codexBuildNumber": "669",
```

`Info.plist` 同步记录：

```text
CFBundleShortVersionString: 26.212.1823 → 26.217.1959
CFBundleVersion: 661 → 669
```

### 2. 应用包文件总数不变，只有少量内容改动 【实证】

bundle 文件数保持 `410 → 410`，没有新增文件，也没有移除文件，内容改动为 `42` 个文件。

这说明从应用包外层看，本版没有引入新的独立组件、插件或大块新增资源；变化主要集中在已有文件内容更新。

关键证据：

```text
文件数: 410 → 410
新增 0 / 移除 0 / 内容改动 42
```

### 3. 主程序体积增加，前端应用包体积下降 【信号】

`Contents/Resources/codex` 从 `72.1 MB` 增至 `73.7 MB`，增加 `1.67 MB`；`Contents/Resources/app.asar` 从 `35.2 MB` 降至 `34.3 MB`，减少 `0.89 MB`。

这指向主程序或打包内容发生了重新组织，但无法从产物还原到源码级意图，也不能据此确认具体功能增加或删除。

关键证据：

| 组件 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/codex` | 72.1 MB | 73.7 MB | +1.67 MB |
| `Contents/Resources/app.asar` | 35.2 MB | 34.3 MB | -0.89 MB |

### 4. 前端内部文件增减明显，但主要受构建 hash 噪音影响 【信号】

`app.asar` 内部文件从 `701` 变为 `611`，机器统计显示新增 `107`、移除 `197`、改动 `10`。但事实包明确说明，`webview/assets` 下大量增减是 Vite(前端构建工具)构建 hash(内容指纹)重命名，属于噪音。

因此这里不能解读为新增或删除了上百个真实功能，只能说明前端打包产物发生了重新生成。

关键证据：

```text
内部文件: 701 → 611 (新增 107 / 移除 197 / 改动 10)
注:webview/assets 下大量增减是 Vite 构建 hash 重命名,属噪音,不要据此判断新功能。
```

### 5. 开发侧新增缓存 lint 命令 【实证】

`package.json` 新增了 `lint:cached` 命令，使用 ESLint(代码检查工具)缓存，并将缓存位置放在 `node_modules/.cache/eslintcache`。

这属于开发流程改进，可能用于加快本地或构建环境中的代码检查；它不是用户可直接感知的功能变化。

关键证据：

```diff
+    "lint:cached": "eslint src --max-warnings 0 --cache --cache-strategy content --cache-location node_modules/.cache/eslintcache",
```

### 6. 未发现新增 `cua_node` 依赖包 【实证】

事实包显示 `cua_node` 新增依赖为 `0` 个包。

这意味着本次可读依赖清单中，没有证据表明 `cua_node` 侧新增了新的第三方能力包。

关键证据：

```text
新增 0 个包
```

## 📊 原始数据

### Bundle 文件级统计

| 指标 | 数值 |
| --- | ---: |
| 文件数 | 410 → 410 |
| 新增文件 | 0 |
| 移除文件 | 0 |
| 内容改动 | 42 |

### 关键组件体积变化

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/codex` | 72.1 MB | 73.7 MB | +1.67 MB |
| `Contents/Resources/app.asar` | 35.2 MB | 34.3 MB | -0.89 MB |
| `Contents/Resources/THIRD_PARTY_NOTICES.txt` | 1.5 MB | 1.5 MB | +0.00 MB |

### 新增依赖清单

| 范围 | 新增依赖 |
| --- | ---: |
| `cua_node` | 0 个包 |

## 🔁 可复现信息

| 制品 | SHA-256 | 大小 | 验证结论 |
| --- | --- | --- | --- |
| 上一版全量包 `Codex-darwin-arm64-26.212.1823.zip` | `3a5c042a59d0e4c2c2e76dbbcc41ddaf4942ed943b18193dfc8f3c593cbedfbb` | 事实包未提供 | 公证验证显示 `CSSMERR_TP_CERT_REVOKED` |
| 最新版全量包 `Codex-darwin-arm64-26.217.1959.zip` | `d0dc7466cb5f062cea96dd7371751184cf9fd8eaa8bacb0245b13ede917d29d5` | 事实包未提供 | 公证验证显示 `CSSMERR_TP_CERT_REVOKED` |

本次事实包说明为两个官方全量包直接比较，未提供 EdDSA(爱德华曲线数字签名算法)验签结果。

## ⚖️ 边界与声明

本文与 OpenAI(人工智能公司)无关，不是官方 release notes(发布说明)。

本文不重分发任何官方二进制文件，只发布基于官方分发产物得到的差异结论。

所有 **【信号】** 条目都只表示产物层面的方向性迹象，不构成源码级事实，也不等同于官方产品意图。

如有遗漏或误读，欢迎基于同一事实包或可复现证据指正。
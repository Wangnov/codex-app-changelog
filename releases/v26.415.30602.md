---
title: "Codex 桌面版 26.415.30602"
version: "26.415.30602"
build: 1773
previous_version: "26.415.21839"
previous_build: 1763
platform: "macOS arm64"
released: "2026-04-17"
compared_from: "2026-04-17"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "本版主要是一次小幅构建更新：版本号与 app.asar 完整性哈希变化，但未看到新增文件、依赖或可明确归纳的新能力。"
official_release_notes: false
---

# Codex 桌面版 `26.415.30602`(build 1773)

> 第三方变更日志 · 非官方 · 基于官方更新包逆向差异

这次从 `26.415.21839` 到 `26.415.30602` 的变化非常克制：可确认的变化集中在版本号、构建号和应用包内容哈希上，未出现新增依赖或新增外层文件。  
English one-liner: a small build refresh with no extracted evidence of new user-facing capabilities.

## 关于这份报告

这不是官方变更日志，而是一份**基于官方分发产物的逆向差异报告**。本次是历史回填：两个版本均已超出官方 Sparkle(应用自动更新框架)增量窗口，因此没有使用增量包重建，而是下载两个官方全量包后直接做结构化 bundle(应用包)差异。

可信度方面，两个版本的 `Codex.app` 均通过 Apple(苹果)公证验证，来源为 `Notarized Developer ID`，签名主体为 `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`。这说明用于比较的产物来自已签名并公证的官方分发包。

本文使用两档证据等级：

- **【实证】**：来自人类可读的直接证据，例如 `Info.plist` 键值、依赖清单、签名与公证结果。
- **【信号】**：来自间接证据，例如前端模块名或二进制体积变化；这类证据只能说明“指向某方向，但无法从产物还原到源码级意图”。

对比范围：`26.415.21839` build `1763`，发布于 `2026-04-17`；到 `26.415.30602` build `1773`，发布于 `2026-04-17`。

## ✨ 重点变化

### 1. 版本与构建号更新 【实证】

`Info.plist` 中的版本号从 `26.415.21839` 更新到 `26.415.30602`，构建号从 `1763` 更新到 `1773`。

这意味着这是一个正式打包发布的新构建，但事实包中没有出现可直接对应到新功能的配置、依赖或文件级证据。

关键证据：

```diff
/CFBundleShortVersionString: '26.415.21839' → '26.415.30602'
/CFBundleVersion: '1763' → '1773'
```

### 2. 应用主体内容发生变化，但幅度很小 【实证】

`Contents/Resources/app.asar` 的完整性哈希发生变化，说明应用主体内容确实不同；但外层 bundle 文件数保持 `498 → 498`，新增 `0`、移除 `0`，只有 `40` 个文件内容改动。

这更像是一次小范围构建刷新或内部内容更新。由于事实包没有给出可读的功能 diff，不能把它解读为具体功能新增。

关键证据：

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
'21600e64c2c0b63f5f167117a0e65047c5cde8727b776d5a9671f63a742b0798'
→
'4060f6ecc7fa407ee3522af3024e504701401f6745cf5df1b523b88f0d644f97'
```

### 3. 没有新增 cua_node 依赖 【实证】

事实包显示 `cua_node` 依赖新增数量为 `0`。

这意味着本次差异中没有看到通过新增本地依赖来扩展能力的证据，例如新的文档处理、图像处理或自动化能力包。这里只能说明“依赖清单未新增”，不能反推出功能完全没有内部变化。

关键证据：

```text
新增 0 个包
```

### 4. app.asar 内部文件增减主要应视为构建噪音 【信号】

`app.asar` 内部文件数量保持 `1000 → 1000`，出现新增 `65`、移除 `65`、改动 `5`。事实包同时明确说明，`webview/assets` 下大量增减是 Vite(前端构建工具)构建 hash(内容指纹)重命名，属于噪音。

这指向前端构建产物发生了重新打包，但无法从这些 hash 文件名还原到源码级意图，也不能据此写成“新增或删除了功能”。

## 📊 原始数据

### Bundle 文件级统计

| 指标 | 数值 |
| --- | ---: |
| 文件数 | `498 → 498` |
| 新增文件 | `0` |
| 移除文件 | `0` |
| 内容改动 | `40` |

### 关键组件体积变化

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | `111.5 MB` | `111.5 MB` | `+0.00 MB` |

### 新增依赖清单

| 范围 | 新增依赖数量 |
| --- | ---: |
| `cua_node` | `0` |

## 🔁 可复现信息

| 制品 | SHA-256 | 验签 / 公证结论 |
| --- | --- | --- |
| 上一版全量包 `Codex-darwin-arm64-26.415.21839.zip` | `504aad9b52d0144e2efa1cfdfb1017b479ffd2feb1f34c4c348d9e335e4a1e02` | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| 最新版全量包 `Codex-darwin-arm64-26.415.30602.zip` | `fc9a2b142c67b44edcaf54fad15cf66d1fbfa8baa30527b0e64862baf3747a64` | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ 边界与声明

本文与 OpenAI 无关，不代表官方立场，也不是官方 release notes(发布说明)。

本文不重分发任何官方二进制，只发布基于官方签名分发产物得到的差异结论。

所有【信号】条目都只表示产物层面的间接迹象，不构成源码级事实，也不应被解读为官方功能意图。

如有事实包提取错误或解读不当，欢迎基于可复现证据指正。
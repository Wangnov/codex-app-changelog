---
title: "Codex 桌面版 26.519.41501"
version: "26.519.41501"
build: 3044
previous_version: "26.519.31651"
previous_build: 3017
platform: "macOS arm64"
released: "2026-05-23"
compared_from: "2026-05-22"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "本版没有新增依赖或明显文件树扩张，主要变化集中在版本同步、前端出现 computer-use 插件图标信号，以及少量原生支撑组件更新。"
official_release_notes: false
---

# Codex 桌面版 `26.519.41501`(build 3044)

> 第三方变更日志 · 非官方 · 基于官方更新包逆向差异

这是一次很小的 macOS arm64(苹果芯片架构) 桌面版更新：包内文件总数不变，新增依赖为 0，最明确的变化是版本号与内置 Browser(应用内浏览器) / Chrome(谷歌浏览器) 插件元数据同步；另有一个 `computer-use-plugin-icon` 前端模块作为功能方向信号出现。  
English one-liner: a small maintenance-style build with plugin version alignment and an early computer-use UI signal.

## 关于这份报告

这不是官方变更日志，而是一份**基于官方分发产物的逆向差异报告**。本次为历史回填：两个版本均已超出官方 Sparkle(应用自动更新框架) 增量窗口，因此事实包改为下载两个官方全量包后直接比较，并生成结构化 bundle(应用包) 差异。

可信度方面，两个版本的 `Codex.app` 均通过 Apple(苹果) 公证与 OpenAI Developer ID(开发者身份签名) 验证。上一版与最新版的 `spctl` 结果均为 `accepted`，来源均为 `Notarized Developer ID`，签名来源均为 `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`。

本文使用两档证据等级：

- **【实证】**：来自人类可读的直接证据，例如 `Info.plist`、插件 `plugin.json`、可读配置或文档 diff(差异)。
- **【信号】**：来自间接证据，例如前端模块名或二进制体积变化。此类条目只表示“指向某方向”，但无法从产物还原到源码级意图。

对比范围：`26.519.31651` build `3017`，发布于 `2026-05-22`；对比到 `26.519.41501` build `3044`，发布于 `2026-05-23`。

## ✨ 重点变化

### 1. 应用版本推进到 `26.519.41501`，build 从 `3017` 到 `3044` 【实证】

`Info.plist` 中的版本字段发生明确变化：`CFBundleShortVersionString` 从 `26.519.31651` 变为 `26.519.41501`，`CFBundleVersion` 从 `3017` 变为 `3044`。

这意味着这是一次正式签名并公证过的桌面版构建更新，而不是单纯的资源文件替换。对应证据还包括 `ElectronAsarIntegrity` 中 `app.asar` 哈希从 `72b2210d1bd4dc5d59a99f1c677e304418cbf1ae60db818d75d6225a43c2d6c1` 变为 `91bd615ccbed8b9e02a9ecd5c142687d021c6aac67edd9eb5a50f0ba29a9c5c8`。

### 2. 内置 Browser / Chrome 插件元数据同步到新版 【实证】

两个 OpenAI(开放人工智能公司) 内置插件的 `plugin.json` 版本号同步更新：`browser` 与 `chrome` 都从 `26.519.31651` 改为 `26.519.41501`。

这说明这些内置自动化能力随应用版本一起重新打包发布。事实包没有显示它们的描述、能力边界或调用说明发生变化，因此不能据此推断 Browser 或 Chrome 插件新增了具体功能。

关键证据：

```diff
-  "version": "26.519.31651",
+  "version": "26.519.41501",
```

### 3. 前端出现 `computer-use-plugin-icon` 模块 【信号】

去除 `webview/assets` 与 `.vite/build` 下 hash(构建指纹) 文件名噪音后，事实包中唯一可见的前端真实新增模块是 `computer-use-plugin-icon`。

这个模块名指向 computer-use(计算机使用) 插件图标或入口展示方向，但这是【信号】级证据：它只能说明产物中出现了相关前端模块名，无法从产物还原到源码级意图，也不能证明该功能已经对用户开放。

### 4. 原生支撑组件有小幅二进制变化 【信号】

体积变化表显示若干 native(原生) 组件发生小幅变化，包括 `bare-modifier-monitor`、`sparkle.node`、`sky.node`、`remote-control-device-key.node`、`browser-use-peer-authorization.node` 等。

这些变化指向应用底层支撑层有维护痕迹，覆盖更新、设备键、浏览器授权或输入监控等方向；但由于证据仅来自二进制体积变化，无法从产物还原到源码级意图，也不能判断具体行为变化。

## 📊 原始数据

### Bundle 文件级统计

| 指标 | 上一版 | 最新版 | 变化 |
| --- | ---: | ---: | ---: |
| bundle 文件数 | 1335 | 1335 | 0 |
| 新增文件 | 0 | 0 | 0 |
| 移除文件 | 0 | 0 | 0 |
| 内容改动文件 | - | - | 59 |

注：`app.asar` 内部文件统计受 Vite(前端构建工具) hash 重命名影响明显，本文不将其新增 / 移除数量列入表格，以免把构建产物重命名误读为真实功能增减。

### 关键组件体积变化

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar.unpacked/node_modules/better-sqlite3/build/Release/better_sqlite3.node` | 2.0 MB | 1.9 MB | -0.03 MB |
| `Contents/Resources/native/bare-modifier-monitor` | 0.2 MB | 0.2 MB | +0.02 MB |
| `Contents/Resources/native/sparkle.node` | 0.1 MB | 0.2 MB | +0.01 MB |
| `Contents/Resources/native/sky.node` | 0.2 MB | 0.2 MB | +0.01 MB |
| `Contents/Resources/native/remote-control-device-key.node` | 0.1 MB | 0.1 MB | +0.01 MB |
| `Contents/Resources/native/browser-use-peer-authorization.node` | 0.1 MB | 0.1 MB | +0.01 MB |
| `Contents/Resources/native/devicecheck.node` | 0.1 MB | 0.1 MB | +0.00 MB |
| `Contents/Resources/app.asar.unpacked/node_modules/node-pty/build/Release/pty.node` | 0.1 MB | 0.1 MB | +0.00 MB |
| `Contents/Resources/app.asar.unpacked/node_modules/node-pty/build/Release/pty.node.dSYM/Contents/Resources/DWARF/pty.node` | 0.0 MB | 0.0 MB | +0.00 MB |
| `Contents/Resources/app.asar` | 159.0 MB | 159.0 MB | +0.00 MB |
| `Contents/Resources/native/launch-services-helper` | 0.1 MB | 0.1 MB | +0.00 MB |

### 新增依赖清单

`cua_node` 新增依赖：0 个。

## 🔁 可复现信息

| 制品 | URL | SHA-256 | 验证结论 |
| --- | --- | --- | --- |
| 上一版全量包 | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.519.31651.zip` | `80c4e7c3073dbfd1107e323d7de11d657837ddb9831b2d99b010b3372bce9e4a` | `spctl`: accepted；source=`Notarized Developer ID`；origin=`Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| 最新版全量包 | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.519.41501.zip` | `7e46af58373b9acf799296b83e7c3cf19a5255d263b30cb437b495c35e9628bd` | `spctl`: accepted；source=`Notarized Developer ID`；origin=`Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

本次对比为两个官方全量包直接比较；事实包未提供 EdDSA(爱德华兹曲线数字签名算法) 增量包验签记录。信任根为 Apple 公证与 OpenAI Developer ID 代码签名。

## ⚖️ 边界与声明

本文与 OpenAI 无关，不代表官方立场，也不是官方 release notes(发布说明)。

本文不重分发任何官方二进制，只发布基于官方签名分发产物得到的差异结论。

所有【信号】条目都不构成源码级事实，只表示产物层面出现了相应方向的迹象。欢迎基于可复现证据指正。
---
title: "Codex 桌面版 26.506.31421"
version: "26.506.31421"
build: 2620
previous_version: "26.506.31004"
previous_build: 2604
platform: "macOS arm64"
released: "2026-05-09"
compared_from: "2026-05-08"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "这是一次非常克制的小版本更新：主程序二进制略有增长，应用层仅少量文件改动，未看到新增依赖或新增 bundle 文件。"
official_release_notes: false
---

# Codex 桌面版 `26.506.31421`(build 2620)

> 第三方变更日志 · 非官方 · 基于官方更新包逆向差异

这一版从产物层看更像是一次小幅修订：版本号和构建号更新，主程序二进制有轻微增长，但没有出现新增依赖、插件或 bundle(应用包)文件。  
English one-liner: a small maintenance-style build with limited observable surface changes.

## 关于这份报告

这不是官方变更日志，而是一份**基于官方分发产物的逆向差异报告**。本次对比范围是 Codex(编程代理桌面应用) 桌面版 `26.506.31004` build `2604`，发布于 `2026-05-08`，到 `26.506.31421` build `2620`，发布于 `2026-05-09`，平台为 macOS(苹果桌面操作系统) arm64(64 位 ARM 架构)。

需要特别说明：事实包显示，本版属于历史回填，两个版本均已超出 Sparkle(macOS 应用更新框架) 增量窗口，因此本次不是通过增量包重建，而是下载两个官方全量包后直接比较。信任根来自 Apple(苹果公司) 公证，以及 OpenAI(人工智能公司) Developer ID(开发者身份签名) 代码签名。上一版与最新版的 `Codex.app` 均通过 `codesign` / `spctl` 验证，结论为 `accepted`，来源为 `Notarized Developer ID`，签名主体为 `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`。

本文使用两档证据等级：

- **【实证】**：有人类可读的直接证据，例如 `Info.plist` 键值、依赖清单、签名验证结论、文件级统计。
- **【信号】**：只有间接证据，例如前端模块名或二进制体积变化。此类证据只能说明“指向某方向，但无法从产物还原到源码级意图”。

## ✨ 重点变化

### 1. 版本与构建号从 `26.506.31004` / `2604` 更新到 `26.506.31421` / `2620` 【实证】

`Info.plist` 显示，应用短版本号从 `26.506.31004` 更新为 `26.506.31421`，构建号从 `2604` 更新为 `2620`。

这意味着这是一个正式签名并发布的新构建，而不是仅由资源哈希变化造成的本地重打包结果。

证据：

```diff
/CFBundleShortVersionString: '26.506.31004' → '26.506.31421'
/CFBundleVersion: '2604' → '2620'
```

### 2. 主程序二进制略有增长，但没有可直接还原的功能说明 【信号】

`Contents/Resources/codex` 从 `190.8 MB` 增至 `191.5 MB`，增加约 `0.72 MB`。

这指向主程序内部发生了实际内容变化，但仅凭二进制体积变化，无法从产物还原到源码级意图，也不能判断具体新增、修复或调整了哪一项用户功能。

关键证据：

| 组件 | 旧 | 新 | 变化 |
| --- | --- | --- | --- |
| `Contents/Resources/codex` | 190.8 MB | 191.5 MB | +0.72 MB |

### 3. 应用包文件树保持稳定，没有新增或移除 bundle 文件 【实证】

bundle 文件总数保持 `1238 → 1238`，新增 `0`，移除 `0`，内容改动 `54`。

这说明从外层应用包结构看，本版没有引入新的独立组件、插件目录或资源文件。对开发者而言，可感知的变化更可能集中在既有文件内容，而不是应用结构扩张。

### 4. `app.asar` 完整性哈希更新，应用层内容确有变化 【实证】

`Info.plist` 中 `ElectronAsarIntegrity` 记录的 `app.asar` 哈希发生变化，说明 Electron(跨平台桌面应用框架) 应用层归档内容并非完全相同。

不过事实包也明确提示：`webview/assets` 与 `.vite/build` 下带 hash 的文件名变化属于 Vite(前端构建工具) 构建产物重命名噪音，不能据此判断新增或删除了功能。

证据：

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
'f2d68d6d80d5a43a7e9c91653bbc4a30ac7e6e24444a8abc899e1ba49a1f74a5'
→
'd735f12f9b353a7b5d4f190919e8181b723073ae23555a0f45d496913daae917'
```

### 5. 没有新增 `cua_node` 依赖包 【实证】

事实包显示 `cua_node` 新增依赖为 `0` 个包。

这意味着本次差异中没有看到通过新增该层依赖来扩展能力的证据；如果存在行为变化，也更可能来自既有依赖或既有二进制内容的修改，而不是新增依赖群。

## 📊 原始数据

### Bundle 文件级统计

| 指标 | 上一版 | 最新版 | 变化 |
| --- | ---: | ---: | ---: |
| 文件数 | 1238 | 1238 | 0 |
| 新增文件 | 0 | 0 | 0 |
| 移除文件 | 0 | 0 | 0 |
| 内容改动 | - | 54 | 54 |

### 关键组件体积变化

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/codex` | 190.8 MB | 191.5 MB | +0.72 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/browser-use/scripts/browser-client.mjs` | 0.7 MB | 0.7 MB | +0.00 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/chrome/scripts/browser-client.mjs` | 0.7 MB | 0.7 MB | +0.00 MB |

### 新增依赖清单

| 依赖区域 | 新增包数量 | 说明 |
| --- | ---: | --- |
| `cua_node` | 0 | 未发现新增依赖包 |

## 🔁 可复现信息

| 制品 | URL | SHA-256 | 验证结论 |
| --- | --- | --- | --- |
| 上一版全量包 `26.506.31004` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.506.31004.zip` | `9a09529d4937d1f24c78f78c7ef7f4b501c594c55b4beb4fecc076d03524dad5` | `Codex.app: accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| 最新版全量包 `26.506.31421` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.506.31421.zip` | `13eba9c4d5a903a7ee80cad99a14a20ef081417b94fc9d0446a22e11bdd56be9` | `Codex.app: accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ 边界与声明

这份报告与 OpenAI 无关，不是官方 release notes(发布说明)，也不代表官方解释。

本文不重分发任何官方二进制，只发布基于官方签名分发包得到的差异结论。

所有【信号】条目都只表示产物层面的间接指向，不构成源码级事实，也不应被解读为确定的产品意图。

如有遗漏或误判，欢迎基于同一事实包或可复现产物指正。
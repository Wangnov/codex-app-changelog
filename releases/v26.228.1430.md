---
title: "Codex 桌面版 26.228.1430"
version: "26.228.1430"
build: 760
previous_version: "26.227.1448"
previous_build: 747
platform: "macOS arm64"
released: "2026-03-01"
compared_from: "2026-02-28"
method: "Sparkle 增量包重建 + 结构化 bundle 差异"
summary: "本版主要体现为版本与应用载荷更新；未从产物中确认新增依赖、插件或 bundle 文件级功能面。"
official_release_notes: false
---

# Codex 桌面版 `26.228.1430` build 760

> 第三方变更日志 · 非官方 · 基于官方更新包逆向差异

本次从 `26.227.1448` build 747 对比到 `26.228.1430` build 760，产物层面可确认的是版本号、build 号和 `app.asar` 内容哈希发生变化，但未看到新增 bundle 文件或新增 `cua_node` 依赖。  
One-liner: this looks like a payload refresh with no confirmed new packaged capability surface.

## 关于这份报告

这不是官方变更日志，而是一份**基于官方分发产物的逆向差异报告**。本版为历史回填；事实包说明官方 appcast 未对这一对版本提供增量包，因此采用两个官方全量包直接比较。

证据等级分两档：

- **【实证】**：来自 Info.plist、依赖清单、文件树统计、签名/公证验证输出等人类可读或结构化事实。
- **【信号】**：仅来自前端模块名、体积变化等间接线索。本次事实包没有足以形成独立条目的前端新增模块信号。

对比范围为 macOS arm64，上一版 `26.227.1448` build 747，发布日期 `2026-02-28`；最新版 `26.228.1430` build 760，发布日期 `2026-03-01`。

## ✨ 重点变化

### 1. 应用版本与 build 号更新到 `26.228.1430` / `760` 【实证】

这是一次从 `26.227.1448` build 747 到 `26.228.1430` build 760 的桌面端版本推进。

对用户或开发者可感知的直接变化首先体现在系统和应用元数据层面：macOS bundle 的 `CFBundleShortVersionString` 与 `CFBundleVersion` 均已更新。

关键证据：

```diff
/CFBundleShortVersionString: '26.227.1448' → '26.228.1430'
/CFBundleVersion: '747' → '760'
```

### 2. 应用载荷发生内容更新，但没有新增 bundle 文件 【实证】

`Contents/Resources/app.asar` 的完整性哈希发生变化，说明应用载荷内容确实更新；同时 bundle 文件总数保持 `410 → 410`，新增 `0`、移除 `0`、内容改动 `40`。

这意味着本版不是通过新增独立 bundle 组件来扩展功能面。从产物层面可以确认有内容更新，但事实包没有提供足够证据把这些变化归因到某个具体用户功能或开发者工作流。

关键证据：

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
'e21f5cb27005a10d0555306a594ad9c564dc3ef8c6fa07ff14459c11da4fdbfc'
→
'61cac92872880821ffb93e52f32fd2573e9cfe4d602a1b8eb156f453436e74c0'
```

### 3. 未确认新增原生依赖或 packaged 能力面 【实证】

`cua_node` 依赖新增数量为 `0`，bundle 文件树也没有新增文件。事实包中没有新增内置插件、服务入口、技能文档、默认提示、命令、设置、权限、错误路径或用户可见文案的可读证据。

因此，本次不能写成“新增了某项能力”。能确认的是：相对于上一版，本版没有通过新增 `cua_node` 包或新增 bundle 文件来引入新的打包能力。

关键证据：

```text
cua_node 新增 0 个包
Bundle 文件数: 410 → 410
新增 0 / 移除 0 / 内容改动 40
```

## 📊 原始数据

### Bundle 文件级统计

| 项目 | 上一版 | 最新版 | 变化 |
| --- | ---: | ---: | ---: |
| Bundle 文件数 | 410 | 410 | 0 |
| 新增文件 | 0 | 0 | 0 |
| 移除文件 | 0 | 0 | 0 |
| 内容改动文件 | 0 | 40 | +40 |

### 关键组件体积变化

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 36.4 MB | 36.4 MB | +0.00 MB |

### 新增依赖清单

| 类别 | 新增数量 | 明细 |
| --- | ---: | --- |
| `cua_node` | 0 | 无 |

## 🔁 可复现信息

| 项目 | 上一版 | 最新版 |
| --- | --- | --- |
| 下载制品 | `Codex-darwin-arm64-26.227.1448.zip` | `Codex-darwin-arm64-26.228.1430.zip` |
| URL | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.227.1448.zip` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.228.1430.zip` |
| SHA-256 | `e5a86a8bdb4b8bb913a8ad587366d5e68dc41a71d64d0e06c205b3a14252ff3f` | `52e3911c4738d48cd842eeab1b08de6210513e7e4a6823fdb6b860678499d69f` |
| 大小 | 未提供 | 未提供 |
| EdDSA 验签 | 未提供 | 未提供 |
| codesign/spctl 公证结论 | `CSSMERR_TP_CERT_REVOKED` | `CSSMERR_TP_CERT_REVOKED` |

部分早期版本的签名证书后来被吊销，验证显示 `CSSMERR_TP_CERT_REVOKED`。这影响该版本能否被系统安全安装，但不影响对其 bundle 内容做差异分析。

## ⚖️ 边界与声明

这份报告与 OpenAI 无关，属于非官方第三方逆向差异整理。

本文不重分发任何官方二进制，只发布基于官方分发产物提取出的差异结论。

本次没有列出 app.asar 内部新增/移除文件计数，因为事实包明确指出其中大量变化来自 Vite 构建 hash 重命名，不应被解读为真实功能增减。

【信号】条目不构成源码级事实；本报告中未使用独立【信号】条目。欢迎基于可复现证据指正。


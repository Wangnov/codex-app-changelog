---
title: "Codex 桌面版 26.228.1430"
version: "26.228.1430"
build: 760
previous_version: "26.227.1448"
previous_build: 747
platform: "macOS arm64"
released: "2026-03-01"
compared_from: "2026-02-28"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "这是一次小幅版本推进：可见变化集中在版本号、build 号与 app.asar 内容哈希更新，未观察到新增依赖或 bundle 文件级新增组件。"
official_release_notes: false
---

# Codex 桌面版 `26.228.1430`(build 760)

> 第三方变更日志 · 非官方 · 基于官方更新包逆向差异

本版从 `26.227.1448` 推进到 `26.228.1430`，外层 bundle(应用包)文件树保持稳定，主要可确认变化是版本元数据与 `app.asar`(Electron 应用资源归档)内容更新。  
English one-liner: a metadata-and-resource update with no observable new bundle-level components.

## 关于这份报告

这不是官方变更日志，而是一份**基于官方分发产物的逆向差异报告**。本次是历史回填：两个版本均已超出官方 Sparkle(桌面应用更新框架)增量窗口，因此事实包采用两个官方全量包直接比较，而不是通过 BinaryDelta(二进制增量差分)重建。

可信度方面，两个输入制品均来自官方全量包地址，并给出了 SHA-256(文件指纹)。公证验证结果显示两个版本均返回 `CSSMERR_TP_CERT_REVOKED`；事实包说明这是部分早期版本的签名证书后来被吊销导致，影响系统安全安装判断，但不影响对 bundle 内容做差异分析。

本文使用两档证据等级：

- **【实证】**：来自人类可读的直接证据，例如 Info.plist(应用元数据文件)键值、依赖清单、签名或验证结论。
- **【信号】**：来自间接证据，例如前端模块名或二进制体积变化；这类信息只指向某方向，但无法从产物还原到源码级意图。

对比范围：`26.227.1448` build `747`，发布于 `2026-02-28`；到 `26.228.1430` build `760`，发布于 `2026-03-01`。平台为 `macOS arm64`。

## ✨ 重点变化

### 1. 版本元数据推进到 `26.228.1430` 【实证】

这是一次明确的桌面版版本推进：`CFBundleShortVersionString` 从 `26.227.1448` 变为 `26.228.1430`，`CFBundleVersion` 从 `747` 变为 `760`。

可感知点是：系统、更新器或诊断界面中展示的应用版本与 build 号会更新；这是本次差异中最直接、最确定的变化。

关键证据：

```diff
/CFBundleShortVersionString: '26.227.1448' → '26.228.1430'
/CFBundleVersion: '747' → '760'
```

### 2. 外层应用包结构没有新增或移除文件 【实证】

bundle 文件数保持 `410 → 410`，文件级统计为新增 `0`、移除 `0`、内容改动 `40`。

这意味着本版没有观察到新的外层组件、插件或独立可执行文件加入；变化更像是已有内容的替换或更新，而不是新增一个可被文件树直接识别的新功能模块。

关键证据：

```text
文件数: 410 → 410
新增 0 / 移除 0 / 内容改动 40
```

### 3. `app.asar` 内容发生变化，但体积几乎不变 【实证】

`Contents/Resources/app.asar` 的完整性哈希发生变化，说明应用层资源确实更新；但体积仍显示为 `36.4 MB → 36.4 MB`，表格中的变化为 `+0.00 MB`。

可感知点是：应用内部逻辑或前端资源可能有小幅更新；但从事实包不能进一步确认具体功能意图。尤其是 `webview/assets` 和 `.vite/build` 下带 hash 的文件名增减属于构建产物重命名，不应解读为真实功能新增或删除。

关键证据：

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
'e21f5cb27005a10d0555306a594ad9c564dc3ef8c6fa07ff14459c11da4fdbfc'
→
'61cac92872880821ffb93e52f32fd2573e9cfe4d602a1b8eb156f453436e74c0'
```

### 4. 未发现新增 `cua_node` 依赖包 【实证】

事实包显示 `cua_node`(Codex 桌面端相关原生/节点依赖集合)新增依赖为 `0` 个。

这意味着本次差异中没有证据表明应用通过新增 `cua_node` 包来扩展底层能力；如果存在行为变化，更可能来自已有包或应用层资源内容更新。

关键证据：

```text
cua_node 新增 0 个包
```

## 📊 原始数据

### Bundle 文件级统计

| 指标 | 上一版 | 最新版 | 变化 |
| --- | ---: | ---: | ---: |
| bundle 文件数 | 410 | 410 | 0 |
| 新增文件 | - | - | 0 |
| 移除文件 | - | - | 0 |
| 内容改动文件 | - | - | 40 |

### 关键组件体积变化

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 36.4 MB | 36.4 MB | +0.00 MB |

### 新增依赖清单

| 依赖区域 | 新增数量 | 说明 |
| --- | ---: | --- |
| `cua_node` | 0 | 未发现新增包 |

## 🔁 可复现信息

| 项目 | 上一版 `26.227.1448` | 最新版 `26.228.1430` |
| --- | --- | --- |
| 下载制品 | `Codex-darwin-arm64-26.227.1448.zip` | `Codex-darwin-arm64-26.228.1430.zip` |
| SHA-256 | `e5a86a8bdb4b8bb913a8ad587366d5e68dc41a71d64d0e06c205b3a14252ff3f` | `52e3911c4738d48cd842eeab1b08de6210513e7e4a6823fdb6b860678499d69f` |
| EdDSA(爱德华兹曲线数字签名算法)验签 | 事实包未提供 | 事实包未提供 |
| codesign / spctl(代码签名与 Gatekeeper 公证检查)结论 | `CSSMERR_TP_CERT_REVOKED` | `CSSMERR_TP_CERT_REVOKED` |
| 说明 | 早期签名证书后来被吊销；影响系统安全安装判断，不影响 bundle 差异分析 | 早期签名证书后来被吊销；影响系统安全安装判断，不影响 bundle 差异分析 |

## ⚖️ 边界与声明

本文与 OpenAI(人工智能公司) 无关，不代表官方立场，也不是官方 release notes(发布说明)。

本文不重分发任何官方二进制，只发布基于官方分发产物得到的差异结论。

所有【信号】条目都只表示产物层面的间接指向，不构成源码级事实或产品意图判断。本次事实包没有提供可用的前端真实新增模块，因此本文没有据此扩展功能推断。

欢迎基于同一制品与同一方法复核、指正。
---
title: "Codex 桌面版 26.616.31447"
version: "26.616.31447"
build: 4133
mac_version: "26.616.31447"
mac_build: 4133
win_version: "26.616.3309.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-18"
method: "官方包逆向 diff(macOS Sparkle / Windows MSIX)"
summary: "macOS 从 26.616.30709 升到 26.616.31447，仅确认到版本号与 asar 完整性哈希更新；Windows 26.616.3309.0 在本批次两份包内容完全一致，尚未体现对应变更。"
official_release_notes: false
---

# Codex 桌面版 26.616.31447(macOS / Windows 对照)

> 第三方跨平台变更日志 · 非官方

本批次的可读变化非常克制：macOS 包完成版本推进，Windows 包在本次对照中没有内容差异。  
One-liner: macOS advances to 26.616.31447, while the compared Windows 26.616.3309.0 package is byte-level unchanged in extracted contents.

## 关于这份报告

本报告基于官方签名分发包的逆向 diff。macOS 使用 Sparkle(macOS 常见应用更新框架)渠道的官方全量包对比；Windows 使用 MSIX(Windows 应用安装包格式)包对比。两边都只引用机器提取事实，不引用官方 release notes。

可信度分两档：

- 【实证】：来自 Info.plist、AppxManifest、plugin.json、依赖清单、类型声明、技能文档等可读文件或明确统计。
- 【信号】：仅来自前端模块名、体积或构建产物变化，能提示方向，但不能还原源码意图。

平台归属标记含义：

- `[macOS+Windows]`：两平台事实包都支持同一变化。
- `[仅 macOS]`：只在 macOS 包中观察到。
- `[仅 Windows]`：只在 Windows 包中观察到。

对比范围：

- macOS arm64：`26.616.30709` build `4108` → `26.616.31447` build `4133`。
- Windows x64：`26.616.3309.0` build `26.616.3309.0` → `26.616.3309.0` build `26.616.3309.0`。
- 本批次发布日期字段按输入记录为 `2026-06-18`。

## ✨ 重点变化

### 【实证】[仅 macOS] 应用版本推进到 26.616.31447

macOS 的 `Info.plist` 显示 `CFBundleShortVersionString` 从 `26.616.30709` 更新到 `26.616.31447`，`CFBundleVersion` 从 `4108` 更新到 `4133`。同时，`ElectronAsarIntegrity/Resources/app.asar/hash` 从 `3d86cae28e6657443c87174307b745c843e74f1154694e4d87ede9b121fcf697` 变为 `df68ece60ec92bf946437dbd262d64426af0efb514b9b94d2c738916482f7d50`。

平台差异很明确：Windows 本批次仍是 `26.616.3309.0` → `26.616.3309.0`，文件内容改动为 `0`，没有观察到对应版本推进。

### 【实证】[仅 macOS] 内置 browser / chrome 插件版本同步更新

macOS 包中两个内置插件的 `.codex-plugin/plugin.json` 版本号同步从 `26.616.30709` 更新到 `26.616.31447`：

- `openai-bundled/plugins/chrome/.codex-plugin/plugin.json`
- `openai-bundled/plugins/browser/.codex-plugin/plugin.json`

这说明插件清单层面已跟随 macOS 应用版本更新。Windows 事实包没有报告任何 plugin.json 或应用层文件变化，因此本项仅能归为 macOS 已有、Windows 未观察到。

### 【实证】[仅 macOS] app.asar 有少量内容变化，但可读证据只指向版本/完整性信息

macOS 的 `app.asar`(Electron 应用归档)内部文件数保持 `3115 → 3115`，统计上出现 `新增 463 / 移除 463 / 改动 14`。事实包同时注明 `webview/assets` 下大量增减是 Vite(前端构建工具)构建 hash 重命名噪音，不能据此判断功能增减。

可读 diff 中能确认的具体变化仅是插件版本号与 asar 完整性哈希。没有足够事实证明新增功能、UI 行为或业务逻辑变化。Windows 的 `app.asar` 内部文件为 `3078 → 3078`，`新增 0 / 移除 0 / 改动 0`。

### 【实证】[macOS+Windows] Computer Use 运行时未见依赖增加

Computer Use 运行时 `cua_node` 的总量两平台都没有变化，且新增依赖包数量均为 `0`：

- macOS：`365.9 MB → 365.9 MB (+0 MB, +0%)`，新增 `0` 个包。
- Windows：`285.6 MB → 285.6 MB (+0 MB, +0%)`，新增 `0` 个包。

这意味着本批次没有证据显示 Computer Use 运行时依赖集发生扩张。两平台的总量不同，但各自对比口径下都保持不变。

### 【实证】[仅 Windows] Windows 包在本批次对照中完全无内容变化

Windows 文件树为 `9216 → 9216`，新增 `0`、移除 `0`、内容改动 `0`。上一版与最新版全量包 SHA-256 也相同，均为 `a73f689a23c1d03df934262c193a313f7beec2510965398dbcb442bc95b3525b`。

因此，Windows 在这次跨平台批次里更像是“同一包复用/回填对照”：没有可报告的应用层、运行时或清单变化。

## 📊 原始数据

### macOS arm64

| 项目 | 上一版 | 最新版 | 变化 |
| --- | ---: | ---: | ---: |
| 版本 | `26.616.30709` | `26.616.31447` | 更新 |
| build | `4108` | `4133` | +25 |
| Bundle 文件数 | `8416` | `8416` | 0 |
| Bundle 新增/移除/改动 | - | - | `0 / 0 / 69` |
| app.asar 内部文件 | `3115` | `3115` | 0 |
| app.asar 新增/移除/改动 | - | - | `463 / 463 / 14` |
| cua_node 总量 | `365.9 MB` | `365.9 MB` | `+0 MB, +0%` |
| cua_node 新增依赖包 | - | - | `0` |

关键体积变化：

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | `155.8 MB` | `155.8 MB` | `+0.00 MB` |
| `Contents/PlugIns/CodexDockTilePlugin.plugin/Contents/MacOS/CodexDockTilePlugin` | `0.1 MB` | `0.1 MB` | `-0.00 MB` |
| `Contents/Resources/plugins/openai-bundled/plugins/chrome/extension-host/macos/arm64/extension-host` | `1.3 MB` | `1.3 MB` | `-0.00 MB` |

验证信息：

- 上一版 SHA-256：`7b2c16517bcaa4e5a8acf56e5ae9885497449b864ecd52df3bd9995a93e5ba0b`
- 最新版 SHA-256：`7c24196e7577ff246adfc8b96b2ba788656bfd97c8bd0bdbdce6a9e0b34bba01`
- 两版公证验证均为 `accepted`，来源为 `Notarized Developer ID`，签名主体为 `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`。

### Windows x64

| 项目 | 上一版 | 最新版 | 变化 |
| --- | ---: | ---: | ---: |
| 版本 | `26.616.3309.0` | `26.616.3309.0` | 0 |
| build | `26.616.3309.0` | `26.616.3309.0` | 0 |
| Bundle 文件数 | `9216` | `9216` | 0 |
| Bundle 新增/移除/改动 | - | - | `0 / 0 / 0` |
| app.asar 内部文件 | `3078` | `3078` | 0 |
| app.asar 新增/移除/改动 | - | - | `0 / 0 / 0` |
| cua_node 总量 | `285.6 MB` | `285.6 MB` | `+0 MB, +0%` |
| cua_node 新增依赖包 | - | - | `0` |

关键体积变化：事实包未列出非 0 字节变化组件。

验证信息：

- 上一版 SHA-256：`a73f689a23c1d03df934262c193a313f7beec2510965398dbcb442bc95b3525b`
- 最新版 SHA-256：`a73f689a23c1d03df934262c193a313f7beec2510965398dbcb442bc95b3525b`

## ⚖️ 边界与声明

这是一份第三方非官方跨平台变更日志，不代表 OpenAI 官方说明。

本文只基于官方签名分发包的逆向 diff，不重分发二进制内容。

标为【信号】的内容只表示构建产物层面的方向性线索，不构成源码事实。本批次主要结论均来自【实证】材料。

若后续出现官方 release notes、更多平台包或更完整的可读差异，应以新证据修正本文结论。


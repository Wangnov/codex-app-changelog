---
title: "Codex 桌面版 26.609.71144"
version: "26.609.71144"
build: 3952
mac_version: "26.609.71144"
mac_build: 3952
win_version: "26.609.4994.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-15"
method: "官方包逆向 diff(macOS Sparkle / Windows MSIX)"
summary: "本批次实质变化集中在 macOS：版本/build 与 app.asar 完整性哈希更新，browser/chrome 内置插件版本同步；Windows 对照包未发生文件内容变化。"
official_release_notes: false
---

# Codex 桌面版 26.609.71144(macOS / Windows 对照)

> 第三方跨平台变更日志 · 非官方

本批次显示 macOS 包已前进到 26.609.71144 build 3952，而 Windows x64 对照包仍停留在 26.609.4994.0 且内容未变。  
English one-liner: This batch is a macOS-side packaging/app update, while the Windows comparison package is unchanged.

## 关于这份报告

本报告基于两份官方签名分发包的逆向 diff：macOS 使用 Sparkle 渠道全量包对比，Windows 使用 MSIX 包对比。报告只引用机器提取事实，不引入源码推断或未验证功能描述。

证据等级分为两档：【实证】表示来自 Info.plist、插件 manifest、依赖清单、文件统计等可读或可复现实证；【信号】表示仅来自前端模块名、体积变化等弱线索。本批次可归纳的主要变化均有可读文件或文件级统计支撑，未单独使用【信号】作为功能判断。

平台归属标记含义为：[macOS+Windows] 表示两平台均有同类变化；[仅 macOS] 表示只在 macOS 包中观察到；[仅 Windows] 表示只在 Windows 包中观察到。本次对比范围为 macOS 26.609.41114 build 3888 → 26.609.71144 build 3952，以及 Windows 26.609.4994.0 → 26.609.4994.0，发布日期字段按本批次给定日期 2026-06-15 记录。

## ✨ 重点变化

### 【实证】[仅 macOS] macOS 版本与 build 前进，Windows 对照包未跟进

macOS 的 `CFBundleShortVersionString` 从 `26.609.41114` 更新到 `26.609.71144`，`CFBundleVersion` 从 `3888` 更新到 `3952`。同时，`ElectronAsarIntegrity/Resources/app.asar/hash` 从 `3e01c3b6a7a8d792570153a7cca28b1cc6ab1271324195fb5ea4033e0ca9eef7` 变为 `cbf25d7d293a4a2750d8fec4ee4b458ee3c1b48050673f24f699a8203dd1a50c`，说明 macOS 的应用层归档内容发生了更新。

Windows 这组对照包的版本号前后均为 `26.609.4994.0`，两个全量包 SHA-256 也相同：`547618a744149221078a27febdfff65c924b46ff85ab2fe1595180e128be8d85`。文件树统计为新增 0、移除 0、内容改动 0，因此不能把 macOS 的本批次变化归为 Windows 已同步。

### 【实证】[仅 macOS] browser 与 chrome 内置插件 manifest 版本同步到新 macOS 版本

macOS 包内两个 OpenAI bundled plugin 的 manifest 版本号发生变化：

- `browser` 插件：`26.609.41114` → `26.609.71144`
- `chrome` 插件：`26.609.41114` → `26.609.71144`

这里的 manifest 指插件元数据文件 `.codex-plugin/plugin.json`。差异只显示版本字段更新，描述文本没有可见语义改动。Windows 对照包没有任何内容改动，因此没有对应的插件 manifest 更新证据。

### 【实证】[macOS+Windows] Computer Use 运行时依赖规模稳定，无新增 cua_node 包

两平台的 Computer Use 运行时，也就是事实包中统计的 `cua_node` 依赖集合，规模均保持不变。

macOS 的 `cua_node` 总量为 `365.8 MB → 365.8 MB (+0 MB, +0%)`，新增包数量为 0。Windows 的 `cua_node` 总量为 `285.5 MB → 285.5 MB (+0 MB, +0%)`，新增包数量同样为 0。两平台都没有证据显示本批次引入新的 Computer Use 运行时依赖。

### 【实证】[仅 macOS] app.asar 内部有小规模文件变化，但不足以还原具体功能

macOS 的应用层 `app.asar` 内部文件数保持 `2870 → 2870`，但有 `新增 11 / 移除 11 / 改动 12`。外层 `app.asar` 体积显示为 `150.7 MB → 150.7 MB (+0.00 MB)`。

这些变化说明 macOS 应用层确实被重新构建或更新，但事实包只给出了少量可读 diff，且明确提示 `webview/assets` 下大量增减属于 Vite 构建 hash 重命名噪音。除 browser/chrome 插件 manifest 版本同步外，不能据此断言具体前端功能变化。Windows 的 `app.asar` 内部文件为 `2815 → 2815 (新增 0 / 移除 0 / 改动 0)`，没有同类变化。

## 📊 原始数据

| 项目 | macOS arm64 | Windows x64 |
| --- | --- | --- |
| 对比版本 | `26.609.41114` build `3888` → `26.609.71144` build `3952` | `26.609.4994.0` → `26.609.4994.0` |
| 外层文件数 | `8298 → 8298` | `11972 → 11972` |
| 外层文件变化 | 新增 0 / 移除 0 / 内容改动 61 | 新增 0 / 移除 0 / 内容改动 0 |
| app.asar 内部文件 | `2870 → 2870` | `2815 → 2815` |
| app.asar 内部变化 | 新增 11 / 移除 11 / 改动 12 | 新增 0 / 移除 0 / 改动 0 |
| app.asar 体积 | `150.7 MB → 150.7 MB (+0.00 MB)` | 无体积变化条目 |
| cua_node 总量 | `365.8 MB → 365.8 MB (+0 MB, +0%)` | `285.5 MB → 285.5 MB (+0 MB, +0%)` |
| cua_node 新增依赖 | 0 个包 | 0 个包 |

关键校验信息：

| 项目 | macOS arm64 | Windows x64 |
| --- | --- | --- |
| 上一版 SHA-256 | `6f69d0de7800bc7f48cda0f87810068f109fe6ecf839df8dec0d093cb23ad362` | `547618a744149221078a27febdfff65c924b46ff85ab2fe1595180e128be8d85` |
| 最新版 SHA-256 | `3086eaa17630ea2a20cb35ea6cae31c69488f8fd29d1f08cb2aaa7c41ea38dbb` | `547618a744149221078a27febdfff65c924b46ff85ab2fe1595180e128be8d85` |
| 签名/验证 | 两版均 accepted；source 为 Notarized Developer ID；origin 为 OpenAI OpCo, LLC | 事实包未给出 Windows 签名验证输出 |

## ⚖️ 边界与声明

这是一份第三方、非官方的逆向 diff 变更日志，不代表 OpenAI 官方发布说明。本文不重分发二进制文件，只引用文件级统计、manifest、plist 与哈希等可复现实证。

【信号】类线索不构成源码事实；本批次没有把 hash 重命名、webview/assets 构建产物变动解读为功能增减。若有读者能提供同批次官方说明或更多可复现实证，欢迎指正。


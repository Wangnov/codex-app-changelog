---
title: "Codex 桌面版 26.609.71144"
version: "26.609.71144"
build: 3952
mac_version: "26.609.71144"
mac_build: 3952
win_version: "26.609.4994.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-15"
method: "官方包逆向 diff: macOS Sparkle, Windows MSIX"
summary: "macOS 包进入 26.609.71144 并更新内置 Browser/Chrome 插件版本，同时出现远程配对升级提示与调试面板收缩信号；Windows 输入样本前后完全相同。"
official_release_notes: false
---

# Codex 桌面版 26.609.71144 macOS / Windows 对照

> 第三方跨平台变更日志 · 非官方

本批次的可确认变化集中在 macOS 包：版本号、app.asar 完整性哈希和内置 Browser/Chrome 插件 manifest 跟随升级；Windows 样本在同一输入中没有文件内容变化。

One-liner: macOS advanced to 26.609.71144 with bundled plugin metadata updates and a few UI-facing signals, while the Windows sample is byte-identical across the compared inputs.

## 关于这份报告

这份报告只使用本次事实包中对官方签名分发产物逆向重建后提取的差异事实。macOS 对比范围是 26.609.41114 build 3888 → 26.609.71144 build 3952；Windows 对比范围是 26.609.4994.0 → 26.609.4994.0。

证据等级分两档：【实证】来自 plist、manifest、依赖清单、文件统计、签名/哈希、服务入口、技能文档、类型声明、package.json，以及不属于“同名前端模块改动线索”的可读字符串；【信号】来自同名前端模块的可读字符串、样式类名和体积变化，只能说明包内 UI/交互方向出现变化线索。

平台归属使用 `[macOS+Windows]`、`[仅 macOS]`、`[仅 Windows]`。由于两平台发布时间和版本号可能错位，本报告分别引用各自事实，不把一个平台的结论套到另一个平台。

## ✨ 重点变化

### 【实证】[仅 macOS] macOS 版本与 app.asar 完整性信息更新

macOS 包从 `26.609.41114` 升至 `26.609.71144`，`CFBundleVersion` 从 `3888` 升至 `3952`。`ElectronAsarIntegrity/Resources/app.asar/hash` 也从 `3e01c3b6...ca9eef7` 变为 `cbf25d7...03dd1a50c`，说明应用层 asar 内容确实进入了新构建。

Windows 输入样本的上一版与最新版均为 `26.609.4994.0`，SHA-256 也相同：`547618a744149221078a27febdfff65c924b46ff85ab2fe1595180e128be8d85`。因此只能说：本输入样本在 Windows 平台无内容变化。

### 【实证】[仅 macOS] 内置 Browser 与 Chrome 插件 manifest 版本随包升级

macOS 包内两个内置插件的 `.codex-plugin/plugin.json` 发生版本号更新：

- `browser`: `26.609.41114` → `26.609.71144`
- `chrome`: `26.609.41114` → `26.609.71144`

事实包没有显示这两个插件的描述、权限或服务入口内容变化；可确认的是插件 manifest 版本与 macOS 应用版本同步。Windows 样本没有插件清单变化。

### 【信号】[仅 macOS] 移动端远程配对流程新增“需要升级应用”的提示线索

`codex-mobile-setup-dialog` 同名前端模块发生改动，体积增加 `522 bytes`，新增可读字符串包括：

- `Please upgrade to the latest version of the Codex App to pair`
- `Message shown instead of remote-control pairing controls when the Codex app must be upgraded`
- `codexMobile.setupPage.waiting.upgradeMessage`

这表明包内移动端设置/远程配对相关界面出现了“当前应用版本不足，需要升级后才能配对”的文案与状态线索。由于这些字符串来自同名前端模块改动线索，本报告只将其标为【信号】，不推断所有用户已经可见。

### 【信号】[仅 macOS] Debug Modal 明显收缩，调试/宿主允许相关文案被移除

`debug-modal` 同名前端模块从约 `0.2 MB` 降至约 `0.0 MB`，减少 `180195 bytes`。被移除的可读字符串包括：

- `(command unavailable)`
- `Account ID`
- `Active browser tab`
- `Active conversation`
- `Allow automatic worktree cleanup in this unpackaged build.`
- `Allow host`
- `Allow host starting`

同时移除了一批弹窗、模糊背景、可访问性和状态类名，例如 `aria-modal`、`aria-label`、`backdrop-blur`、`bg-token-background` 等。证据边界是：这能作为调试面板 UI/状态收缩或重组的强信号，但不能直接断言对应功能被删除。

### 【实证】[macOS+Windows] Computer Use 运行时未新增依赖包，体积口径稳定

macOS 的 `cua_node` 总量为 `365.8 MB → 365.8 MB (+0 MB, +0%)`，新增依赖包 `0` 个。Windows 的 `cua_node` 总量为 `285.5 MB → 285.5 MB (+0 MB, +0%)`，新增依赖包同样为 `0` 个。

两平台都没有显示 Computer Use 运行时依赖新增；但 macOS 与 Windows 的运行时总量不同，应分别引用，不应合并成一个跨平台体积数字。

## 🔎 低层信号索引

### 前端模块与 UI 状态线索

- 【信号】[仅 macOS] `codex-mobile-setup-dialog`: 新增升级提示文案和 `max-w-[280px]` 样式类名，指向配对等待页的提示状态变化。
- 【信号】[仅 macOS] `debug-modal`: 大幅缩小并移除多项调试、宿主允许、账户/会话相关字符串，指向调试界面重组或收缩。

这些线索来自同名前端模块的差异集合，只能作为 UI、状态或交互方向信号。

### 插件与打包元数据

- 【实证】[仅 macOS] `Contents/Resources/plugins/openai-bundled/plugins/browser/.codex-plugin/plugin.json`: 插件版本更新到 `26.609.71144`。
- 【实证】[仅 macOS] `Contents/Resources/plugins/openai-bundled/plugins/chrome/.codex-plugin/plugin.json`: 插件版本更新到 `26.609.71144`。

事实包没有列出新增内置插件明细，也没有显示新的插件服务入口、技能说明、默认提示或最大文件变化。

### Windows 样本边界

- 【实证】[仅 Windows] Windows 文件数 `11972 → 11972`，新增 `0`、移除 `0`、内容改动 `0`。
- 【实证】[仅 Windows] app.asar 内部文件 `2815 → 2815`，新增 `0`、移除 `0`、改动 `0`。

因此，本输入样本在 Windows 平台无内容变化；这不等价于断言 Windows 用户没有获得其他批次变化。

## 📊 原始数据

### macOS arm64

- 版本：`26.609.41114` build `3888` → `26.609.71144` build `3952`
- 上一版全量包 SHA-256：`6f69d0de7800bc7f48cda0f87810068f109fe6ecf839df8dec0d093cb23ad362`
- 最新版全量包 SHA-256：`3086eaa17630ea2a20cb35ea6cae31c69488f8fd29d1f08cb2aaa7c41ea38dbb`
- 公证验证：上一版与最新版均为 `accepted`，来源 `Notarized Developer ID`，签名主体 `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`
- Bundle 文件数：`8298 → 8298`
- Bundle 文件变化：新增 `0` / 移除 `0` / 内容改动 `61`
- `app.asar`：`150.7 MB → 150.7 MB (+0.00 MB)`
- app.asar 内部文件：`2870 → 2870`，新增 `11` / 移除 `11` / 改动 `12`
- Computer Use 运行时：`365.8 MB → 365.8 MB (+0 MB, +0%)`
- `cua_node` 新增依赖包：`0`

### Windows x64

- 版本：`26.609.4994.0` build `26.609.4994.0` → `26.609.4994.0` build `26.609.4994.0`
- 上一版全量包 SHA-256：`547618a744149221078a27febdfff65c924b46ff85ab2fe1595180e128be8d85`
- 最新版全量包 SHA-256：`547618a744149221078a27febdfff65c924b46ff85ab2fe1595180e128be8d85`
- Bundle 文件数：`11972 → 11972`
- Bundle 文件变化：新增 `0` / 移除 `0` / 内容改动 `0`
- app.asar 内部文件：`2815 → 2815`，新增 `0` / 移除 `0` / 改动 `0`
- Computer Use 运行时：`285.5 MB → 285.5 MB (+0 MB, +0%)`
- `cua_node` 新增依赖包：`0`

## ⚖️ 边界与声明

这是一份第三方非官方变更日志，不代表 OpenAI 官方发布说明。分析基于官方签名分发包的逆向 diff，不重分发二进制内容。

【实证】表示事实包中存在可核验的文件、manifest、plist、哈希、统计或可读字符串证据；【信号】只表示压缩前端 bundle 中出现模块、字符串、样式或体积方向线索，不构成源码级事实，也不保证所有用户已经可见。

若后续有新的官方说明、更多平台样本或更完整的增量窗口数据，应以新证据修正本报告。


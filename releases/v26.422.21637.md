---
title: "Codex 桌面版 26.422.21637"
version: "26.422.21637"
build: 2056
previous_version: "26.422.21459"
previous_build: 2049
platform: "macOS arm64"
released: "2026-04-24"
compared_from: "2026-04-24"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "本版是一次同日小幅构建更新：版本与 app.asar 完整性哈希更新，前端仅出现极小的 FREE 状态类名线索变化，未见新增依赖或新增 bundle 文件。"
official_release_notes: false
---

# Codex 桌面版 `26.422.21637` build 2056

> 第三方变更日志 · 非官方 · 基于官方更新包逆向差异

这是从 `26.422.21459` build 2049 到 `26.422.21637` build 2056 的 macOS arm64 产物差异报告。  
A same-day maintenance build with very small observable surface change.

## 关于这份报告

这不是官方变更日志，而是一份**基于官方分发产物的逆向差异报告**。这一对历史版本没有可用的 Sparkle 增量包，因此本次改为下载两个官方全量包后直接比较。

对比范围为 macOS arm64：上一版 `26.422.21459` build 2049，最新版 `26.422.21637` build 2056，二者发布日期均为 `2026-04-24`。

证据分为两档：

- **【实证】**：来自 Info.plist、签名与公证验证、bundle 文件树、依赖清单、完整性哈希等可读结构化证据。
- **【信号】**：来自同名前端模块的 hash 产物差异或二进制体积变化，只能指向某方向，但无法从产物还原到源码级意图。

## ✨ 重点变化

### 1. 版本与构建号推进到 `26.422.21637` build 2056 【实证】

这是一次同日发布的小版本构建更新，`CFBundleShortVersionString` 从 `26.422.21459` 变为 `26.422.21637`，`CFBundleVersion` 从 `2049` 变为 `2056`。

对用户和开发者而言，这说明安装包身份和应用内版本标识已更新，但事实包没有给出官方 release notes 或具体功能说明。

证据：

```diff
CFBundleShortVersionString: 26.422.21459 -> 26.422.21637
CFBundleVersion: 2049 -> 2056
```

### 2. app.asar 内容发生变化，但体积几乎不变 【实证】

核心应用包 `Contents/Resources/app.asar` 的完整性哈希发生变化，说明应用层内容确有改动；但 bundle 总文件数仍为 `507 → 507`，新增 `0`、移除 `0`，内容改动 `47`。

可感知层面更像是小范围修订，而不是明显的功能面扩张；事实包也没有显示新增依赖或新增 bundle 文件。

证据：

```diff
ElectronAsarIntegrity/Resources/app.asar/hash:
ad43823456452d17fbb4208b0b9e8299dcaefa90b77dda5f9797384d7036f40a
-> 50aa3dbd3b33c995917fdc534bf978285ad7f01a32204cc044986db63ba02e1c
```

### 3. 前端 `index` 模块出现极小 FREE 状态类名变化 【信号】

同名前端模块 `index` 的构建产物从 `webview/assets/index-n5_rScsP.js` 变为 `webview/assets/index-gATb9Tvd.js`，体积从 `2.9 MB` 到 `2.9 MB`，仅增加 `108 bytes`。可读差异中新增 `mv.FREE:case`，移除 `pv.FREE:case`。

这条线索指向某个 FREE 状态、样式或分支标识的微调，但无法从产物还原到源码级意图，也不能单独判断为功能新增。

证据：

```diff
+ mv.FREE:case
- pv.FREE:case
```

### 4. 未观察到新增 cua_node 依赖 【实证】

`cua_node` 依赖变化中新增包数量为 `0`。这意味着本次没有通过该依赖层引入新的可归纳能力面。

可感知点是：事实包没有支持“新增底层能力包”这一类结论。

证据：

```text
新增 0 个包
```

## 📊 原始数据

### Bundle 文件级统计

| 指标 | 上一版 | 最新版 | 变化 |
| --- | ---: | ---: | ---: |
| Bundle 文件数 | 507 | 507 | 0 |
| 新增文件 | 0 | 0 | 0 |
| 移除文件 | 0 | 0 | 0 |
| 内容改动文件 | 0 | 47 | +47 |

### 关键组件体积变化

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 119.9 MB | 119.9 MB | +0.00 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/browser-use/scripts/browser-client.mjs` | 0.8 MB | 0.8 MB | +0.00 MB |

### 新增依赖清单

| 范围 | 新增数量 | 明细 |
| --- | ---: | --- |
| `cua_node` | 0 | 无 |

## 🔁 可复现信息

| 项目 | 上一版 | 最新版 |
| --- | --- | --- |
| 下载制品 | `Codex-darwin-arm64-26.422.21459.zip` | `Codex-darwin-arm64-26.422.21637.zip` |
| SHA-256 | `5ce4b5b8427b9e0d66d2c3b286fec7b54b25cd7b0e3f29d90f46ca529d9902f7` | `6e733818af68baa0e1c30afbac877a4f18401b094766f1acba0cc10d1e33270c` |
| 大小 | 事实包未提供 | 事实包未提供 |
| EdDSA 验签结论 | 事实包未提供 | 事实包未提供 |
| codesign/spctl 公证结论 | `accepted` / `Notarized Developer ID` / `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` | `accepted` / `Notarized Developer ID` / `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ 边界与声明

本文与 OpenAI 无关，属于非官方第三方逆向差异报告。

本文不重分发任何官方二进制，只发布基于官方签名分发产物提取出的差异结论。

【信号】条目只代表产物层观察到的方向性线索，不构成源码级事实，也不应被解读为官方功能说明。

欢迎基于同一事实包或可复现验证结果指正。


---
title: "Codex 桌面版 26.422.21637"
version: "26.422.21637"
build: 2056
previous_version: "26.422.21459"
previous_build: 2049
platform: "macOS arm64"
released: "2026-04-24"
compared_from: "2026-04-24"
method: "官方全量包直接对比 + 结构化 bundle 差异"
summary: "本版主要是版本号与打包内容的小幅更新，未观察到新增依赖、文件树扩张或可明确归因的新功能。"
official_release_notes: false
---

# Codex 桌面版 `26.422.21637`(build 2056)

> 第三方变更日志 · 非官方 · 基于官方更新包逆向差异

这是一次非常克制的小版本更新：从可见产物看，主要变化集中在版本号、`app.asar`(Electron 应用资源归档)完整性哈希和少量内容改动，没有出现新增依赖或新增外层组件。  
English one-liner: a small packaging-level update with no clear user-facing feature evidence in the extracted facts.

## 关于这份报告

这不是官方变更日志，而是一份**基于官方分发产物的逆向差异报告**。本次属于历史回填：两个版本均已超出官方 Sparkle(应用更新框架)增量窗口，因此没有使用“上一版全量包 + 增量包重建”的路径，而是下载两个官方全量包后直接做结构化 bundle(应用包)差异。

可信度方面，两个 `Codex.app` 均通过 Apple(苹果)公证与 OpenAI Developer ID(开发者身份签名)验证：

- 上一版 `26.422.21459`: `accepted`, `source=Notarized Developer ID`
- 最新版 `26.422.21637`: `accepted`, `source=Notarized Developer ID`
- 签名来源均为 `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`

证据等级分两档：

- **【实证】**：来自 Info.plist(应用元信息文件)、依赖清单、签名 / 公证输出、明确文件统计等人类可读证据。
- **【信号】**：来自前端模块名、二进制体积变化等间接线索，只能指向某方向，但无法从产物还原到源码级意图。

对比范围：`26.422.21459` build `2049`，发布于 `2026-04-24`，到 `26.422.21637` build `2056`，同样发布于 `2026-04-24`。

## ✨ 重点变化

### 1. 版本号与构建号推进 【实证】

应用元信息从 `26.422.21459` / build `2049` 更新到 `26.422.21637` / build `2056`。

这说明这是一次正式重新打包发布的版本，而不是同一构建的重新签名或单纯下载地址变化。最直接证据来自 `Info.plist`：

```diff
CFBundleShortVersionString: 26.422.21459 -> 26.422.21637
CFBundleVersion: 2049 -> 2056
```

### 2. 应用资源归档内容发生少量变化，但没有可明确命名的新功能 【实证】

`Contents/Resources/app.asar` 的完整性哈希发生变化，说明 Electron 应用层内容确实改过；同时 app.asar 内部文件总数保持 `1031 -> 1031`，统计为新增 `18`、移除 `18`、改动 `5`。

需要注意的是，事实包明确标注 `webview/assets` 下大量增减属于 Vite(前端构建工具)构建 hash(内容指纹)重命名噪音，因此不能据此写成新增或删除功能。可确认的是“应用层内容有改动”，但无法从事实包进一步归纳出具体用户功能。

关键证据：

```diff
ElectronAsarIntegrity/Resources/app.asar/hash:
ad43823456452d17fbb4208b0b9e8299dcaefa90b77dda5f9797384d7036f40a
-> 50aa3dbd3b33c995917fdc534bf978285ad7f01a32204cc044986db63ba02e1c
```

### 3. 外层 bundle 文件树保持稳定 【实证】

应用包外层文件数保持 `507 -> 507`，新增 `0`、移除 `0`、内容改动 `47`。

这意味着本版没有新增新的外层插件、框架目录或资源目录；从开发者视角看，更像是对既有内容的替换式更新，而不是一次带来新组件边界的功能扩展。

### 4. 依赖清单没有新增包 【实证】

`cua_node` 依赖变化中，新增包数量为 `0`。

因此，不能把本版解读为新增了某类底层能力。事实包没有给出新增依赖、接口变化或权限放行等证据，无法支撑“新增 PDF、OCR、图像处理、浏览器自动化”等能力判断。

## 📊 原始数据

### Bundle 文件级统计

| 指标 | 上一版 | 最新版 | 变化 |
| --- | ---: | ---: | ---: |
| 外层 bundle 文件数 | 507 | 507 | 0 |
| 新增文件 | 0 | 0 | 0 |
| 移除文件 | 0 | 0 | 0 |
| 内容改动文件 | 47 | 47 | 47 个文件发生内容变化 |

### 关键组件体积变化

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 119.9 MB | 119.9 MB | +0.00 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/browser-use/scripts/browser-client.mjs` | 0.8 MB | 0.8 MB | +0.00 MB |

### 新增依赖清单

| 范围 | 新增依赖数量 | 说明 |
| --- | ---: | --- |
| `cua_node` | 0 | 未观察到新增包 |

## 🔁 可复现信息

| 制品 | URL | SHA-256 | 验证结论 |
| --- | --- | --- | --- |
| 上一版全量包 `26.422.21459` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.422.21459.zip` | `5ce4b5b8427b9e0d66d2c3b286fec7b54b25cd7b0e3f29d90f46ca529d9902f7` | `spctl`: accepted；来源为 Notarized Developer ID |
| 最新版全量包 `26.422.21637` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.422.21637.zip` | `6e733818af68baa0e1c30afbac877a4f18401b094766f1acba0cc10d1e33270c` | `spctl`: accepted；来源为 Notarized Developer ID |

| 验证项 | 结论 |
| --- | --- |
| EdDSA(一种数字签名算法)增量包验签 | 不适用：事实包说明本次两个版本均超出官方 Sparkle 增量窗口，采用两个官方全量包直接比较 |
| Apple 公证 | 两个版本均为 `accepted` |
| Developer ID 签名来源 | `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ 边界与声明

这份报告与 OpenAI 无关，不代表官方说明或官方发布节奏。

本文不重分发任何官方二进制，只基于官方签名分发产物的结构化差异发布结论。

凡标注为【信号】的条目，都不构成源码级事实；本版事实包中没有足够材料支撑新增用户功能的判断。

欢迎基于同一制品、哈希和验证路径复核并指正。
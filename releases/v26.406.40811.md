---
title: "Codex 桌面版 26.406.40811"
version: "26.406.40811"
build: 1457
previous_version: "26.406.31014"
previous_build: 1395
platform: "macOS arm64"
released: "2026-04-11"
compared_from: "2026-04-08"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "本版主要是一次小幅维护更新：版本号与 app.asar 完整性哈希更新，前端主入口仅出现极小规模布局信号变化。"
official_release_notes: false
---

# Codex 桌面版 `26.406.40811` build 1457

> 第三方变更日志 · 非官方 · 基于官方更新包逆向差异

这是一次体积和文件树都高度克制的 macOS arm64 小版本更新，产物层面没有出现新的依赖、插件或明显新增能力面。  
English one-liner: a tightly scoped maintenance build with only minimal frontend layout signals visible from the bundle diff.

## 关于这份报告

这不是官方变更日志，而是一份**基于官方分发产物的逆向差异报告**。本次对比范围是 macOS arm64 的 `26.406.31014` build 1395 到 `26.406.40811` build 1457，发布日期分别为 2026-04-08 与 2026-04-11。

本版属于历史回填：事实包显示官方 appcast 未对这一对版本提供增量包，因此采用两个官方全量包直接比较，并结合结构化 bundle 差异归纳。报告只使用事实包中出现的事实，不把 Vite hash 文件名变化当作功能增删。

证据等级分为两档：

- **【实证】**：来自 Info.plist、签名/公证结果、依赖清单、bundle 文件统计等可读直接证据。
- **【信号】**：来自同名前端模块改动或体积变化，只能说明指向某方向，但无法从产物还原到源码级意图。

## ✨ 重点变化

### 1. 版本与构建号推进到 `26.406.40811` build 1457 【实证】

本版在 `Info.plist` 中将 `CFBundleShortVersionString` 从 `26.406.31014` 更新到 `26.406.40811`，`CFBundleVersion` 从 `1395` 更新到 `1457`。

这意味着这是一个正式打包发布的新 macOS arm64 构建，而不是仅由文件哈希或外部元数据变化造成的重签名结果。

关键证据：

```diff
CFBundleShortVersionString: 26.406.31014 -> 26.406.40811
CFBundleVersion: 1395 -> 1457
```

### 2. 未观察到新增依赖、插件或文件级能力扩张 【实证】

bundle 文件数保持 `416 → 416`，新增文件为 `0`、移除文件为 `0`、内容改动为 `39`。`cua_node` 依赖新增包数量为 `0`。

这说明从产物结构看，本版没有引入新的可见组件或依赖面。事实包也没有给出新增内置插件、服务入口、技能文档、权限或用户可见新命令。

关键证据：

| 项目 | 变化 |
| --- | --- |
| Bundle 文件数 | `416 → 416` |
| 新增 / 移除 / 内容改动 | `0 / 0 / 39` |
| `cua_node` 新增依赖 | `0` |

### 3. `app.asar` 内容哈希变化，但体积几乎不变 【实证】

主应用归档 `Contents/Resources/app.asar` 的标称体积保持 `43.0 MB → 43.0 MB`，变化显示为 `+0.00 MB`，但 `ElectronAsarIntegrity` 中的 hash 已更新。

这意味着应用层内容确实发生了改动，但从体积和文件树看，改动范围很小，不能据此推断出新增功能。

关键证据：

```diff
app.asar: 43.0 MB -> 43.0 MB (+0.00 MB)
ElectronAsarIntegrity hash:
09a16a5f2833f1dcebcd6daa5cc0fd81fd08c0a8780d50cb66352d38e42121aa
-> c6e625133b42bfe77bfb8ae2468e0ce27fa16d2e89c1c48fd57a16be0757e946
```

### 4. 前端主入口出现极小布局线索变化 【信号】

同名前端模块 `index` 从 `webview/assets/index-C6yWjN3W.js` 变为 `webview/assets/index-I0ioI9QD.js`，体积从 `3.6 MB` 到 `3.6 MB`，仅增加 `72 bytes`，并移除了样式/状态类名 `min-w-80`。

这指向某处 UI 最小宽度或布局约束可能被调整，但无法从产物还原到源码级意图，也不能确认具体界面位置或用户影响范围。

关键证据：

```diff
index js: 3.6 MB -> 3.6 MB (+72 bytes)
removed class/state: min-w-80
```

## 📊 原始数据

### Bundle 文件级统计

| 指标 | 上一版 | 最新版 | 变化 |
| --- | ---: | ---: | ---: |
| Bundle 文件数 | 416 | 416 | 0 |
| 新增文件 | 0 | 0 | 0 |
| 移除文件 | 0 | 0 | 0 |
| 内容改动文件 | 39 | 39 | 39 |

### 关键组件体积变化

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 43.0 MB | 43.0 MB | +0.00 MB |

### 新增依赖清单

| 依赖区域 | 新增包数量 | 说明 |
| --- | ---: | --- |
| `cua_node` | 0 | 事实包未显示新增依赖 |

## 🔁 可复现信息

| 项目 | 上一版 `26.406.31014` | 最新版 `26.406.40811` |
| --- | --- | --- |
| 下载制品 | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.406.31014.zip` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.406.40811.zip` |
| SHA-256 | `cd87fba4179ef4c096f5e85a8e4b16452a54b5bce75097b65e139951367f23b8` | `33a66113d6fb9023c1451f448a63ece12255cc2d84bd56106fb02a8835cbea77` |
| 大小 | 事实包未提供 | 事实包未提供 |
| EdDSA 验签结论 | 事实包未提供 | 事实包未提供 |
| codesign/spctl 公证结论 | `CSSMERR_TP_CERT_REVOKED` | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

上一版验证出现 `CSSMERR_TP_CERT_REVOKED`。事实包说明这类早期版本证书吊销会影响系统安全安装判断，但不影响对 bundle 内容做差异分析。

## ⚖️ 边界与声明

本文与 OpenAI 无关，不代表官方立场，也不是官方 release notes。

本文不重分发任何官方二进制，只发布基于官方分发产物得到的差异结论。

所有【信号】条目只表示产物层面的间接线索，不构成源码级事实或功能承诺。欢迎基于可复现证据指正。


---
title: "Codex 桌面版 26.429.61741"
version: "26.429.61741"
build: 2429
previous_version: "26.429.30905"
previous_build: 2345
platform: "macOS arm64"
released: "2026-05-05"
compared_from: "2026-05-01"
method: "Sparkle 增量包重建 + 结构化 bundle 差异"
summary: "本版主要体现为版本与应用层内容更新；bundle 文件结构、cua_node 依赖与分发签名验证均未显示新增能力面。"
official_release_notes: false
---

# Codex 桌面版 `26.429.61741` build 2429

> 第三方变更日志 · 非官方 · 基于官方更新包逆向差异

这是一版非常克制的 macOS arm64 更新：可确认的是版本号、build 与 app.asar 内容哈希发生变化，但事实包未显示新增插件、依赖或 bundle 文件结构变化。  
English one-liner: a narrow desktop update with confirmed version/build changes and app.asar content changes, but no observed new dependency or bundle-level capability surface.

## 关于这份报告

这不是官方变更日志，而是一份**基于官方分发产物的逆向差异报告**。本次为历史回填：官方 appcast 未对 `26.429.30905` 到 `26.429.61741` 提供增量包，因此改为下载两个官方全量包直接比较。

对比范围是 macOS arm64 的 `26.429.30905` build `2345` 与 `26.429.61741` build `2429`。报告只使用事实包中的机器提取事实，不把 Vite hash 文件名变化解读为功能变化。

证据等级分两档：**【实证】**表示有 Info.plist、签名、公证、依赖清单、文件树统计等直接可读证据；**【信号】**表示只有模块名或体积变化等间接证据，指向某方向，但无法从产物还原到源码级意图。本事实包没有可用的同名前端模块改动线索，因此不单独展开 UI 推断。

## ✨ 重点变化

### 1. 应用版本推进到 `26.429.61741` build `2429` 【实证】

`Info.plist` 中的短版本号从 `26.429.30905` 更新为 `26.429.61741`，build 从 `2345` 更新为 `2429`。

这意味着系统和应用自身可见的版本身份已经变化；除此之外，事实包没有提供官方 release notes 或用户可见文案变化。

关键证据：

```diff
CFBundleShortVersionString: 26.429.30905 → 26.429.61741
CFBundleVersion: 2345 → 2429
```

### 2. 应用层内容确有更新，但无法还原出具体功能意图 【实证】

`Contents/Resources/app.asar` 的完整性哈希发生变化，bundle 文件树中共有 `46` 个内容改动。app.asar 内部统计显示文件总数仍为 `1208`，内部新增 `2`、移除 `2`、改动 `6`。

这说明应用层代码或资源确实被重新打包更新；但事实包没有提供技能文档、类型声明、服务入口、设置项、权限、错误路径或用户可见字符串的新增证据，因此不能进一步写成某个确定功能上线。

关键证据：

```diff
ElectronAsarIntegrity/Resources/app.asar/hash:
cb297e87e825012472fbd6fdfc2065f050b666711070f42a3a971428eceb0970
→ bf70509067572e84bb19c3c8f04dcd48d66723935a773c7c8a299045a7eb6993
```

### 3. 未观察到新的 bundle 组件或 cua_node 依赖 【实证】

bundle 文件数保持 `508 → 508`，新增 `0`、移除 `0`、内容改动 `46`；`cua_node` 新增依赖为 `0` 个包。

这意味着从产物层面看，本版没有引入新的顶层应用组件，也没有通过 `cua_node` 依赖清单暴露新的本地能力面。与依赖驱动的新功能相比，本次更像是既有应用层内容的窄幅更新。

关键证据：

```text
Bundle files: 508 → 508
Added: 0 / Removed: 0 / Modified: 46
cua_node added packages: 0
```

### 4. 两个对比版本均通过 Developer ID 公证验证 【实证】

上一版与最新版的 `Codex.app` 均显示 `accepted`，来源为 `Notarized Developer ID`，签名来源为 `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`。

这为本次 bundle 差异分析提供了可复现的分发来源基础；该结论只说明用于比较的两个 app bundle 通过了实际公证验证，不等同于官方发布说明。

关键证据：

```text
source=Notarized Developer ID
origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)
```

## 📊 原始数据

### Bundle 文件级统计

| 指标 | 上一版 | 最新版 | 变化 |
| --- | ---: | ---: | ---: |
| 文件数 | 508 | 508 | 0 |
| 新增文件 | 0 | 0 | 0 |
| 移除文件 | 0 | 0 | 0 |
| 内容改动文件 | 0 | 46 | +46 |

### 关键组件体积变化

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 130.0 MB | 130.0 MB | +0.00 MB |

### 新增依赖清单

| 范围 | 新增依赖 |
| --- | ---: |
| `cua_node` | 0 个包 |

## 🔁 可复现信息

| 项目 | 上一版 | 最新版 |
| --- | --- | --- |
| 版本 | `26.429.30905` | `26.429.61741` |
| Build | `2345` | `2429` |
| 发布日期 | `2026-05-01` | `2026-05-05` |
| 下载制品 | `Codex-darwin-arm64-26.429.30905.zip` | `Codex-darwin-arm64-26.429.61741.zip` |
| SHA-256 | `3a6229d7f6ac5a7a147ea4fe34102874a0ad1a8789347de0ceae797bd863dc97` | `c325741ec38a801889518d62ad756db7d6df1035d755db90a046373c96fb5198` |
| EdDSA 验签结论 | 事实包未提供 | 事实包未提供 |
| codesign/spctl 公证结论 | `accepted`; `Notarized Developer ID`; `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` | `accepted`; `Notarized Developer ID`; `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ 边界与声明

本文与 OpenAI 无关，不代表官方立场，也不是官方 release notes。

本文不重分发任何官方二进制，只发布基于官方签名分发产物得到的差异结论。

【信号】条目不构成源码级事实。本期事实包没有足够的前端模块线索来支持具体 UI 或交互推断。

如有事实包解析错误、复现偏差或证据归类问题，欢迎指正。


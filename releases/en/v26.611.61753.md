
---
title: "Codex Desktop 26.611.61753"
version: "26.611.61753"
build: 4008
mac_version: "26.611.61753"
mac_build: 4008
win_version: "26.611.8273.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-15"
method: "官方包逆向 diff: macOS Sparkle, Windows MSIX"
summary: "This batch mainly aligns bundled Browser and Chrome plugin versions across platforms; Windows also updates Computer Use plugin metadata and the owl shell install timestamp, while macOS updates Info.plist version/build metadata and the app.asar integrity hash."
official_release_notes: false
---

# Codex Desktop 26.611.61753 macOS / Windows Comparison

> Third-party cross-platform changelog · Unofficial

This batch does not show new top-level files or new dependencies; the changes are concentrated in bundled plugin version metadata, platform metadata, and small app.asar content differences.

本批次主要是跨平台内置插件元数据对齐，并包含 Windows 独有的 Computer Use 元数据变化与 macOS 独有的 bundle 元数据变化。

## About This Report

This report is based on machine-extracted diffs from two official signed distribution packages: macOS arm64 compares `26.611.61049 build 3996` → `26.611.61753 build 4008`, while Windows x64 compares `26.611.7849.0` → `26.611.8273.0`. The platform version numbers and release windows are not identical, so each conclusion below is scoped to its own fact package.

Evidence is split into two levels. `[Confirmed]` means the fact comes from plist, manifest, dependency lists, plugin manifests, service entrypoints, signing data, readable config, or explicit file-level diffs in the fact package. `[Signal]` means the fact comes only from module names, size movement, or compressed bundle directionality. This batch does not provide same-name frontend module clues, newly readable strings, CSP diffs, AppxManifest diffs, or newly bundled plugin details, so those areas are described only as boundaries and not expanded into inferred changes.

Platform tags are `[macOS+Windows]`, `[macOS only]`, and `[Windows only]`. webview/assets and .vite/build hash renames are treated as build noise as instructed by the input, not as evidence of feature additions or removals.

## ✨ Highlights

### [Confirmed] [macOS+Windows] Browser Plugin Version Aligned to 26.611.61753

On both platforms, the bundled `browser` plugin manifest shows only a version field change: `26.611.61049` → `26.611.61753`.

The plugin description itself is not shown as changed in the fact package. It still points to the in-app browser, local target opening, clicking, typing, screenshots, and local web targets such as localhost, 127.0.0.1, and file://. Therefore this confirms that the plugin package metadata moved into the new batch, but it does not prove new browser automation capability.

Platform difference: macOS uses `Contents/Resources/plugins/openai-bundled/plugins/browser/.codex-plugin/plugin.json`; Windows uses `resources/plugins/openai-bundled/plugins/browser/.codex-plugin/plugin.json`. The field-level change is the same.

### [Confirmed] [macOS+Windows] Chrome Plugin Version Aligned to 26.611.61753

On both platforms, the bundled `chrome` plugin manifest shows a version field change: `26.611.61049` → `26.611.61753`.

The description in the fact package still covers automation that depends on the user’s existing Chrome state, including tabs, logged-in sessions, cookies, extensions, and Codex Chrome Extension setup. It also says that if purpose-built connectors, APIs, or CLIs fail due to authentication, the user should be asked to reauthenticate or explicitly approve Chrome as a fallback. This confirms a manifest version update, not a confirmed expansion of Chrome capability.

Platform difference: the same change appears on both platforms, with only bundle path prefixes differing.

### [Confirmed] [Windows only] Computer Use Plugin Version Metadata Updated

In the Windows package, `resources/plugins/openai-bundled/plugins/computer-use/.codex-plugin/plugin.json` shows the `computer-use` plugin version changing from `26.611.61049` to `26.611.61753`.

The plugin description is: “Control desktop apps on Windows from Codex through Computer Use.” This confirms that the Windows Computer Use plugin manifest now carries the same plugin version marker as the macOS short version. The fact package does not include corresponding service entrypoint, skill documentation, or default prompt diffs, so it does not support a stronger claim about behavior changes.

Platform difference: the macOS fact package does not show a `computer-use` plugin manifest diff; this readable config change is Windows-only.

### [Confirmed] [Windows only] owl shell Install Timestamp Updated, Without a Shown runtimeInfo Content Change

In the Windows root `.installed-owl-shell.json`, `installedAt` changes from `2026-06-16T20:33:01.480Z` to `2026-06-17T02:33:52.524Z`.

The same diff snippet shows the `cacheKey` still pointing to the same owl archive URL, `runtimeInfo.arch` as `x64`, and `archiveSha` as `09944e70901184dbd560e6349ba46b80b426b60cf6a11ebd8122aab9d0b46737`. So the confirmed change is the install record timestamp. Within the provided facts, this cannot be interpreted as a binary content change in owl shell.

Platform difference: only the Windows fact package includes `.installed-owl-shell.json`.

### [Confirmed] [macOS only] Info.plist Version, Build, and app.asar Integrity Hash Updated

The macOS `Info.plist` shows:

`CFBundleShortVersionString`: `26.611.61049` → `26.611.61753`

`CFBundleVersion`: `3996` → `4008`

`ElectronAsarIntegrity/Resources/app.asar/hash`: `bdc881f00dd574cabfab0e56ca06975555f85ca31ebf8a9415f8a2e3d491d607` → `b1c35ee9911fd72ce5229f4f106275361e39b104bf4a659a0d914b8b5beef33b`

This confirms updated macOS app bundle metadata and an updated app.asar integrity record. The fact package does not provide a Windows AppxManifest change, so no Windows manifest counterpart is claimed.

### [Confirmed] [macOS+Windows] app.asar Structure Is Nearly Stable, With Small Content Changes on Both Platforms

macOS `app.asar` internal files remain `3074 → 3074`, with 7 added, 7 removed, and 11 changed. Windows `app.asar` internal files remain `3024 → 3024`, with 7 added, 7 removed, and 10 changed.

Neither platform has a net file-count change. The input explicitly states that many webview/assets additions and removals are Vite build hash-renaming noise. Therefore this only confirms small application-layer package replacements and edits; it does not directly prove new UI or functionality.

Platform difference: macOS has a 3074-file baseline, Windows has a 3024-file baseline; macOS shows one more changed file than Windows.

### [Confirmed] [macOS+Windows] Computer Use Runtime Size and cua_node Dependency Count Are Unchanged

macOS Computer Use runtime total is `365.9 MB → 365.9 MB (+0 MB, +0%)`; Windows is `285.6 MB → 285.6 MB (+0 MB, +0%)`. Both platforms show 0 newly added `cua_node` packages.

Under the fact package’s measurement method, this batch does not show Computer Use runtime growth and does not add cua_node packages. The main platform difference is baseline size: 365.9 MB on macOS and 285.6 MB on Windows.

## 🔎 Low-Level Signal Index

### Packaging Layer and File Tree

[Confirmed] [macOS+Windows] Total bundle file counts are unchanged on both platforms. macOS is `8267 → 8267`, with 0 added, 0 removed, and 62 content-changed files. Windows is `11943 → 11943`, with 0 added, 0 removed, and 16 content-changed files. Boundary: this is file-level accounting, not proof of user-visible feature change.

### app.asar Size

[Signal] [macOS+Windows] macOS `Contents/Resources/app.asar` is `153.8 MB → 153.8 MB (-0.00 MB)`; Windows `resources/app.asar` is `157.6 MB → 157.6 MB (-0.00 MB)`. Boundary: the size movement is effectively zero, so it is only a weak signal of packaging-level content churn.

### Frontend Modules and Readable Strings

[Confirmed] [macOS+Windows] This input does not provide concrete entries for “real frontend module additions/removals,” “same-name frontend module change clues,” or “new readable strings in main process and frontend bundles.” Boundary: this report therefore does not claim confirmed UI, copy, state-flow, or interaction changes.

### CSP, AppxManifest, New Components, and Resources

[Confirmed] [macOS+Windows] This input does not provide CSP changes, Windows AppxManifest changes, or new component/resource details. Boundary: no functional change is inferred for security policy, Windows app declarations, or resource surfaces.

### Newly Bundled Plugin Details

[Confirmed] [macOS+Windows] This input does not include newly bundled plugin details. Boundary: the confirmed plugin-related changes are version fields in existing `browser`, `chrome`, and Windows `computer-use` manifests, not new plugin capability surfaces.

## 📊 Raw Data

### macOS arm64

Previous version: `26.611.61049 build 3996`

Latest version: `26.611.61753 build 4008`

Previous full package SHA-256: `2a96fe47d4bec9cb667ec58cd480e06b4b7cda5c88bb8d6466b6a16d4191fa66`

Latest full package SHA-256: `2423fa8cc7a837c24f6000ba0f60354ff45b3c6b7a9d1a93448e82d91a49e81c`

Notarization verification: both previous and latest are `accepted`, source `Notarized Developer ID`, signing origin `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

File tree: `8267 → 8267`, 0 added, 0 removed, 62 content-changed.

Largest component movement: `Contents/Resources/app.asar`, `153.8 MB → 153.8 MB`, change `-0.00 MB`.

Computer Use runtime: `365.9 MB → 365.9 MB (+0 MB, +0%)`.

New cua_node dependencies: 0.

app.asar internals: `3074 → 3074`, 7 added, 7 removed, 11 changed.

Key file changes: `browser` plugin version, `chrome` plugin version, `Info.plist` version/build/app.asar hash.

### Windows x64

Previous version: `26.611.7849.0 build 26.611.7849.0`

Latest version: `26.611.8273.0 build 26.611.8273.0`

Previous full package SHA-256: `d19ebe0d2620d7c58f6681c3ed3ef23e12f6396354874db7721086bbd75f6fec`

Latest full package SHA-256: `32cf55c6d9a656eb18066890bac655b77e277bb7be84b942b223d27e86631d21`

File tree: `11943 → 11943`, 0 added, 0 removed, 16 content-changed.

Largest component movement: `resources/app.asar`, `157.6 MB → 157.6 MB`, change `-0.00 MB`.

Computer Use runtime: `285.6 MB → 285.6 MB (+0 MB, +0%)`.

New cua_node dependencies: 0.

app.asar internals: `3024 → 3024`, 7 added, 7 removed, 10 changed.

Key file changes: `computer-use` plugin version, `chrome` plugin version, `browser` plugin version, `.installed-owl-shell.json` install timestamp.

## ⚖️ Boundaries and Disclaimer

This is an unofficial third-party reverse-diff changelog and does not represent OpenAI official release notes.

This report does not redistribute binaries. It only cites file-level statistics, manifest/plist diffs, signing verification summaries, SHA-256 values, and readable configuration changes from the provided fact packages.

`[Signal]` means directional evidence inside packaged artifacts only. It is not source-code fact and does not mean the feature is visible to all users.

If official release notes, additional platform packages, or more complete module-level diffs become available, this report should be corrected against those new facts.

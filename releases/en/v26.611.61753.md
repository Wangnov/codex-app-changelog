
---
title: "Codex Desktop 26.611.61753"
version: "26.611.61753"
build: 4008
mac_version: "26.611.61753"
mac_build: 4008
win_version: "26.611.8273.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-15"
method: "Official package reverse diff(macOS Sparkle / Windows MSIX)"
summary: "This batch is a small app-layer refresh on both platforms with no Computer Use runtime or dependency changes; macOS additionally has readable evidence that the bundled browser/chrome plugin versions were bumped, while the Windows fact pack shows no matching readable change."
official_release_notes: false
---

# Codex Desktop 26.611.61753(macOS / Windows Comparison)

> Third-party cross-platform changelog · Unofficial

This batch looks like a small cross-platform sync: `app.asar` changed slightly, while file counts, the Computer Use runtime, and `cua_node` dependencies stayed unchanged.  
One-liner: a small cross-platform app-layer refresh, with confirmed bundled plugin version bumps visible on macOS only.

## About This Report

This report is based on reverse diffs of official signed distribution packages: the macOS side compares Sparkle-distributed arm64 full packages, and the Windows side compares x64 MSIX full packages. The macOS comparison is `26.611.61049` build `3996` → `26.611.61753` build `4008`; the Windows comparison is `26.611.7849.0` → `26.611.8273.0`. The release date is recorded from the batch input as `2026-06-15`.

Evidence is split into two levels:

- [Confirmed]: readable metadata, config, skill/plugin documents, dependency lists, type declarations, manifests, or explicit file-level statistics.
- [Signal]: frontend module names, build artifact sizes, or file-change direction only; useful as a pointer, but not enough to reconstruct source intent.

Platform markers mean:

- `[macOS+Windows]`: the same direction of evidence appears in both fact packs.
- `[macOS only]`: evidence appears only in the macOS fact pack.
- `[Windows only]`: evidence appears only in the Windows fact pack.

`webview/assets`, `.vite/build`, and similar build hash renames are treated as noise, as stated in the fact packs, and are not counted as feature additions or removals.

## ✨ Highlights

### [Confirmed] [macOS+Windows] Version batch advanced

macOS moved from `26.611.61049` build `3996` to `26.611.61753` build `4008`, with matching changes in `CFBundleShortVersionString` and `CFBundleVersion` inside `Info.plist`. Windows moved from `26.611.7849.0` to `26.611.8273.0`.

The platform difference is the versioning scheme: macOS uses a short version plus build number, while Windows uses a four-part MSIX-style version. This report uses the macOS short version `26.611.61753` for indexing.

### [Confirmed] [macOS+Windows] Small app-layer update, with total file counts unchanged

The outer file-tree counts did not change on either platform: macOS stayed at `8267 → 8267`, and Windows stayed at `11943 → 11943`. Neither platform added or removed outer files; only file contents changed: `62` files on macOS and `16` files on Windows.

The app layer, `app.asar`, also kept the same internal file count while showing small add/remove/modify churn: macOS internal files stayed `3074 → 3074`, with `7` added, `7` removed, and `11` modified; Windows internal files stayed `3024 → 3024`, with `7` added, `7` removed, and `10` modified.

This indicates an app-layer refresh on both platforms, but not a large file-structure change based on the provided facts.

### [Confirmed] [macOS+Windows] Computer Use runtime shows no size or dependency change

The Computer Use runtime’s `cua_node` total stayed unchanged: macOS was `365.9 MB → 365.9 MB (+0 MB, +0%)`, and Windows was `285.6 MB → 285.6 MB (+0 MB, +0%)`. Both platforms report `0` newly added `cua_node` dependency packages.

The main platform difference is the baseline size: `365.9 MB` on macOS and `285.6 MB` on Windows. Within this batch, neither platform added runtime dependencies.

### [Confirmed] [macOS only] Bundled browser/chrome plugin metadata versions bumped

In the macOS package, two bundled plugins had their `.codex-plugin/plugin.json` version fields updated with the main version:

- `browser` plugin: `26.611.61049` → `26.611.61753`
- `chrome` plugin: `26.611.61049` → `26.611.61753`

The `browser` plugin description still points to the Codex in-app browser for opening, inspecting, navigating, testing, clicking, typing, or screenshotting local web targets. The `chrome` plugin description still points to automation scenarios that depend on the user’s existing Chrome state, such as tabs, logged-in sessions, cookies, extensions, and Codex Chrome Extension setup.

The Windows fact pack does not provide a corresponding `plugin.json` diff, so this report cannot claim that Windows synchronized those plugin metadata version bumps. This item is therefore marked `[macOS only]`.

### [Confirmed] [macOS only] app.asar integrity hash changed

In macOS `Info.plist`, `ElectronAsarIntegrity/Resources/app.asar/hash` changed from `bdc881f00dd574cabfab0e56ca06975555f85ca31ebf8a9415f8a2e3d491d607` to `b1c35ee9911fd72ce5229f4f106275361e39b104bf4a659a0d914b8b5beef33b`.

This is consistent with the macOS `app.asar` content changing. The Windows fact pack does not list an equivalent integrity hash field, so no cross-platform inference is made.

## 📊 Raw Data

### macOS arm64

- Version: `26.611.61049` build `3996` → `26.611.61753` build `4008`
- Outer file tree: `8267 → 8267`
- Outer changes: added `0` / removed `0` / content changed `62`
- `app.asar`: `153.8 MB → 153.8 MB`, change `-0.00 MB`
- `app.asar` internal files: `3074 → 3074`, added `7` / removed `7` / changed `11`
- `cua_node` total: `365.9 MB → 365.9 MB (+0 MB, +0%)`
- Newly added `cua_node` dependencies: `0`
- Previous full package SHA-256: `2a96fe47d4bec9cb667ec58cd480e06b4b7cda5c88bb8d6466b6a16d4191fa66`
- Latest full package SHA-256: `2423fa8cc7a837c24f6000ba0f60354ff45b3c6b7a9d1a93448e82d91a49e81c`
- Notarization verification: both previous and latest builds were `accepted`, source `Notarized Developer ID`, origin `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`

### Windows x64

- Version: `26.611.7849.0` → `26.611.8273.0`
- Outer file tree: `11943 → 11943`
- Outer changes: added `0` / removed `0` / content changed `16`
- `app.asar`: `157.6 MB → 157.6 MB`, change `-0.00 MB`
- `app.asar` internal files: `3024 → 3024`, added `7` / removed `7` / changed `10`
- `cua_node` total: `285.6 MB → 285.6 MB (+0 MB, +0%)`
- Newly added `cua_node` dependencies: `0`
- Previous full package SHA-256: `d19ebe0d2620d7c58f6681c3ed3ef23e12f6396354874db7721086bbd75f6fec`
- Latest full package SHA-256: `32cf55c6d9a656eb18066890bac655b77e277bb7be84b942b223d27e86631d21`

## ⚖️ Boundaries and Disclaimer

This is a third-party, unofficial reverse-engineered changelog and does not represent official OpenAI release notes. It does not redistribute any binaries; it summarizes file-level diffs and readable metadata from official distribution artifacts.

Items marked [Signal] are not source-level facts; in this batch, build hash renames are not treated as functional changes. If official release notes, source-level evidence, or additional platform packages become available for cross-checking, these conclusions should be updated with the stronger evidence. Corrections using the same fact standard are welcome.

---
title: "Codex Desktop 26.513.31313"
version: "26.513.31313"
build: 2867
previous_version: "26.513.20950"
previous_build: 2816
platform: "macOS arm64"
released: "2026-05-16"
compared_from: "2026-05-14"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "This release is mainly a small Computer Use component update, with new frontend signals related to pinning and persisted state."
official_release_notes: false
---

# Codex Desktop `26.513.31313`(build 2867)

> Third-party changelog · Unofficial · Based on reverse-diffing official update packages

From an artifact perspective, this is a relatively small update: the core changes are concentrated in a Computer Use plugin version bump, along with several frontend modules that signal movement toward pinning and persisted UI state.  
English one-liner: a small desktop update centered on Computer Use component bumps and early UI-state signals.

## About this report

This is not an official changelog, but a **reverse-diff report based on officially distributed artifacts**. This entry is a historical backfill: both versions are already outside the official Sparkle incremental-update window, so this comparison uses two official full packages, one for the previous version and one for the latest version, rather than reconstructing an incremental package.

The comparison range is from `26.513.20950` build `2816`, released on `2026-05-14`, to `26.513.31313` build `2867`, released on `2026-05-16`, for macOS arm64.

In terms of confidence, the trust roots for both artifacts come from Apple notarization and OpenAI Developer ID signing. The notarization checks for both the previous and latest artifacts returned `accepted`; both sources are `Notarized Developer ID`, and both signing origins are shown as `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

This report uses two evidence levels:

- **[Confirmed]**: Direct human-readable evidence, such as Info.plist, plugin configuration diffs, signing results, and notarization results.
- **[Signal]**: Indirect evidence, such as frontend module names or binary size changes. This kind of evidence only indicates “a direction,” but cannot reconstruct source-level intent from the artifact.

## ✨ Highlights

### 1. Computer Use plugin version bumped from `1.0.791` to `1.0.793` [Confirmed]

In the `.codex-plugin/plugin.json` file for the `computer-use` plugin, the version number was updated from `1.0.791` to `1.0.793`.

This means the desktop-control-related plugin was indeed updated. What can be confirmed from the factual package is the plugin version bump, but this does not by itself establish which specific behaviors, capabilities, or interactions changed at the source level.

Key evidence:

```diff
-  "version": "1.0.791",
+  "version": "1.0.793",
```

### 2. Small binary size increases in Computer Use components [Signal]

The largest file size changes are concentrated in Computer Use-related components: `CUALockScreenGuardian` increased by `+0.17 MB`, `SkyComputerUseService` increased by `+0.14 MB`, and `SkyComputerUseClient` increased by `+0.08 MB`.

This is consistent with the plugin version bump and points to updates in the underlying Computer Use service, client, or lock-screen guardian logic. However, size changes only show that binary contents changed; they cannot reconstruct source-level intent from the artifact, nor can they directly prove newly added user-facing functionality.

Key evidence:

| Component | Change |
| --- | ---: |
| `CUALockScreenGuardian` | `+0.17 MB` |
| `SkyComputerUseService` | `+0.14 MB` |
| `SkyComputerUseClient` | `+0.08 MB` |

### 3. New frontend pinning-related modules: `pin` / `pin-filled` [Signal]

After removing Vite hash filename noise, the genuinely new frontend modules include `pin` and `pin-filled`.

This points to possible preparation for a “pin / fixed” visual or interaction element in the interface. However, the presence of these modules does not mean the feature has been exposed to users, and it cannot reconstruct source-level intent from the artifact.

Key evidence:

```text
新增模块: persisted-signal, pin, pin-filled
```

### 4. Frontend state management adds `persisted-signal` and removes `settings-route-state` [Signal]

The genuinely new frontend modules include `persisted-signal`, while `settings-route-state` was removed.

This points to a possible adjustment in how frontend state persistence or route state is handled. However, this is only indirect evidence at the module-name level; it does not confirm any specific user path, settings-page behavior, or source-level design intent.

Key evidence:

```text
新增模块: persisted-signal
移除模块: settings-route-state
```

### 5. App version and asar integrity hash updated [Confirmed]

In Info.plist, the version number changed from `26.513.20950` to `26.513.31313`, the build changed from `2816` to `2867`, and the integrity hash for `app.asar` also changed.

This shows that the main application bundle content was indeed updated. However, many of the additions and removals inside `app.asar` mainly come from build-hash renames under `webview/assets`, and should not be interpreted as thousands of real functional changes.

Key evidence:

```text
CFBundleShortVersionString: 26.513.20950 → 26.513.31313
CFBundleVersion: 2816 → 2867
ElectronAsarIntegrity hash: fd8467...e84e → 8242...509
```

## 📊 Raw data

### Bundle file-level statistics

| Metric | Value |
| --- | ---: |
| File count | `1331 → 1331` |
| Added | `0` |
| Removed | `0` |
| Content changed | `96` |

### Key component size changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/plugins/openai-bundled/plugins/computer-use/Codex Computer Use.app/Contents/SharedSupport/CUALockScreenGuardian.app/Contents/MacOS/CUALockScreenGuardian` | 14.7 MB | 14.9 MB | +0.17 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/computer-use/Codex Computer Use.app/Contents/MacOS/SkyComputerUseService` | 15.0 MB | 15.1 MB | +0.14 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/computer-use/Codex Computer Use.app/Contents/SharedSupport/SkyComputerUseClient.app/Contents/MacOS/SkyComputerUseClient` | 10.1 MB | 10.2 MB | +0.08 MB |
| `Contents/Resources/app.asar.unpacked/node_modules/better-sqlite3/build/Release/better_sqlite3.node` | 1.9 MB | 2.0 MB | +0.03 MB |
| `Contents/Resources/node_repl` | 15.0 MB | 15.1 MB | +0.01 MB |
| `Contents/Resources/app.asar` | 147.7 MB | 147.7 MB | +0.01 MB |

### Newly added dependency list

New dependencies added to `cua_node`: `0`.

## 🔁 Reproducibility information

| Artifact | SHA-256 | Verification / conclusion |
| --- | --- | --- |
| Previous full package `Codex-darwin-arm64-26.513.20950.zip` | `cd29516a850973879115b7b4f2a4bfa32a9a05b7d6d84a638f78656c4980e82c` | Notarization verification `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest full package `Codex-darwin-arm64-26.513.31313.zip` | `6001ed876cc8b62e1ae41b7c9e1246c31e7bd13eb11aa5ba7ff163bd9ef88c80` | Notarization verification `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Boundaries and disclaimer

This report is not affiliated with OpenAI and does not represent any official statement or official release notes.

This report does not redistribute any official binaries. It only publishes diff conclusions derived from officially signed distribution artifacts.

All [Signal] items only represent directional clues at the artifact level. They are not source-level facts and are not equivalent to confirmation that user-visible functionality has shipped.

If anything is missing or misread, corrections based on the same factual package are welcome.
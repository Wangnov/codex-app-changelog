
---
title: "Codex Desktop 26.506.31421"
version: "26.506.31421"
build: 2620
mac_version: "26.506.31421"
mac_build: 2620
win_version: "26.506.3741.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-05-08"
method: "Official package reverse diff: macOS Sparkle, Windows MSIX"
summary: "This batch mainly shows small updates to the core codex binaries and app.asar on both platforms; macOS has explicit Info.plist and asar integrity hash changes, while Windows additionally shows a sandbox setup binary size change."
official_release_notes: false
---

# Codex Desktop 26.506.31421 macOS / Windows Comparison

> Third-party cross-platform changelog · Unofficial

This report compares machine-extracted reverse diff facts from official signed distribution packages, with macOS arm64 and Windows x64 treated separately.  
One-liner: this batch is a small cross-platform binary/app bundle refresh, with macOS metadata changes and a Windows sandbox setup binary size change.

## About This Report

This report only uses facts provided in the input fact packages. The macOS sample compares `26.506.31004 build 2604` → `26.506.31421 build 2620`; the Windows sample compares `26.506.3484.0` → `26.506.3741.0`.

Evidence levels:

- [Confirmed]: facts from plist, manifest, integrity hashes, file trees, asar statistics, dependency lists, signing/notarization checks, explicit file paths, and size changes.
- [Signal]: weaker evidence from frontend module names, compressed bundle direction, or size-only clues. This batch does not include same-name frontend module clues or newly readable strings, so no UI or feature visibility is stated as fact.

Platform tags are `[macOS+Windows]`, `[macOS only]`, and `[Windows only]`. The two platforms come from different packages of the same Codex codebase, but their version numbers and builds are not aligned, so this report cites each platform’s facts separately.

The input does not provide built-in plugin list changes, Computer Use runtime totals, real frontend module additions/removals, same-name frontend module change clues, newly readable strings, content security policy changes, newly added components/resources, or detailed key-file diffs. Those areas are therefore only covered as evidence boundaries, not inferred conclusions.

## ✨ Highlights

### [Confirmed] [macOS+Windows] Core codex executables grew slightly

The core codex components grew on both platforms.

On macOS, `Contents/Resources/codex` changed from `190.8 MB` to `191.5 MB`, an increase of `+0.72 MB`. On Windows, `resources/codex.exe` changed from `245.0 MB` to `245.8 MB`, an increase of `+0.85 MB`; `resources/codex` also changed from `217.9 MB` to `218.7 MB`, an increase of `+0.84 MB`.

This confirms that core runtime components changed on both platforms. Because the input does not include readable strings, command lists, or concrete code diffs, it does not support claims about specific user-visible capabilities.

### [Confirmed] [macOS+Windows] app.asar stayed structurally stable with small content changes

On macOS, `app.asar` internal files stayed at `1382 → 1382`, with `1` added, `1` removed, and `8` changed. On Windows, `app.asar` internal files stayed at `1336 → 1336`, with `1` added, `1` removed, and `10` changed.

Both platforms show unchanged total app-layer file counts with a small number of additions, removals, and modifications. The fact packages explicitly say that many changes under `webview/assets` are Vite build hash rename noise, so they should not be interpreted as new functionality.

### [Confirmed] [macOS only] Info.plist version, build, and app.asar integrity hash changed

The macOS `Info.plist` shows:

- `CFBundleShortVersionString`: `26.506.31004` → `26.506.31421`
- `CFBundleVersion`: `2604` → `2620`
- `ElectronAsarIntegrity/Resources/app.asar/hash`: `f2d68d6d80d5a43a7e9c91653bbc4a30ac7e6e24444a8abc899e1ba49a1f74a5` → `d735f12f9b353a7b5d4f190919e8181b723073ae23555a0f45d496913daae917`

These are explicit macOS package metadata and asar integrity changes. The Windows input does not provide AppxManifest changes, so this cannot be described as a cross-platform manifest change.

### [Confirmed] [Windows only] The sandbox setup executable changed slightly in size

In the Windows package, `resources/codex-windows-sandbox-setup.exe` is listed as `8.1 MB` to `8.1 MB`, with a recorded change of `+0.03 MB`.

This shows a small content change in the Windows sandbox setup binary. The input does not provide strings, manifest details, or behavior diffs for that executable, so it does not support further claims about sandbox logic changes.

### [Confirmed] [macOS+Windows] browser-use and chrome plugin scripts changed, with no new dependency packages

Both macOS and Windows list size-change entries for:

- `plugins/openai-bundled/plugins/browser-use/scripts/browser-client.mjs`
- `plugins/openai-bundled/plugins/chrome/scripts/browser-client.mjs`

Both appear as approximately `0.7 MB → 0.7 MB`, with `+0.00 MB` after rounding. This means they are present in the change table, but their rounded size difference is not visible.

At the same time, both platforms report `0` newly added `cua_node` dependency packages. Therefore, this batch provides no evidence of newly added `cua_node` dependencies. The plugin scripts show content-change evidence, but the input does not provide plugin manifests, service entries, skill docs, or default prompt changes, so this cannot be expanded into a newly added capability surface.

### [Confirmed] [macOS only] Both macOS full packages passed notarization validation

The previous and latest macOS `Codex.app` packages both show:

- `accepted`
- `source=Notarized Developer ID`
- `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`

This supports that the macOS diff targets official signed and notarized distribution artifacts. The Windows input provides full package locations and SHA-256 hashes, but not equivalent signing validation output.

### [Confirmed] [macOS+Windows] No file-count-level expansion in this batch

The macOS bundle file count stayed `1238 → 1238`, with `0` added, `0` removed, and `54` content changes. The Windows bundle file count stayed `815 → 815`, with `0` added, `0` removed, and `16` content changes.

Both outer file trees had no file additions or removals, with changes concentrated in existing file contents. macOS has more content-changed files than Windows in this input, but that is only a file-level statistic and does not directly imply a feature difference.

## 🔎 Low-Level Signal Index

### Plugin scripts

- [Confirmed] [macOS+Windows] `browser-use/scripts/browser-client.mjs` and `chrome/scripts/browser-client.mjs` appear in the size-change tables on both platforms.
- Evidence boundary: the fact package does not include newly added built-in plugin details, manifest changes, service entries, skill docs, default prompts, or maximum-file information. This can only be recorded as existing script content changes, not new plugin capabilities.

### app.asar structure

- [Confirmed] [macOS+Windows] On both platforms, `app.asar` total file counts stayed unchanged with small numbers of additions, removals, and modifications.
- Evidence boundary: the input does not list added filenames, removed filenames, same-name frontend module clues, or new strings. This cannot be interpreted as specific frontend UI, state, or interaction changes.

### Hash noise

- [Confirmed] [macOS+Windows] Both platform fact packages state that many changes under `webview/assets` are Vite build hash rename noise.
- Evidence boundary: hash-named file changes are not counted as functional additions or removals.

### Platform metadata

- [Confirmed] [macOS only] `Info.plist` version, build, and `ElectronAsarIntegrity` hash changes are explicit.
- [Confirmed] [Windows only] The input does not provide AppxManifest change details.
- Evidence boundary: the Windows facts’ “version and channel” section contains a platform/architecture line saying `macOS arm64`, while the outer platform heading and task input identify the sample as Windows x64 MSIX. This report follows the outer platform section and task description for the Windows sample.

## 📊 Raw Data

### macOS arm64

- Comparison range: `26.506.31004 build 2604` → `26.506.31421 build 2620`
- Previous full package SHA-256: `9a09529d4937d1f24c78f78c7ef7f4b501c594c55b4beb4fecc076d03524dad5`
- Latest full package SHA-256: `13eba9c4d5a903a7ee80cad99a14a20ef081417b94fc9d0446a22e11bdd56be9`
- Bundle file count: `1238 → 1238`
- Bundle added/removed/changed: `0 / 0 / 54`
- `app.asar` internal files: `1382 → 1382`
- `app.asar` added/removed/changed: `1 / 1 / 8`
- `Contents/Resources/codex`: `190.8 MB → 191.5 MB`, `+0.72 MB`
- `browser-use/scripts/browser-client.mjs`: `0.7 MB → 0.7 MB`, `+0.00 MB`
- `chrome/scripts/browser-client.mjs`: `0.7 MB → 0.7 MB`, `+0.00 MB`
- Newly added `cua_node` dependency packages: `0`

### Windows x64

- Comparison range: `26.506.3484.0` → `26.506.3741.0`
- Previous full package SHA-256: `554ab8b534305bb26338c8b89dd745210625d52f78850ec7996c56772821e4ee`
- Latest full package SHA-256: `f2b2930dc1caf326c57f0bf2525d2a3cde2179ceab9944623614839eb6da6fe5`
- Bundle file count: `815 → 815`
- Bundle added/removed/changed: `0 / 0 / 16`
- `app.asar` internal files: `1336 → 1336`
- `app.asar` added/removed/changed: `1 / 1 / 10`
- `resources/codex.exe`: `245.0 MB → 245.8 MB`, `+0.85 MB`
- `resources/codex`: `217.9 MB → 218.7 MB`, `+0.84 MB`
- `resources/codex-windows-sandbox-setup.exe`: `8.1 MB → 8.1 MB`, `+0.03 MB`
- `chrome/scripts/browser-client.mjs`: `0.7 MB → 0.7 MB`, `+0.00 MB`
- `browser-use/scripts/browser-client.mjs`: `0.7 MB → 0.7 MB`, `+0.00 MB`
- Newly added `cua_node` dependency packages: `0`

## ⚖️ Boundaries and Disclaimer

This is a third-party unofficial changelog and does not represent OpenAI official release notes. It does not redistribute binaries; it only summarizes the provided reverse diff facts from official signed distribution packages.

[Signal] does not constitute source-code fact and does not mean a product change is visible to all users. This batch does not provide newly readable strings, same-name frontend module clues, content security policy changes, AppxManifest changes, Computer Use runtime totals, or newly added built-in plugin details, so this report makes no functional claims in those areas.

Corrections are welcome when backed by additional fact packages, official notes, or reproducible diff evidence.

---
title: "Codex Desktop 26.506.31421"
version: "26.506.31421"
build: 2620
previous_version: "26.506.31004"
previous_build: 2604
platform: "macOS arm64"
released: "2026-05-09"
compared_from: "2026-05-08"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "This is a very restrained minor release: the main application binary grew slightly, only a small number of application-layer files changed, and no new dependencies or bundle files were observed."
official_release_notes: false
---

# Codex Desktop `26.506.31421`(build 2620)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

From the artifact perspective, this release looks more like a small revision: the version and build numbers were updated, the main application binary grew slightly, but no new dependencies, plugins, or bundle files appeared.  
English one-liner: a small maintenance-style build with limited observable surface changes.

## About this report

This is not an official changelog, but a **reverse-engineered diff report based on officially distributed artifacts**. The comparison covers Codex Desktop `26.506.31004` build `2604`, released on `2026-05-08`, through `26.506.31421` build `2620`, released on `2026-05-09`, for macOS arm64.

One important note: the fact package shows that this release is a historical backfill, and both versions are already outside the Sparkle delta update window. Therefore, this comparison was not reconstructed from delta packages; instead, both official full packages were downloaded and compared directly. The trust roots are Apple notarization and the OpenAI Developer ID code signature. Both the previous and latest `Codex.app` passed `codesign` / `spctl` verification, with the result `accepted`, source `Notarized Developer ID`, and signing subject `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

This report uses two evidence levels:

- **[Confirmed]**: Direct human-readable evidence exists, such as `Info.plist` key values, dependency manifests, signature verification results, or file-level statistics.
- **[Signal]**: Only indirect evidence exists, such as frontend module names or binary size changes. This type of evidence can only indicate “points in a certain direction,” but cannot reconstruct source-level intent from the artifact.

## ✨ Key changes

### 1. Version and build number updated from `26.506.31004` / `2604` to `26.506.31421` / `2620` [Confirmed]

`Info.plist` shows that the application short version changed from `26.506.31004` to `26.506.31421`, and the build number changed from `2604` to `2620`.

This means this is a newly signed and released build, rather than a local repackaging result caused only by resource hash changes.

Evidence:

```diff
/CFBundleShortVersionString: '26.506.31004' → '26.506.31421'
/CFBundleVersion: '2604' → '2620'
```

### 2. The main application binary grew slightly, but there is no directly recoverable functional explanation [Signal]

`Contents/Resources/codex` increased from `190.8 MB` to `191.5 MB`, a gain of approximately `0.72 MB`.

This points to actual content changes inside the main application, but binary size changes alone cannot reconstruct source-level intent from the artifact, nor can they determine which specific user-facing feature was added, fixed, or adjusted.

Key evidence:

| Component | Old | New | Change |
| --- | --- | --- | --- |
| `Contents/Resources/codex` | 190.8 MB | 191.5 MB | +0.72 MB |

### 3. The application bundle file tree remained stable, with no bundle files added or removed [Confirmed]

The total number of bundle files remained `1238 → 1238`, with `0` added, `0` removed, and `54` content changes.

This indicates that, from the outer application bundle structure, this release did not introduce new standalone components, plugin directories, or resource files. For developers, the observable changes are more likely concentrated in the contents of existing files rather than an expansion of the application structure.

### 4. The `app.asar` integrity hash changed, confirming application-layer content changes [Confirmed]

The `app.asar` hash recorded under `ElectronAsarIntegrity` in `Info.plist` changed, indicating that the Electron application-layer archive content is not identical.

However, the fact package also explicitly notes that hashed filename changes under `webview/assets` and `.vite/build` are Vite build artifact renaming noise, and cannot be used to infer that features were added or removed.

Evidence:

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
'f2d68d6d80d5a43a7e9c91653bbc4a30ac7e6e24444a8abc899e1ba49a1f74a5'
→
'd735f12f9b353a7b5d4f190919e8181b723073ae23555a0f45d496913daae917'
```

### 5. No new `cua_node` dependencies were added [Confirmed]

The fact package shows `0` newly added `cua_node` dependency packages.

This means the diff contains no evidence of expanded capability through newly added dependencies at this layer. If behavioral changes exist, they are more likely to come from modifications to existing dependencies or existing binary content, rather than from a newly added dependency set.

## 📊 Raw data

### Bundle file-level statistics

| Metric | Previous version | Latest version | Change |
| --- | ---: | ---: | ---: |
| File count | 1238 | 1238 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Content changes | - | 54 | 54 |

### Key component size changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/codex` | 190.8 MB | 191.5 MB | +0.72 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/browser-use/scripts/browser-client.mjs` | 0.7 MB | 0.7 MB | +0.00 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/chrome/scripts/browser-client.mjs` | 0.7 MB | 0.7 MB | +0.00 MB |

### Newly added dependency list

| Dependency area | Number of added packages | Notes |
| --- | ---: | --- |
| `cua_node` | 0 | No newly added dependency packages found |

## 🔁 Reproducibility information

| Artifact | URL | SHA-256 | Verification result |
| --- | --- | --- | --- |
| Previous full package `26.506.31004` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.506.31004.zip` | `9a09529d4937d1f24c78f78c7ef7f4b501c594c55b4beb4fecc076d03524dad5` | `Codex.app: accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest full package `26.506.31421` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.506.31421.zip` | `13eba9c4d5a903a7ee80cad99a14a20ef081417b94fc9d0446a22e11bdd56be9` | `Codex.app: accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Boundaries and disclaimer

This report is not affiliated with OpenAI, is not official release notes, and does not represent an official explanation.

This report does not redistribute any official binaries. It only publishes diff conclusions derived from officially signed distribution packages.

All [Signal] items indicate only indirect artifact-level direction. They do not constitute source-level facts and should not be interpreted as definitive product intent.

If there are omissions or misjudgments, corrections based on the same fact package or reproducible artifacts are welcome.

---
title: "Codex Desktop 26.506.31421"
version: "26.506.31421"
build: 2620
mac_version: "26.506.31421"
mac_build: 2620
win_version: "26.506.3741.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-05-08"
method: "Official package reverse diff(macOS Sparkle / Windows MSIX)"
summary: "This batch mainly shows small core executable and app.asar application-layer updates on both platforms; Windows also carries a sandbox setup component delta, while macOS updates Info.plist and the asar integrity hash."
official_release_notes: false
---

# Codex Desktop 26.506.31421(macOS / Windows comparison)

> Third-party cross-platform changelog · Unofficial

This batch has no readable official release notes; the reverse diff shows shared core binary and application-layer updates across both platforms, but limited recoverable feature semantics.  
One-liner: this cross-platform batch looks like a compact core/app-layer refresh, with Windows carrying an additional sandbox setup delta.

## About this report

This report is based on reverse diffs of official signed distribution packages: the macOS side compares Sparkle full packages, and the Windows side compares MSIX packages. The macOS comparison range is 26.506.31004 build 2604 → 26.506.31421 build 2620; the Windows comparison range is 26.506.3484.0 → 26.506.3741.0. The release date is taken from the provided batch input: 2026-05-08.

Evidence is split into two levels: [Confirmed] means the item comes from readable evidence such as plist, package.json, dependency lists, type declarations, skill docs, readable manifests, or explicit file-level statistics. [Signal] means only module names, filenames, or size changes are visible; it can indicate direction, but cannot recover source intent.

Platform tags mean: [macOS+Windows] appears on both platforms; [macOS only] appears only in the macOS fact package; [Windows only] appears only in the Windows fact package. Hash-only renames under webview/assets and .vite/build are treated as build noise as requested, and are not used as evidence of feature additions or removals.

Both macOS full packages passed notarization validation, with source shown as Notarized Developer ID and origin as OpenAI OpCo, LLC. The Windows fact package provides SHA-256 checksums for the previous and latest packages, but does not include an equivalent readable signature validation result.

## ✨ Highlights

### Core executable grew slightly [Signal] [macOS+Windows]

The core Codex binary grew on both platforms. On macOS, `Contents/Resources/codex` went from 190.8 MB to 191.5 MB, up 0.72 MB. On Windows, `resources/codex.exe` went from 245.0 MB to 245.8 MB, up 0.85 MB, while `resources/codex` went from 217.9 MB to 218.7 MB, up 0.84 MB.

This shows that both platforms include a core-layer update, but the fact packages do not provide symbols, source, or a readable changelog, so no specific feature claim can be made.

### app.asar application layer changed slightly [Confirmed] [macOS+Windows]

The app.asar archive kept the same total internal file count on both platforms, while showing a “1 added / 1 removed / several changed” pattern. macOS stayed at 1382 → 1382, with 1 added, 1 removed, and 8 changed. Windows stayed at 1336 → 1336, with 1 added, 1 removed, and 10 changed.

The platform difference is that Windows has slightly more changed files, but neither platform has net file-count growth. Since hash renames under webview/assets and .vite/build are excluded as build noise, this should only be read as a small application-layer update, not as proof of a specific UI or feature change.

### Browser-related plugin client scripts changed [Signal] [macOS+Windows]

Both platforms list browser client script changes under the `openai-bundled` plugin set for Chrome and browser-use: `plugins/chrome/scripts/browser-client.mjs` and `plugins/browser-use/scripts/browser-client.mjs` both appear in the size-change table, shown as 0.7 MB → 0.7 MB, with rounded delta +0.00 MB.

This is a directional signal: browser plugin client code was touched. The fact packages only provide filenames and rounded sizes, so this cannot be classified as a fix, refactor, or behavior change.

### No new cua_node dependencies [Confirmed] [macOS+Windows]

Both platforms report “0 new packages” for cua_node dependency changes.

That means no newly added cua_node packages were observed in this batch. This only covers the “new package” fact; it does not prove that existing package contents or runtime behavior were unchanged.

### macOS version metadata and asar integrity hash updated [Confirmed] [macOS only]

The macOS Info.plist shows version metadata updates: `CFBundleShortVersionString` changed from 26.506.31004 to 26.506.31421, and `CFBundleVersion` changed from 2604 to 2620. `ElectronAsarIntegrity/Resources/app.asar/hash` also changed from the previous value to a new one.

This is a clear macOS packaging metadata change. The Windows fact package does not provide corresponding AppxManifest changes, so this should not be presented as a shared cross-platform item.

### Windows sandbox setup component changed [Signal] [Windows only]

On Windows, `resources/codex-windows-sandbox-setup.exe` changed from 8.1 MB to 8.1 MB, with a +0.03 MB delta.

This indicates a Windows-only content or packaging change in the sandbox setup component. The fact package does not provide readable installer logic, version fields, or configuration diffs, so this remains a Windows-only signal rather than evidence of a new sandbox capability.

## 📊 Raw data

| Item | macOS arm64 | Windows x64 |
| --- | --- | --- |
| Previous version | 26.506.31004 build 2604 | 26.506.3484.0 |
| Latest version | 26.506.31421 build 2620 | 26.506.3741.0 |
| File count | 1238 → 1238 | 815 → 815 |
| Added / removed / content changed | 0 / 0 / 54 | 0 / 0 / 16 |
| app.asar internal files | 1382 → 1382 | 1336 → 1336 |
| app.asar added / removed / changed | 1 / 1 / 8 | 1 / 1 / 10 |
| New cua_node packages | 0 | 0 |

| Key component | macOS arm64 | Windows x64 |
| --- | --- | --- |
| Core binary | `Contents/Resources/codex`: 190.8 MB → 191.5 MB, +0.72 MB | `resources/codex.exe`: 245.0 MB → 245.8 MB, +0.85 MB; `resources/codex`: 217.9 MB → 218.7 MB, +0.84 MB |
| Windows sandbox setup component | Not listed | `resources/codex-windows-sandbox-setup.exe`: 8.1 MB → 8.1 MB, +0.03 MB |
| Chrome plugin client | 0.7 MB → 0.7 MB, +0.00 MB | 0.7 MB → 0.7 MB, +0.00 MB |
| browser-use plugin client | 0.7 MB → 0.7 MB, +0.00 MB | 0.7 MB → 0.7 MB, +0.00 MB |

macOS package checksums: previous SHA-256 `9a09529d4937d1f24c78f78c7ef7f4b501c594c55b4beb4fecc076d03524dad5`; latest SHA-256 `13eba9c4d5a903a7ee80cad99a14a20ef081417b94fc9d0446a22e11bdd56be9`.  
Windows package checksums: previous SHA-256 `554ab8b534305bb26338c8b89dd745210625d52f78850ec7996c56772821e4ee`; latest SHA-256 `f2b2930dc1caf326c57f0bf2525d2a3cde2179ceab9944623614839eb6da6fe5`.

## ⚖️ Boundaries and disclaimer

This is a third-party unofficial changelog and does not represent OpenAI official release notes. It does not redistribute binaries; it only summarizes the provided reverse-diff facts from official packages.

All [Signal] items are not source-level facts. They only mean that the relevant files or components changed observably in the distributed package. If official release notes or more granular readable evidence become available later, those stronger sources should be used to revise this interpretation. Corrections on versioning, platform differences, and evidence interpretation are welcome.

---
title: "Codex Desktop 26.429.30905"
version: "26.429.30905"
build: 2345
previous_version: "26.429.20946"
previous_build: 2312
platform: "macOS arm64"
released: "2026-05-01"
compared_from: "2026-04-30"
method: "Official full-package comparison + structured bundle diff"
summary: "This build mainly shows signals for an Amazon Bedrock login surface, a smaller plugin / skills frontend surface, and a modest app.asar size reduction."
official_release_notes: false
---

# Codex Desktop `26.429.30905` build 2345

> Third-party changelog · Unofficial · Based on reverse diffing official update artifacts

This build moves from `26.429.20946` to `26.429.30905`, with visible changes centered on Amazon Bedrock auth UI signals, plugin and skills page frontend adjustments, and a smaller `app.asar`.  
One-liner: this build points to Amazon Bedrock auth UI work and trims part of the plugin / skills frontend surface.

## About This Report

This is not an official changelog. It is a **reverse-diff report based on official distribution artifacts**. This entry is a historical backfill: the official appcast did not provide a delta package for `26.429.20946` → `26.429.30905`, so the comparison was performed directly between the two official full packages.

Evidence is classified into two levels: **[Confirmed]** means the change comes from directly verifiable artifact data such as Info.plist values, readable strings, signatures, notarization results, dependency lists, or file-level statistics. **[Signal]** means the evidence comes only from frontend module names, same-name frontend module string diffs, or size movement; it points in a direction, but cannot be reconstructed into source-level intent.

The comparison scope is macOS arm64. The previous version is `26.429.20946` build `2312`, released on `2026-04-30`; the latest version is `26.429.30905` build `2345`, released on `2026-05-01`.

## ✨ Highlights

### 1. Profile UI shows Amazon Bedrock login-related strings [Signal]

The `composer` frontend module added readable strings related to Amazon Bedrock authentication, pointing to Bedrock login state being surfaced in a profile dropdown or auth status area.

The user- or developer-visible implication is that model or account-related UI may be starting to expose Amazon Bedrock identity state. This evidence comes from a same-name frontend module diff, so it only supports a UI and state-direction signal, not source-level intent.

Key evidence:

```text
Added strings:
Label indicating the user is authenticated with Amazon Bedrock
Logged in with Amazon Bedrock
codex.profileDropdown.amazonBedrockAuth
```

### 2. Plugin detail add and enablement surfaces appear reduced [Signal]

The `plugin-detail-page` module shrank from `0.1 MB` to `0.0 MB`, down `84639 bytes`, and removed several readable strings related to adding plugins, the MCP server enablement toggle, skill preview menus, and page navigation.

This points to UI, state, or interaction changes on the plugin detail page, especially around strings such as `Add to Codex`, `Adding to Codex`, and `App unavailable`. Since these are same-name frontend module clues, they should be treated only as frontend behavior signals.

Key evidence:

```text
Removed strings:
Add to Codex
Adding to Codex
App unavailable
Accessible label for the MCP server enablement toggle on the plugin detail page
Accessible label for the section navigation on the plugin detail page
```

### 3. Skills page copy for installed lists and refresh actions was removed [Signal]

The `skills-page` module decreased by `11676 bytes` and removed copy related to creating a skill, reloading the skills list, installed skills sections, empty states, and filtered empty states.

This points to a reduced or rearranged skills page information structure, but the artifact alone cannot distinguish feature removal from route migration or frontend bundle restructuring.

Key evidence:

```text
Removed strings:
Button label for creating a new skill
Button label for reloading skills list
Header title for the skills page
Heading for the installed skills section
Empty state on the skills page
```

### 4. Main-process bundle adds parse-error and shell execution strings [Confirmed]

The new main-process bundle contains two newly added readable strings: `Parse Error` and `exec /bin/sh -c `. Their presence in the artifact is confirmed, so they are solid evidence of a new or changed error path and command execution direction.

This may correspond to parsing failure handling and a path that executes commands through `/bin/sh -c`. Because the evidence comes from minified main-process bundle strings, it does not reveal the complete source-level semantics.

Key evidence:

```text
Added strings:
Parse Error
exec /bin/sh -c 
```

### 5. App-server handshake timeout copy disappeared from readable main-process strings [Confirmed]

The new main-process bundle removed `Codex app-server initialize handshake timed out`. This is a direct readable-string change.

It may correspond to a changed message, changed error handling, or a different bundle location for the app-server initialization timeout path. The fact package only proves the string removal, not whether the error path itself was deleted.

Key evidence:

```text
Removed string:
Codex app-server initialize handshake timed out
```

### 6. Version metadata and asar integrity hash were updated [Confirmed]

`Info.plist` updates the short version from `26.429.20946` to `26.429.30905`, the build from `2312` to `2345`, and the `ElectronAsarIntegrity` hash for `app.asar`.

This confirms that the update is not just outer packaging churn: the main application payload in `app.asar` changed.

Key evidence:

```text
CFBundleShortVersionString: 26.429.20946 → 26.429.30905
CFBundleVersion: 2312 → 2345
ElectronAsarIntegrity/Resources/app.asar/hash:
6340fbb80cccaa419bb3d9df13e5019954e3cfa31ef142b88161f36976892f5d
→ cb297e87e825012472fbd6fdfc2065f050b666711070f42a3a971428eceb0970
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 508 | 508 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Modified files | - | - | 46 |

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 131.5 MB | 130.0 MB | -1.52 MB |

### Added Dependencies

| Dependency area | Added count | Details |
| --- | ---: | --- |
| `cua_node` | 0 | No added packages |

## 🔁 Reproducibility

| Item | Previous | Latest |
| --- | --- | --- |
| Download artifact | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.429.20946.zip` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.429.30905.zip` |
| SHA-256 | `d548dc497553f003588f5b445dc14725a2c619011f770b737070a6d80866ba11` | `3a6229d7f6ac5a7a147ea4fe34102874a0ad1a8789347de0ceae797bd863dc97` |
| Size | Not provided in the fact package | Not provided in the fact package |
| EdDSA verification | Not provided in the fact package | Not provided in the fact package |
| codesign / spctl notarization result | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Boundaries and Disclaimer

This document is not affiliated with OpenAI and does not represent an official position or official release notes.

It does not redistribute any official binaries; it only publishes conclusions derived from differences in official signed distribution artifacts.

All [Signal] entries indicate directions suggested by artifact diffs and are not source-level facts. Same-name frontend module diffs in particular must not be read on their own as definitive feature additions or removals.

Corrections based on the same artifacts and reproducibility data are welcome.

---
title: "Codex Desktop 26.429.30905"
version: "26.429.30905"
build: 2345
previous_version: "26.429.20946"
previous_build: 2312
platform: "macOS arm64"
released: "2026-05-01"
compared_from: "2026-04-30"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This release is mainly a minor version bump: the version number and build were updated, the bundle file count stayed unchanged, and only the app.asar contents and integrity hash changed."
official_release_notes: false
---

# Codex Desktop `26.429.30905` (build 2345)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

This is a minor macOS arm64 version comparison from `26.429.20946` to `26.429.30905`; at the artifact level, no new dependencies or bundle files were observed.  
English one-liner: a small bundle-level update with unchanged file counts and a smaller `app.asar`.

## About This Report

This is not an official changelog, but a **reverse-engineered diff report based on officially distributed artifacts**. This comparison is a historical backfill: both versions are already outside the official Sparkle delta window, so the delta-package reconstruction workflow was not used. Instead, both official full packages were downloaded and compared directly using a structured bundle diff.

In terms of trustworthiness, the trust root for this comparison comes from Apple notarization and the OpenAI Developer ID code signature. Both the previous version and the latest version passed `spctl` notarization verification with the result `accepted`, source `Notarized Developer ID`, and signing origin `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

This report uses two evidence levels:

- **[Confirmed]**: Direct, human-readable evidence, such as `Info.plist` key values, signature verification results, file statistics, and dependency lists.
- **[Signal]**: Indirect evidence only, such as frontend module names or binary size changes. It can point in a certain direction, but cannot reconstruct source-level intent from the artifact.

Comparison scope: `26.429.20946` build `2312`, released on `2026-04-30`; to `26.429.30905` build `2345`, released on `2026-05-01`.

## ✨ Key Changes

### 1. Version and build advanced to `26.429.30905` / `2345` [Confirmed]

The latest version’s `Info.plist` shows that the app version was updated from `26.429.20946` to `26.429.30905`, and the build from `2312` to `2345`.

This means macOS, the updater, and the app itself can recognize it as a new official build. However, the evidence package does not provide official release notes, nor does it show new permission entries or added components.

Key evidence:

```diff
CFBundleShortVersionString: 26.429.20946 → 26.429.30905
CFBundleVersion: 2312 → 2345
```

### 2. Bundle file count stayed unchanged, with only 46 files changed in content [Confirmed]

The total number of files in the app bundle went from `508` to `508`, with `0` added, `0` removed, and `46` changed in content.

This looks more like a replacement or repackaging of existing app contents than the addition of independent functional components. The evidence package does not show new plugins, extensions, dependency packages, or newly added external files.

Key evidence:

```text
File count: 508 → 508
Added 0 / Removed 0 / Content changed 46
```

### 3. `app.asar` shrank by about 1.52 MB, and the internal build hash changed accordingly [Confirmed]

The core app archive `Contents/Resources/app.asar` changed from `131.5 MB` to `130.0 MB`, a decrease of `1.52 MB`. At the same time, the ElectronAsarIntegrity hash recorded in `Info.plist` also changed.

This shows that the app-layer contents were indeed replaced; however, the evidence package does not provide readable source-level changes, so it is not possible to further determine the specific feature intent.

Key evidence:

```diff
Contents/Resources/app.asar: 131.5 MB → 130.0 MB (-1.52 MB)
ElectronAsarIntegrity/Resources/app.asar/hash:
6340fbb80cccaa419bb3d9df13e5019954e3cfa31ef142b88161f36976892f5d
→ cb297e87e825012472fbd6fdfc2065f050b666711070f42a3a971428eceb0970
```

### 4. No new `cua_node` dependencies [Confirmed]

The `cua_node` dependency list shows `0` newly added packages.

This means that, based on the current evidence package, this version did not expand `cua_node` capabilities by adding new low-level dependencies. Since the evidence package also does not list removal or upgrade details, no inference can be made about whether there were other internal dependency changes.

Key evidence:

```text
0 packages added
```

### 5. Large additions/removals inside app.asar are build-artifact renames and are not interpreted as functional changes [Confirmed]

The internal file count inside `app.asar` remained `1208 → 1208`; the machine diff shows `184` added, `184` removed, and `7` changed. However, the evidence package explicitly states that the large number of additions and removals under `webview/assets` are Vite build-hash rename noise and cannot be used to infer new features.

Therefore, this report does not describe these hashed filename changes as “features added/removed.”

Key evidence:

```text
Internal files: 1208 → 1208 (Added 184 / Removed 184 / Changed 7)
Note: Large additions/removals under webview/assets are Vite build hash renames and are noise
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Previous version | Latest version | Change |
| --- | ---: | ---: | ---: |
| bundle file count | 508 | 508 | 0 |
| added files | - | - | 0 |
| removed files | - | - | 0 |
| content-changed files | - | - | 46 |

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 131.5 MB | 130.0 MB | -1.52 MB |

### Added Dependency List

| Scope | Number of added dependencies |
| --- | ---: |
| `cua_node` | 0 |

## 🔁 Reproducibility Information

| Artifact | URL | SHA-256 | Verification result |
| --- | --- | --- | --- |
| Previous full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.429.20946.zip` | `d548dc497553f003588f5b445dc14725a2c619011f770b737070a6d80866ba11` | `spctl`: accepted; source=`Notarized Developer ID`; origin=`Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.429.30905.zip` | `3a6229d7f6ac5a7a147ea4fe34102874a0ad1a8789347de0ceae797bd863dc97` | `spctl`: accepted; source=`Notarized Developer ID`; origin=`Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Scope and Disclaimer

This article is unrelated to OpenAI. It is a third-party unofficial analysis and does not represent official statements.

This article does not redistribute any official binaries. It only publishes diff conclusions derived from officially signed distribution artifacts.

[Signal]-level entries only indicate indirect artifact-level signals and do not constitute source-level facts. The available changes in this evidence package are mainly [Confirmed]-level version, file, size, hash, and signature-verification information.

If there are factual or interpretive errors, corrections based on the same distribution artifacts and reproducible evidence are welcome.
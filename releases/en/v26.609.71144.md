
---
title: "Codex Desktop 26.609.71144"
version: "26.609.71144"
build: 3952
mac_version: "26.609.71144"
mac_build: 3952
win_version: "26.609.4994.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-15"
method: "official package reverse diff: macOS Sparkle, Windows MSIX"
summary: "The macOS package moved to 26.609.71144 and updated bundled Browser/Chrome plugin versions, with signals around remote pairing upgrade prompts and debug modal shrinkage; the Windows input sample is unchanged."
official_release_notes: false
---

# Codex Desktop 26.609.71144 macOS / Windows Comparison

> Third-party cross-platform changelog · Unofficial

The confirmed changes in this batch are concentrated in the macOS package: version metadata, the app.asar integrity hash, and bundled Browser/Chrome plugin manifests were updated; the Windows sample has no file content changes in this input.

One-liner: macOS advanced to 26.609.71144 with bundled plugin metadata updates and a few UI-facing signals, while the Windows sample is byte-identical across the compared inputs.

## About This Report

This report only uses machine-extracted diff facts from reverse-reconstructed official signed distribution packages. The macOS comparison range is 26.609.41114 build 3888 → 26.609.71144 build 3952; the Windows comparison range is 26.609.4994.0 → 26.609.4994.0.

Evidence is split into two levels: [Confirmed] comes from plist, manifest, dependency lists, file statistics, signatures/hashes, service entries, skill documentation, type declarations, package.json, and readable strings outside the “same-name frontend module change clues” section; [Signal] comes from same-name frontend module readable strings, CSS class names, and size changes, and only indicates packaged UI/interaction direction.

Platform scope is marked as `[macOS+Windows]`, `[macOS only]`, or `[Windows only]`. Because release timing and version numbers may differ between platforms, this report cites each platform’s facts separately and does not project one platform’s findings onto the other.

## ✨ Highlights

### [Confirmed] [macOS only] macOS Version and app.asar Integrity Metadata Updated

The macOS package moved from `26.609.41114` to `26.609.71144`, and `CFBundleVersion` moved from `3888` to `3952`. `ElectronAsarIntegrity/Resources/app.asar/hash` also changed from `3e01c3b6...ca9eef7` to `cbf25d7...03dd1a50c`, confirming that the application-layer asar content entered a new build.

The Windows input sample has the same previous and latest version, `26.609.4994.0`, and the same SHA-256: `547618a744149221078a27febdfff65c924b46ff85ab2fe1595180e128be8d85`. Therefore the only supported statement is: this input sample has no content changes on Windows.

### [Confirmed] [macOS only] Bundled Browser and Chrome Plugin Manifests Bumped with the Package

Two bundled macOS plugin `.codex-plugin/plugin.json` files changed version numbers:

- `browser`: `26.609.41114` → `26.609.71144`
- `chrome`: `26.609.41114` → `26.609.71144`

The fact package does not show changes to their descriptions, permissions, or service entries. The confirmed fact is that these plugin manifests were synchronized with the macOS app version. The Windows sample has no plugin manifest changes.

### [Signal] [macOS only] Mobile Remote Pairing Flow Gains an “Upgrade Required” Prompt Signal

The same-name frontend module `codex-mobile-setup-dialog` changed, growing by `522 bytes`, with newly added readable strings including:

- `Please upgrade to the latest version of the Codex App to pair`
- `Message shown instead of remote-control pairing controls when the Codex app must be upgraded`
- `codexMobile.setupPage.waiting.upgradeMessage`

This indicates a packaged UI/status signal for mobile setup or remote pairing where the current app version must be upgraded before pairing. Because these strings come from same-name frontend module change clues, this report marks them as [Signal] only and does not infer that all users can already see the flow.

### [Signal] [macOS only] Debug Modal Shrinks Substantially and Removes Debug/Host-Allowance Text

The same-name frontend module `debug-modal` shrank from about `0.2 MB` to about `0.0 MB`, decreasing by `180195 bytes`. Removed readable strings include:

- `(command unavailable)`
- `Account ID`
- `Active browser tab`
- `Active conversation`
- `Allow automatic worktree cleanup in this unpackaged build.`
- `Allow host`
- `Allow host starting`

A set of modal, backdrop, accessibility, and state class names was also removed, including `aria-modal`, `aria-label`, `backdrop-blur`, and `bg-token-background`. The evidence boundary is that this is a strong signal of debug modal UI/status contraction or restructuring, but it does not prove that the underlying capabilities were removed.

### [Confirmed] [macOS+Windows] Computer Use Runtime Added No New Dependency Packages, with Stable Size Metrics

On macOS, `cua_node` totals `365.8 MB → 365.8 MB (+0 MB, +0%)`, with `0` newly added dependency packages. On Windows, `cua_node` totals `285.5 MB → 285.5 MB (+0 MB, +0%)`, also with `0` newly added dependency packages.

Neither platform shows newly added Computer Use runtime dependencies. The runtime totals differ between macOS and Windows, so they should be cited separately rather than merged into one cross-platform size number.

## 🔎 Low-Level Signal Index

### Frontend Modules and UI State Signals

- [Signal] [macOS only] `codex-mobile-setup-dialog`: added upgrade prompt text and the `max-w-[280px]` class name, pointing to a changed prompt state on the pairing wait page.
- [Signal] [macOS only] `debug-modal`: shrank sharply and removed multiple debug, host-allowance, account, and conversation-related strings, pointing to debug UI restructuring or contraction.

These clues come from same-name frontend module diff sets and should only be read as UI, state, or interaction-direction signals.

### Plugin and Packaging Metadata

- [Confirmed] [macOS only] `Contents/Resources/plugins/openai-bundled/plugins/browser/.codex-plugin/plugin.json`: plugin version updated to `26.609.71144`.
- [Confirmed] [macOS only] `Contents/Resources/plugins/openai-bundled/plugins/chrome/.codex-plugin/plugin.json`: plugin version updated to `26.609.71144`.

The fact package does not list newly bundled plugin details, nor does it show new plugin service entries, skill instructions, default prompts, or largest-file changes.

### Windows Sample Boundary

- [Confirmed] [Windows only] Windows file count is `11972 → 11972`, with `0` added, `0` removed, and `0` content-modified files.
- [Confirmed] [Windows only] app.asar internal files are `2815 → 2815`, with `0` added, `0` removed, and `0` modified.

Therefore, this input sample has no content changes on Windows. This is not equivalent to claiming that Windows users received no changes in other batches.

## 📊 Raw Data

### macOS arm64

- Version: `26.609.41114` build `3888` → `26.609.71144` build `3952`
- Previous full package SHA-256: `6f69d0de7800bc7f48cda0f87810068f109fe6ecf839df8dec0d093cb23ad362`
- Latest full package SHA-256: `3086eaa17630ea2a20cb35ea6cae31c69488f8fd29d1f08cb2aaa7c41ea38dbb`
- Notarization: both previous and latest are `accepted`, source `Notarized Developer ID`, origin `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`
- Bundle files: `8298 → 8298`
- Bundle file changes: added `0` / removed `0` / content modified `61`
- `app.asar`: `150.7 MB → 150.7 MB (+0.00 MB)`
- app.asar internal files: `2870 → 2870`, added `11` / removed `11` / modified `12`
- Computer Use runtime: `365.8 MB → 365.8 MB (+0 MB, +0%)`
- Newly added `cua_node` dependency packages: `0`

### Windows x64

- Version: `26.609.4994.0` build `26.609.4994.0` → `26.609.4994.0` build `26.609.4994.0`
- Previous full package SHA-256: `547618a744149221078a27febdfff65c924b46ff85ab2fe1595180e128be8d85`
- Latest full package SHA-256: `547618a744149221078a27febdfff65c924b46ff85ab2fe1595180e128be8d85`
- Bundle files: `11972 → 11972`
- Bundle file changes: added `0` / removed `0` / content modified `0`
- app.asar internal files: `2815 → 2815`, added `0` / removed `0` / modified `0`
- Computer Use runtime: `285.5 MB → 285.5 MB (+0 MB, +0%)`
- Newly added `cua_node` dependency packages: `0`

## ⚖️ Boundaries and Disclaimer

This is a third-party unofficial changelog and does not represent OpenAI’s official release notes. The analysis is based on reverse diffs of official signed distribution packages and does not redistribute binary contents.

[Confirmed] means the fact package contains verifiable file, manifest, plist, hash, statistic, or readable-string evidence. [Signal] only means that a compressed frontend bundle shows module, string, class-name, or size-direction clues; it is not source-level fact and does not guarantee user-visible availability.

If later official notes, more platform samples, or more complete delta-window data become available, this report should be revised against the newer evidence.

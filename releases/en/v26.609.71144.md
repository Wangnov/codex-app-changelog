
---
title: "Codex Desktop 26.609.71144"
version: "26.609.71144"
build: 3952
mac_version: "26.609.71144"
mac_build: 3952
win_version: "26.609.4994.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-15"
method: "Official package reverse diff(macOS Sparkle / Windows MSIX)"
summary: "The substantive changes in this batch are macOS-side: version/build and app.asar integrity updates, plus browser/chrome bundled plugin version sync; the Windows comparison package has no file content changes."
official_release_notes: false
---

# Codex Desktop 26.609.71144(macOS / Windows comparison)

> Third-party cross-platform changelog · Unofficial

This batch shows the macOS package moving to 26.609.71144 build 3952, while the Windows x64 comparison package remains at 26.609.4994.0 with no content changes.  
English one-liner: This batch is a macOS-side packaging/app update, while the Windows comparison package is unchanged.

## About This Report

This report is based on reverse diffs of two official signed distribution packages: full-package comparison from the macOS Sparkle channel, and MSIX package comparison for Windows. It only uses machine-extracted facts, with no source-code speculation or unverified feature claims.

Evidence is split into two levels: [Confirmed] means the claim comes from readable or reproducible evidence such as Info.plist, plugin manifests, dependency lists, or file statistics; [Signal] means weaker clues such as frontend module names or size shifts. The main changes in this batch are backed by readable files or file-level statistics; no standalone [Signal] item is used to assert functionality.

Platform labels mean: [macOS+Windows] indicates the same class of change appears on both platforms; [macOS only] means it is observed only in the macOS package; [Windows only] means it is observed only in the Windows package. The comparison scope is macOS 26.609.41114 build 3888 → 26.609.71144 build 3952, and Windows 26.609.4994.0 → 26.609.4994.0. The release date is recorded as the provided batch date, 2026-06-15.

## ✨ Highlights

### [Confirmed] [macOS only] macOS version and build advanced; the Windows comparison package did not follow

On macOS, `CFBundleShortVersionString` changed from `26.609.41114` to `26.609.71144`, and `CFBundleVersion` changed from `3888` to `3952`. The `ElectronAsarIntegrity/Resources/app.asar/hash` also changed from `3e01c3b6a7a8d792570153a7cca28b1cc6ab1271324195fb5ea4033e0ca9eef7` to `cbf25d7d293a4a2750d8fec4ee4b458ee3c1b48050673f24f699a8203dd1a50c`, showing that the macOS application archive was updated.

The Windows comparison package reports `26.609.4994.0` before and after, and both full packages have the same SHA-256: `547618a744149221078a27febdfff65c924b46ff85ab2fe1595180e128be8d85`. Its file-tree statistics show 0 added, 0 removed, and 0 content-changed files, so the macOS changes should not be treated as synced to Windows in this pair.

### [Confirmed] [macOS only] browser and chrome bundled plugin manifests were synced to the new macOS version

Two OpenAI bundled plugin manifests changed version on macOS:

- `browser` plugin: `26.609.41114` → `26.609.71144`
- `chrome` plugin: `26.609.41114` → `26.609.71144`

A manifest here means the plugin metadata file `.codex-plugin/plugin.json`. The visible diff only shows the version field changing; there is no visible semantic change in the description text. The Windows comparison package has no content changes, so there is no evidence of the corresponding plugin manifest update on Windows.

### [Confirmed] [macOS+Windows] Computer Use runtime dependency size stayed stable, with no new cua_node packages

On both platforms, the Computer Use runtime dependency set reported as `cua_node` stayed stable.

On macOS, total `cua_node` size is `365.8 MB → 365.8 MB (+0 MB, +0%)`, with 0 new packages. On Windows, total `cua_node` size is `285.5 MB → 285.5 MB (+0 MB, +0%)`, also with 0 new packages. There is no evidence that this batch introduced new Computer Use runtime dependencies on either platform.

### [Confirmed] [macOS only] app.asar has small internal file changes, but not enough evidence to reconstruct specific features

Inside macOS `app.asar`, the file count remains `2870 → 2870`, with `11 added / 11 removed / 12 changed`. The outer `app.asar` size is reported as `150.7 MB → 150.7 MB (+0.00 MB)`.

This shows that the macOS application layer was rebuilt or updated, but the fact package only includes a small amount of readable diff and explicitly warns that many changes under `webview/assets` are Vite build-hash renames. Apart from the browser/chrome plugin manifest version sync, this is not enough to claim specific frontend feature changes. On Windows, `app.asar` remains `2815 → 2815 (0 added / 0 removed / 0 changed)`, with no comparable change.

## 📊 Raw Data

| Item | macOS arm64 | Windows x64 |
| --- | --- | --- |
| Compared versions | `26.609.41114` build `3888` → `26.609.71144` build `3952` | `26.609.4994.0` → `26.609.4994.0` |
| Outer file count | `8298 → 8298` | `11972 → 11972` |
| Outer file changes | 0 added / 0 removed / 61 content-changed | 0 added / 0 removed / 0 content-changed |
| app.asar internal files | `2870 → 2870` | `2815 → 2815` |
| app.asar internal changes | 11 added / 11 removed / 12 changed | 0 added / 0 removed / 0 changed |
| app.asar size | `150.7 MB → 150.7 MB (+0.00 MB)` | No size-change entry |
| cua_node total size | `365.8 MB → 365.8 MB (+0 MB, +0%)` | `285.5 MB → 285.5 MB (+0 MB, +0%)` |
| New cua_node dependencies | 0 packages | 0 packages |

Key verification data:

| Item | macOS arm64 | Windows x64 |
| --- | --- | --- |
| Previous SHA-256 | `6f69d0de7800bc7f48cda0f87810068f109fe6ecf839df8dec0d093cb23ad362` | `547618a744149221078a27febdfff65c924b46ff85ab2fe1595180e128be8d85` |
| Latest SHA-256 | `3086eaa17630ea2a20cb35ea6cae31c69488f8fd29d1f08cb2aaa7c41ea38dbb` | `547618a744149221078a27febdfff65c924b46ff85ab2fe1595180e128be8d85` |
| Signature / verification | Both versions accepted; source is Notarized Developer ID; origin is OpenAI OpCo, LLC | The fact package does not provide Windows signature verification output |

## ⚖️ Boundaries and Notes

This is a third-party, unofficial reverse-diff changelog and does not represent official OpenAI release notes. It does not redistribute binaries; it only cites reproducible evidence such as file-level statistics, manifests, plists, and hashes.

[Signal] clues are not source-code facts. In this batch, hash renames and `webview/assets` build-output changes are not interpreted as feature additions or removals. Corrections are welcome if readers can provide official notes for the same batch or additional reproducible evidence.

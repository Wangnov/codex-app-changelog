---
title: "Codex Desktop 26.602.71036"
version: "26.602.71036"
build: 3685
previous_version: "26.602.40724"
previous_build: 3593
platform: "macOS arm64"
released: "2026-06-09"
compared_from: "2026-06-05"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This release is mainly a version sync with minor packaging content updates; no confirmable new user-facing features or dependency capability expansions were observed."
official_release_notes: false
---

# Codex Desktop `26.602.71036` (build 3685)

> Third-party changelog · Unofficial · Based on reverse-diffing official update packages

This is a very restrained minor release: the outer Bundle file count is unchanged, and the readable differences are mainly concentrated in version synchronization and changes to the integrity hash of `app.asar`.  
English one-liner: a small packaging/version-sync update with no directly evidenced new user-facing capability.

## About This Report

This is not an official changelog, but a **reverse-diff report based on officially distributed artifacts**. This comparison is a historical backfill: both versions have already fallen outside the official Sparkle incremental update window, so the method was changed to downloading and directly comparing the two official full packages.

The comparison scope is `26.602.40724` build `3593` (2026-06-05) → `26.602.71036` build `3685` (2026-06-09), on macOS arm64.

In terms of trustworthiness, both app versions passed Apple notarization and OpenAI Developer ID signature verification:

- Previous version: `accepted`, `source=Notarized Developer ID`, `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`
- Latest version: `accepted`, `source=Notarized Developer ID`, `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`

This report uses two evidence levels:

- **[Confirmed]**: Direct, human-readable evidence, such as `Info.plist`, plugin configuration, or signature verification results.
- **[Signal]**: Indirect evidence only, such as frontend module names or binary size changes; this type of information can point in a direction, but cannot reconstruct source-level intent from the artifact.

## ✨ Key Changes

### 1. Desktop App Version Bumped from `26.602.40724` to `26.602.71036` [Confirmed]

The version fields in `Info.plist` have been updated:

```diff
/CFBundleShortVersionString: '26.602.40724' → '26.602.71036'
/CFBundleVersion: '3593' → '3685'
```

This means this is a newly packaged official release build. However, based on the readable diff, there is currently insufficient evidence to say it introduces a new user interface or new capabilities.

### 2. Browser and Chrome Plugin Versions Synced with the Main App [Confirmed]

The `plugin.json` versions for the built-in Browser plugin and Chrome plugin were both updated from `26.602.40724` to `26.602.71036`:

```diff
"version": "26.602.40724" → "26.602.71036"
```

The observable point is that the plugin packages were re-released together with the main app. However, the fact package does not show changes to plugin descriptions, permissions, script entry points, or capability boundaries. Therefore, only version synchronization can be confirmed; no new browser capabilities can be inferred.

### 3. `app.asar` Content Changed Slightly, but No Specific Functional Intent Can Be Confirmed [Confirmed]

The `ElectronAsarIntegrity` hash in `Info.plist` changed:

```diff
0d60982859a8e93d9df7bd629e63cf70b2d83c482420bee78c3af36fb0bf5e67
→ 1eaacb8bd09c1647a501ac76f4718bc83badec23ad7160e1691ec161790790ab
```

The total number of internal app-layer files remains `2830 → 2830`, with `285` added, `285` removed, and `12` changed. The fact package explicitly states that many hash-bearing filename additions/removals under `webview/assets` and `.vite/build` are Vite build artifact renaming noise, and should not be used to infer new features.

Therefore, what can be confirmed here is that the resource package content changed; it cannot be confirmed that these changes correspond to any specific user-facing feature.

### 4. No New `cua_node` Dependency Capability Expansion Observed [Confirmed]

The number of newly added `cua_node` dependencies is `0`:

```text
新增 0 个包
```

This means the fact package does not show any new lower-level dependency capabilities being added in this release. In other words, there is no evidence that this version added new dependencies to fill capability gaps around PDF, OCR, image processing, browser automation, or similar areas.

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Previous Version | Latest Version | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 2075 | 2075 | 0 |
| Added files | - | - | 0 |
| Removed files | - | - | 0 |
| Files with content changes | - | - | 53 |

> Note: `app.asar` internal added/removed file counts are not included in this table, because the fact package states that these changes are mainly affected by Vite hash filename renaming and can easily be misread.

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 143.1 MB | 143.1 MB | +0.00 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/browser/scripts/browser-client.mjs` | 0.9 MB | 0.9 MB | +0.00 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/chrome/scripts/browser-client.mjs` | 0.9 MB | 0.9 MB | +0.00 MB |

### Newly Added Dependency List

| Scope | Number of newly added dependencies |
| --- | ---: |
| `cua_node` | 0 |

## 🔁 Reproducibility Information

| Artifact | URL | SHA-256 | Size | EdDSA signature verification | codesign / spctl notarization |
| --- | --- | --- | --- | --- | --- |
| Previous full package `26.602.40724` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.602.40724.zip` | `be1a6ae44613c3f440d4e3d342040ebe08f273f975a372e812082967dc0dc30d` | Not provided by the fact package | Not applicable: this comparison directly uses full packages | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest full package `26.602.71036` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.602.71036.zip` | `327851effb2411ffde830f44af32c3a7f785cfc72fd2a49fc2933c42ef0b5be5` | Not provided by the fact package | Not applicable: this comparison directly uses full packages | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Boundaries and Disclaimer

This report is not affiliated with OpenAI and is not official release notes.

This report does not redistribute any official binaries; it only publishes diff conclusions derived from officially distributed artifacts.

All [Signal] items do not constitute source-level facts. This report only describes what can be observed at the artifact level, and does not infer product intent unsupported by the fact package.

Corrections based on reproducible evidence are welcome.
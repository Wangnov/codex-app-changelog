
---
title: "Codex Desktop 26.602.71036"
version: "26.602.71036"
build: 3685
mac_version: "26.602.71036"
mac_build: 3685
win_version: "26.602.9276.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-07"
method: "官方包逆向 diff: macOS Sparkle, Windows MSIX"
summary: "This batch updates the bundled Browser and Chrome plugin versions on both platforms; Windows additionally carries Computer Use plugin and owl shell metadata updates, while frontend module signals diverge across sharing, settings, announcements, and diagnostics."
official_release_notes: false
---

# Codex Desktop 26.602.71036 macOS / Windows Comparison

> Third-party cross-platform changelog · Unofficial

The core of this batch is not broad file churn, but bundled plugin versioning, Windows runtime metadata, and packaging-state changes across several frontend surfaces.

本批次的重点不是大规模文件增减，而是内置插件版本、Windows runtime metadata，以及若干 frontend 功能面的跨平台差异。

## About This Report

This report is based on machine-extracted diffs from reverse-reconstructed official signed packages: macOS arm64 `26.602.40724` → `26.602.71036`, and Windows x64 MSIX `26.602.4764.0` → `26.602.9276.0`. The release date is the provided `2026-06-07`.

Evidence is split into two levels: `[Confirmed]` for plist, manifest, plugin metadata, install metadata, readable strings, dependency lists, and file-level diffs; `[Signal]` for same-name frontend modules, size movement, class names, and compressed bundle directionality. Platform ownership is marked as `[macOS+Windows]`, `[macOS only]`, or `[Windows only]`.

One source caveat: the Windows fact pack’s “Version and channel” section labels platform/architecture as `macOS arm64`, but the input section and task statement identify it as Windows x64 MSIX. This report attributes it according to `=== PLATFORM: Windows ===`. No unsupported CSP, AppxManifest, new-resource, or user-visible rollout claims are inferred.

## ✨ Highlights

### [Confirmed] [macOS+Windows] Browser and Chrome bundled plugins now align on 26.602.71036

On both platforms, the `browser` and `chrome` plugin manifests only change their version field: `26.602.40724` → `26.602.71036`. Their descriptions continue to point to the same capability surfaces: Browser for the in-app browser, local targets, screenshots, clicks, typing, and localhost workflows; Chrome for automation that depends on the user’s existing Chrome state.

The platform difference is only path and package layout: macOS uses `Contents/Resources/plugins/openai-bundled/plugins/...`, while Windows uses `resources/plugins/openai-bundled/plugins/...`. The fact packs do not show substantive changes to service entrypoints, skill text, or permission text for these two plugins, so this is best read as bundled plugin version alignment rather than a new capability surface.

### [Confirmed] [Windows only] Computer Use plugin manifest version moved with the package

On Windows, `resources/plugins/openai-bundled/plugins/computer-use/.codex-plugin/plugin.json` shows the `computer-use` plugin version moving from `26.602.40724` to `26.602.71036`. Its description says it controls Windows desktop apps from Codex through Computer Use.

The macOS fact pack does not list a corresponding `computer-use` plugin manifest change, so this cannot be expanded into a cross-platform change. It is a clear plugin manifest update in the Windows sample.

### [Confirmed] [Windows only] owl shell install metadata refreshed, with no new cua_node packages shown

The Windows root `.installed-owl-shell.json` updates `installedAt` from `2026-06-05T16:55:50.166Z` to `2026-06-08T23:05:30.351Z`. The same diff retains `runtimeInfo.arch: x64` and `archiveSha: 76a82b584f412bc890db1bfffdc24456a772f47cae7324a4e22eff7bb838c86f`.

The fact pack only provides install metadata movement, not runtime-content or behavior differences. Therefore, the confirmed point is that the Windows package records a refreshed owl shell install timestamp. It does not prove a Computer Use runtime capability change. Both platforms report `0` newly added `cua_node` packages.

### [Signal] [macOS only] Appgen sharing dialog points to a fuller site-sharing state

On macOS, the same-name `appgen-share-dialog` frontend module grows by `19146 bytes` and adds readable strings including `Add people or groups`, `Anyone in this workspace with the link`, `Anyone on the internet`, `Can visit`, `Copy link`, `Copied to clipboard`, plus button labels for copying and externally opening a live site URL.

Because these are same-name frontend module clues, they are `[Signal]`, not source facts. Together, they point toward workspace user/group search, link permission scopes, access levels, and copy-link interactions inside the site sharing dialog. Windows moves in the opposite direction: the same module removes the same sharing strings and shrinks by `19146 bytes`, so this is not a shared cross-platform addition.

### [Signal] [macOS only] @ mention list gained agent, MCP, and desktop-app category signals

The macOS `at-mention-list` module grows by `22531 bytes` and adds strings including `Computer use`, `Custom agents`, `Desktop apps`, `Live agents`, `MCP servers`, `Mac apps`, and `No results`, alongside class names such as `mcp-capability`, `mcp-capability-mentions`, and `mcp-servers`.

This signal points to more granular mention-list groupings around agents, MCP servers, Computer Use, and desktop apps. The Windows fact pack does not include a same-name module change, so this can only be marked as a macOS sample signal, not a claim that Windows has or lacks the same UI.

### [Signal] [Windows only] Settings gained permission, font, environment, dictation, and popout-window clues

The Windows `general-settings` module grows by `90992 bytes`. Newly added strings cover the default permissions toggle, composer context usage indicator, work mode options, code font size, Codex UI font size, `Agent environment`, plus `hold-to-dictate hotkey capture input`, `hotkey window hotkey capture input`, `dictation dictionary entry`, and `Popout Window projectless default setting toggle`.

This indicates Windows-packaged settings UI signals for permissions, context usage visibility, work modes, font sizing, agent environment, dictation hotkeys/dictionary, and default popout-window behavior. macOS moves in the opposite direction, removing a shorter set of settings strings and shrinking by `93168 bytes`, so this should not be written as a shared settings addition.

### [Signal] [Windows only] Home announcements gained rate-limit, Fast mode, multi-agent, remote connection, and ambient suggestions banner clues

The Windows `home-announcements` module grows by `29639 bytes`. Newly added strings include Codex rate limit reset, Fast mode, backend-driven Codex app home banner, multi-agent composer banner, remote connections banner, `Add Connections`, ambient suggestions connected apps banner, a button to disable ambient suggestions, and a button that opens the rate limit resets modal.

This signal clusters around home banners and announcements: rate limit resets, Fast mode, multi-agent composer, remote connections, connection entrypoints, and ambient suggestions. The macOS same-name module removes the corresponding banner strings and shrinks by `29639 bytes`, so the two platform samples diverge sharply here.

### [Signal] [Windows only] Debug modal gained app-state, browser-state, and session diagnostics clues

The Windows `debug-modal` module grows by `176718 bytes`. Newly added strings include `(command unavailable)`, `Account ID`, `Active browser tab`, `Active conversation`, `Allow automatic worktree cleanup in this unpackaged build.`, `Allow host`, `Allow host starting`, `Already current`, `Ambient suggestion chats`, `App Actions`, `App Server`, and `App session ID`.

This points to richer diagnostic/status text packaged into the Windows debug modal, covering command availability, account, active browser tab, current conversation, host allowlisting, app service, and session ID. The macOS same-name module removes part of this string family and shrinks by `178828 bytes`, so this is a Windows-only signal in the provided samples.

### [Signal] [macOS+Windows] Worktree init v2 removed local-environment failure recovery copy on both platforms

Both platforms’ `worktree-init-v2-page` same-name frontend modules remove strings related to worktree creation and local environment failure recovery, including `Continue anyway`, opening local environment settings, starting a repair thread, canceling worktree setup and starting a local conversation, and the `Creating a worktree...` status copy.

This is a shared frontend signal. The macOS module shrinks by `6963 bytes`, while Windows shrinks by `4964 bytes`. Because the evidence comes from same-name frontend module clues, it does not prove the worktree init flow was removed; it only shows those UI strings/states disappearing from the corresponding bundle diffs.

### [Confirmed] [macOS only] Info.plist version, build, and asar integrity hash updated

macOS `Info.plist` shows `/CFBundleShortVersionString` moving from `26.602.40724` to `26.602.71036`, and `/CFBundleVersion` from `3593` to `3685`. `/ElectronAsarIntegrity/Resources/app.asar/hash` changes from `0d60982859a8e93d9df7bd629e63cf70b2d83c482420bee78c3af36fb0bf5e67` to `1eaacb8bd09c1647a501ac76f4718bc83badec23ad7160e1691ec161790790ab`.

The Windows fact pack does not provide AppxManifest changes, so no Windows manifest field is inferred.

## 🔎 Low-Level Signal Index

### Sharing and Site Access Control

[Signal] [macOS only] `appgen-share-dialog` adds workspace users/groups search, people/groups addition, workspace link, internet link, Can visit, Copy link, and external live-site opening strings. Evidence boundary: these strings entered the macOS frontend bundle; they do not prove broad user availability.

[Signal] [Windows only] `appgen-share-dialog` removes the same family of sharing strings. Evidence boundary: this only says the same-name Windows module no longer contains these diff strings in this sample; it does not prove the Windows product line permanently lacks the capability.

### Mentions, Agents, and MCP

[Signal] [macOS only] `at-mention-list` adds `Computer use`, `Custom agents`, `Desktop apps`, `Live agents`, `MCP servers`, `Mac apps`, and `mcp-capability` class names. Evidence boundary: this is UI signal for mention-list categories and capability entrypoints, not backend capability proof.

### Settings, Input, and Window Behavior

[Signal] [Windows only] `general-settings` adds strings for default permissions, composer context usage indicator, work mode, code/UI font size, Agent environment, hold-to-dictate, hotkey window, dictation dictionary, and Popout Window projectless default setting. Evidence boundary: this is settings-page UI signal, not proof that every setting is operable.

[Signal] [macOS only] `general-settings` removes a shorter set covering default permissions, context usage indicator, work mode, font sizing, and Agent environment. Evidence boundary: this only describes the removal direction in the module diff, not product strategy.

### Home Announcements and Remote Connections

[Signal] [Windows only] `home-announcements` adds rate limit reset, Fast mode, backend-driven banner, multi-agent composer, remote connections, `Add Connections`, ambient suggestions, and related dismiss/disable/open button copy. Evidence boundary: these are home-banner strings entering the bundle, not proof that the banners always display.

[Signal] [macOS only] `home-announcements` removes corresponding banner copy. Evidence boundary: this only reflects the macOS bundle diff direction in the input sample.

### Debugging and Diagnostics

[Signal] [Windows only] `debug-modal` adds account, active browser tab, active conversation, automatic worktree cleanup, host allow, App Actions, App Server, and App session ID strings. Evidence boundary: this shows debug-modal-related text entering the Windows bundle, not that it is a public user entrypoint.

[Signal] [macOS only] `debug-modal` removes part of the command, account, active browser tab, active conversation, and host allow string set. Evidence boundary: same-name frontend module diff only.

### Worktree Init

[Signal] [macOS+Windows] `worktree-init-v2-page` removes `Continue anyway`, local environment settings, repair thread, cancel setup then start local conversation, and worktree creation status copy on both platforms. Evidence boundary: this is UI-copy shrinkage, not proof of flow deletion.

## 📊 Raw Data

### macOS arm64

- Version: `26.602.40724` build `3593` → `26.602.71036` build `3685`
- Distribution and verification: two official full packages compared directly; previous SHA-256 `be1a6ae44613c3f440d4e3d342040ebe08f273f975a372e812082967dc0dc30d`; latest SHA-256 `327851effb2411ffde830f44af32c3a7f785cfc72fd2a49fc2933c42ef0b5be5`; both notarization checks returned `accepted`, source `Notarized Developer ID`, origin `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`
- Bundle tree: `2075 → 2075`; added `0`, removed `0`, content changed `53`
- `app.asar`: `143.1 MB → 143.1 MB`, delta `+0.00 MB`
- Internal `app.asar` files: `2830 → 2830`; added `285`, removed `285`, changed `12`
- `cua_node` dependencies: `0` newly added packages
- Size-movement components: `browser-client.mjs` in both browser and chrome plugins stays `0.9 MB → 0.9 MB`, delta `+0.00 MB`
- Key frontend modules: `appgen-share-dialog` `+19146 bytes`; `at-mention-list` `+22531 bytes`; `debug-modal` `-178828 bytes`; `general-settings` `-93168 bytes`; `home-announcements` `-29639 bytes`; `worktree-init-v2-page` `-6963 bytes`
- Info.plist: short version `26.602.40724 → 26.602.71036`; build `3593 → 3685`; asar integrity hash updated

### Windows x64

- Version: `26.602.4764.0` build `26.602.4764.0` → `26.602.9276.0` build `26.602.9276.0`
- Distribution and verification: two full packages from mirror releases; previous SHA-256 `cf9aa8d704a322efb9ee46cedc2b245b2d7bee517cacade2a635a09323d953ca`; latest SHA-256 `0e36b99846ec66685f8a44e5da678bd9332f369becd809952ddbbb13ff7341ac`
- Bundle tree: `8724 → 8724`; added `0`, removed `0`, content changed `15`
- `resources/app.asar`: `146.9 MB → 146.9 MB`, delta `+0.00 MB`
- Internal `app.asar` files: `2778 → 2778`; added `285`, removed `285`, changed `11`
- `cua_node` dependencies: `0` newly added packages
- Size-movement components: `browser-client.mjs` in both chrome and browser plugins stays `0.9 MB → 0.9 MB`, delta `+0.00 MB`
- Key frontend modules: `debug-modal` `+176718 bytes`; `general-settings` `+90992 bytes`; `home-announcements` `+29639 bytes`; `appgen-share-dialog` `-19146 bytes`; `worktree-init-v2-page` `-4964 bytes`
- Plugin and runtime metadata: `computer-use`, `chrome`, and `browser` plugin manifests update to version `26.602.71036`; `.installed-owl-shell.json` updates `installedAt` to `2026-06-08T23:05:30.351Z`

## ⚖️ Boundaries and Disclaimer

This is a third-party, unofficial changelog based on reverse diffs of official signed distribution packages. It is not OpenAI official release notes.

This report does not redistribute binaries, and it does not treat hash-named `webview/assets` or `.vite/build` renames as feature additions/removals. The fact packs do not provide CSP changes, Windows AppxManifest changes, or new-resource details, so those are not invented here.

All `[Signal]` items only mean that the corresponding strings, module names, class names, or size directions entered or left the packaged artifacts. They are not source facts and do not prove that a feature is visible to all users. Corrections based on additional official packages, runtime observation, or reproducible experiments are welcome.


---
title: "Codex Desktop 26.325.31654"
version: "26.325.31654"
build: 1272
previous_version: "26.325.21211"
previous_build: 1255
platform: "macOS arm64"
released: "2026-03-28"
compared_from: "2026-03-26"
method: "Sparkle 增量包重建 + 结构化 bundle 差异"
summary: "This release is mainly a small version bump with limited bundle content changes, with no observed added files, removed files, or new cua_node dependencies."
official_release_notes: false
---

# Codex Desktop `26.325.31654` build 1272

> Third-party changelog · Unofficial · Based on reverse diffing the official update artifacts

This is a narrow macOS arm64 update: the version and build advanced, the bundle file count stayed unchanged, and only a small set of contents changed.  
One-liner: a narrowly scoped desktop build bump with minimal observable bundle-level changes.

## About This Report

This is not an official changelog. It is a **reverse-diff report based on official distribution artifacts**. The comparison covers macOS arm64 from `26.325.21211` build `1255` to `26.325.31654` build `1272`, released on `2026-03-26` and `2026-03-28`.

The fact package states that this is a historical backfill: the official appcast did not provide a delta package for this version pair, so the two official full packages were downloaded and compared directly. This report only uses structured bundle diffs, Info.plist data, file statistics, size changes, dependency changes, and verification results.

Evidence is marked in two levels: **[Confirmed]** means there is direct human-readable evidence, such as Info.plist keys, dependency lists, signatures, or verification output; **[Signal]** means the evidence is indirect, such as frontend module names or binary size changes, pointing in a direction but not recoverable into source-level intent.

## ✨ Highlights

### 1. Version advanced from `26.325.21211` to `26.325.31654` [Confirmed]

The app short version and build both changed: `CFBundleShortVersionString` moved from `26.325.21211` to `26.325.31654`, and `CFBundleVersion` moved from `1255` to `1272`.

This indicates a formally packaged new macOS arm64 desktop build, not just a resigning or external metadata change.

```diff
CFBundleShortVersionString: 26.325.21211 -> 26.325.31654
CFBundleVersion: 1255 -> 1272
```

### 2. The bundle tree stayed mostly stable, with only limited content changes [Confirmed]

The top-level bundle file count stayed at `414 → 414`, with `0` added files, `0` removed files, and `39` content-modified files. The app-layer `app.asar` internal file count also stayed at `806 → 806`, with `0` added, `0` removed, and `6` modified.

At the file-structure level, this update is very constrained. No new component, new resource directory, or broad application-layer reshuffle is observed. Hash-named changes under `webview/assets` and `.vite/build` are treated as build noise and are not used as evidence of feature additions or removals.

### 3. The main `codex` component grew only slightly [Signal]

The largest non-noise component size change is `Contents/Resources/codex`, which increased from `148.2 MB` to `148.3 MB`, a `+0.05 MB` change.

This points to a small adjustment in underlying executable or resource content, but it cannot be recovered from the artifact into source-level intent. This size change alone does not establish any specific feature, performance, or behavior change.

### 4. No new cua_node dependency was observed [Confirmed]

The cua_node dependency diff in the fact package reports `0` newly added packages.

This means the update did not expand the directly observable runtime dependency surface through new cua_node packages. Any user-visible change is more likely to come from modifications to existing files rather than newly introduced dependencies.

### 5. The `app.asar` integrity hash changed [Confirmed]

The `ElectronAsarIntegrity/Resources/app.asar/hash` value in Info.plist changed from `eb71a16a54eef07f5c843b16915673e18c0cde132335bc8758e30c17db3ad46a` to `6744a67925299872f539233eab9a361abcccfd25a26a4d662e34e648efa0f91f`.

This matches the `6` modified files inside `app.asar`, confirming that the Electron application-layer artifact did change. The fact package does not provide readable semantics for those changes, so this report does not infer feature intent from them.

## 📊 Raw Data

### Bundle File Statistics

| Metric | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 414 | 414 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Content-modified files | 0 | 39 | 39 |

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/codex` | 148.2 MB | 148.3 MB | +0.05 MB |

### Newly Added Dependencies

| Dependency scope | Added count | Details |
| --- | ---: | --- |
| cua_node | 0 | No new packages |

## 🔁 Reproducibility

| Artifact | URL | SHA-256 | Size | EdDSA verification | codesign / spctl notarization result |
| --- | --- | --- | --- | --- | --- |
| Previous full package `26.325.21211` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.325.21211.zip` | `274e3f9bc171494629285b24daa054bf9eebb551cf8c1dcfcf56fb2482bcc2b7` | Not provided in fact package | Not provided in fact package | `CSSMERR_TP_CERT_REVOKED` |
| Latest full package `26.325.31654` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.325.31654.zip` | `e19a9397f8ba1bc0116bf1514b3ec70e09a5e3e7361d3d5ec5f6ec53ae9c3ff7` | Not provided in fact package | Not provided in fact package | `CSSMERR_TP_CERT_REVOKED` |

Some earlier versions had signing certificates revoked later, which causes verification to show `CSSMERR_TP_CERT_REVOKED`. This affects whether the version can be safely installed by the system, but it does not affect bundle content diff analysis.

## ⚖️ Boundaries And Disclaimer

This report is not affiliated with OpenAI and does not represent an official position or official release notes.

It does not redistribute any official binaries; it only publishes diff conclusions derived from official distribution artifacts.

All [Signal] items are indirect artifact-level observations and do not constitute source-level facts. Corrections are welcome.

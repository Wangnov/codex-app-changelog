---
title: "Codex Desktop 26.325.31654"
version: "26.325.31654"
build: 1272
previous_version: "26.325.21211"
previous_build: 1255
platform: "macOS arm64"
released: "2026-03-28"
compared_from: "2026-03-26"
method: "官方全量包对比 + 结构化 bundle 差异"
summary: "This release has almost no visible functional changes, consisting mainly of version updates, the app.asar integrity hash, and a small amount of content refresh."
official_release_notes: false
---

# Codex Desktop `26.325.31654` (build 1272)

> Third-party changelog · Unofficial · Based on reverse-engineered differences in official update packages

This is a very small macOS arm64 desktop update: the total file count is unchanged, no dependencies were added, and only a small amount of bundle content changed.  
English one-liner: a minimal maintenance-style build with no clear new user-facing capability exposed by the artifact diff.

## About This Report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. According to the fact pack, this is a historical backfill: both versions are already outside the official Sparkle delta-update window, so the comparison uses two official full packages directly rather than reconstructing a delta package.

The comparison covers `26.325.21211` build `1255`, released on `2026-03-26`, through `26.325.31654` build `1272`, released on `2026-03-28`.

In terms of confidence, the fact pack provides SHA-256 hashes for both official full packages as well as notarization verification results. Verification for both versions returned `CSSMERR_TP_CERT_REVOKED`. The fact pack also notes that signing certificates for some early versions were later revoked. This affects the system’s security assessment for installation, but does not affect bundle content diff analysis.

This report uses two evidence levels:

- **[Confirmed]**: Direct human-readable evidence from Info.plist, dependency lists, signing / notarization results, structured diffs, and similar sources.
- **[Signal]**: Indirect evidence from frontend module names, binary size changes, and similar sources; it only indicates “points in a certain direction, but cannot be reconstructed from the artifact into source-level intent.”

## ✨ Key Changes

### 1. Version and Build Number Updated [Confirmed]

This release updates the short version from `26.325.21211` to `26.325.31654`, and the build number from `1255` to `1272`.

This indicates that it is an officially incremented desktop build, but the fact pack provides no official release notes and contains no directly nameable evidence of new functionality.

Key evidence:

```diff
CFBundleShortVersionString: 26.325.21211 → 26.325.31654
CFBundleVersion: 1255 → 1272
```

### 2. Small App-Layer Content Changes, but No Files Added or Removed [Confirmed]

The internal file count of `app.asar` remained `806 → 806`, with `0` added, `0` removed, and `6` changed.

This means the application layer did receive content updates, but not through the addition of large batches of modules or the removal of old ones. Because the fact pack does not list the semantic contents of these 6 changed files, no more specific functional change can be inferred.

Key evidence:

```text
app.asar: 806 → 806
新增 0 / 移除 0 / 改动 6
```

### 3. Main Program Component Shows Only a Slight Size Increase [Signal]

`Contents/Resources/codex` increased from `148.2 MB` to `148.3 MB`, a change of `+0.05 MB`.

This is an indirect signal: it points to a small update in the main program content, but it cannot be reconstructed from the artifact into source-level intent, nor can it justify claiming that any specific capability was added.

Key evidence:

| Component Path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/codex` | 148.2 MB | 148.3 MB | +0.05 MB |

### 4. No New cua_node Dependencies [Confirmed]

The fact pack shows `0` newly added packages under `cua_node`.

This means the diff contains no evidence of capability expansion through newly added runtime dependencies; for example, this cannot support claims of newly added PDF, OCR, image processing, or browser automation capabilities.

Key evidence:

```text
cua_node 新增 0 个包
```

### 5. Bundle File Tree Remains Stable [Confirmed]

The overall bundle file count remained `414 → 414`, with `0` added, `0` removed, and `39` content changes.

From a user- or developer-visible perspective, this looks more like a small-scope maintenance update than a structural feature expansion. The fact pack also specifically warns that hashed filename changes under `webview/assets` and `.vite/build` are build artifact noise and should not be described as added or removed functionality.

Key evidence:

```text
文件数: 414 → 414
新增 0 / 移除 0 / 内容改动 39
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Value |
| --- | ---: |
| Previous version file count | 414 |
| Latest version file count | 414 |
| Added files | 0 |
| Removed files | 0 |
| Content changes | 39 |

### Key Component Size Changes

| Component Path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/codex` | 148.2 MB | 148.3 MB | +0.05 MB |

### Added Dependency List

| Scope | Number of Added Dependencies |
| --- | ---: |
| `cua_node` | 0 |

## 🔁 Reproducibility Information

| Artifact | URL | SHA-256 | Size | EdDSA Verification Result | codesign / spctl Result |
| --- | --- | --- | --- | --- | --- |
| Previous full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.325.21211.zip` | `274e3f9bc171494629285b24daa054bf9eebb551cf8c1dcfcf56fb2482bcc2b7` | 事实包未提供 | Not applicable: this comparison directly uses full packages, and the fact pack does not provide EdDSA results | `CSSMERR_TP_CERT_REVOKED` |
| Latest full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.325.31654.zip` | `e19a9397f8ba1bc0116bf1514b3ec70e09a5e3e7361d3d5ec5f6ec53ae9c3ff7` | 事实包未提供 | Not applicable: this comparison directly uses full packages, and the fact pack does not provide EdDSA results | `CSSMERR_TP_CERT_REVOKED` |

## ⚖️ Scope and Disclaimer

This report is not affiliated with OpenAI, does not represent OpenAI’s official position, and is not an official release note.

This report does not redistribute any official binaries; it only publishes diff conclusions based on officially signed distribution artifacts.

All [Signal] items indicate only indirect artifact-level evidence. They are not source-level facts and do not represent official product intent.

If more complete official notes, reproducible verification materials, or fact pack corrections become available later, corrections based on them are welcome.
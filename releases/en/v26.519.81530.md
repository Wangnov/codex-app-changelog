
---
title: "Codex Desktop 26.519.81530"
version: "26.519.81530"
build: 3178
mac_version: "26.519.81530"
mac_build: 3178
win_version: "26.519.11010.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-05-27"
method: "official package reverse diff(macOS Sparkle / Windows MSIX)"
summary: "The verifiable changes in this batch are concentrated on macOS: version 26.519.81530, build 3178, and a small core executable size increase; the compared Windows package shows no content change."
official_release_notes: false
---

# Codex Desktop 26.519.81530(macOS / Windows Comparison)

> Third-party cross-platform changelog · Unofficial

The main conclusion is straightforward: the macOS package moved forward, while the compared Windows x64 package did not show observable content changes.  
One-liner: this batch is a macOS-side package update, while the compared Windows MSIX stayed byte-identical.

## About This Report

This report is based on reverse diffs of officially signed distribution packages: full-package Sparkle comparison on macOS, and MSIX package comparison on Windows. The macOS comparison covers 26.519.41501 build 3044 → 26.519.81530 build 3178. The Windows comparison covers 26.519.11010.0 → 26.519.11010.0.

Evidence is split into two levels: [Confirmed] means readable evidence from plist, AppxManifest, CSP, skill docs, type declarations, package.json, dependency manifests, or similar files; [Signal] means only module names or size movement are visible, which can indicate direction but cannot recover source intent.

Platform labels mean: [macOS+Windows] was observed on both platforms; [macOS only] was observed only in the macOS package; [Windows only] was observed only in the Windows package.

## ✨ Highlights

### [Confirmed] [macOS only] macOS Version and Build Updated

The macOS package moved from `26.519.41501` to `26.519.81530`, and `CFBundleVersion` moved from `3044` to `3178`. The `ElectronAsarIntegrity` hash for `app.asar` also changed, confirming that the application archive content was replaced.

The platform difference is clear: the Windows comparison package remains `26.519.11010.0`, and the previous and latest Windows SHA-256 values are identical, so no corresponding content update is visible there.

### [Confirmed] [macOS only] Bundled Browser and Chrome Plugin Versions Synced to the New macOS Version

Two bundled macOS plugin `plugin.json` files updated their versions:

- `browser` plugin: `26.519.41501` → `26.519.81530`
- `chrome` plugin: `26.519.41501` → `26.519.81530`

The fact package does not show semantic changes in the Browser or Chrome plugin descriptions. The confirmed point is that their metadata versions were synced with the new macOS package.

Windows shows no corresponding change: file-tree content changes are `0`, and internal `app.asar` changes are also `0`.

### [Signal] [macOS only] Core Executable Components Grew Slightly

Two same-sized macOS core components grew from `192.1 MB` to `193.8 MB`, a `+1.70 MB` change:

- `Contents/Resources/codex`
- `Contents/Resources/plugins/openai-bundled/plugins/chrome/app-server-runtime/codex`

This is a size signal: the binary content grew, but the fact package does not provide source-level explanation, so this report cannot assert which capability changed.

Windows has no comparable size movement in the provided facts, and its content-change count is `0`.

### [Confirmed] [macOS+Windows] No Confirmed Shared Functional Change in This Round

After cross-platform comparison, there is no readable configuration, dependency, or application-layer change that appears on both macOS and Windows. macOS has 61 bundle content changes, while Windows has 0 content changes.

That means the substantive changelog entries for this batch are macOS-centered; the Windows MSIX represented in this fact package has not yet reflected the same batch of updates.

### [Confirmed] [macOS+Windows] No New cua_node Packages

Both platform fact packages report `0` newly added `cua_node` packages. This only confirms that no new `cua_node` dependency package was observed in this comparison; it does not rule out other internal implementation changes.

## 📊 Raw Data

| Item | macOS arm64 | Windows x64 |
| --- | --- | --- |
| Previous version | `26.519.41501` build `3044` | `26.519.11010.0` build `26.519.11010.0` |
| Latest version | `26.519.81530` build `3178` | `26.519.11010.0` build `26.519.11010.0` |
| File count | `1335 → 1335` | `831 → 831` |
| Added / removed / content changed | `0 / 0 / 61` | `0 / 0 / 0` |
| Internal app.asar files | `1577 → 1577` | `1531 → 1531` |
| app.asar added / removed / changed | `409 / 409 / 10` | `0 / 0 / 0` |
| New cua_node packages | `0` | `0` |
| Key SHA-256 | Latest `4c11416391b0d12f2739784db7c102481f7610888d34cf09140f2928b52952f4` | Previous and latest both `b02ee75756074f77965178be4110b2082c5f0b1c2f1b1e04ff26afbde086f5d7` |

Key macOS size changes:

| Component path | Old | New | Change |
| --- | --- | --- | --- |
| `Contents/Resources/plugins/openai-bundled/plugins/chrome/app-server-runtime/codex` | `192.1 MB` | `193.8 MB` | `+1.70 MB` |
| `Contents/Resources/codex` | `192.1 MB` | `193.8 MB` | `+1.70 MB` |
| `Contents/Resources/app.asar` | `159.0 MB` | `159.0 MB` | `+0.00 MB` |
| `Contents/Resources/plugins/openai-bundled/plugins/browser/scripts/browser-client.mjs` | `0.9 MB` | `0.9 MB` | `+0.00 MB` |
| `Contents/Resources/plugins/openai-bundled/plugins/chrome/scripts/browser-client.mjs` | `0.9 MB` | `0.9 MB` | `+0.00 MB` |

Key Windows size changes: the fact package lists no non-zero size changes.

## ⚖️ Boundaries and Notice

This is an unofficial third-party changelog and does not represent official OpenAI release notes.

This report does not redistribute binaries. It summarizes machine-extracted facts from officially signed distribution artifacts.

Items marked [Signal] are not source facts; they are reverse-engineering observations only. Corrections are welcome if stronger official notes or reproducible evidence are available.

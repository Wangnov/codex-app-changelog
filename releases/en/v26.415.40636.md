---
title: "Codex Desktop 26.415.40636"
version: "26.415.40636"
build: 1799
previous_version: "26.415.32059"
previous_build: 1789
platform: "macOS arm64"
released: "2026-04-18"
compared_from: "2026-04-18"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This is a very small same-day build update. Apart from the version number, build number, and app.asar integrity hash, no direct evidence was observed that can be summarized as a user- or developer-perceptible functional change."
official_release_notes: false
---

# Codex Desktop `26.415.40636` (build 1799)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

This release is a small same-day build update from `26.415.32059` to `26.415.40636`: the file count remains unchanged, and no evidence was found of new dependencies, permission surface, or clearly identifiable new functionality.  
English one-liner: a small same-day build update with no clear user-facing feature delta in the extracted facts.

## About This Report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. The fact package for this release explicitly states that, because both versions fall outside the official Sparkle incremental update window, this comparison was not reconstructed from incremental packages. Instead, the two official full packages were downloaded directly, compared, and used to generate a structured bundle diff.

In terms of trust, the trust root for this analysis comes from Apple notarization and OpenAI Developer ID code-signing verification. The `Codex.app` bundles for both the previous and latest versions passed `spctl` notarization verification, with the result `accepted`, source `Notarized Developer ID`, and signing origin `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

Evidence levels are divided into two tiers:

- **[Confirmed]**: Direct human-readable evidence, such as `Info.plist` key values, signature verification results, file-level statistics, and dependency lists.
- **[Signal]**: Indirect evidence only, such as frontend module names or binary size changes. This type of evidence can only indicate that something points in a certain direction, but cannot reconstruct source-level intent from the artifact.

Comparison scope: from `26.415.32059` build `1789`, released on `2026-04-18`, to `26.415.40636` build `1799`, released on `2026-04-18`. Platform: `macOS arm64`.

## ✨ Key Changes

### 1. Version and build advanced to `26.415.40636` / build `1799` [Confirmed]

The latest version updates `CFBundleShortVersionString` from `26.415.32059` to `26.415.40636`, and `CFBundleVersion` from `1789` to `1799`.

This indicates that this is a new officially signed build. However, based solely on the readable evidence in the fact package, it is not possible to further infer any new user features, permission changes, or developer API changes.

Key evidence:

```diff
/CFBundleShortVersionString: '26.415.32059' → '26.415.40636'
/CFBundleVersion: '1789' → '1799'
```

### 2. The main application content changed slightly, but there is no identifiable evidence of new capabilities [Confirmed]

The total number of bundle files remains `498 → 498`, with no added or removed files and `40` files changed in content. Inside the application-level `app.asar`, the internal file count remains `1000 → 1000`; machine statistics show `1` added, `1` removed, and `5` changed.

These changes show that the main application content did in fact change. However, the fact package does not provide direct evidence at the level of new module names, dependencies, interfaces, permissions, or documentation, so this should not be described as the launch of any specific feature.

Key evidence:

```text
Bundle file count: 498 → 498
Added 0 / Removed 0 / Content changed 40

Internal files in app.asar: 1000 → 1000
Added 1 / Removed 1 / Changed 5
```

### 3. The `app.asar` integrity hash was updated, consistent with validation after content changes [Confirmed]

The `app.asar` hash recorded under `ElectronAsarIntegrity` changed.

This means the application resource package differs from the previous version. However, the hash itself only proves that the content is different; it does not explain the product intent or user-visible behavior behind the change.

Key evidence:

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
'7aa7db298d9b72c91594737c441e2273f6b057b2f9d15d30a6d2f6121434dd3c'
→
'6b92163fce92a3896545d37a0a06abf4654c9bfbb46ed6ba0fdabac775f423a2'
```

### 4. No new dependencies or expanded capability surface were found [Confirmed]

The `cua_node` dependency changes show `0` newly added packages. The fact package also does not list any largest newly added files.

This means that, at least from this structured dependency list, there is no dependency evidence directly indicating expanded capabilities such as PDF, OCR, image processing, or browser automation.

Key evidence:

```text
cua_node added 0 packages
Largest newly added files: no excerpt content
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Previous version | Latest version | Change |
| --- | ---: | ---: | ---: |
| File count | 498 | 498 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Content changed | - | - | 40 |

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 111.5 MB | 111.5 MB | +0.00 MB |

### Added Dependency List

| Dependency source | Added count | Notes |
| --- | ---: | --- |
| `cua_node` | 0 | No newly added packages found |

## 🔁 Reproducibility Information

| Artifact / Verification item | Result |
| --- | --- |
| Previous full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.415.32059.zip` |
| Previous SHA-256 | `8d2cdf5a922f0820acc1903c2b4006c1cb2ffdeed0d53bdf0607ec4b3183bd97` |
| Latest full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.415.40636.zip` |
| Latest SHA-256 | `d382ab435b981d508f488167859dedab70601366ffa9c85050e81943908b5e9e` |
| EdDSA signature verification | Not applicable; the fact package states that this release was historically backfilled and compared directly using the two official full packages |
| Previous notarization verification | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest notarization verification | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| codesign conclusion | Trust root is OpenAI Developer ID code signing |
| spctl notarization conclusion | Both the previous and latest versions passed, with result `accepted` |

## ⚖️ Boundaries and Disclaimer

This report is not affiliated with OpenAI, does not represent an official position, and is not official release notes.

This report does not redistribute any official binary files. It only publishes diff conclusions derived from officially signed distribution artifacts.

The fact package explicitly notes that filename additions and removals involving hashed filenames under `webview/assets` and `.vite/build` are build-artifact renaming noise. This report does not describe those changes as added or removed functionality.

No [Signal] items constitute source-level facts. This report does not use any [Signal]-level items to infer specific functional changes. Corrections based on the same fact package or more complete readable evidence are welcome.
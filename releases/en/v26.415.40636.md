
---
title: "Codex Desktop 26.415.40636"
version: "26.415.40636"
build: 1799
previous_version: "26.415.32059"
previous_build: 1789
platform: "macOS arm64"
released: "2026-04-18"
compared_from: "2026-04-18"
method: "Official full-package comparison + structured bundle diff"
summary: "This historical backfill shows a version and app.asar integrity-hash update, while the bundle tree, key payload size, and cua_node dependency surface remain stable."
official_release_notes: false
---

# Codex Desktop `26.415.40636` build 1799

> Third-party changelog · Unofficial · Based on reverse diffing official update artifacts

This release looks like a narrow app-layer refresh: the confirmed public changes are the version number, build number, and `app.asar` integrity hash, with no confirmed expansion in dependencies or bundle-level components.

## About This Report

This is not an official changelog. It is a **reverse diff report based on official distribution artifacts**. This entry is a historical backfill: the official appcast did not provide a delta package for `26.415.32059` → `26.415.40636`, so the comparison was performed by downloading both official full packages and running a structured bundle diff.

There are two evidence levels: **[Confirmed]** means direct evidence from Info.plist, dependency lists, signing / notarization results, or other structured readable facts; **[Signal]** means the conclusion comes only from indirect evidence such as size, hashes, or file churn. Signal-level items point in a direction, but cannot be reconstructed into source-level intent.

The comparison scope is macOS arm64: previous version `26.415.32059` build `1789` versus latest version `26.415.40636` build `1799`. Both are dated `2026-04-18`.

## ✨ Highlights

### 1. Version and build advance to `26.415.40636` / `1799` [Confirmed]

This is a bundle-level version update: `CFBundleShortVersionString` changed from `26.415.32059` to `26.415.40636`, and `CFBundleVersion` changed from `1789` to `1799`.

That means macOS, update flows, and diagnostics will recognize this as a newer desktop build. The fact package does not include official release notes.

```diff
/CFBundleShortVersionString: '26.415.32059' → '26.415.40636'
/CFBundleVersion: '1789' → '1799'
```

### 2. The outer bundle structure did not expand, and no new native dependencies were observed [Confirmed]

The file tree stayed at `498 → 498`, with `0` added files, `0` removed files, and `40` content-modified files. The `cua_node` dependency diff shows `0` newly added packages.

This means there is no evidence, at the outer bundle or local dependency layer, of new plugins, external components, or new native package capabilities. The observable changes are more likely inside the existing application payload than in a newly identifiable macOS component set.

Key evidence:

```text
File count: 498 → 498
Added 0 / Removed 0 / Content-modified 40
cua_node added 0 packages
```

### 3. `app.asar` was updated, but the semantic change remains a low-level signal [Signal]

The `app.asar` hash under `ElectronAsarIntegrity` changed, confirming that the application-layer payload was updated. At the same time, the key size table shows `Contents/Resources/app.asar` staying at `111.5 MB → 111.5 MB`, with a `+0.00 MB` delta.

This points to a small or size-neutral application-layer refresh, but the fact package does not provide new readable strings, service entries, skill documents, default prompts, commands, settings, permissions, or user-visible copy. It therefore cannot be reconstructed into source-level intent.

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
'7aa7db298d9b72c91594737c441e2273f6b057b2f9d15d30a6d2f6121434dd3c'
→
'6b92163fce92a3896545d37a0a06abf4654c9bfbb46ed6ba0fdabac775f423a2'
```

## 📊 Raw Data

**Bundle File-Level Statistics**

| Metric | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 498 | 498 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Content-modified files | 0 | 40 | +40 |

**Key Component Size Changes**

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 111.5 MB | 111.5 MB | +0.00 MB |

**New Dependency List**

| Scope | Added package count | Details |
| --- | ---: | --- |
| `cua_node` | 0 | No newly added packages |

## 🔁 Reproducibility

| Artifact | Download URL | SHA-256 | Size | EdDSA verification | codesign / spctl notarization result |
| --- | --- | --- | --- | --- | --- |
| Previous `26.415.32059` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.415.32059.zip` | `8d2cdf5a922f0820acc1903c2b4006c1cb2ffdeed0d53bdf0607ec4b3183bd97` | Not provided in the fact package | Not provided in the fact package | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest `26.415.40636` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.415.40636.zip` | `d382ab435b981d508f488167859dedab70601366ffa9c85050e81943908b5e9e` | Not provided in the fact package | Not provided in the fact package | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Boundaries and Disclaimer

This report is unrelated to OpenAI and is unofficial. It does not represent OpenAI release notes or product commitments.

No official binaries are redistributed here. This document only publishes conclusions derived from diffing official signed distribution artifacts.

All [Signal] items describe artifact-level indications only and should not be treated as source-level facts. If official release notes or more complete reproducible evidence become available later, the newer direct evidence should take precedence. Corrections based on reproducible materials are welcome.

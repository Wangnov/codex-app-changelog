---
title: "Codex Desktop 26.519.31651"
version: "26.519.31651"
build: 3017
previous_version: "26.519.22136"
previous_build: 3003
platform: "macOS arm64"
released: "2026-05-22"
compared_from: "2026-05-21"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "This is an extremely small update: the app and bundled browser-related plugins synchronized their version numbers, with no confirmed changes that can be characterized as new capabilities."
official_release_notes: false
---

# Codex Desktop `26.519.31651`(build 3017)

> Third-party changelog · Unofficial · Based on reverse-engineered differences in official update packages

From an artifact perspective, this release is a small maintenance update, with the main visible changes concentrated in version numbers and package integrity metadata synchronization.  
English one-liner: a very small maintenance build with no evidence of user-facing feature additions.

## About This Report

This is not an official OpenAI changelog, but a **reverse-engineered diff report based on officially distributed artifacts**. This entry is a historical backfill: both versions fall outside the official Sparkle incremental update window, so the comparison was performed by downloading both official full packages directly and generating a structured bundle diff.

In terms of trust, the root of trust for this comparison comes from Apple notarization and the OpenAI Developer ID. Both the previous and latest versions passed `spctl` notarization verification, both sources were `Notarized Developer ID`, and both signing identities were `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

Evidence is divided into two levels:

- **[Confirmed]**: Direct human-readable evidence, such as `Info.plist`, plugin configuration diffs, and signing or notarization results.
- **[Signal]**: Indirect evidence, such as frontend module names or binary size changes; this can only indicate “pointing in a certain direction,” but cannot reconstruct source-level intent from the artifact.

Comparison scope: from `26.519.22136` build `3003`, released on `2026-05-21`, to `26.519.31651` build `3017`, released on `2026-05-22`. Platform: `macOS arm64`.

## ✨ Highlights

### 1. App Version and Build Number Advanced to `26.519.31651` / build `3017` [Confirmed]

This is a version advancement: `CFBundleShortVersionString` changed from `26.519.22136` to `26.519.31651`, and `CFBundleVersion` changed from `3003` to `3017`.

This means the version metadata visible to macOS and the app itself has been updated, but the fact package does not provide enough evidence to show that specific user-facing functionality was added.

Key evidence:

```diff
/CFBundleShortVersionString: '26.519.22136' → '26.519.31651'
/CFBundleVersion: '3003' → '3017'
```

### 2. Bundled Chrome and Browser Plugin Version Numbers Synchronized with the Main App [Confirmed]

The `.codex-plugin/plugin.json` version numbers for the bundled `chrome` and `browser` plugins were both updated from `26.519.22136` to `26.519.31651`.

This indicates that these two browser-related plugins packaged with the app remain synchronized with the main app version. The fact package does not show any changes to their descriptions, capability boundaries, or interface text, so this should not be interpreted as newly added browser automation capability.

Key evidence:

```diff
"version": "26.519.22136" → "26.519.31651"
```

Affected plugins:

| Plugin | Visible Change |
| --- | --- |
| `chrome` | Plugin version synchronized to `26.519.31651` |
| `browser` | Plugin version synchronized to `26.519.31651` |

### 3. `app.asar` Integrity Hash Changed, but Does Not Map to a Describable New Feature [Confirmed]

The hash for `app.asar` in `ElectronAsarIntegrity` changed, indicating that the application resource archive content is indeed different.

However, file-level statistics show the total bundle file count remained `1335 → 1335`, with `0` added files, `0` removed files, and `59` content changes; key component sizes were also basically unchanged. The fact package also explicitly notes that filename additions and removals under `webview/assets` and `.vite/build` with hashes are build-artifact renaming noise, so they cannot be used to infer new functionality.

Key evidence:

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
'05f4d31d74bb97e851579022d9d33bd852097d21d9753290762572a61c8e39f7'
→
'72b2210d1bd4dc5d59a99f1c677e304418cbf1ae60db818d75d6225a43c2d6c1'
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Value |
| --- | --- |
| File count | `1335 → 1335` |
| Added files | `0` |
| Removed files | `0` |
| Content changes | `59` |

Note: Added / removed file counts inside `app.asar` are not listed, because the fact package states that this portion is affected by Vite hash filename renaming, which can easily lead to misinterpretation.

### Key Component Size Changes

| Component Path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | `159.0 MB` | `159.0 MB` | `+0.00 MB` |

### Newly Added Dependencies

| Scope | Newly Added Dependencies |
| --- | ---: |
| `cua_node` | `0` |

## 🔁 Reproducibility Information

| Artifact / Verification Item | Result |
| --- | --- |
| Previous full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.519.22136.zip` |
| Previous SHA-256 | `66c314178d0505ded059f7de4ab64ed1f897858d9127c814d1270fe864a80111` |
| Latest full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.519.31651.zip` |
| Latest SHA-256 | `80c4e7c3073dbfd1107e323d7de11d657837ddb9831b2d99b010b3372bce9e4a` |
| Previous notarization verification | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest notarization verification | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| EdDSA signature verification | Not provided by this fact package; this comparison directly compares two official full packages |
| BinaryDelta reconstruction | Not provided by this fact package; this comparison did not use the incremental package reconstruction path |
| Download size | Not provided by this fact package |

## ⚖️ Boundaries and Disclaimer

This report is not affiliated with OpenAI and does not represent official statements or official release records.

This article does not redistribute any official binaries; it only publishes diff conclusions derived from officially signed distribution artifacts.

This article uses only information provided in the fact package; it does not infer capability changes, source-level intent, reasons for internal refactoring, or unpublished release notes not provided by the fact package.

[Signal] entries only indicate that artifact differences point in a certain direction, but cannot reconstruct source-level intent from the artifact. This fact package does not contain enough [Signal]-level material to summarize as an independent change.

Review and corrections based on the same artifacts and the same verification information are welcome.
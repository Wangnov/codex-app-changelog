
---
title: "Codex Desktop 26.519.31651"
version: "26.519.31651"
build: 3017
mac_version: "26.519.31651"
mac_build: 3017
win_version: "26.519.3891.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-05-16"
method: "official package reverse diff(macOS Sparkle / Windows MSIX)"
summary: "Both platforms refreshed app.asar application contents without clear exposed feature evidence; macOS additionally confirms Browser and Chrome plugin metadata version bumps to 26.519.31651."
official_release_notes: false
---

# Codex Desktop 26.519.31651(macOS / Windows comparison)

> Third-party cross-platform changelog · unofficial

This batch looks like a cross-platform application-layer refresh: both packages changed their `app.asar` contents, while macOS also exposes plugin metadata version updates.  
One-liner: both platforms refreshed the application bundle, while only macOS exposes confirmed Browser and Chrome plugin metadata bumps.

## About This Report

This report is based on reverse diff analysis of official signed distribution packages: macOS via Sparkle full-package comparison, and Windows via MSIX package comparison. The macOS comparison is `26.519.22136` build `3003` → `26.519.31651` build `3017`; the Windows comparison is `26.519.2736.0` → `26.519.3891.0`.

Evidence is split into two levels: [Confirmed] means readable evidence from files such as `Info.plist`, plugin `plugin.json`, dependency manifests, or package manifests; [Signal] means the evidence comes only from file names, file counts, module sizes, or packaged artifact changes, and cannot reconstruct source intent.

Platform labels mean: `[macOS+Windows]` appears in both platform fact packs; `[macOS only]` appears only in the macOS fact pack; `[Windows only]` appears only in the Windows fact pack.

## ✨ Key Changes

### [Signal] [macOS+Windows] Application package contents were refreshed

Both platforms changed internal `app.asar` contents, while the displayed total size remained `159.0 MB → 159.0 MB`, delta `+0.00 MB`. macOS internal files stayed at `1576 → 1576`, with `15` added, `15` removed, and `9` changed. Windows internal files stayed at `1530 → 1530`, with `15` added, `15` removed, and `11` changed.

The platform difference is that both sides have the same add/remove counts, but Windows has `2` more changed files than macOS. The fact packs also state that heavy churn under `webview/assets` and `.vite/build` hash renames is build noise, so this report does not interpret those changes as concrete feature additions or removals.

### [Confirmed] [macOS only] Browser and Chrome plugin metadata versions were synchronized

Two OpenAI bundled plugin `plugin.json` files in the macOS package have readable diffs:

The `browser` plugin version changed from `26.519.22136` to `26.519.31651`; the `chrome` plugin version also changed from `26.519.22136` to `26.519.31651`.

The platform difference is that the Windows fact pack does not list corresponding readable `plugin.json` diffs. This report can therefore confirm the metadata update on macOS only, and cannot claim that Windows made the same plugin metadata change.

### [Confirmed] [macOS only] macOS app version and integrity hash were updated

macOS `Info.plist` shows `CFBundleShortVersionString` changed from `26.519.22136` to `26.519.31651`, `CFBundleVersion` changed from `3003` to `3017`, and the `ElectronAsarIntegrity` hash for `app.asar` changed from `05f4d31d74bb97e851579022d9d33bd852097d21d9753290762572a61c8e39f7` to `72b2210d1bd4dc5d59a99f1c677e304418cbf1ae60db818d75d6225a43c2d6c1`.

The platform difference is that this is macOS bundle-level manifest evidence. The Windows fact pack provides version and package-level statistics, but no `AppxManifest` field diff.

### [Confirmed] [macOS+Windows] No new cua_node dependencies in this batch

Both platform fact packs show `0` newly added `cua_node` packages. At the dependency-list level, this batch did not add packages to that dependency group.

Platform difference: none. macOS and Windows are aligned on this item.

## 📊 Raw Data

| Item | macOS arm64 | Windows x64 |
| --- | --- | --- |
| Previous version | `26.519.22136` build `3003` | `26.519.2736.0` |
| Latest version | `26.519.31651` build `3017` | `26.519.3891.0` |
| Package-level file count | `1335 → 1335` | `832 → 832` |
| Package-level added / removed / changed | `0 / 0 / 59` | `0 / 0 / 12` |
| `app.asar` size | `159.0 MB → 159.0 MB` | `159.0 MB → 159.0 MB` |
| `app.asar` internal files | `1576 → 1576` | `1530 → 1530` |
| `app.asar` internal added / removed / changed | `15 / 15 / 9` | `15 / 15 / 11` |
| New `cua_node` dependencies | `0` | `0` |

The macOS previous full package SHA-256 is `66c314178d0505ded059f7de4ab64ed1f897858d9127c814d1270fe864a80111`, and the latest is `80c4e7c3073dbfd1107e323d7de11d657837ddb9831b2d99b010b3372bce9e4a`. Both versions passed notarization verification with `accepted`, source `Notarized Developer ID`, and origin `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

The Windows previous full package SHA-256 is `bf284a70f0c62da1911a62700465c31ce21f849278cd9a8290428c9b43accda1`, and the latest is `461e9e796fa5c8bb662fb4177ddef17ca38386ea3e34e611a7d4d12ea7a82069`.

## ⚖️ Boundaries and Disclaimer

This is a third-party, unofficial changelog and does not represent official OpenAI release notes. This report does not redistribute binaries; it only analyzes diffs between obtained official signed distribution packages.

Items marked [Signal] only show that packaged artifacts changed. They are not source-code facts and should not be read as confirmed feature releases. If official notes or more complete readable diffs become available later, stronger evidence should supersede the conclusions here. Corrections backed by reproducible evidence are welcome.


---
title: "Codex Desktop 26.623.42026"
version: "26.623.42026"
build: 4514
mac_version: "26.623.42026"
mac_build: 4514
win_version: "26.623.5546.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-26"
method: "Official package reverse diff: macOS Sparkle, Windows MSIX"
summary: "This batch mainly syncs bundled plugin manifests and reshapes settings, skills, and remote-task frontend surfaces; macOS shows broader AppGen, update-recovery, and remote-task signals, while the Windows sample emphasizes skills-page and settings signals."
official_release_notes: false
---

# Codex Desktop 26.623.42026 macOS / Windows Comparison

> Third-party cross-platform changelog · Unofficial

This batch shows shared settings and workbook-related frontend signals, while macOS carries broader AppGen, remote-task, update-recovery, and plugin-detail churn.

One-liner: Cross-platform settings and workbook signals landed, while macOS carries broader AppGen, remote-task, update-recovery, and plugin-detail UI churn.

## About This Report

This report uses only the machine-extracted facts in the provided packages. The macOS comparison is `26.623.41415 build 4505` → `26.623.42026 build 4514`; the Windows comparison is `26.623.5175.0` → `26.623.5546.0`. The two platforms have different version and packaging cadence, so all conclusions below are cited per platform.

Evidence is split into two levels: [Confirmed] comes from plist, manifests, dependency inventories, bundled plugin manifests, key configs, human-readable diffs, and package statistics; [Signal] comes from frontend module names, changed strings/classes in same-name frontend modules, and compressed bundle size direction. Platform markers are `[macOS+Windows]`, `[macOS only]`, and `[Windows only]`.

Hash renames, `webview/assets`, and `.vite/build` artifact churn are not treated as feature additions or removals. The fact packages do not include a CSP changes block or Windows AppxManifest change details, so this report does not infer beyond that.

## ✨ Highlights

### [Confirmed] [macOS+Windows] Bundled plugin manifests were synced to 26.623.42026

On macOS, the `chrome` and `browser` bundled plugin manifests changed their version from `26.623.41415` to `26.623.42026`. On Windows, the `computer-use`, `chrome`, and `browser` bundled plugin manifests show the same version update.

This is not evidence of a newly added plugin: the fact packages do not contain a “new bundled plugin details” section. The confirmed change is the manifest version field. The outer Windows MSIX version remains separately cited as `26.623.5546.0`.

### [Confirmed] [macOS+Windows] Computer Use runtime size and cua_node dependencies did not increase

The macOS Computer Use runtime total is `377.4 MB → 377.4 MB (+0 MB, +0%)`; Windows is `298.9 MB → 298.9 MB (+0 MB, +0%)`. Both platforms report `0` newly added `cua_node` dependency packages.

Windows additionally has a `computer-use` plugin manifest version update. In the macOS fact package, only `chrome` and `browser` plugin manifest version updates are listed.

### [Signal] [macOS+Windows] Settings added permission, work-mode, voice, and display strings

Both platforms’ `general-settings` same-name frontend module added readable strings for disabled default permissions, work mode settings, code base size, hold-to-dictate hotkey capture, hotkey window capture, dictation dictionary entries, Popout Window projectless default, ambient suggestions, color diff marker, and the Chrome theme contrast slider.

This indicates a frontend-package signal for an expanded settings control surface, but because the evidence comes from same-name frontend module clues, it does not prove every entry is user-visible.

### [Signal] [macOS only] macOS settings also added environment, terminal, file-open, and code-review defaults

The macOS `pets-settings~appearance-settings~general-settings` module added strings such as `Agent environment`, `Auto detect`, `Default file open destination`, `Default microphone input`, `Default permissions`, `Default terminal location`, `Default to projectless chat`, and `Detached code review option label`.

These are macOS-only signals in the provided facts. The Windows sample does not contain the corresponding string group.

### [Signal] [Windows only] Skills page added create, refresh, search, and recommended/installed grouping strings

The Windows `skills-page` module added strings for creating a new skill, reloading the skills list, refreshing after newly installed skills, filtered empty states, the skills page title, installed skills, recommended skills, search input, and loading state.

The macOS fact package contains added module-name combinations involving `skills-settings` and `plugins-settings`, but not the same `skills-page` string details, so the Windows skills-page evidence should not be projected onto macOS.

### [Signal] [macOS only] Plugin detail added sharing, MCP toggle, and tools-menu strings

The macOS `plugin-detail-page` module added strings for the plugin share dialog, workspace user/group search, MCP server enablement toggle, section navigation, skill card actions menu, app tools modal, and skill preview modal.

This is a high-visibility plugin management signal, but it remains same-name frontend-module evidence. It proves the strings entered the macOS frontend package, not that the full permission or sharing flow is enabled.

### [Signal] [Windows only] Windows plugin detail removed similar sharing/MCP strings

The Windows `plugin-detail-page` module removed strings for the plugin share dialog, workspace link access, MCP server enablement toggle, section navigation, `App unavailable`, and related labels. Its size went from about `0.2 MB` to `0.0 MB`, a `-154755 bytes` change.

This points in the opposite direction from the macOS plugin-detail signal in the same batch and should be read per platform.

### [Signal] [macOS only] AppGen, site sharing, and public publishing paths were heavily reshaped

macOS added module combinations around `appgen-library-page`, `appgen-settings-page`, and `appgen-publication-terms-route`. The `appgen-share-dialog` also added strings for saving site sharing, searching workspace users/groups, copying a live site URL, opening a live site externally, publishing to the public internet, `Anyone on the Internet`, `Can visit`, and `Copied to clipboard`.

These signals cluster around AppGen site sharing, link access, and publishing flow reshaping. The Windows fact package does not show corresponding added modules or strings.

### [Signal] [macOS only] Remote conversations, Codex Cloud, recent tasks, and Chrome side-panel navigation signals increased

The macOS `header` module added strings for the recent tasks menu, `All tasks`, `Cloud tasks`, `Codex for Chrome`, `Chrome computer use settings`, `App settings`, and `Clear search`. Sidebar-related modules also added `All chats`, `Codex Cloud`, cloud task sections, connection/project thread groups, recent threads, and `features.remote_connections`.

Project hover-card strings also added labels for opening the project source, editing a project, pinning a project, inline project-name editing, rename-failure toast, and status text for conversations running in Codex Cloud, a local git worktree, or locally.

### [Signal] [Windows only] Header removed recent-task and app-settings strings

The Windows `header` module removed strings for the recent tasks menu, unread task updates, `All environments filter option`, `All tasks`, `App settings`, back navigation, Chrome extension side-panel more actions, and clearing search.

This is also a Windows-only same-name frontend module signal and should not be merged with the macOS additions.

### [Signal] [macOS only] Update recovery and usage-limit entry points entered the frontend bundle

The macOS `automations-page` module added strings for desktop app update download, install, download-with-progress, update confirmation/cancel buttons, requesting a workspace monthly usage limit increase, rate limit summary, available rate limit resets, and `Learn more about rate limits`.

Another macOS module added full-screen error page recovery strings such as `Checking for updates`, `Downloading update`, `Installing update`, and `Update Codex`. The Windows fact package has no equivalent added string group.

### [Signal] [macOS+Windows] Worktree setup failure fallback copy was removed

Both platforms’ `worktree-init-v2-page` modules removed strings such as `Continue anyway`, `Edit environment`, opening local environment settings, starting a repair thread, canceling worktree setup and starting a local conversation, canceling worktree creation, and local task creation errors.

This is a frontend signal that worktree setup failure copy or paths were reduced or reshaped. Since it comes from same-name frontend module clues, it does not directly prove feature removal.

### [Signal] [macOS only] Electron worker bridge and process/spawn error paths entered the frontend package

A large macOS frontend module added strings including `Electron worker bridge is unavailable`, `Mismatched worker response method`, `Missing cwd`, `Missing git metadata`, `Unknown method: process/spawn`, `operation not permitted`, and `permission denied`.

These are frontend-package signals for command execution, worker bridge, and permission error paths. The Windows fact package does not contain the same added group.

### [Signal] [macOS+Windows] Workbook payload grew sharply; macOS also gained notebook/docx preview module names

Both platforms’ `workbook` module grew from about `0.0 MB` to `2.8 MB`: macOS by `2764364 bytes`, Windows by `2761257 bytes`. Added strings include unnamed ranges, workbook help/valid-values hints, and many `data-granola-*` structural/style classes.

macOS also added a `settings-page~projects-index-page~notebook-preview-panel~docx-preview-panel` module name. Together these form a document, spreadsheet, or preview-related frontend payload signal, not confirmed user-visible functionality.

### [Signal] [macOS only] Profile upload failure entered the package while old profile/onboarding payload dropped sharply

The macOS `app-initial~app-main~onboarding-page~profile` module shrank from `6.1 MB` to `0.0 MB`, a `-6067006 bytes` change, while adding `Profile photo upload failed` and removing a batch of diff/editor-related strings.

This is a profile/onboarding packaging-structure signal and should not be read as profile functionality disappearing.

### [Signal] [Windows only] Profile stats query strings increased while profile-model was removed

The Windows `profile-queries` module added formatted strings for profile streak day count and durations in hours, minutes, and seconds. The true frontend module add/remove list also shows `profile-model` removed.

This is a Windows-only signal around profile stats and data-model packaging.

## 🔎 Low-Level Signal Index

- [Signal] [macOS only] Language/grammar payloads: `cpp`, `scss`, `yaml`, `postcss`, `tex`, `angular-html`, `json`, `wasm`, `lua`, `glsl`, `csv`, and `html-derivative` added grammar scope strings; `tsx` moved in the reduction direction. The boundary is syntax-highlighting or text-parsing payload change, not confirmed editor behavior.
- [Signal] [macOS only] Plugin runtime and host bridge: `esm` added callback, context cancel, failed deliver/load/unload, promise timeout, and plugin Error strings; another plugin-detail-related chunk added `VS Code API is unavailable` and React runtime error strings.
- [Signal] [macOS only] Debug and home announcements: `debug-modal` removed Account ID, Active browser tab, Active conversation, Allow host, and related strings; `home-announcements` removed multiple home banner dismiss labels and remote connections banner copy.
- [Signal] [macOS only] Route chunk reshaping: added module names cluster around onboarding, first-run, new-thread-panel, remote-conversation, projects-index, pull-requests, plugin-detail, AppGen, and worktree-init-v2. Module names only prove bundle graph changes.
- [Signal] [Windows only] Narrower module surface: the true frontend module add/remove list only shows `profile-model` removed; the main Windows signals come from same-name changes in `general-settings`, `skills-page`, `workbook`, `profile-queries`, `plugin-detail-page`, `header`, and `worktree-init-v2-page`.
- [Confirmed] [macOS+Windows] Outer bundle file counts had no added or removed files: macOS `8875 → 8875`, Windows `9749 → 9749`. Content changes were `67` and `16`, respectively.

## 📊 Raw Data

| Item | macOS arm64 | Windows x64 |
| --- | --- | --- |
| Compared versions | `26.623.41415 build 4505` → `26.623.42026 build 4514` | `26.623.5175.0` → `26.623.5546.0` |
| Outer file count | `8875 → 8875` | `9749 → 9749` |
| Outer added/removed/changed | `0 / 0 / 67` | `0 / 0 / 16` |
| app.asar | `164.7 MB → 164.7 MB (+0.04 MB)` | `168.8 MB → 168.8 MB (+0.00 MB)` |
| app.asar internal files | `2653 → 2654`, added `983`, removed `982`, changed `11` | `3125 → 3124`, added `119`, removed `120`, changed `11` |
| Computer Use runtime | `377.4 MB → 377.4 MB (+0 MB, +0%)` | `298.9 MB → 298.9 MB (+0 MB, +0%)` |
| New cua_node dependencies | `0` | `0` |
| manifest/plist | `CFBundleShortVersionString`, `CFBundleVersion`, and `ElectronAsarIntegrity` hash updated | No AppxManifest change details were provided in the fact package |
| Bundled plugin manifests | `chrome` and `browser` version updated | `computer-use`, `chrome`, and `browser` version updated |

Key frontend size changes: macOS `workbook` `+2764364 bytes`, `cpp` `+626180 bytes`, `wasm` `-610342 bytes`, `automations-page` `-573179 bytes`, `onboarding-page~profile` `-6067006 bytes`, `plugin-detail-page` `+153647 bytes`, and `general-settings` `+117665 bytes`. Windows `workbook` `+2761257 bytes`, `plugin-detail-page` `-154755 bytes`, `general-settings` `+115131 bytes`, `header` `-40005 bytes`, and `skills-page` `+15215 bytes`.

## ⚖️ Boundaries And Statement

This is a third-party reverse-diff changelog, not official OpenAI release notes, and it does not redistribute any binary content.

[Signal] means module names, strings, classes, or size movement appeared, disappeared, or were reshaped in the frontend package. It is not source-level proof and does not prove the related feature is enabled for all users.

The macOS and Windows samples must be read separately for versions, builds, package statistics, plugin manifests, and frontend signals. This report does not project one platform’s findings onto the other. Corrections backed by reproducible fact packages are welcome.

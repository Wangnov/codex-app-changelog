
---
title: "Codex Desktop 26.228.1430"
version: "26.228.1430"
build: 760
previous_version: "26.227.1448"
previous_build: 747
platform: "macOS arm64"
released: "2026-03-01"
compared_from: "2026-02-28"
method: "Sparkle 增量包重建 + 结构化 bundle 差异"
summary: "This release appears to be a version and application payload refresh, with no confirmed new dependencies, plugins, or bundle-level capability surface."
official_release_notes: false
---

# Codex Desktop `26.228.1430` build 760

> Third-party changelog · Unofficial · Based on reverse diffs of official update artifacts

This comparison covers `26.227.1448` build 747 to `26.228.1430` build 760. At the artifact level, the confirmed changes are the version metadata and the `app.asar` integrity hash; no new bundle files or `cua_node` dependencies are visible.  
One-liner: this looks like a payload refresh with no confirmed new packaged capability surface.

## About This Report

This is not an official changelog. It is a **reverse-diff report based on official distribution artifacts**. This entry is a historical backfill; the fact package states that the official appcast did not provide a delta package for this version pair, so two official full packages were downloaded and compared directly.

Evidence levels:

- **[Confirmed]**: based on human-readable or structured facts such as Info.plist values, dependency lists, file-tree statistics, and signing/notarization verification output.
- **[Signal]**: based only on indirect evidence such as frontend module names or binary size changes. This fact package does not contain enough frontend-added-module evidence to support a standalone signal item.

The comparison scope is macOS arm64, from `26.227.1448` build 747 released on `2026-02-28` to `26.228.1430` build 760 released on `2026-03-01`.

## ✨ Highlights

### 1. App version and build updated to `26.228.1430` / `760` [Confirmed]

This is a desktop version bump from `26.227.1448` build 747 to `26.228.1430` build 760.

The directly visible change for users and developers is in the macOS bundle metadata: both `CFBundleShortVersionString` and `CFBundleVersion` changed.

Key evidence:

```diff
/CFBundleShortVersionString: '26.227.1448' → '26.228.1430'
/CFBundleVersion: '747' → '760'
```

### 2. Application payload changed, with no new bundle files [Confirmed]

The integrity hash for `Contents/Resources/app.asar` changed, confirming that the application payload was updated. At the same time, the bundle file count stayed at `410 → 410`, with `0` added files, `0` removed files, and `40` modified files.

This means the release does not expand its packaged surface through new standalone bundle components. The artifact confirms a payload update, but the fact package does not provide enough evidence to attribute it to a specific user feature or developer workflow.

Key evidence:

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
'e21f5cb27005a10d0555306a594ad9c564dc3ef8c6fa07ff14459c11da4fdbfc'
→
'61cac92872880821ffb93e52f32fd2573e9cfe4d602a1b8eb156f453436e74c0'
```

### 3. No confirmed new native dependency or packaged capability surface [Confirmed]

The number of newly added `cua_node` dependencies is `0`, and the bundle file tree has no added files. The fact package contains no readable evidence for new built-in plugins, service entry points, skill documentation, default prompts, commands, settings, permissions, error paths, or user-visible copy.

Therefore, this release should not be described as adding a specific new capability. What can be confirmed is narrower: compared with the previous version, it does not introduce new packaged capability through added `cua_node` packages or added bundle files.

Key evidence:

```text
cua_node added packages: 0
Bundle file count: 410 → 410
Added 0 / removed 0 / modified 40
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Item | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 410 | 410 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Modified files | 0 | 40 | +40 |

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 36.4 MB | 36.4 MB | +0.00 MB |

### Added Dependencies

| Category | Added count | Details |
| --- | ---: | --- |
| `cua_node` | 0 | None |

## 🔁 Reproducibility

| Item | Previous | Latest |
| --- | --- | --- |
| Artifact | `Codex-darwin-arm64-26.227.1448.zip` | `Codex-darwin-arm64-26.228.1430.zip` |
| URL | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.227.1448.zip` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.228.1430.zip` |
| SHA-256 | `e5a86a8bdb4b8bb913a8ad587366d5e68dc41a71d64d0e06c205b3a14252ff3f` | `52e3911c4738d48cd842eeab1b08de6210513e7e4a6823fdb6b860678499d69f` |
| Size | Not provided | Not provided |
| EdDSA verification | Not provided | Not provided |
| codesign/spctl notarization result | `CSSMERR_TP_CERT_REVOKED` | `CSSMERR_TP_CERT_REVOKED` |

Some older versions later had their signing certificates revoked, so verification reports `CSSMERR_TP_CERT_REVOKED`. That affects whether the version can be safely installed by the system, but it does not prevent bundle-content diff analysis.

## ⚖️ Boundaries and Disclaimer

This report is independent of OpenAI and is an unofficial third-party reverse-diff summary.

It does not redistribute any official binaries. It only publishes conclusions derived from official distribution artifacts.

This report does not list added or removed files inside app.asar, because the fact package explicitly states that much of that churn comes from Vite build hash renaming and must not be interpreted as real feature additions or removals.

[Signal] items are not source-level facts; this report does not use any standalone [Signal] item. Corrections based on reproducible evidence are welcome.

---
title: "Codex Desktop 26.228.1430"
version: "26.228.1430"
build: 760
previous_version: "26.227.1448"
previous_build: 747
platform: "macOS arm64"
released: "2026-03-01"
compared_from: "2026-02-28"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This is a minor version bump: the visible changes are concentrated in version number, build number, and app.asar content hash updates, with no observed new dependencies or newly added bundle-level components."
official_release_notes: false
---

# Codex Desktop `26.228.1430` (build 760)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

This release advances from `26.227.1448` to `26.228.1430`. The outer bundle file tree remains stable; the main confirmed changes are version metadata updates and changes to the contents of `app.asar`.  
English one-liner: a metadata-and-resource update with no observable new bundle-level components.

## About this report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. This is a historical backfill: both versions have already fallen outside the official Sparkle delta window, so the fact package uses a direct comparison of two official full packages rather than reconstruction through BinaryDelta.

In terms of confidence, both input artifacts come from official full-package URLs and include SHA-256 hashes. Notarization verification shows that both versions return `CSSMERR_TP_CERT_REVOKED`; the fact package explains that this is because the signing certificate used by some earlier versions was later revoked. This affects the system’s security installation judgment, but does not affect bundle content diff analysis.

This report uses two evidence levels:

- **[Confirmed]**: Direct, human-readable evidence, such as Info.plist key values, dependency lists, signing, or verification conclusions.
- **[Signal]**: Indirect evidence, such as frontend module names or binary size changes. This type of information points in a direction, but cannot be traced from the artifact back to source-level intent.

Comparison scope: from `26.227.1448` build `747`, released on `2026-02-28`, to `26.228.1430` build `760`, released on `2026-03-01`. Platform: `macOS arm64`.

## ✨ Key Changes

### 1. Version metadata advanced to `26.228.1430` [Confirmed]

This is a clear desktop version bump: `CFBundleShortVersionString` changed from `26.227.1448` to `26.228.1430`, and `CFBundleVersion` changed from `747` to `760`.

The user-visible impact is that the application version and build number shown in the system, updater, or diagnostic interfaces will be updated. This is the most direct and certain change in this diff.

Key evidence:

```diff
/CFBundleShortVersionString: '26.227.1448' → '26.228.1430'
/CFBundleVersion: '747' → '760'
```

### 2. No files were added to or removed from the outer app bundle structure [Confirmed]

The bundle file count remains `410 → 410`; file-level statistics show `0` added, `0` removed, and `40` content-modified files.

This means no new outer components, plugins, or standalone executables were observed in this release. The changes look more like replacements or updates to existing content than the addition of a new feature module directly identifiable from the file tree.

Key evidence:

```text
File count: 410 → 410
Added 0 / Removed 0 / Content modified 40
```

### 3. `app.asar` content changed, but its size is almost unchanged [Confirmed]

The integrity hash of `Contents/Resources/app.asar` changed, confirming that application-layer resources were updated. However, its size is still shown as `36.4 MB → 36.4 MB`, with the table reporting a change of `+0.00 MB`.

The user-visible implication is that internal application logic or frontend resources may have received a small update. However, the fact package does not allow a more specific conclusion about feature intent. In particular, additions and removals of hashed filenames under `webview/assets` and `.vite/build` are build artifact renames and should not be interpreted as real feature additions or removals.

Key evidence:

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
'e21f5cb27005a10d0555306a594ad9c564dc3ef8c6fa07ff14459c11da4fdbfc'
→
'61cac92872880821ffb93e52f32fd2573e9cfe4d602a1b8eb156f453436e74c0'
```

### 4. No new `cua_node` dependencies found [Confirmed]

The fact package shows `0` newly added `cua_node` dependencies.

This means there is no evidence in this diff that the application expanded lower-level capabilities by adding new `cua_node` packages. If behavioral changes exist, they are more likely to come from existing packages or updated application-layer resources.

Key evidence:

```text
cua_node added 0 packages
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Previous Version | Latest Version | Change |
| --- | ---: | ---: | ---: |
| bundle file count | 410 | 410 | 0 |
| added files | - | - | 0 |
| removed files | - | - | 0 |
| content-modified files | - | - | 40 |

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 36.4 MB | 36.4 MB | +0.00 MB |

### Added Dependency List

| Dependency area | Added count | Notes |
| --- | ---: | --- |
| `cua_node` | 0 | No new packages found |

## 🔁 Reproducibility Information

| Item | Previous Version `26.227.1448` | Latest Version `26.228.1430` |
| --- | --- | --- |
| Download artifact | `Codex-darwin-arm64-26.227.1448.zip` | `Codex-darwin-arm64-26.228.1430.zip` |
| SHA-256 | `e5a86a8bdb4b8bb913a8ad587366d5e68dc41a71d64d0e06c205b3a14252ff3f` | `52e3911c4738d48cd842eeab1b08de6210513e7e4a6823fdb6b860678499d69f` |
| EdDSA verification | Not provided by the fact package | Not provided by the fact package |
| codesign / spctl conclusion | `CSSMERR_TP_CERT_REVOKED` | `CSSMERR_TP_CERT_REVOKED` |
| Notes | The earlier signing certificate was later revoked; this affects the system’s security installation judgment, but not bundle diff analysis | The earlier signing certificate was later revoked; this affects the system’s security installation judgment, but not bundle diff analysis |

## ⚖️ Boundaries and Disclaimer

This report is not affiliated with OpenAI, does not represent any official position, and is not an official release note.

This report does not redistribute any official binaries; it only publishes diff conclusions derived from official distribution artifacts.

All [Signal] entries only indicate indirect artifact-level evidence and do not constitute source-level facts or judgments about product intent. This fact package does not provide usable genuinely new frontend modules, so this report does not extend feature inferences on that basis.

Review and corrections based on the same artifacts and the same method are welcome.
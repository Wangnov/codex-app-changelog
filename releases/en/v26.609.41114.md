
---
title: "Codex Desktop 26.609.41114"
version: "26.609.41114"
build: 3888
previous_version: "26.609.30741"
previous_build: 3808
platform: "macOS arm64"
released: "2026-06-12"
compared_from: "2026-06-11"
method: "Official full-package pair comparison + structured bundle diff"
summary: "The clearest change is a new local SQLite recovery/backfill flow, while overall bundle size and the Computer Use runtime remain essentially unchanged."
official_release_notes: false
---

# Codex Desktop `26.609.41114`(build 3888)

> Third-party changelog · Unofficial · Based on reverse-diffing official update artifacts

This looks like a very small desktop maintenance release, with the main visible change pointing to local conversation database recovery, backup, and rebuild flows.  
English one-liner: a small maintenance release with visible signals around local SQLite recovery and rebuild UX.

## About This Report

This is not an official changelog. It is a **reverse-diff report based on official distribution artifacts**. For this fact package, the comparison uses two official full packages directly: `26.609.30741` → `26.609.41114`. The official appcast provides a delta package for this version pair, but this workflow deliberately chose the full-package pair to cover the complete bundle contents, so it does not use the EdDSA verification result bundled with the official delta.

For confidence, this report only uses the machine-extracted structured diff facts. The fact package provides SHA-256 hashes for both official full packages. It also reports `spctl` notarization validation output for both versions as `bundle format unrecognized, invalid, or unsuitable`, so this report does not claim notarization success.

Evidence levels:

- [Confirmed]: Direct human-readable evidence, such as Info.plist values, readable strings, configuration diffs, or plugin manifests.
- [Signal]: Indirect evidence, such as new strings in minified frontend bundles or binary size movement. These point in a direction, but they cannot be reverse-mapped to source-level intent.

Comparison scope: macOS arm64, from `26.609.30741`(build 3808, released 2026-06-11) to `26.609.41114`(build 3888, released 2026-06-12).

## ✨ Highlights

### 1. New local SQLite recovery and backfill UI copy [Confirmed]

The new main/frontend bundle contains a cluster of readable strings around SQLite recovery, backup, backfill progress, and conversation history rebuilds, including `Back Up and Rebuild`, `Codex rebuilt its local database.`, `Rebuilding conversation history`, and `This may take several minutes — keep the app open`.

This indicates that the desktop app now includes, or at least exposes, a user path for local state database recovery confirmation, backup-and-rebuild, and progress display. The visible user impact may be prompts showing a database path, backup folder, rebuild action, and conversation history rebuild progress when local database repair is needed.

Key evidence:

```text
App-server SQLite recovery config warning received
Back Up and Rebuild
Backup folder:
Codex rebuilt its local database.
Database path:
Rebuilding conversation history
state db backfill
timed out waiting for state db backfill
```

### 2. Bundled Browser and Chrome plugins were version-synced [Confirmed]

The bundled Browser and Chrome plugin `plugin.json` files changed their version from `26.609.30741` to `26.609.41114`. The supplied diffs do not show capability-level description changes.

This means these packaged automation plugins were version-synced with the app, but the fact package does not show new commands, permissions, or documentation text. It should not be read as evidence of expanded plugin capability.

Key evidence:

```diff
-  "version": "26.609.30741",
+  "version": "26.609.41114",
```

### 3. The main app changed slightly while total size stayed stable [Confirmed]

The bundle file count stayed at `8298 → 8298`, with `0` added files, `0` removed files, and `61` content-changed files. `Contents/Resources/app.asar` remained `150.7 MB → 150.7 MB`, changing by only `+0.01 MB`.

This points to a small application-layer logic and copy update rather than a broad capability package change. The app.asar internal `295 added / 295 removed` count is dominated by Vite hash filename churn and should not be interpreted as real feature addition or removal.

### 4. No visible expansion in the Computer Use runtime or cua_node dependencies [Confirmed]

The Computer Use runtime `cua_node` total stayed at `365.8 MB → 365.8 MB`, a `+0 MB, +0%` change. `cua_node` added `0` packages.

This means there is no direct evidence in this release of runtime capability expansion or new low-level dependency packages. The visible change is concentrated in the app-layer database recovery path, not in the Computer Use runtime.

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 8298 | 8298 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Content-changed files | - | - | 61 |

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 150.7 MB | 150.7 MB | +0.01 MB |
| `cua_node` total file content size | 365.8 MB | 365.8 MB | +0 MB / +0% |

### Newly Added Dependencies

| Scope | Added dependency count | Notes |
| --- | ---: | --- |
| `cua_node` | 0 | No new packages found |

## 🔁 Reproducibility Information

| Artifact / Check | Result |
| --- | --- |
| Previous full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.609.30741.zip` |
| Previous SHA-256 | `24cd7b226c70aefb9143571cdcfb2d39fb38794a98ba3935d406d89e7202bf7e` |
| Latest full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.609.41114.zip` |
| Latest SHA-256 | `6f69d0de7800bc7f48cda0f87810068f109fe6ecf839df8dec0d093cb23ad362` |
| EdDSA verification | This workflow did not use the EdDSA verification result bundled with the official delta |
| Previous `spctl` notarization validation | `work/new/26.609.41114/previous-extract/Codex.app: bundle format unrecognized, invalid, or unsuitable` |
| Latest `spctl` notarization validation | `work/new/26.609.41114/latest-reconstructed/Codex.app: bundle format unrecognized, invalid, or unsuitable` |
| codesign validation | No codesign output was provided in the fact package |

## ⚖️ Boundaries and Disclaimer

This report is not affiliated with OpenAI and does not represent an official position or official release notes.

It does not redistribute any official binaries; it only publishes conclusions derived from the provided diff facts.

[Signal] items only represent artifact-level directional evidence, not source-level facts. Corrections backed by reproducible evidence are welcome.

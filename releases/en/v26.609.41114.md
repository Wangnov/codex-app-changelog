
---
title: "Codex Desktop 26.609.41114"
version: "26.609.41114"
build: 3888
previous_version: "26.609.30741"
previous_build: 3808
platform: "macOS arm64"
released: "2026-06-12"
compared_from: "2026-06-11"
method: "Official full-package comparison + structured bundle diff"
summary: "A very small update: the outer bundle file count is unchanged, with only version metadata, the app.asar integrity hash, and bundled Browser / Chrome plugin metadata moving forward."
official_release_notes: false
---

# Codex Desktop `26.609.41114`(build 3888)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs from official update artifacts

At the artifact level, this release looks like a maintenance-style version bump: no added files, no removed files, and only a small set of content changes.  
One-liner: a very small maintenance-style update with version metadata and bundled plugin version alignment.

## About This Report

This is not an official changelog. It is a **reverse-engineered diff report based on official distribution artifacts**. This entry compares two official full packages directly (full-pair) rather than reconstructing from a delta update — full comparison covers the complete bundle contents, at the cost of not citing the EdDSA signature carried by the official delta package.

The comparison range is `26.609.30741`(build 3808, 2026-06-11) → `26.609.41114`(build 3888, 2026-06-12), for macOS arm64.

For reproducibility, the fact package provides SHA-256 hashes for both official full packages. Notarization verification reports `bundle format unrecognized, invalid, or unsuitable` for both bundles. The fact package does not provide an EdDSA delta-signature result, nor a successful codesign result, so this report does not infer one.

Evidence levels:

- [Confirmed]: Direct human-readable evidence, such as Info.plist entries, plugin configuration diffs, hashes, or version strings.
- [Signal]: Indirect evidence only, such as frontend module names or binary size changes; these can point in a direction, but cannot recover source-level intent from the artifact.

## ✨ Highlights

### 1. App Version and Build Advanced to `26.609.41114` / build `3888` [Confirmed]

The app metadata short version changed from `26.609.30741` to `26.609.41114`, and the build number changed from `3808` to `3888`.

This confirms the desktop app moved to the next build released on 2026-06-12, but this fact alone does not establish any specific user-facing feature change.

Evidence:

```diff
/CFBundleShortVersionString: '26.609.30741' → '26.609.41114'
/CFBundleVersion: '3808' → '3888'
```

### 2. Bundled Browser / Chrome Plugin Metadata Now Matches the App Version [Confirmed]

The bundled Browser and Chrome plugins both changed their `plugin.json` version from `26.609.30741` to `26.609.41114`.

This shows these OpenAI bundled plugins were repackaged alongside the main app. The fact package does not show any change to their descriptions, capability scope, or interface semantics.

Evidence:

```diff
"version": "26.609.30741" → "26.609.41114"
```

### 3. `app.asar` Changed Slightly, but No New Capability Is Confirmed [Confirmed]

`Contents/Resources/app.asar` stayed at `150.7 MB → 150.7 MB`, with a reported delta of `+0.01 MB`. The `app.asar` hash under ElectronAsarIntegrity also changed.

This means the application-layer package was regenerated or slightly modified, but the readable diffs only show version metadata changes. Hash-named file additions and removals under `webview/assets` and `.vite/build` are build-artifact rename noise and should not be interpreted as added or removed features.

Evidence:

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
'2c29d20e105f798212771402404fd2bc27d5cd6e4ac8a42f8eeac88772c63378'
→ '3e01c3b6a7a8d792570153a7cca28b1cc6ab1271324195fb5ea4033e0ca9eef7'
```

### 4. Computer Use Runtime and `cua_node` Dependencies Show No Change [Confirmed]

The Computer Use runtime total stayed at `365.8 MB → 365.8 MB`, reported as `+0 MB, +0%`; `cua_node` added `0` dependencies.

At the level of file size and dependency inventory, this release does not appear to expand that runtime surface. This is an artifact-level conclusion only, not proof that the source code had no internal adjustments.

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Value |
| --- | --- |
| File count | `8298 → 8298` |
| Added files | `0` |
| Removed files | `0` |
| Modified contents | `61` |

### Key Component Size Changes

| Component path | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | `150.7 MB` | `150.7 MB` | `+0.01 MB` |
| Computer Use runtime(`cua_node`) | `365.8 MB` | `365.8 MB` | `+0 MB / +0%` |

### Added Dependencies

| Scope | Added dependencies |
| --- | ---: |
| `cua_node` | `0` |

## 🔁 Reproducibility

| Artifact | URL | SHA-256 | Size | EdDSA Verification | Notarization / Signing Result |
| --- | --- | --- | --- | --- | --- |
| Previous full package `26.609.30741` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.609.30741.zip` | `24cd7b226c70aefb9143571cdcfb2d39fb38794a98ba3935d406d89e7202bf7e` | Not provided in fact package | Not applicable: this comparison uses official full packages directly; no EdDSA result is provided | `bundle format unrecognized, invalid, or unsuitable` |
| Latest full package `26.609.41114` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.609.41114.zip` | `6f69d0de7800bc7f48cda0f87810068f109fe6ecf839df8dec0d093cb23ad362` | Not provided in fact package | Not applicable: this comparison uses official full packages directly; no EdDSA result is provided | `bundle format unrecognized, invalid, or unsuitable` |

## ⚖️ Boundaries and Disclaimer

This report is not affiliated with OpenAI, does not represent OpenAI’s position, and is not an official release note.

It does not redistribute any official binaries; it only publishes conclusions derived from diffs of official distribution artifacts.

[Signal] items are not source-level facts. They only indicate that an artifact difference points in a certain direction, but cannot recover source-level intent. All concrete numbers, versions, hashes, and paths in this report come from the provided fact package. Corrections backed by reproducible evidence are welcome.

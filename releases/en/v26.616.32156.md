
---
title: "Codex Desktop 26.616.32156"
version: "26.616.32156"
build: 4157
mac_version: "26.616.32156"
mac_build: 4157
win_version: "26.616.4196.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-18"
method: "官方包逆向 diff(macOS Sparkle / Windows MSIX)"
summary: "macOS 26.616.32156 build 4157 shows no observed file-level changes; Windows moves from 26.616.3767.0 to 26.616.4196.0 with a small app.asar refresh, but no dependency or Computer Use runtime changes."
official_release_notes: false
---

# Codex Desktop 26.616.32156(macOS / Windows Comparison)

> Third-party cross-platform changelog · Unofficial

The main finding is restrained: the macOS package is byte-for-byte unchanged in this batch, while Windows shows a small app-layer rebuild signal without confirmed new functionality.

One-liner: macOS is a no-op package diff, while Windows shows a small app-layer refresh without dependency or CUA runtime changes.

## About This Report

This report is based on reverse diffs of official signed distribution artifacts: Sparkle packages for macOS and MSIX packages for Windows. The two platforms were released with slightly different timing, so this is treated as a cross-platform batch comparison rather than a single identical build.

Evidence levels:

- [Confirmed]: Readable evidence from plist, AppxManifest, package file trees, app.asar inventories, dependency lists, or signing/notarization checks.
- [Signal]: Evidence only from frontend module names, size changes, or build artifacts. It can suggest direction but cannot reconstruct source intent.

Platform labels:

- `[macOS+Windows]`: observed on both platforms.
- `[macOS only]`: observed only in the macOS package.
- `[Windows only]`: observed only in the Windows package.

Comparison scope:

- macOS arm64: 26.616.32156 build 4157 → 26.616.32156 build 4157.
- Windows x64: 26.616.3767.0 → 26.616.4196.0.

## ✨ Highlights

### [Confirmed] [macOS only] macOS Is a No-Op Backfill Diff

The two macOS full packages have the same download URL and the same SHA-256:

`caa5b3ca8e22ca37cc47e594644627f1590f5edf2c7fa01d800b9ab31379090c`

The package file tree remains 8416 → 8416, with 0 added, 0 removed, and 0 changed files. The app.asar inventory also remains 3115 → 3115, with 0 added, 0 removed, and 0 changed files.

Platform-wise, macOS shows no package-content change in this batch; Windows does show a small set of content changes.

### [Signal] [Windows only] Windows Has a Small app.asar Refresh

The Windows package file count stays at 9216 → 9216, but 15 files changed. Inside app.asar, the file count stays at 3078 → 3078, while 66 files were added, 66 removed, and 12 changed.

Because the fact pack explicitly notes that many changes under `webview/assets` are Vite build hash-renaming noise, these changes cannot be used to claim a new feature. The only supported conclusion is that the Windows app-layer artifact was lightly replaced or rebuilt.

Platform-wise, this appears only on Windows; the corresponding macOS batch has no app.asar content changes.

### [Confirmed] [macOS+Windows] Computer Use Runtime Size Is Unchanged

The Computer Use runtime, represented in the fact packs as `cua_node`, shows no total-size change on either platform:

- macOS: 365.9 MB → 365.9 MB, +0 MB, +0%.
- Windows: 285.6 MB → 285.6 MB, +0 MB, +0%.

The baseline sizes differ by platform, but each platform is unchanged across its own comparison.

### [Confirmed] [macOS+Windows] No New cua_node Dependencies

Both platforms report 0 newly added `cua_node` packages.

This means there is no readable evidence that the Computer Use runtime dependency stack expanded in this batch. The Windows app-layer refresh also does not coincide with new `cua_node` dependencies.

### [Confirmed] [Windows only] Windows app.asar Size Is Essentially Flat

Windows `resources/app.asar` remains 159.6 MB → 159.6 MB, with the recorded delta shown as -0.00 MB.

This matches the observation that a small number of internal files changed while the overall artifact size stayed effectively flat. macOS has no corresponding size-change entry because its content diff is zero.

## 📊 Raw Data

| Item | macOS arm64 | Windows x64 |
| --- | --- | --- |
| Compared versions | 26.616.32156 build 4157 → 26.616.32156 build 4157 | 26.616.3767.0 → 26.616.4196.0 |
| Outer file count | 8416 → 8416 | 9216 → 9216 |
| Outer added / removed / changed | 0 / 0 / 0 | 0 / 0 / 15 |
| app.asar internal files | 3115 → 3115 | 3078 → 3078 |
| app.asar added / removed / changed | 0 / 0 / 0 | 66 / 66 / 12 |
| Computer Use runtime total | 365.9 MB → 365.9 MB | 285.6 MB → 285.6 MB |
| New cua_node dependencies | 0 | 0 |
| Key size delta | None | `resources/app.asar` 159.6 MB → 159.6 MB, -0.00 MB |

Verification data:

- Both macOS full packages have SHA-256 `caa5b3ca8e22ca37cc47e594644627f1590f5edf2c7fa01d800b9ab31379090c`.
- Both macOS packages passed notarization verification with source `Notarized Developer ID` and origin `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.
- Windows previous SHA-256: `0ea93d18970fc780b0470448bd5d181ce69f5e3089d36886d328556ee557165a`.
- Windows latest SHA-256: `922e3cf7dee602c84bd168b02fab4d6c925720983f2798b091c307b9c3b3e222`.

## ⚖️ Boundaries And Notes

This is a third-party reverse-engineered changelog, not official OpenAI release notes.

No binaries are redistributed here. The report only summarizes machine-extracted diffs from official signed distribution packages.

Items marked [Signal] are not source-code facts and should not be treated as official feature descriptions. In particular, `webview/assets` and `.vite/build` hash-renaming noise has been excluded from feature interpretation.

Corrections are welcome when backed by official notes, reproducible experiments, or more granular fact packs.

---
title: "Codex Desktop 26.415.32059"
version: "26.415.32059"
build: 1789
previous_version: "26.415.30602"
previous_build: 1773
platform: "macOS arm64"
released: "2026-04-18"
compared_from: "2026-04-17"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "This release is mainly a minor version and packaging-content update, with no new dependencies, file tree expansion, or clearly attributable new capabilities observed."
official_release_notes: false
---

# Codex Desktop `26.415.32059`(build 1789)

> Third-party changelog · Unofficial · Based on reverse-engineered differences in official update packages

This is a very restrained minor release: the confirmed changes are concentrated in the version number, build, and the content hash update for `app.asar`, with no newly observed dependencies or new bundle files.  
English one-liner: a small packaging/version update with no clearly evidenced new user-facing capability.

## About this report

This is not an official changelog, but a **reverse-engineered diff report based on officially distributed artifacts**. This comparison is a historical backfill: the fact package states that both versions fall outside the official Sparkle delta update window, so the comparison was not reconstructed from delta packages. Instead, both official full packages were downloaded and compared directly through a structured diff.

In terms of trustworthiness, both compared artifacts were verified through Apple notarization and OpenAI Developer ID signing:

- Previous version `26.415.30602`: `accepted`, source `Notarized Developer ID`
- Latest version `26.415.32059`: `accepted`, source `Notarized Developer ID`
- The signing source for both is `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`

There are two evidence levels:

- **[Confirmed]**: Direct human-readable evidence, such as `Info.plist` keys and values, dependency lists, signing, and notarization results.
- **[Signal]**: Indirect evidence, such as frontend module names or binary size changes; this type of evidence can only indicate a direction and cannot recover source-level intent from the artifact.

Scope of this report: from `26.415.30602` build `1773`, released on `2026-04-17`, to `26.415.32059` build `1789`, released on `2026-04-18`.

## ✨ Highlights

### 1. Version updated from `26.415.30602` to `26.415.32059` [Confirmed]

This is the clearest change in this release: both the app’s short version number and build were updated.

This means the desktop version shown to users changes from `26.415.30602` to `26.415.32059`, and the build number changes from `1773` to `1789`.

Key evidence:

```diff
/CFBundleShortVersionString: '26.415.30602' → '26.415.32059'
/CFBundleVersion: '1773' → '1789'
```

### 2. `app.asar` content changed, but its size is almost unchanged [Confirmed]

The integrity hash for `Contents/Resources/app.asar` changed, indicating that the application-layer resource package was indeed repackaged or modified. However, the size recorded in the fact package remains `111.5 MB → 111.5 MB`, a change of `+0.00 MB`.

From an observable perspective, this does not look like an update that adds a large feature module or resource package. It is closer to a small content adjustment, a change in build output, or an internal resource update. No further claim can be made here about specific functional changes.

Key evidence:

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
4060f6ecc7fa407ee3522af3024e504701401f6745cf5df1b523b88f0d644f97
→
7aa7db298d9b72c91594737c441e2273f6b057b2f9d15d30a6d2f6121434dd3c
```

### 3. No new bundle files and no new `cua_node` dependencies [Confirmed]

The outer bundle file count remains `498 → 498`, with `0` added files, `0` removed files, and `40` content changes. The number of newly added `cua_node` dependencies is `0`.

This means that, at the artifact level, this release did not introduce new outer components, nor did it add a local dependency group that can be summarized as a “new capability dependency cluster.” The fact package does not provide dependency changes related to PDF, OCR, image processing, or browser automation, so these cannot be described as newly added capabilities.

Key evidence:

```text
Bundle 文件数: 498 → 498
新增 0 / 移除 0 / 内容改动 40
cua_node 新增 0 个包
```

### 4. Files were added and removed inside `app.asar`, but mainly as build-hash noise [Confirmed]

The internal application-layer file count remains `1000 → 1000`; the machine diff shows `66` added, `66` removed, and `6` changed. However, the fact package explicitly notes that file additions and removals with hashed filenames under `webview/assets` and `.vite/build` are Vite build artifact renaming noise, and should not be used to infer added or removed functionality.

Therefore, this only indicates that the frontend build artifacts were regenerated. It should not be presented on its own as a user-visible new feature.

Key evidence:

```text
内部文件: 1000 → 1000 (新增 66 / 移除 66 / 改动 6)
注:webview/assets 下大量增减是 Vite 构建 hash 重命名,属噪音,不要据此判断新功能。
```

## 📊 Raw data

### Bundle file-level statistics

| Metric | Previous version | Latest version | Change |
| --- | ---: | ---: | ---: |
| File count | 498 | 498 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Content changes | - | - | 40 |

### Key component size changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 111.5 MB | 111.5 MB | +0.00 MB |

### Newly added dependency list

| Scope | Number added | Notes |
| --- | ---: | --- |
| `cua_node` | 0 | No new dependency packages added |

## 🔁 Reproducibility information

| Artifact | URL | SHA-256 | Verification conclusion |
| --- | --- | --- | --- |
| Previous full package `26.415.30602` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.415.30602.zip` | `fc9a2b142c67b44edcaf54fad15cf66d1fbfa8baa30527b0e64862baf3747a64` | `spctl` result is `accepted`; source `Notarized Developer ID` |
| Latest full package `26.415.32059` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.415.32059.zip` | `8d2cdf5a922f0820acc1903c2b4006c1cb2ffdeed0d53bdf0607ec4b3183bd97` | `spctl` result is `accepted`; source `Notarized Developer ID` |

| Item | Conclusion |
| --- | --- |
| EdDSA delta package signature verification | Not applicable; the fact package states that this is a historical backfill, and both versions fall outside the official Sparkle delta update window, so the comparison was changed to direct comparison of two downloaded official full packages |
| `codesign` / `spctl` notarization conclusion | Both versions are `accepted` |
| Signing source | `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Boundaries and disclaimer

This report is not affiliated with OpenAI and is not official release notes.

This article does not redistribute any official binaries. It only publishes conclusions organized from the diff fact package.

No [Signal] item constitutes a source-level fact; this release’s fact package also does not contain enough [Signal]-level material to summarize any newly added user-facing functionality.

If anything is missing or the fact package has been interpreted incorrectly, corrections based on reproducible evidence are welcome.
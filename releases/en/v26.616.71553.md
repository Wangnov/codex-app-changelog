
---
title: "Codex Desktop 26.616.71553"
version: "26.616.71553"
build: 4265
mac_version: "26.616.71553"
mac_build: 4265
win_version: "26.616.6631.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-21"
method: "官方包逆向 diff: macOS Sparkle, Windows MSIX"
summary: "This batch updates the macOS package to 26.616.71553 with settings, Markdown, home banner, RPC, and bundled browser plugin changes; the Windows input sample is identical on both sides and shows no content change."
official_release_notes: false
---

# Codex Desktop 26.616.71553 macOS / Windows Comparison

> Third-party cross-platform changelog · unofficial

The substantive changes in this batch are concentrated in the macOS arm64 package; the Windows x64 MSIX input sample has identical from and to packages, so the only supported conclusion is that this sample has no content change.  
One-liner: macOS moved forward with app UI and bundled browser plugin updates, while the Windows sample is byte-identical across the compared pair.

## About This Report

This report uses only machine-extracted facts from reverse-reconstructed official signed distribution packages. It reviews version and channel data, bundle trees, bundled plugins, Computer Use runtime totals, `cua_node` dependencies, `app.asar`, frontend modules, readable strings, platform manifests, CSP, resources, and key file diffs.

Evidence has two levels: [Confirmed] comes from plist, manifest, plugin manifests, service entries, skill docs, dependency lists, signature data, key file diffs, and readable strings outside the “same-name frontend module change hints” section; [Signal] comes from same-name frontend module size changes, module names, class names, and strings in that hint section. Platform tags are `[macOS+Windows]`, `[macOS only]`, and `[Windows only]`.

The comparison ranges are macOS `26.616.51431 build 4212` → `26.616.71553 build 4265`, and Windows `26.616.6631.0` → `26.616.6631.0`. The Windows sample has the same SHA-256 on both sides, which must not be generalized into a claim about all later Windows delivery; it only proves no content change in this input sample.

## ✨ Highlights

### [Confirmed] [macOS only] Version and signature state changed

macOS arm64 moved from `26.616.51431` to `26.616.71553`, with build `4212` → `4265`. `Info.plist` changed at `CFBundleShortVersionString`, `CFBundleVersion`, and `ElectronAsarIntegrity/Resources/app.asar/hash`.

Both macOS full packages passed notarization verification, with source `Notarized Developer ID` and origin `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

The Windows comparison sample has `26.616.6631.0` on both sides, with the same SHA-256: `b572c1820575f005f1492dd61276d1ee935e70f4f0c85f64c2d631438fde533e`. Its file contents did not change.

### [Confirmed] [macOS only] Bundled browser and chrome plugins synced their versions

Two OpenAI bundled plugin manifests in the macOS package updated their version fields:

- `plugins/openai-bundled/plugins/browser/.codex-plugin/plugin.json`: `26.616.51431` → `26.616.71553`
- `plugins/openai-bundled/plugins/chrome/.codex-plugin/plugin.json`: `26.616.51431` → `26.616.71553`

The `browser` plugin manifest still describes the in-app browser for opening, inspecting, navigating, testing, clicking, typing, or screenshotting local web targets such as `localhost`, `127.0.0.1`, `::1`, `file://` URLs, or the current in-app browser tab. The `chrome` plugin manifest still targets automation that depends on the user’s existing Chrome state, including tabs, logged-in sessions, cookies, extensions, and Codex Chrome Extension setup.

The Windows sample has no corresponding content change.

### [Signal] [macOS only] Settings hints expanded around permissions, context usage, work mode, voice, and default window behavior

The `general-settings` frontend module grew from about `0.0 MB` to `0.1 MB`, adding `120320 bytes`. New strings and aria labels point to several settings surfaces:

- disabled default permissions toggle
- composer context usage indicator setting toggle
- work mode settings options
- base size used for code across chats and diffs
- hold-to-dictate, realtime voice, and wake word
- hotkey window capture input
- Popout Window projectless default setting toggle
- dictation dictionary entry

This is a same-name frontend module change hint. It supports a UI, accessibility-label, and state-structure direction, but it does not prove all toggles are visible to users or that related behavior is enabled. The Windows sample has no corresponding change.

### [Signal] [macOS only] Markdown rendering and citation interactions gained new hints

The `markdown` module added `72518 bytes`. New strings cluster around file citations, media fallback, table copying, and “Add to chat”:

- file citation chips, including location information, extensionless files, and extensionless files with location information
- markdown image/video states without alt text, while loading, and on failure
- `Copy table`
- `Add to chat`
- a button label for adding currently selected overlay text to the chat
- `data-markdown-copy`, `data-markdown-image-preview-trigger`, and `data-markdown-table`

This is a frontend signal for Markdown content interaction and accessibility. The Windows sample has no corresponding change.

### [Signal] [macOS only] Home announcements gained hints for remote connections, multi-agent composer, rate-limit resets, and Fast mode

The `home-announcements` module added `29452 bytes`. New strings show expanded home banner states:

- Codex rate limit reset home banner
- Fast mode home banner
- backend-driven Codex app home banner
- multi-agent composer banner
- remote connections banner above the home composer
- `Add Connections`
- ambient suggestions connected apps home banner
- available Codex rate limit resets modal
- `has-seen-fast-mode-home-banner`
- `has-seen-multi-agent-composer-banner`

These are UI signals for home announcements, connection entry points, usage reset surfaces, multi-agent composer, and Fast mode. They are not confirmed evidence of the full rollout scope. The Windows sample has no corresponding change.

### [Signal] [macOS only] RPC added error paths for stream stubs, disposed state, and promise export reuse

The `rpc` module added `56900 bytes`. New readable strings include RPC stubs not being usable as constructors, access after disposal, disallowing property access or method calls on ReadableStream/WritableStream stubs, inability to create pipes without an RPC session, and a system error for peer promise export ID reuse.

This indicates richer RPC error-path and stream-stub handling entered the frontend bundle. The Windows sample has no corresponding change.

### [Signal] [macOS only] Plugin detail page strings for sharing and MCP enablement decreased

The `plugin-detail-page` module shrank by `133332 bytes`. Removed strings include plugin share dialog user/group search, removing a workspace share target, `Add plugin`, `Add people or groups`, workspace link sharing, and an accessible label for the MCP server enablement toggle.

This is only a same-name frontend module removal signal. It suggests related sharing, workspace link, and MCP enablement UI text was reduced or reorganized in this bundle; it does not prove feature removal. The Windows sample has no corresponding change.

### [Confirmed] [macOS only] Main executable grew; Computer Use runtime and `cua_node` dependencies did not

On macOS, `Contents/Resources/codex` grew from `245.2 MB` to `249.3 MB`, a `+4.11 MB` increase. `app.asar` stayed around `155.8 MB`, with the fact package reporting `+0.00 MB`.

The Computer Use `cua_node` runtime total stayed `377.4 MB` → `377.4 MB`, with `0` newly added dependency packages. On Windows, the `cua_node` total stayed `298.9 MB` → `298.9 MB`, also with `0` newly added dependency packages.

## 🔎 Low-Level Signal Index

### [Signal] [macOS only] Dense accessibility-label additions

`general-settings`, `markdown`, and `home-announcements` added multiple `aria-label`, `aria-describedby`, `aria-expanded`, `aria-hidden`, and `aria-labelledby` class or state names, alongside many `Accessible label` strings. This points to broader accessibility coverage. The evidence boundary is frontend bundle signal only.

### [Signal] [macOS only] Input, selection, table, and media preview states

`markdown` includes `cursor-zoom-in`, `data-hex-color-swatch`, `data-markdown-copy`, `data-markdown-image-preview-trigger`, and `data-markdown-table`. These support directional readings around Markdown image preview, table copy, color swatches, or interaction states, but they do not reconstruct source implementation.

### [Signal] [macOS only] Home connection and banner persistence states

`home-announcements` added `http://`, `https://`, `Add Connections`, multiple dismiss accessible labels, and `has-seen-*` state class names. This supports the conclusion that home banners gained more state and entry-point hints, while server-side switches and user visibility are outside the fact package.

### [Signal] [macOS only] Plugin detail page restructuring

`plugin-detail-page` removed multiple `aria-*`, background, border, warning style classes, and sharing-related strings. The low-level signal points to page structure or interaction reorganization, not confirmed standalone feature removal.

## 📊 Raw Data

### macOS arm64

- Version: `26.616.51431 build 4212` → `26.616.71553 build 4265`
- File count: `8827` → `8827`
- Bundle files: added `0` / removed `0` / content changed `68`
- `app.asar` internal files: `3115` → `3115`, added `457` / removed `457` / changed `13`
- `Contents/Resources/codex`: `245.2 MB` → `249.3 MB`, `+4.11 MB`
- `Contents/Resources/app.asar`: `155.8 MB` → `155.8 MB`, `+0.00 MB`
- `cua_node`: `377.4 MB` → `377.4 MB`, `+0 MB`
- Newly added `cua_node` dependencies: `0`
- `browser-client.mjs`: browser and chrome paths both about `1.0 MB` → `1.0 MB`, with the fact package reporting `+0.00 MB`
- Largest new files excerpt: no specific new files listed in the fact package
- Content Security Policy changes: not listed in the fact package
- New components and resources: no standalone entry listed in the fact package

### Windows x64

- Version: `26.616.6631.0` → `26.616.6631.0`
- File count: `9697` → `9697`
- Bundle files: added `0` / removed `0` / content changed `0`
- `app.asar` internal files: `3078` → `3078`, added `0` / removed `0` / changed `0`
- `cua_node`: `298.9 MB` → `298.9 MB`, `+0 MB`
- Newly added `cua_node` dependencies: `0`
- Same SHA-256 on both sides: `b572c1820575f005f1492dd61276d1ee935e70f4f0c85f64c2d631438fde533e`
- Key size changes, AppxManifest changes, CSP changes, new components and resources: no changes listed in the fact package

## ⚖️ Boundaries and Notice

This is a third-party reverse-diff changelog, not official OpenAI release notes, and it does not redistribute any binary content.

[Confirmed] means the fact package contains verifiable file, manifest, plist, signature, dependency, or key diff evidence. [Signal] means only compressed frontend bundle module names, strings, or class names indicate a direction; it is not source-code fact and does not guarantee user visibility.

The Windows comparison sample has identical from and to inputs, so this report only says “this Windows input sample has no content change.” It does not generalize that claim to the whole Windows channel. Corrections or additions based on new official-package fact bundles are welcome.

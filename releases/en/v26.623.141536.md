
---
title: "Codex Desktop 26.623.141536"
version: "26.623.141536"
build: 4753
mac_version: "26.623.141536"
mac_build: 4753
win_version: "26.623.19656.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-07-01"
method: "official package reverse diff: macOS Sparkle, Windows MSIX"
summary: "This batch updates bundled browser/chrome plugin versions on both platforms and shows shared thread side/bottom panel signals; macOS additionally exposes broad appgen, settings, permissions, hotkey, remote project, and syntax-highlighting frontend changes, while the Windows sample is mostly limited to plugin versions and the thread panel delta."
official_release_notes: false
---

# Codex Desktop 26.623.141536 macOS / Windows Comparison

> Third-party cross-platform changelog · Unofficial

This batch is much more active on the macOS frontend surface, while the Windows package shows a narrower set of visible changes.  
One-liner: macOS carries broad UI and workflow signals, while Windows mainly mirrors plugin version bumps and the thread panel update.

## About This Report

This report uses only the machine-extracted facts in the input packages. The macOS comparison covers `26.623.101652` build `4674` to `26.623.141536` build `4753`; the Windows comparison covers `26.623.13972.0` to `26.623.19656.0`, while its plugin manifests update from `26.623.101652` to `26.623.141536`.

Evidence has two levels: [Confirmed] comes from plist, manifest, CSP, dependency lists, plugin lists, service entries, skill documents, key file diffs, and readable strings outside the same-name frontend module clue section; [Signal] comes from frontend module names, size movement, compressed bundle readable strings, and style class changes. Platform scope is marked as `[macOS+Windows]`, `[macOS only]`, or `[Windows only]`.

## ✨ Highlights

### [Confirmed] [macOS+Windows] Bundled browser and chrome plugin versions updated with the package

Both platforms update the `version` field in the bundled `browser` and `chrome` plugin manifests from `26.623.101652` to `26.623.141536`. The descriptions do not show capability text changes.

The `browser` plugin description still covers the in-app browser, `@browser` / `@browser-use`, localhost / file URLs, click, type, screenshot, and opening a local target after significant frontend changes. The `chrome` plugin description still covers the user’s existing Chrome state, including tabs, logged-in sessions, cookies, extensions, and Codex Chrome Extension setup.

### [Confirmed] [Windows only] computer-use plugin manifest version updated

In the Windows package, `resources/plugins/openai-bundled/plugins/computer-use/.codex-plugin/plugin.json` updates its `version` from `26.623.101652` to `26.623.141536`. Its description remains about controlling Windows desktop apps from Codex through Computer Use.

The macOS fact package does not list a computer-use plugin manifest change. macOS `cua_node` total size stays `377.4 MB → 377.4 MB`, while Windows stays `298.9 MB → 298.9 MB`.

### [Signal] [macOS+Windows] Thread pages show side-panel and bottom-panel entry changes

Both platforms add the same `thread-app-shell-chrome` readable strings: opening side chat, browser tab, review tab, and workspace file browser from the thread side panel; opening terminal and History from panel New tab pages; plus `Bottom panel`, closing the thread bottom panel, and opening another tab from the bottom or side panel.

This comes from same-name frontend module clues, so it cannot prove broad user availability. Still, because it appears on both platforms, thread shell panel navigation, tab expansion, and bottom-panel layout are clear shared directions in this batch.

### [Signal] [macOS only] New thread, project, remote conversation, and appgen route chunks were reorganized heavily

The macOS true frontend module add/remove list shows many new combined chunks around `new-thread-panel-page`, `remote-conversation-page`, `projects-index-page`, `worktree-init-v2-page`, `pull-requests-page`, `appgen-page`, `appgen-library-page`, `appgen-publication-terms-route`, `appgen-settings-page`, and `plugin-detail-page`.

This is not just hash rename noise: the fact package lists these after filtering hash-only churn. The visible direction is chunk-boundary movement around new thread panels, remote conversations, project indexes, PRs, worktree initialization, and appgen publication/library/settings surfaces. The Windows sample does not provide a matching true added-module list.

### [Signal] [macOS only] Settings expand around hotkeys, default permissions, work mode, dictation, and appearance details

The macOS `general-settings` chunk grows from about `0.0 MB` to `0.1 MB`, adding strings for disabled default permissions toggle, work mode settings options, code base size for chats and diffs, hold-to-dictate hotkey, hotkey window hotkey, dictation dictionary entry, Popout Window projectless default, ambient suggestions, color diff marker, and chrome theme contrast slider.

These are signals that settings text entered the frontend bundle, not proof that every setting is visible. The Windows fact package does not show this settings module change.

### [Signal] [macOS only] Browser Use any-website permission confirmation text entered the permission panel

The macOS `pending-request-item-panel` adds `Allow Codex to interact with any website without asking?`, `Allow any website`, confirm/cancel button labels, an external browser connection title, a risk warning, and `composer.browserWebsiteAccess.confirm.*` keys.

This is a high-visibility permission and safety signal: confirmation, warning, and external-browser connection paths for allowing Browser Use on any website are present in the package. The Windows sample does not show matching text changes.

### [Signal] [macOS only] Automations page shifts toward remote projects, pinned threads, and archive-task flows

The macOS `automations-page` adds strings for remote connection group/status loading, remote connection color picker, project type dialog, `Add your remote project here`, pin/unpin thread, `Archive all chats`, `Archive and remove`, and `Archive chat and remove scheduled task?`.

The same module removes several pet descriptions and external agent import/onboarding strings. The evidence boundary is frontend signal: it shows automation-related UI text entering or leaving the bundle, not actual entry availability.

### [Signal] [macOS only] Onboarding grows sharply and gains editor/PDF/accessibility clues

The macOS `app-initial~app-main~onboarding-page` grows from `3.5 MB` to `4.9 MB`, an increase of about `1,393,567 bytes`. Added strings include ProseMirror-related warnings, a recommendation to load `style/prosemirror.css`, `xfaNonInteractive input, .xfaNonInteractive textarea`, system fonts, forced-colors, and dark-mode media queries. Removed strings include image attachment upload progress, reasoning effort slider, remove image attachment button, and conversational onboarding task already starting.

This suggests the onboarding chunk was reorganized and absorbed editor, PDF/XFA, or accessibility-related code signals. It remains bundle evidence, not source-level feature proof.

### [Signal] [macOS only] Syntax highlighting and grammar resources were redistributed

Several macOS language bundles gain grammar scopes: `typescript` +`181,117 bytes`, `php` +`111,110 bytes`, `c` +`72,155 bytes`, `haxe` +`35,206 bytes`, `java` +`27,256 bytes`, `stylus` +`31,111 bytes`, `gdshader` +`6,368 bytes`, and `diff` +`2,611 bytes`. At the same time, `javascript`, `less`, `python`, `scss`, `postcss`, and `go` shrink and remove groups of scope strings.

This looks more like syntax-highlighting resource splitting or upgrading than simple language support addition/removal. The Windows sample does not list these module changes.

### [Signal] [macOS only] Math rendering, YAML, and event/plugin error paths changed in package text

The macOS `katex` chunk grows to about `0.3 MB`, adding macro definition, control sequence, MathML, color string, and rendering-related errors. `chunk-XPW4576I` adds YAML/TAG directive error text. `esm` adds `Callback Error`, `Context Cancel`, `Failed to deliver`, `Failed to load destination`, and `plugin Error`, while removing event bus debug-path text.

These are runtime error-path and parser-related signals. Because they come from same-name frontend module clues, they only prove text changes inside the package.

### [Confirmed] [macOS only] CSP does not show a policy expansion in this batch, only entry resource and preload changes

The macOS `webview/index.html` Content-Security-Policy still includes `default-src 'none'` and keeps the existing shape of image, child/frame, worker, script, style, font, media, and connect allowlists. The visible diff changes the entry script hash filename and modulepreload resources, not CSP rules.

The Windows fact package does not provide AppxManifest or CSP diffs.

## 🔎 Low-Level Signal Index

### [macOS+Windows] Panel navigation strings

`thread-app-shell-chrome` adds History, side chat, browser tab, review tab, terminal, workspace file browser, and Bottom panel strings on both platforms. This is the clearest cross-platform UI direction signal in the batch. Evidence boundary: same-name frontend module diff.

### [macOS only] Appgen / publication / library / plugin detail

New module names repeatedly combine `appgen-library-page`, `appgen-publication-terms-route`, `appgen-settings-page`, `appgen-page`, and `plugin-detail-page`. Evidence boundary: chunk names imply route and packaging boundaries, not user-visible gates.

### [macOS only] Hotkeys, mobile, debug, and pets

New chunks include `keyboard-shortcuts-settings`, `codex-mobile-page~debug-window-page~remote-connections-settings~debug-modal`, `debug-window-page~agent-settings~debug-modal`, `pets-settings~appearance-settings~general-settings`, and `pet-install-modal-host~avatar-overlay-page~avatar-overlay-native-page`. Older `pets-settings`, `agent-settings`, and `personalization-settings~appearance-settings~general-settings` chunks are removed. Evidence boundary: module organization signal.

### [macOS only] File search and local tasks

One worktree/new-thread combined module adds `Error fetching fuzzy file search` and `Failed to close fuzzy file search session`; another appgen/library combined module adds `Error forking local task` and `environment.toml`. Evidence boundary: error paths entered the package, with no trigger condition proven.

### [macOS only] Home announcements and share/plugin-detail text reductions

`home-announcements` removes dismiss text for Codex gift credits, rate limit reset, Fast mode, backend-driven banners, multi-agent composer banners, and remote connections banners. `plugin-detail-page` removes text for workspace share targets, workspace users/groups search, MCP server enablement toggle, section navigation, link sharing, and app unavailable. Evidence boundary: frontend text reduction signal.

### [macOS only] Local conversation layout

`local-conversation-page` adds `react.memo_cache_sentinel` plus layout classes such as `max-w-[60%]`, `ml-auto`, `opacity-80`, `text-sm`, and `text-xs`. Evidence boundary: layout/rendering signal.

## 📊 Raw Data

macOS arm64: file count `8875 → 8875`, added `0`, removed `0`, content changed `67`. `app.asar` is `164.6 MB → 164.7 MB`, changing by `+0.11 MB`. `cua_node` is `377.4 MB → 377.4 MB`, with `0` new dependencies. Inside `app.asar`, files move `2635 → 2650`, with `980` added, `965` removed, and `11` changed; many `webview/assets` changes are Vite hash noise.

Windows x64: file count `9749 → 9749`, added `0`, removed `0`, content changed `16`. `resources/app.asar` is `168.9 MB → 168.9 MB`, changing by `+0.00 MB`. `cua_node` is `298.9 MB → 298.9 MB`, with `0` new dependencies. Inside `app.asar`, files remain `3124 → 3124`, with `15` added, `15` removed, and `11` changed.

Key signing/version facts: macOS `CFBundleShortVersionString` changes `26.623.101652 → 26.623.141536`, `CFBundleVersion` changes `4674 → 4753`, and `ElectronAsarIntegrity/Resources/app.asar/hash` is updated. The Windows input sample does not provide AppxManifest diffs, but it does provide `computer-use`, `chrome`, and `browser` plugin manifest version-field changes.

## ⚖️ Boundaries And Notice

This is an unofficial third-party reverse-diff report. It is not OpenAI’s official release notes and does not redistribute binary content.

[Signal] means only that module names, bundle text, style classes, or package sizes changed inside the package. It is not source-code fact and does not guarantee visibility for all users. Platform conclusions are limited to this input sample: macOS and Windows versions, builds, sizes, and dependency counts are cited separately and not projected across platforms. Corrections backed by equally reproducible evidence are welcome.

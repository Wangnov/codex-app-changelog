
---
title: "Codex Desktop 26.623.70822"
version: "26.623.70822"
build: 4559
mac_version: "26.623.70822"
mac_build: 4559
win_version: "26.623.9142.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-27"
method: "官方包逆向 diff: macOS Sparkle, Windows MSIX"
summary: "This batch confirms bundled browser/chrome plugin version bumps on both platforms, adds a Windows computer-use plugin bump, and shows broader macOS-only frontend signals around routes, settings, tasks, plugin sharing, Chrome side panels, and code rendering."
official_release_notes: false
---

# Codex Desktop 26.623.70822 macOS / Windows Comparison

> Third-party cross-platform changelog · Unofficial

This batch shows a clear cross-platform mismatch: the Windows package mainly exposes binary and plugin-manifest updates, while the macOS package also contains many frontend route, settings-page, and UI-copy signals.  
One-liner: this batch confirms bundled plugin version bumps on both platforms, while macOS exposes broader UI and workflow signals in the packaged frontend.

## About This Report

This report uses only the machine-extracted facts in the input packages. It covers macOS arm64 `26.623.61825 build 4548 → 26.623.70822 build 4559` and Windows x64 `26.623.8305.0 → 26.623.9142.0`. The two platforms have different version and release-numbering surfaces, so each claim cites its own platform facts.

Evidence is split into two levels. `[Confirmed]` means the claim is backed by plist, manifest, plugin list, service entry, skill docs, dependency list, readable config, or readable strings outside the same-name frontend-module clue category. `[Signal]` means the claim comes from frontend module names, compressed bundle string deltas, style-class deltas, or size direction. Platform markers are `[macOS+Windows]`, `[macOS only]`, and `[Windows only]`.

The review covered versions and channels, bundle trees, bundled plugin manifests, Computer Use runtime totals, `cua_node` dependencies, `app.asar` summaries, real frontend module additions/removals, same-name frontend module clues, readable strings, Info.plist/AppxManifest, CSP, added components/resources, and concrete key-file diffs. The input does not provide Windows AppxManifest/CSP deltas, nor detailed newly added bundled plugin directories for either platform.

## ✨ Highlights

### [Confirmed] [macOS+Windows] Bundled browser and chrome plugins were version-bumped with the app

Both platforms confirm that the bundled `browser` and `chrome` plugin manifests changed their `version` from `26.623.61825` to `26.623.70822`.

The `browser` plugin description still points to the Codex in-app browser capability, with aliases including `@browser`, `@browser-use`, `browser-use`, and `Browser`. It is described as the tool for opening, inspecting, navigating, testing, clicking, typing, or screenshotting local web targets such as `localhost`, `127.0.0.1`, `::1`, `file://`, or the current in-app browser tab.  
The `chrome` plugin remains scoped to automation that depends on the user’s existing Chrome state, including tabs, logged-in sessions, cookies, extensions, and Codex Chrome Extension setup.

The platform difference: the macOS fact pack lists only `browser` and `chrome` plugin manifest changes; Windows also lists a `computer-use` plugin manifest change.

### [Confirmed] [Windows only] The computer-use plugin manifest was updated

In the Windows package, `resources/plugins/openai-bundled/plugins/computer-use/.codex-plugin/plugin.json` changed its `version` from `26.623.61825` to `26.623.70822`. Its description says it controls Windows desktop apps from Codex through Computer Use.

The macOS fact pack does not list a matching `computer-use` plugin manifest change, so this item is Windows-only for this input sample.

### [Signal] [macOS only] Frontend routes and entrypoints were substantially reshuffled around conversations, projects, AppGen, onboarding, settings, and hotkey windows

Inside macOS `app.asar`, file count changed from `2694 → 2645`. After excluding Vite hash noise, the real module-name additions still show broad route and chunk reshaping. Added modules include combinations such as `local-conversation-page`, `new-thread-panel-page`, `first-run~new-thread-panel-page`, `projects-index-page~local-conversation-page`, `remote-conversation-page~appgen-library-page~local-conversation-page`, `worktree-init-v2-page`, `appgen-publication-terms-route`, `appgen-settings-page`, and `settings-page~open-source-licenses-page~skills-settings~plugins-settings`.

These module names do not prove that the features are enabled for all users, but they do show significant frontend route and code-splitting movement in the macOS package. The Windows `app.asar` only shows `3124 → 3124`, with 1 added file, 1 removed file, and 10 changed files; the input does not provide a frontend module-name list suitable for similar interpretation.

### [Signal] [macOS only] Thread-list task, archive, pinning, and scheduled-task UI signals appeared

The macOS same-name module `remote-conversation-page~projects-index-page` added readable strings for scheduled-task icon accessibility labels, `pin/unpin thread`, `Archive chat`, `Archive chat and remove scheduled task?`, `Archive task`, `Awaiting approval`, and a button label for permanently disabling an input request timeout.

This points to task status, archive confirmation, pinning actions, and approval-state UI near the thread list, projects page, or remote conversation entrypoints. Because the evidence comes from same-name frontend-module deltas, it remains a `[Signal]`, not a confirmed user-visible feature.

### [Signal] [macOS only] Referral invite and rate-limit reset dialog copy entered the frontend bundle

The macOS `remote-conversation-page~new-thread-panel-page~appgen-library-page~hot~djo67r4n` module added strings for referral invite tracking, rate-limit reset referral dialogs, `Add emails`, an all-time period label, closing the invite modal, removing an email, and returning to the invite form.

The same module also added `All chats` and `Archived chats`, indicating related conversation filtering or archive-entry changes in the same batch. The evidence boundary is frontend strings and styles; it does not prove these dialogs are visible to every account.

### [Signal] [macOS only] Update download/install flow and Fast mode home-banner copy appeared

The macOS `automations-page` module added strings for installing a downloaded desktop app update, downloading an update, downloading with progress, installing, cancel/confirm buttons for an update confirmation dialog, and a confirmation subtitle shown before installing an app update while a local session is active.

The same module also added Fast mode home-banner copy for hours, minutes, duration, and a personalized body. This confirms the copy entered the packaged frontend bundle, but not that the update flow or banner is displayed to all users.

### [Signal] [macOS only] Plugin detail gained signals for sharing, workspace links, and MCP server enablement

The macOS `plugin-detail-page` grew from near-empty to about `0.2 MB` and added strings for searching workspace users and groups in a plugin share dialog, removing selected workspace share targets, `Anyone at workspace with the link`, `Anyone in this workspace with the link`, `App unavailable`, more-actions menus in skill preview and app tools modals, and an `MCP server enablement toggle`.

This is a high-visibility plugin-management signal: the plugin detail page appears to be carrying sharing, availability, skill preview, and MCP server enablement UI. The evidence still comes from frontend module clues, so it remains a signal.

### [Signal] [macOS only] Settings signals expanded around work mode, permissions, fonts, dictation, popout windows, ambient suggestions, and diff markers

The macOS `general-settings` module added strings for a disabled default permissions toggle, work mode settings options, code base size, hold-to-dictate hotkey capture input, hotkey window hotkey capture input, one dictation dictionary entry, Popout Window projectless default setting, ambient suggestions toggle, color diff marker option, and chrome theme contrast slider.

These strings show broader settings coverage around default permissions, work mode, code/diff sizing, dictation dictionary, popout windows, ambient suggestions, diff markers, and Chrome theme controls. This is still a frontend bundle signal.

### [Signal] [macOS only] Chrome side panel and recent-task entrypoints entered header and thread shell bundles

The macOS `header` module added strings for a recent tasks menu, unread task updates, `All environments`, `All tasks`, `Cloud tasks`, `Chrome computer use settings`, and `Codex for Chrome`. The `thread-app-shell-chrome` module added side-panel actions for opening History, a side chat, browser tab, review tab, terminal, workspace file browser, bottom panel, and a new-tab-page empty state.

Together, these point to task entrypoints, panel navigation, and multi-panel workspace behavior in Chrome extension or Chrome side-panel contexts. The Windows input has no corresponding frontend string deltas.

### [Signal] [macOS only] Remote control environments, remote connections, and MCP config error paths entered the frontend bundle

The macOS `worktree-init-v2-page~remote-conversation-page~pull-requests-page~new~ozr5a6hk` module added strings including `ChatGPT auth is required to load remote control environments.`, `Failed to load config`, `Failed to load config requirements`, `Failed to load layered config`, `Failed to refresh recent conversations`, `Failed to update MCP server enabled state`, `Failed to write MCP server config`, `features.remote_connections`, and `chatgpt.com`.

This is a frontend error-path signal for remote environment loading, config reads/writes, MCP server state, and ChatGPT authentication. It is not enough to conclude that remote control environments are broadly launched.

### [Signal] [macOS only] Reviewer-agent and approval-state copy moved through onboarding-related bundles

The macOS `onboarding-page` module added strings such as `A carefully prompted reviewer agent is reviewing this request before Codex runs it.`, reviewed, stopped reviewing, and timed out variants. Another appgen/hot module removed the same reviewer-agent copy. `projects-index-page` also added `Awaiting approval`.

This looks more like frontend chunk or flow ownership movement than a simple new feature. The copy is confirmed to appear in the macOS package, but its behavioral implications are not confirmed.

### [Signal] [macOS only] Pull request, diff staging, and report-reason UI signals strengthened

The macOS `onboarding-page` module added `A pull request already exists for this branch`, `Accept edits to stage them`, and `Accessible label for Codex thread report reason options`. Style classes also added diff-header container, dragging/resizing, and ARIA combobox/listbox-related states.

These signals point to PR-exists messaging, accepting edits into staging, thread report reasons, and diff/layout interaction changes. Since they come from same-name module clues, they are not source-level facts.

### [Signal] [macOS only] Older browser settings entrypoints were removed or reorganized

The macOS `browser-use-settings` module went from `0.1 MB → 0.0 MB` and removed strings including `Access history without asking`, embedded browser downloads/history/settings back/forward/breadcrumbs, contact info settings, and extension settings.

This suggests the old browser-use settings module was reduced, removed, or migrated. Because the evidence is module size and string deltas, it should not be written as a confirmed feature deletion; the safer conclusion is that the settings entrypoint or code split changed.

### [Signal] [macOS only] Code highlighting and rendering dependencies show broad grammar-scope movement

On macOS, same-name modules for `typescript`, `php`, `fortran-free-form`, `c`, `haxe`, `stylus`, `shellscript`, `regexp`, `xml`, and `sql` added many TextMate scope strings, while `javascript` and `csharp` removed groups of scope strings. `katex` grew from near-empty to about `0.3 MB` and added parser-error, macro-definition, color-string, and MathML-related strings.

This looks like repackaging or updating code rendering, syntax highlighting, or markdown/math rendering dependencies. It may affect developer experience, but the facts do not support specific claims about which language highlighting improved or regressed.

### [Confirmed] [macOS only] Info.plist version and app.asar integrity hash were updated

The macOS `Info.plist` shows `/CFBundleShortVersionString` changing from `26.623.61825` to `26.623.70822`, `/CFBundleVersion` from `4548` to `4559`, and `/ElectronAsarIntegrity/Resources/app.asar/hash` from `34ba50c30de570df080f886c787432321cc109b7d6317d27078624fc15bbfa49` to `cc1442eaf30d8034a1fb22ed06f9b62efa79e6d6d629e18b0b1af568c5d11baf`.

This is direct evidence of the macOS app version and asar integrity update.

### [Confirmed] [macOS+Windows] Computer Use runtime size is effectively unchanged, with no new `cua_node` packages

The macOS Computer Use runtime total stayed at `377.4 MB → 377.4 MB (+0 MB, +0%)`; Windows stayed at `298.9 MB → 298.9 MB (+0 MB, +0%)`. Both platforms show `0` newly added `cua_node` packages.

This means the input sample does not show new Computer Use runtime dependencies. The Windows computer-use plugin manifest bump does not imply runtime dependency growth.

## 🔎 Low-Level Signal Index

### [Signal] [macOS only] AppGen, publication terms, and library routes

Added module names repeatedly include `appgen-page`, `appgen-library-page`, `appgen-settings-page`, and `appgen-publication-terms-route`, combined with `remote-conversation-page`, `projects-index-page`, `pull-requests-page`, and `plugin-detail-page`. Boundary: this is route and code-splitting evidence, not proof that AppGen capability changes are enabled for users.

### [Signal] [macOS only] Hotkey window, projectless mode, and workspace roots

New modules and strings include `hotkey-window-thread-page`, `hotkey-window-worktree-init-page`, `keyboard-shortcuts`, `hotkey-window-projectless-default-enabled`, `active-workspace-roots`, and `workspace-root-options`. Boundary: this shows packaged frontend state around hotkey windows and workspace roots, not confirmed default behavior changes.

### [Signal] [macOS only] Task states, hooks, and remote project state

`remote-conversation-page~hotkey-window-thread-page~automations-page` added style/state strings such as `active-remote-project-id`, `hook/started`, `hook/completed`, `host-id-remote-control-allowed`, `enabled-reasoning-efforts`, and `mac-menu-bar-enabled`. Boundary: these are compressed bundle state strings, not configuration-file facts.

### [Signal] [macOS only] Workspace admin and billing entrypoints

One appgen/hot module added `Cannot save workspace admin request without account`, along with `https://chatgpt.com/#settings/Billing`, `plan-pricing`, `professional-email-domain`, `workspace-admin-requests`, and `workspace-monthly-usage`. Boundary: the strings are in the package; this does not prove billing or admin-request flows are visible to every user.

### [Signal] [macOS only] Home announcements copy was reduced

`home-announcements` removed dismiss/body copy for Codex gift credits, rate-limit reset, Fast mode, backend-driven app banner, multi-agent composer banner, remote connections banner, `Add Connections`, and ambient suggestions connected apps. Boundary: this may reflect code migration or conditional delivery changes, not necessarily deletion of announcement functionality.

### [Signal] [macOS only] Avatar/pets modules were reorganized

Added modules include `pet-install-modal-host~avatar-overlay-page~avatar-overlay-native-page` and `pets-settings~appearance-settings~general-settings`, while the older `avatar-overlay-page~avatar-overlay-native-page~pets-settings` module was removed. Boundary: the module-combination change is clear, but the fact pack does not include enough user-facing copy to treat this as a highlight.

### [Signal] [macOS only] Telemetry middleware fields

`middleware` added strings including `Loaded a Page`, `Loaded a Screen`, `context.device`, `context.ip`, `context.page.referrer`, `context.traits.email`, `context.userAgent`, and `integrations.all`. Boundary: these are packaged strings and do not establish a telemetry policy change.

### [Confirmed] [Windows only] Windows app.asar only shows a small file-level delta

Windows `app.asar` internal files stayed at `3124 → 3124`, with 1 added file, 1 removed file, and 10 changed files. The fact pack does not list same-name frontend module string deltas, real frontend module additions/removals, AppxManifest, or CSP changes, so no Windows frontend feature signal can be responsibly summarized.

## 📊 Raw Data

### macOS arm64

Version and verification: `26.623.61825 build 4548 → 26.623.70822 build 4559`. The previous full package SHA-256 is `a488b3a5e88e8c16c0f76374c54b7573dec2fbdbf9789c4666fb53ec1265debf`; the latest is `4a29862faa4e4ceb177f7cb9fcbf93b65f42a3ddfda69ed9c1e8dfe2a0edb58b`. Both notarization checks were accepted, with source `Notarized Developer ID` and origin `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

Bundle tree: file count `8875 → 8875`, with `0` added, `0` removed, and `67` content-changed files. `Contents/Resources/app.asar` changed from `164.9 MB → 164.7 MB`, a `-0.22 MB` delta. `Contents/Resources/codex` changed from `249.4 MB → 249.4 MB`, a `+0.02 MB` delta.

Application layer: `app.asar` internal files changed from `2694 → 2645`, with `974` added, `1023` removed, and `11` changed. The fact pack notes that many `webview/assets` additions/removals are Vite hash rename noise.

Dependencies and runtime: Computer Use runtime `377.4 MB → 377.4 MB (+0 MB, +0%)`; `cua_node` added packages: `0`.

Key files: the `chrome` and `browser` plugin manifest versions changed to `26.623.70822`; `Info.plist` changed short version, build, and app.asar integrity hash; `avatar-overlay-composition-surface.html` had a large modulepreload reshuffle, adding references to many `worktree-init-v2`, `remote-conversation`, `appgen`, `onboarding`, `settings`, `pets`, `hotkey-window`, and `local-conversation` chunks.

### Windows x64

Version and verification: `26.623.8305.0 build 26.623.8305.0 → 26.623.9142.0 build 26.623.9142.0`. The previous full package SHA-256 is `2e3461f808ba7479903202e76e39a1fe208e328e86b6a69a6e264dd45295cef1`; the latest is `44389719a86dedec96a95cadee870df1a2ae2517fe9293f050b7c71b868e2d86`.

Bundle tree: file count `9749 → 9749`, with `0` added, `0` removed, and `20` content-changed files. `resources/codex.exe` changed from `323.0 MB → 323.1 MB`, a `+0.05 MB` delta. `resources/codex` changed from `285.9 MB → 285.9 MB`, a `+0.03 MB` delta. `resources/codex-windows-sandbox-setup.exe` changed from `8.8 MB → 8.8 MB`, a `+0.00 MB` delta. `resources/codex-command-runner.exe` changed from `1.3 MB → 1.3 MB`, a `-0.00 MB` delta.

Application layer: `app.asar` internal files stayed at `3124 → 3124`, with `1` added, `1` removed, and `10` changed. The fact pack notes that many `webview/assets` additions/removals are Vite hash rename noise.

Dependencies and runtime: Computer Use runtime `298.9 MB → 298.9 MB (+0 MB, +0%)`; `cua_node` added packages: `0`.

Key files: the `computer-use`, `chrome`, and `browser` bundled plugin manifests changed their `version` from `26.623.61825` to `26.623.70822`. The input does not provide AppxManifest, CSP, same-name frontend-module string deltas, or newly added bundled plugin details.

## ⚖️ Boundaries And Notes

This is an unofficial third-party reverse-diff changelog and does not represent OpenAI official release notes. It does not redistribute binaries, and it does not treat compressed bundle strings or module names as source-level facts.

`[Confirmed]` means the claim is directly supported by a manifest, plist, dependency list, configuration, or readable string in the fact pack. `[Signal]` means only frontend modules, style classes, strings, or size direction changed. In particular, frontend copy entering the package does not mean it is visible to all users or that the feature is enabled. Corrections based on the same official signed packages are welcome.

---
title: "Codex Desktop 26.527.60818"
version: "26.527.60818"
build: 3437
previous_version: "26.527.31326"
previous_build: 3390
platform: "macOS arm64"
released: "2026-06-01"
compared_from: "2026-05-30"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This release is mainly a version bump with bundled browser / Chrome plugin version alignment, plus a small increase in the size of the underlying codex executable component; no new files or new cua_node dependencies were observed."
official_release_notes: false
---

# Codex Desktop `26.527.60818`(build 3437)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

The changes from `26.527.31326` to `26.527.60818` are quite contained: the readable evidence is mainly concentrated in version number updates, bundled plugin version alignment, and an increase in the size of the underlying `codex` component.  
English one-liner: a narrow build bump with bundled plugin version alignment and a slightly larger core binary.

## About this report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. This entry is a historical backfill: both versions are outside the official Sparkle incremental update window, so the evidence package uses a direct comparison of two official full packages rather than reconstruction from an incremental package.

In terms of confidence, both compared artifacts come from official download URLs and passed Apple notarization and OpenAI Developer ID validation. The `spctl` results for both the previous and latest versions are `accepted`; both report `Notarized Developer ID` as the source, and both are signed by `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

This report uses two evidence levels:

- **[Confirmed]**: Direct human-readable evidence, such as `Info.plist`, plugin `plugin.json`, and signing / notarization output.
- **[Signal]**: Indirect evidence, such as binary size changes. It points in a direction, but cannot be used to recover source-level intent from the artifact.

Comparison scope: `26.527.31326` build `3390`, released on `2026-05-30`; compared against `26.527.60818` build `3437`, released on `2026-06-01`. Platform: macOS arm64.

## ✨ Highlights

### 1. Desktop app version advanced to `26.527.60818` [Confirmed]

In the app’s `Info.plist`, the short version was updated from `26.527.31326` to `26.527.60818`, and the build was updated from `3390` to `3437`.

This means it is a newly signed and notarized desktop release, not merely a repackaging with internal resource changes.

Key evidence:

```diff
CFBundleShortVersionString: 26.527.31326 → 26.527.60818
CFBundleVersion: 3390 → 3437
```

### 2. Bundled Browser plugin version aligned with the main app [Confirmed]

In the `browser` plugin’s `plugin.json`, the plugin version was updated from `26.527.31326` to `26.527.60818`. The plugin description still points to Codex’s built-in browser capabilities, including opening, inspecting, navigating, testing, clicking, typing, and taking screenshots of local Web targets such as `localhost`, `127.0.0.1`, `::1`, and `file://`.

The observable takeaway is that this release confirms the built-in Browser plugin continues to be bundled and maintained in sync; however, the evidence package only shows version alignment and does not show any change to the plugin capability text.

Key evidence:

```diff
"name": "browser",
"version": "26.527.31326" → "26.527.60818"
```

### 3. Bundled Chrome plugin version aligned with the main app [Confirmed]

In the `chrome` plugin’s `plugin.json`, the plugin version was likewise updated from `26.527.31326` to `26.527.60818`. Its description still points to Chrome automation capabilities covering remote URLs, pages that require login or user profiles, existing tabs, cookies, extensions, and Codex Chrome Extension setup.

This indicates that automation capabilities related to the user’s existing Chrome state are still distributed as a bundled plugin in this release; based on the readable diff, however, the change is limited to the version number.

Key evidence:

```diff
"name": "chrome",
"version": "26.527.31326" → "26.527.60818"
```

### 4. Underlying `codex` component increased by about `2.81 MB` [Signal]

`Contents/Resources/codex` increased from `198.6 MB` to `201.4 MB`, a change of `+2.81 MB`.

This is an indirect signal that the underlying executable component or its packaged contents changed, but source-level intent cannot be recovered from the artifact. The evidence package does not provide a readable diff, so no further claim can be made about specific new functionality.

Key evidence:

```text
Contents/Resources/codex: 198.6 MB → 201.4 MB (+2.81 MB)
```

### 5. No file-tree-level additions / removals, and no new `cua_node` dependencies [Confirmed]

The Bundle file count remained `1982 → 1982`; at the file-tree level there were `0` additions, `0` removals, and `51` content changes. The number of newly added `cua_node` dependencies was `0`.

This makes the release look more like a localized update than a large-scale component addition or dependency expansion. Although there were `216` additions and `216` removals inside `app.asar`, the evidence package explicitly notes that many changes under `webview/assets` are Vite hash-renaming noise and cannot be used to infer new features.

## 📊 Raw data

### Bundle file-level statistics

| Metric | Previous version | Latest version | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 1982 | 1982 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Files with content changes | - | - | 51 |

### Key component size changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/codex` | 198.6 MB | 201.4 MB | +2.81 MB |
| `Contents/Resources/app.asar` | 140.3 MB | 140.3 MB | -0.00 MB |

### Newly added dependency list

| Scope | Number of newly added dependencies | Notes |
| --- | ---: | --- |
| `cua_node` | 0 | No newly added packages recorded in the evidence package |

## 🔁 Reproducibility information

| Artifact / verification item | Result |
| --- | --- |
| Previous full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.527.31326.zip` |
| Previous SHA-256 | `cfaa14d6a8a8a8de403028f927f6b64ba6692c3c9759eafec0fd33764124b9d2` |
| Latest full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.527.60818.zip` |
| Latest SHA-256 | `9fc39db92a88769cbbe409a465931d7406959210d127e742d193e0617d06585f` |
| EdDSA signature verification | Not applicable; the evidence package notes this is a historical backfill and did not use a Sparkle incremental package |
| Previous `spctl` notarization | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest `spctl` notarization | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| `codesign` / `spctl` conclusion | Trust root is Apple notarization + OpenAI Developer ID code signing |

## ⚖️ Boundaries and statement

This report is not affiliated with OpenAI and does not represent official release notes.

This report does not redistribute any official binaries; it only publishes diff conclusions derived from officially signed distribution artifacts.

All [Signal] entries represent only indirect artifact-level indications. They are not source-level facts and should not be interpreted as official intent.

If official statements or more complete evidence outside the evidence package become available, the conclusions in this report should be revised accordingly.
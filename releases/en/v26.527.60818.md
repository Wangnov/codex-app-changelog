
---
title: "Codex Desktop 26.527.60818"
version: "26.527.60818"
build: 3437
mac_version: "26.527.60818"
mac_build: 3437
win_version: "26.527.7698.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-05-30"
method: "official package reverse diff: macOS Sparkle, Windows MSIX"
summary: "This batch shares Browser/Chrome plugin version bumps plus home announcement and site sharing UI signals; Windows additionally shows Computer Use, browser permission settings, debug panel, and worktree recovery signals."
official_release_notes: false
---

# Codex Desktop 26.527.60818 macOS / Windows Comparison

> Third-party cross-platform changelog · unofficial

This batch looks like a shared-code update with platform packaging versions out of alignment: common changes cluster around plugin versions, home entry points, and sharing UI, while the Windows package exposes more signals around local automation, browser permissions, and environment recovery.  
One-liner: shared plugin/version and home/share UI updates, with Windows-only signals around Computer Use, browser controls, debug state, and worktree recovery.

## About This Report

This report is based only on machine-extracted reverse diffs from two official signed distribution packages. The macOS comparison is `26.527.31326 build 3390` → `26.527.60818 build 3437`; the Windows comparison is `26.527.3686.0` → `26.527.7698.0`. Platform versions and builds are cited separately and not projected across platforms.

Evidence has two levels: [Confirmed] comes from plist, manifest, plugin config, readable strings, file statistics, dependency lists, or explicit diffs; [Signal] comes from same-name frontend modules, size movement, style class names, and compressed bundle clues. Platform ownership is marked as `[macOS+Windows]`, `[macOS only]`, or `[Windows only]`.

Reviewed input sections include versions/channels, bundle file tree, bundled plugin changes, cua_node dependencies, app.asar, same-name frontend modules, readable strings, plist/manifest data, and concrete file diffs. The fact packs do not provide standalone Computer Use runtime totals, content security policy changes, or added component/resource details; therefore this report does not infer them.

## ✨ Highlights

### [Confirmed] [macOS+Windows] Bundled Browser and Chrome Plugins Bump to 26.527.60818

On both platforms, the `browser` and `chrome` plugin manifests update their version from `26.527.31326` to `26.527.60818`. The Browser description still covers the in-app browser, local URLs, file URLs, screenshots, clicks, typing, and localhost navigation; the Chrome description still targets remote URLs, authenticated/profile-dependent pages, existing tabs, cookies, extensions, and Codex Chrome Extension setup.

Platform difference: the macOS fact pack only shows Browser and Chrome plugin manifest version changes; Windows also shows a Computer Use plugin version change.

### [Confirmed] [Windows only] Computer Use Plugin Manifest Follows the Version Bump

In the Windows package, `resources/plugins/openai-bundled/plugins/computer-use/.codex-plugin/plugin.json` updates from `26.527.31326` to `26.527.60818`. Its description says it controls Windows desktop apps from Codex through Computer Use.

Platform difference: the macOS fact pack has no corresponding Computer Use plugin manifest diff. That is not proof that macOS had no capability change; it only means this input sample does not provide matching confirmed evidence.

### [Signal] [macOS+Windows] Home Announcements Add Fast Mode, Remote Connections, Connected Apps, and Multi-Agent Work Copy

Both platforms show the same `home-announcements` module change. New readable strings include Fast mode home banner, backend-driven Codex app home banner, multi-agent composer banner, remote connections banner, `Add Connections`, “Codex suggests what to do next by searching project files and connected apps,” and “Delegate work to subagents that work in parallel. Note: may increase token usage.”

This indicates packaged home-surface signals for fast mode, remote connections, ambient suggestions from connected apps, and parallel subagents. The evidence boundary is frontend-module signal; it does not prove all users see these items or that they are enabled by default.

### [Signal] [macOS+Windows] App/Site Sharing UI Gains Fuller Access Scope and Link Copy Copy

Both platforms’ `appgen-share-dialog` modules add site-sharing strings: adding people/groups, workspace-link access, internet access, `Can visit`, copying a live site URL, opening a live site externally, saving state, and custom access policy labels.

Platform difference: macOS and Windows both show the same signal class, although the Windows old file name differs. This points toward expanded appgen/live-site sharing and permissions UI, but remains a frontend-module signal.

### [Signal] [macOS only] Plugin Detail Page Adds Install, Enablement, Sharing, and Accessibility Clues

The macOS `plugin-detail-page` module grows by about `130713 bytes` and adds strings including `Add to Codex`, `Adding to Codex`, `App enabled`, `App disabled`, `App unavailable`, MCP server enablement toggle, plugin share dialog, workspace link, more-actions menu, and section navigation accessible labels.

Platform difference: the Windows fact pack has no same-name `plugin-detail-page` change signal. This is only a macOS input-sample frontend signal and cannot be promoted to confirmed plugin-system behavior.

### [Signal] [Windows only] Browser Use Settings Add History Access, Domain Allow/Block, and Upload/Download Domain Controls

The Windows `browser-use-settings` module grows by about `59354 bytes`. New strings include `Access history without asking`, embedded extension/password manager/site settings, show/hide browser data clearing options, disabled toggles for unavailable plugins, and entries for adding allowed/blocked domains, download domains, and upload domains.

This is a high-visibility signal around browser permissions and site controls. Platform difference: the macOS fact pack has no corresponding module signal.

### [Signal] [Windows only] Debug Modal Expands into Account, Session, Browser Tab, App Actions, and Automatic Worktree Cleanup State

The Windows `debug-modal` module grows by about `171302 bytes`. New strings include `(command unavailable)`, `Account ID`, `Active browser tab`, `Active conversation`, `Ambient suggestion chats`, `App Actions`, `App Server`, `App session ID`, and `Allow automatic worktree cleanup in this unpackaged build.`

This points to a debug UI covering runtime state, session state, browser state, app-server state, and an unpackaged-build worktree cleanup switch. The evidence boundary remains same-name frontend-module signal.

### [Signal] [Windows only] @ Mention List Adds Computer Use, Desktop Apps, Agents, and App Capability Grouping

The Windows `at-mention-list` module adds `Computer use`, `Desktop apps`, `Mac apps`, `Custom agents`, `Live agents`, a Computer Use label, and section headers for app capability mention server/scope.

This indicates UI signal for grouping capabilities, apps, and agents in the mention menu. Because the Windows package also contains `Mac apps`, that string must not be projected back onto macOS UI; it is only a Windows package string signal.

### [Signal] [Windows only] Worktree Initialization Failure Flow Adds Continue, Repair, and Environment Settings Paths

The Windows `worktree-init-v2-page` module adds strings such as `Continue anyway`, `Edit environment`, `Fix this project`, canceling worktree creation, continuing after local environment setup failure, opening local environment settings, starting a repair thread, creating a worktree and running setup, and forking a conversation.

This is a recovery-path signal for local environment initialization failures. Platform difference: the macOS fact pack has no matching module change signal.

### [Signal] [macOS+Windows] Composer Styling/State Shows a Small Convergence

On both platforms, the `composer` module removes `data-tooltip-overflow-target`, `h-token-button-composer`, and `model-provider`; the size delta is `-1442 bytes` on both.

This only indicates a small internal state or styling adjustment in the composer bundle. It is not enough to infer a feature removal.

### [Signal] [macOS only] Thread Side Panel and Markdown Modules Show Larger Removal Signals

On macOS, `thread-side-panel-tabs` shrinks by about `291755 bytes`, removing strings related to CI diagnosis prompts, external logs, permission/credential checks, existing PRs, and accepting edits to stage them. The `markdown` module shrinks by about `63388 bytes`, removing strings related to KaTeX annotation, file citation chips, missing image/video alt text, load failures, and preview triggers.

These are removal signals inside the macOS frontend bundle. Without source-level config or manifest evidence, they should not be written as confirmed feature removals.

## 🔎 Low-Level Signal Index

- [Signal] [macOS+Windows] `app.asar` internal files show `216` added and `216` removed on both platforms, and the fact packs explicitly say many `webview/assets` changes are Vite hash-renaming noise, not feature evidence.
- [Signal] [macOS only] `dist` removes multiple ARIA and selection state class names and adds only `data-state`; this looks like lower-level component state implementation movement.
- [Signal] [macOS only] `plugin-detail-page` adds many `aria-*`, menu, and disabled-state class names, reinforcing the plugin detail accessibility, enablement-toggle, and sharing-menu signals.
- [Signal] [Windows only] `debug-modal` adds multiple chart/danger/background classes; together with state strings, it points to richer debug state display.
- [Signal] [Windows only] `at-mention-list` adds dropdown, hotkey, hover, and selected classes; together with Computer Use and agents copy, it points to mention-menu grouping and interaction changes.
- [Confirmed] [macOS+Windows] cua_node dependency changes show `0` new packages on both platforms; this fact pack does not support a claim of new Node dependencies.
- [Confirmed] [macOS+Windows] No largest-added-file entries are listed; both top-level bundle trees show `0` added and `0` removed files.

## 📊 Raw Data

macOS arm64: `26.527.31326 build 3390` → `26.527.60818 build 3437`. Bundle files `1982 → 1982`, with `0` added, `0` removed, and `51` content-changed. `Contents/Resources/codex` grows from `198.6 MB` to `201.4 MB`, up `2.81 MB`; `Contents/Resources/app.asar` is about `140.3 MB → 140.3 MB`, shown as `-0.00 MB`. app.asar internals are `2774 → 2774`, with `216` added, `216` removed, and `12` changed. Info.plist updates `CFBundleShortVersionString` to `26.527.60818`, `CFBundleVersion` to `3437`, and the app.asar integrity hash. Full-package SHA-256 values are `cfaa14d6a8a8a8de403028f927f6b64ba6692c3c9759eafec0fd33764124b9d2` and `9fc39db92a88769cbbe409a465931d7406959210d127e742d193e0617d06585f`. Notarization verification output is `Too many open files` for both packages.

Windows x64: `26.527.3686.0` → `26.527.7698.0`. Bundle files `8648 → 8648`, with `0` added, `0` removed, and `17` content-changed. `resources/codex.exe` grows from `253.5 MB` to `258.0 MB`, up `4.51 MB`; `resources/codex` grows from `223.2 MB` to `226.7 MB`, up `3.48 MB`; `resources/codex-command-runner.exe` grows by `0.01 MB`; `resources/codex-windows-sandbox-setup.exe` shows `+0.00 MB`; `resources/app.asar` is about `144.1 MB → 144.1 MB`, shown as `-0.00 MB`. app.asar internals are `2722 → 2722`, with `216` added, `216` removed, and `11` changed. Full-package SHA-256 values are `9eaca939262036442a09633031a401112db54d422bcec53f7513a0e64b0810d1` and `eb038b71b9138d739e652d51adc15b3628cac34441727916b64e6e4699d259a4`. `.installed-owl-shell.json` changes `installedAt` from `2026-05-29T21:44:41.484Z` to `2026-06-01T17:19:28.117Z`; cacheKey and archiveSha are not shown as changed in the excerpt.

## ⚖️ Boundaries and Disclaimer

This is an unofficial third-party reverse-diff changelog. It is not an OpenAI official release note and does not redistribute binaries. This report only uses facts from the provided fact packs; same-name frontend modules and size deltas are treated as [Signal], not source-level facts, and they do not prove that all users can see or use the behavior by default. The Windows fact pack’s “platform/architecture” line says macOS arm64, but this report treats that package as the Windows x64 MSIX sample according to the input section and task instructions. Corrections are welcome based on official notes, additional platform samples, or reproducible diffs.

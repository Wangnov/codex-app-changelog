
---
title: "Codex Desktop 26.616.41845"
version: "26.616.41845"
build: 4198
mac_version: "26.616.41845"
mac_build: 4198
win_version: "26.616.5445.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-19"
method: "Official package reverse diff: macOS Sparkle, Windows MSIX"
summary: "The macOS input sample is byte-identical; Windows updates bundled plugin versions and Codex program components, with frontend signals around recent tasks, cloud tasks, Chrome/Browser Use settings, and the debug modal."
official_release_notes: false
---

# Codex Desktop 26.616.41845 macOS / Windows Comparison

> Third-party cross-platform changelog · Unofficial

In this batch, the macOS sample shows no content changes; the Windows sample shows program and bundled plugin version movement, plus visible signals around tasks, settings, debugging, and browser-related UI.  
One-liner: macOS is byte-identical in this sample, while Windows advances bundled plugin versions and shows task, settings, debug, and browser-use UI signals.

## About This Report

This report uses only machine-extracted facts from reverse-reconstructed official signed distribution packages. The macOS comparison range is `26.616.41845 (build 4198)` to the same version and build; the Windows comparison range is `26.616.4196.0` to `26.616.5445.0`.

Evidence is split into two levels: [Confirmed] comes from plist, manifest, CSP, skill docs, type declarations, package.json, dependency lists, plugin manifests, service entry points, signing information, and readable strings outside the “same-name frontend module change clues” section; [Signal] comes from frontend module names, size deltas, style class names, and compressed bundle direction clues.

Platform ownership is marked as `[macOS+Windows]`, `[macOS only]`, or `[Windows only]`. Hash renames under webview/assets and .vite/build are treated as build noise per the input and are not used as feature additions or removals.

## ✨ Highlights

### [Confirmed] [macOS only] This macOS Input Sample Has No Content Change

The macOS arm64 previous and latest packages are both `26.616.41845 (build 4198)`. The two full-package URLs are the same, and the SHA-256 is also identical: `8db6319b1116dd35e70fabba67e500515e530f9595d0b18df0e8d471b3895357`.

The file tree statistics are also identical: file count `8416 → 8416`, added `0`, removed `0`, content changed `0`. The `app.asar` internal file count is `3115 → 3115`, with added `0`, removed `0`, changed `0`. Therefore this report can only state that this input sample has no macOS content changes; it cannot be extended to mean macOS received no changes in other batches.

### [Confirmed] [Windows only] Bundled Plugin Versions Advance to the macOS Short Version

In the Windows package, three OpenAI bundled plugins update their `plugin.json` version from `26.616.32156` to `26.616.41845`:

- `computer-use`
- `chrome`
- `browser`

The `computer-use` description still refers to controlling Windows desktop apps through Computer Use; the `chrome` description still covers automation based on the user’s existing Chrome state; the `browser` description still covers the Codex in-app browser, local target navigation, inspection, clicking, typing, and screenshots. The fact package only shows the version field changing, not changes to these description texts.

### [Confirmed] [Windows only] Codex Program Components Changed

The Windows top-level file count stays `9216 → 9216`, with no added or removed files, but `19` content changes. Size movement is concentrated in program-related components:

- `resources/codex`: `278.4 MB → 282.0 MB`, up `3.60 MB`
- `resources/codex.exe`: `318.0 MB → 318.2 MB`, up `0.25 MB`
- `resources/codex-windows-sandbox-setup.exe`: `8.8 MB → 8.8 MB`, up `0.01 MB`
- `resources/codex-command-runner.exe`: `1.3 MB → 1.3 MB`, up `0.00 MB`
- `resources/app.asar`: `159.6 MB → 159.6 MB`, up `0.00 MB`

This confirms that the Windows package content advanced, but the input facts do not expose source-level functional differences inside these binaries.

### [Confirmed] [macOS+Windows] Computer Use Runtime Totals Are Unchanged, with No New cua_node Packages

The macOS Computer Use runtime total is `365.9 MB → 365.9 MB`; the Windows total is `285.6 MB → 285.6 MB`. Both are `+0 MB, +0%`. Both platforms also report `0` newly added `cua_node` packages.

The platform difference is that macOS and Windows have different runtime totals under this measurement, but this batch’s input does not show new runtime dependencies on either platform.

### [Signal] [Windows only] Header Signals Recent Tasks, Cloud Tasks, Settings, and Chrome Extension Thread Entry Points

The Windows `header` frontend module increases by `32318 bytes`. New readable strings include `All tasks`, `Cloud tasks`, `All environments filter option`, `App settings`, `Chrome computer use settings`, `Clear search`, plus accessibility labels for opening the recent tasks menu, clearing search, opening settings from the header, and opening the active Chrome extension thread in the Codex desktop app.

These are frontend UI and interaction direction signals: task lists, cloud task filtering, settings entry points, and a Chrome extension thread entry may have changed in the header area. Because this evidence comes from same-name frontend module change clues, it does not prove that all users can already see the UI, and it is not source-level fact.

### [Signal] [Windows only] Debug Modal Expands Signals Around Sessions, Browser Tabs, Experiments, and Host Allowlisting

The Windows `debug-modal` module grows from roughly `0.0 MB` to `0.2 MB`, adding `191954 bytes`. New readable strings include `Account ID`, `Active browser tab`, `Active conversation`, `Ambient suggestion chats`, `App Server`, `App session ID`, `Assigned experiments`, `Allow host`, `Allow host starting`, `Already current`, `(command unavailable)`, and `Allow automatic worktree cleanup in this unpackaged build.`

This is a relatively strong debug and internal-state panel signal, covering account, session, browser tab, experiment assignment, host authorization, and an unpackaged-build worktree cleanup option. The evidence still comes from same-name frontend module diffs, so it should be treated as a UI/state direction signal.

### [Signal] [Windows only] Browser Use Settings Removes Multiple Embedded Browser Settings Strings

The Windows `browser-use-settings` module decreases by `104501 bytes`. Removed strings include `Access history without asking`, plus accessibility labels for embedded browser downloads, settings breadcrumbs, contact info settings, extension settings, password manager settings, site settings, and browser data clearing options.

This indicates that the Browser Use settings UI structure or copy may have been reduced, moved, or reorganized. The facts do not provide a replacement location or new wording, so this cannot be treated as proof that the functionality was removed.

### [Signal] [Windows only] Markdown Media and File Citation Accessibility Copy Moves Out of the Current Module

The Windows `markdown` module decreases by `71991 bytes`. Removed strings concentrate around file citation chips, markdown image buttons, image fallback, image placeholder, video fallback, and video without alt text accessibility labels.

This points to a location or implementation change around Markdown-rendered images, videos, and file citation chips. Because this is only a same-name module clue, it does not prove Markdown media support was removed.

### [Signal] [Windows only] Home Announcements Removes Several Home Banner Strings

The Windows `home-announcements` module decreases by `29452 bytes`. Removed strings include dismiss labels for Codex rate limit reset, Fast mode, backend-driven home banner, multi-agent composer banner, and remote connections banner, plus `Add Connections`, the ambient suggestions connected apps banner body, and the multi-agent composer banner body.

This shows cleanup or reorganization in home announcement and banner-related frontend code. The input does not prove that the corresponding announcements or capabilities were removed; it only confirms these strings no longer appear in the newer same-name module.

### [Signal] [Windows only] Appgen Share Dialog Copy Shrinks

The Windows `appgen-share-dialog` module decreases by `20238 bytes`. Removed strings include `Add people or groups`, `Anyone in this workspace with the link`, `Anyone on the Internet`, plus accessibility labels for copying a live site URL, opening a live site externally, searching workspace users, and the save button state.

This is a UI signal around site sharing, visibility, and live site operations. It does not prove sharing capability disappeared; it only shows that this copy moved out of or was rewritten in the current same-name frontend module.

### [Signal] [Windows only] @mention List Removes Computer Use, Custom Agents, MCP, and Desktop App Category Copy

The Windows `at-mention-list` module decreases by `31156 bytes`. Removed strings include `Computer use`, `Custom agents`, `Desktop apps`, `Files and chats`, `Live agents`, `MCP servers`, `Mac apps`, and `Label for the Computer Use feature`.

This suggests that @mention candidate categories or capability entry presentation may have changed. Given the module name, the change is scoped to the mention list signal and does not directly prove those capabilities were removed.

## 🔎 Low-Level Signal Index

### [Windows only] Accessibility and Live State Class Names

`header` adds clues such as `aria-live`, `aria-atomic`, `aria-hidden`, and `aria-label`; `debug-modal` adds `aria-expanded`, `aria-label`, and `aria-modal`. Boundary: these show frontend interaction and accessibility state changes, but they do not reconstruct the concrete component tree.

### [Windows only] Visual Style and Layout Class Names

`debug-modal` adds `backdrop-blur`, `backdrop-blur-xl`, several `bg-token-charts-*` classes, and `bg-token-dropdown-background/*`; `header` adds several `gap-*`, `flex-*`, `font-medium`, and `group/inline` classes. Boundary: these are UI layout and visual-state signals, not functional facts.

### [Windows only] Settings and Content Copy Moved Out of Modules

`browser-use-settings`, `markdown`, `home-announcements`, `appgen-share-dialog`, and `at-mention-list` all show grouped string removals. Boundary: the facts locate the removals only to same-name frontend modules. They do not prove final product feature removal, nor do they prove the copy was not migrated to another bundle.

### [macOS only] Empty Index

The macOS input sample has no added, removed, or changed files, and it provides no frontend module change clues, Info.plist changes, content security policy changes, or new components/resources to index.

## 📊 Raw Data

### macOS arm64

- Version: `26.616.41845 (build 4198)` → `26.616.41845 (build 4198)`
- Full-package SHA-256: `8db6319b1116dd35e70fabba67e500515e530f9595d0b18df0e8d471b3895357` → same SHA-256
- Notarization verification: previous and latest are both `accepted`, `source=Notarized Developer ID`, `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`
- Bundle file count: `8416 → 8416`
- Bundle changes: added `0` / removed `0` / content changed `0`
- `app.asar` internal files: `3115 → 3115`, added `0` / removed `0` / changed `0`
- Computer Use runtime: `365.9 MB → 365.9 MB`, `+0 MB, +0%`
- New `cua_node` dependencies: `0`
- Info.plist changes: no concrete changes listed in the fact package

### Windows x64

- Version: `26.616.4196.0` → `26.616.5445.0`
- Full-package SHA-256: `922e3cf7dee602c84bd168b02fab4d6c925720983f2798b091c307b9c3b3e222` → `ff6ecb27d0a3c93cf2acfe337af964ec155bd1a770ca4874ad4bb8f6c2bbeb32`
- Bundle file count: `9216 → 9216`
- Bundle changes: added `0` / removed `0` / content changed `19`
- `app.asar` internal files: `3078 → 3078`, added `451` / removed `451` / changed `12`
- `resources/codex`: `278.4 MB → 282.0 MB`, `+3.60 MB`
- `resources/codex.exe`: `318.0 MB → 318.2 MB`, `+0.25 MB`
- `resources/codex-windows-sandbox-setup.exe`: `8.8 MB → 8.8 MB`, `+0.01 MB`
- `resources/codex-command-runner.exe`: `1.3 MB → 1.3 MB`, `+0.00 MB`
- `resources/app.asar`: `159.6 MB → 159.6 MB`, `+0.00 MB`
- Computer Use runtime: `285.6 MB → 285.6 MB`, `+0 MB, +0%`
- New `cua_node` dependencies: `0`
- Plugin versions: `computer-use`, `chrome`, and `browser` all move from `26.616.32156` to `26.616.41845`

## ⚖️ Boundaries and Disclaimer

This is a third-party unofficial reverse-diff report, not OpenAI official release notes. It does not redistribute binaries, and it does not claim source reconstruction beyond the supplied fact package.

[Confirmed] means the corresponding file, config, manifest, plugin list, or readable string is present in the package. [Signal] means only that frontend module names, sizes, class names, or same-name bundle copy changes point in a direction; it is not source-level fact and does not guarantee visibility to all users. Corrections are welcome when based on the same official packages and reproducible extraction results.

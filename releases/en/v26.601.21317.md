---
title: "Codex Desktop 26.601.21317"
version: "26.601.21317"
build: 3511
previous_version: "26.601.20914"
previous_build: 3497
platform: "macOS arm64"
released: "2026-06-03"
compared_from: "2026-06-02"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This is a very small maintenance update: the main app, built-in Browser plugin, and Chrome plugin were synchronized to the new version number, but the factual package contains no confirmable evidence of new functionality."
official_release_notes: false
---

# Codex Desktop `26.601.21317`(build 3511)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

This release appears to be a minor maintenance update. The main confirmed changes are concentrated in version numbers, bundled plugin version alignment, and updates to the main app resource archive.  
English one-liner: A small maintenance update with bundled plugin version alignment and a changed app resource archive, but no confirmed new user-facing feature in the extracted facts.

## About This Report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. This entry is a historical backfill: both versions are outside the official Sparkle incremental update window, so the factual package uses a direct comparison between two official full packages rather than reconstructing from an incremental package.

The comparison process comes from machine-extracted facts: download the previous and latest official full packages, use Apple notarization and the OpenAI Developer ID code signature as the trust root, then perform a structured bundle diff analysis. The factual package shows that both versions of `Codex.app` passed `codesign / spctl` notarization-related verification, with the source reported as `Notarized Developer ID` and the signing identity as `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

This report uses two evidence levels:

- **[Confirmed]**: Direct human-readable evidence, such as `Info.plist`, plugin `plugin.json`, configuration diffs, and signing/notarization results.
- **[Signal]**: Indirect evidence, such as frontend module names or binary size changes; this kind of evidence only indicates “points in a certain direction, but cannot be reconstructed from the artifact into source-level intent.”

Comparison scope: from `26.601.20914` build `3497`, released on `2026-06-02`, to `26.601.21317` build `3511`, released on `2026-06-03`.

## ✨ Highlights

### 1. Main App Version Advanced to `26.601.21317` [Confirmed]

This is a clear version update: `CFBundleShortVersionString` changed from `26.601.20914` to `26.601.21317`, and `CFBundleVersion` changed from `3497` to `3511`.

This means macOS can recognize the new app version and build number at the system level; however, these key-value changes alone do not indicate specific functional changes.

Key evidence:

```diff
/CFBundleShortVersionString: '26.601.20914' → '26.601.21317'
/CFBundleVersion: '3497' → '3511'
```

### 2. Built-in Browser and Chrome Plugins Synchronized to the New Version [Confirmed]

In the `plugin.json` files for the built-in `browser` and `chrome` plugins, the plugin version numbers were both updated from `26.601.20914` to `26.601.21317`.

This indicates that these two automation plugins bundled with the app were republished together with the main app. The factual package does not show changes to their descriptions, capability boundaries, or configuration, so this should not be described as “new browser capabilities.”

Key evidence:

```diff
"version": "26.601.20914" → "26.601.21317"
```

Affected components:

| Plugin | Evidence file |
| --- | --- |
| `browser` | `Contents/Resources/plugins/openai-bundled/plugins/browser/.codex-plugin/plugin.json` |
| `chrome` | `Contents/Resources/plugins/openai-bundled/plugins/chrome/.codex-plugin/plugin.json` |

### 3. Minor Change in Main App Resource Archive Content [Signal]

`Contents/Resources/app.asar` changed from `141.6 MB` to `141.3 MB`, a decrease of `0.33 MB`; meanwhile, the `app.asar` hash under `ElectronAsarIntegrity` in `Info.plist` also changed.

This indicates that the app-layer resource content was indeed rebuilt or adjusted, but source-level intent cannot be reconstructed from the artifact. The factual package also explicitly notes that many additions and deletions under `webview/assets` are Vite hash filename rename noise, and should not be used to infer added or removed functionality.

Key evidence:

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
'8293044a58d98dd06f740b67b9cc68e26b0b74c476baa48caae1161cdd130356'
→
'97e10d2004ddc2988ee1a145a5950be9e90e3a4c7c9957961e278490b47c4fd3'
```

### 4. Runtime Archive Fingerprint Updated [Confirmed]

The `runtimeArchiveSha` in `owl-electron-app.json` changed, while `runtimeName` remains `owl`.

This indicates that the runtime archive fingerprint referenced during packaging was updated; however, the factual package does not provide internal runtime diffs, so it is not possible to further determine what behavioral changes it may have introduced.

Key evidence:

```diff
"runtimeArchiveSha": "9084289a644128f65ba44b97caa360ad87cc27291951f6bca141742ca4bb7035"
→
"runtimeArchiveSha": "c6a2b52fed12c736be826f39d524cc764b3bd232ee0727c92e44626e662e06fe"
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Item | Value |
| --- | ---: |
| Total files | `2042 → 2042` |
| Added files | `0` |
| Removed files | `0` |
| Content changes | `53` |

Note: internal added/removed file counts for `app.asar` are not listed, because the factual package already states that many of those changes are dominated by hash filename renames under `webview/assets` and `.vite/build`, which can easily be misread as real functional changes.

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | `141.6 MB` | `141.3 MB` | `-0.33 MB` |

### New Dependency List

| Scope | New dependencies |
| --- | ---: |
| `cua_node` | `0` |

The factual package does not provide any other newly added dependencies.

## 🔁 Reproducibility Information

| Artifact | URL | SHA-256 | Size | EdDSA verification result | codesign / spctl notarization result |
| --- | --- | --- | --- | --- | --- |
| Previous full package `26.601.20914` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.601.20914.zip` | `058d56343c92c6e2d040a5c1a3d4bfea24ceb4c491e4ffb5d41ca6fd8294aff2` | Not provided by the factual package | Not applicable: this backfill uses direct comparison between two official full packages | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest full package `26.601.21317` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.601.21317.zip` | `2b62d763fef6564ea18f9b49b2282a9d0f3df05b7e630a34449888026e490c8b` | Not provided by the factual package | Not applicable: this backfill uses direct comparison between two official full packages | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Boundaries and Disclaimer

This report is not affiliated with OpenAI and does not represent official statements or official release notes.

This report does not redistribute any official binaries; it only publishes diff conclusions derived from officially signed distribution artifacts.

All [Signal] entries indicate only indirect artifact-level evidence. They do not constitute source-level facts and do not mean product intent can be confirmed.

If any interpretation of the factual package is incorrect, corrections based on reproducible evidence are welcome.
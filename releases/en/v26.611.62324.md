
---
title: "Codex Desktop 26.611.62324"
version: "26.611.62324"
build: 4028
mac_version: "26.611.62324"
mac_build: 4028
win_version: "26.611.8604.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-15"
method: "official package reverse diff(macOS Sparkle / Windows MSIX)"
summary: "In this batch, the macOS package is identical to its comparison baseline; Windows shows a small app.asar content change and new temporary app-server state reconciliation strings, while Computer Use runtime size and cua_node dependencies remain unchanged on both platforms."
official_release_notes: false
---

# Codex Desktop 26.611.62324(macOS / Windows comparison)

> Third-party cross-platform changelog · Unofficial

The main finding in this batch is that macOS has no file-level changes, while Windows has a very small app-layer change whose clearest signal points to temporary app-server state reconciliation.  
One-liner: macOS is byte-identical to its baseline, while Windows adds a small app-layer change around temporary app-server state reconciliation.

## About This Report

This report is based on reverse diffs of official signed distribution packages: macOS was compared from Sparkle full packages, and Windows from MSIX distribution packages. The macOS comparison is `26.611.62324` build `4028` → `26.611.62324` build `4028`; the Windows comparison is `26.611.8273.0` → `26.611.8604.0`. The release date is recorded from the batch input as `2026-06-15`.

Evidence is split into two levels: [Confirmed] means directly verifiable material such as readable manifests, package structure, dependency lists, string literals, or file statistics; [Signal] means the direction is inferred from frontend/main-process bundles, size changes, or module movement, without reconstructing source intent.

Platform tags mean: [macOS+Windows] appears in both platform fact packages; [macOS only] appears only in the macOS facts; [Windows only] appears only in the Windows facts.

## ✨ Key Changes

### Application Package Content

[Confirmed] [macOS only] The macOS package has no actual content change in this batch. File count is `8267 → 8267`, with `0` added, `0` removed, and `0` content-modified files; `app.asar` internal files are `3074 → 3074`, with `0` added, `0` removed, and `0` changed. The previous and latest full packages have the same SHA-256: `aa5ffa86f0f629e8b02a7ae08f43cdaa87fca7e176b5b9b7e672c1c21a9c1963`.

[Confirmed] [Windows only] The Windows package has a small amount of content change. File count is `11943 → 11943`, with `0` added, `0` removed, and `16` content-modified files; `app.asar` internal files are `3024 → 3024`, with `313` added, `313` removed, and `12` changed. The fact package explicitly notes that large churn under `webview/assets` is Vite build hash-renaming noise and should not be treated as evidence of new features.

### Temporary app-server State Reconciliation

[Confirmed] [Windows only] The new Windows main-process/frontend bundle contains two newly observed readable strings: `Failed to reconcile temporary app-server state` and `Reconciled temporary app-server state`. This confirms that the package contains string literals or log text related to temporary app-server state reconciliation.

[Signal] [Windows only] These strings point toward some kind of temporary app-server state repair, reconciliation, or migration path, but the fact package only exposes strings from a minified bundle. It does not confirm trigger conditions, user visibility, full workflow, or source implementation. The macOS batch has no content change, so this signal is not present there.

### Computer Use Runtime and cua_node Dependencies

[Confirmed] [macOS+Windows] The Computer Use runtime total size is unchanged on both platforms. macOS remains `365.9 MB → 365.9 MB`, `+0 MB, +0%`; Windows remains `285.6 MB → 285.6 MB`, `+0 MB, +0%`.

[Confirmed] [macOS+Windows] Neither platform adds any new `cua_node` dependency packages; the added package count is `0` on both. This means the fact packages contain no evidence of Computer Use runtime dependency expansion in this batch.

### Signing and Source Verification

[Confirmed] [macOS only] Both previous and latest macOS packages pass notarization verification with result `accepted`, source `Notarized Developer ID`, and origin `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

[Confirmed] [Windows only] The Windows fact package provides distribution URLs and SHA-256 hashes for both packages. The previous SHA-256 is `32cf55c6d9a656eb18066890bac655b77e277bb7be84b942b223d27e86631d21`; the latest SHA-256 is `37f4d4e5ec263797b2b0d05a26e900f21aa1942e40ada380c19e8982dc7018c1`.

## 📊 Raw Data

### macOS arm64

- Version: `26.611.62324` build `4028` → `26.611.62324` build `4028`
- Bundle file count: `8267 → 8267`
- Bundle changes: added `0` / removed `0` / content-modified `0`
- `app.asar` internal files: `3074 → 3074`
- `app.asar` changes: added `0` / removed `0` / changed `0`
- Computer Use runtime: `365.9 MB → 365.9 MB`, `+0 MB, +0%`
- New `cua_node` dependency packages: `0`
- Previous and latest SHA-256: `aa5ffa86f0f629e8b02a7ae08f43cdaa87fca7e176b5b9b7e672c1c21a9c1963`

### Windows x64

- Version: `26.611.8273.0` → `26.611.8604.0`
- Bundle file count: `11943 → 11943`
- Bundle changes: added `0` / removed `0` / content-modified `16`
- `resources/app.asar`: `157.6 MB → 157.6 MB`, change `+0.00 MB`
- `app.asar` internal files: `3024 → 3024`
- `app.asar` changes: added `313` / removed `313` / changed `12`
- Computer Use runtime: `285.6 MB → 285.6 MB`, `+0 MB, +0%`
- New `cua_node` dependency packages: `0`
- Previous SHA-256: `32cf55c6d9a656eb18066890bac655b77e277bb7be84b942b223d27e86631d21`
- Latest SHA-256: `37f4d4e5ec263797b2b0d05a26e900f21aa1942e40ada380c19e8982dc7018c1`

## ⚖️ Boundaries and Disclaimer

This is a third-party reverse-engineered changelog and does not represent official OpenAI release notes.

This report does not redistribute binaries. It summarizes file-level, manifest-level, and string-level differences from official signed distribution artifacts.

Items marked [Signal] only mean that the package contains evidence pointing in that direction. They are not source facts and do not guarantee a user-visible feature. Corrections backed by reproducible evidence are welcome.

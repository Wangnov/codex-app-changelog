
---
title: "Codex Desktop 26.429.61741"
version: "26.429.61741"
build: 2429
previous_version: "26.429.30905"
previous_build: 2345
platform: "macOS arm64"
released: "2026-05-05"
compared_from: "2026-05-01"
method: "Sparkle 增量包重建 + 结构化 bundle 差异"
summary: "This release mainly updates the app version and app.asar contents; the bundle file structure, cua_node dependencies, and distribution signature checks do not show a new capability surface."
official_release_notes: false
---

# Codex Desktop `26.429.61741` build 2429

> Third-party changelog · Unofficial · Based on reverse diffs of official update artifacts

This is a very narrow macOS arm64 update: the version, build, and app.asar integrity hash changed, but the fact pack does not show new plugins, dependencies, or bundle-level structural changes.  
English one-liner: a narrow desktop update with confirmed version/build changes and app.asar content changes, but no observed new dependency or bundle-level capability surface.

## About This Report

This is not an official changelog. It is a **reverse-diff report based on official distribution artifacts**. This entry is a historical backfill: the official appcast did not provide a delta package for `26.429.30905` to `26.429.61741`, so both official full packages were downloaded and compared directly.

The comparison covers macOS arm64 `26.429.30905` build `2345` and `26.429.61741` build `2429`. This report only uses machine-extracted facts from the supplied fact pack, and it does not interpret Vite hash filename churn as feature work.

Evidence has two levels: **[Confirmed]** means there is direct readable evidence such as Info.plist values, signatures, notarization results, dependency lists, or file-tree statistics; **[Signal]** means only indirect evidence exists, such as module names or binary size movement, pointing in a direction but not recoverable to source-level intent. This fact pack does not include usable same-name frontend module change clues, so no UI inference is expanded here.

## ✨ Highlights

### 1. App Version Advanced to `26.429.61741` build `2429` [Confirmed]

The short version in `Info.plist` changed from `26.429.30905` to `26.429.61741`, and the build changed from `2345` to `2429`.

This means the app identity visible to macOS and the app itself has changed. Beyond that, the fact pack does not provide official release notes or user-visible copy changes.

Key evidence:

```diff
CFBundleShortVersionString: 26.429.30905 → 26.429.61741
CFBundleVersion: 2345 → 2429
```

### 2. App-Layer Contents Changed, but No Specific Feature Intent Can Be Recovered [Confirmed]

The integrity hash for `Contents/Resources/app.asar` changed, and the bundle file tree shows `46` content-modified files. Inside app.asar, the file count stayed at `1208`, with `2` internal additions, `2` removals, and `6` modified files.

This confirms the application code or resources were repackaged with changes. However, the fact pack does not provide evidence of new skill docs, type declarations, service entries, settings, permissions, error paths, or user-visible strings, so this cannot be written up as a specific feature launch.

Key evidence:

```diff
ElectronAsarIntegrity/Resources/app.asar/hash:
cb297e87e825012472fbd6fdfc2065f050b666711070f42a3a971428eceb0970
→ bf70509067572e84bb19c3c8f04dcd48d66723935a773c7c8a299045a7eb6993
```

### 3. No New Bundle Components or cua_node Dependencies Observed [Confirmed]

The bundle file count stayed at `508 → 508`, with `0` added files, `0` removed files, and `46` content-modified files. The `cua_node` added dependency count is `0` packages.

At the artifact level, this means the release did not introduce new top-level app components and did not expose a new native capability surface through the `cua_node` dependency list. Compared with dependency-led feature additions, this looks more like a narrow update to existing app-layer contents.

Key evidence:

```text
Bundle files: 508 → 508
Added: 0 / Removed: 0 / Modified: 46
cua_node added packages: 0
```

### 4. Both Compared Builds Pass Developer ID Notarization Checks [Confirmed]

Both the previous and latest `Codex.app` bundles show `accepted`, with source `Notarized Developer ID` and origin `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

This provides a reproducible distribution-source basis for the bundle diff. The conclusion only says the two app bundles used for comparison passed the actual notarization checks; it is not an official release statement.

Key evidence:

```text
source=Notarized Developer ID
origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| File count | 508 | 508 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Content-modified files | 0 | 46 | +46 |

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 130.0 MB | 130.0 MB | +0.00 MB |

### Added Dependencies

| Scope | Added dependencies |
| --- | ---: |
| `cua_node` | 0 packages |

## 🔁 Reproducibility

| Item | Previous | Latest |
| --- | --- | --- |
| Version | `26.429.30905` | `26.429.61741` |
| Build | `2345` | `2429` |
| Release date | `2026-05-01` | `2026-05-05` |
| Artifact | `Codex-darwin-arm64-26.429.30905.zip` | `Codex-darwin-arm64-26.429.61741.zip` |
| SHA-256 | `3a6229d7f6ac5a7a147ea4fe34102874a0ad1a8789347de0ceae797bd863dc97` | `c325741ec38a801889518d62ad756db7d6df1035d755db90a046373c96fb5198` |
| EdDSA verification result | Not provided in the fact pack | Not provided in the fact pack |
| codesign/spctl notarization result | `accepted`; `Notarized Developer ID`; `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` | `accepted`; `Notarized Developer ID`; `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Boundaries and Disclaimer

This report is independent from OpenAI and does not represent an official position or official release notes.

No official binary is redistributed here; this document only publishes conclusions derived from diffs of official signed distribution artifacts.

[Signal] items are not source-level facts. This fact pack does not contain enough frontend module evidence to support concrete UI or interaction inferences.

Corrections are welcome for fact-pack parsing errors, reproduction drift, or evidence classification issues.

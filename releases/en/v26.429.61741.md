---
title: "Codex Desktop 26.429.61741"
version: "26.429.61741"
build: 2429
previous_version: "26.429.30905"
previous_build: 2345
platform: "macOS arm64"
released: "2026-05-05"
compared_from: "2026-05-01"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This is a very restrained minor update: the version and build numbers advance, the main application content changes slightly, but no new dependencies, file tree expansion, or directly attributable new features were found."
official_release_notes: false
---

# Codex Desktop `26.429.61741`(build 2429)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

This release advances from `26.429.30905` to `26.429.61741`, and overall looks more like a low-noise maintenance update than a major expansion of the feature surface.  
English one-liner: a small maintenance-style desktop release with minimal bundle-level movement.

## About this report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. This entry is a historical backfill: both versions have fallen outside Sparkle's incremental update window, so the comparison uses two official full packages directly rather than reconstructing the incremental package path.

The comparison method was: download the official full packages for the previous and latest versions → use Apple notarization and the OpenAI Developer ID as the trust root → verify through codesign / spctl → perform a structured bundle diff.

In terms of confidence, both the previous and latest `Codex.app` passed notarization validation with a result of `accepted`; both sources were `Notarized Developer ID`, and both signing origins were `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

Evidence levels are divided into two tiers:

- **[Confirmed]**: Direct, human-readable evidence from Info.plist, signature verification results, file tree statistics, dependency lists, and similar sources.
- **[Signal]**: Indirect evidence from frontend module names, binary size changes, and similar indicators; it can only suggest a direction, but cannot reconstruct source-level intent from the artifact.

Scope of this report: from `26.429.30905` build `2345`, released on `2026-05-01`; to `26.429.61741` build `2429`, released on `2026-05-05`.

## ✨ Key changes

### 1. Version and build advanced to `26.429.61741` / build `2429` [Confirmed]

In the latest version, `Info.plist` updates the short version string from `26.429.30905` to `26.429.61741`, and the build number from `2345` to `2429`.

This means the desktop app did enter a new signed release version, but based only on this evidence package, the changes are mainly concentrated in a small amount of application content and metadata updates.

Key evidence:

```diff
CFBundleShortVersionString: 26.429.30905 → 26.429.61741
CFBundleVersion: 2345 → 2429
```

### 2. The app bundle file tree remains stable, with no added or removed files [Confirmed]

The bundle file count remains `508 → 508`, with `0` additions, `0` removals, and `46` content changes.

This indicates that this release did not introduce new external components, plugins, or resource directories at the macOS app bundle level; the changes are more likely replacements of existing file contents rather than structural expansion.

Key evidence:

```text
File count: 508 → 508
Added 0 / Removed 0 / Content changed 46
```

### 3. The main application `app.asar` has minor content changes, but its size is virtually unchanged [Confirmed]

`Contents/Resources/app.asar` went from `130.0 MB` to `130.0 MB`, with the recorded size change being `+0.00 MB`. Meanwhile, internal app-layer files were `1208 → 1208`, with `2` additions, `2` removals, and `6` changes.

This points to a very small app-layer adjustment, but the evidence package does not provide the names of the added / removed files, nor readable source-level diffs, so it cannot be further described as a specific functional change. In particular, filename changes under `webview/assets` and `.vite/build` that include hash values are build-artifact renaming noise and cannot be used to infer that features were added or removed.

Key evidence:

```text
Contents/Resources/app.asar: 130.0 MB → 130.0 MB (+0.00 MB)
Internal app.asar files: 1208 → 1208 (Added 2 / Removed 2 / Changed 6)
```

### 4. No new `cua_node` dependencies found [Confirmed]

In this round of `cua_node` dependency changes, the number of newly added packages is `0`.

This means that, within the visible scope of the evidence package, this release did not expand low-level capabilities through newly added dependencies; at minimum, it does not support concluding that new PDF reading, OCR, image processing, browser automation, or similar capabilities were added.

Key evidence:

```text
0 packages added
```

## 📊 Raw data

### Bundle file-level statistics

| Metric | Previous version | Latest version | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 508 | 508 | 0 |
| Added files | - | - | 0 |
| Removed files | - | - | 0 |
| Content-changed files | - | - | 46 |

### Key component size changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 130.0 MB | 130.0 MB | +0.00 MB |

### Newly added dependency list

| Scope | Additions | Notes |
| --- | ---: | --- |
| `cua_node` | 0 | No new packages found |

## 🔁 Reproducibility information

| Artifact / Verification item | Result |
| --- | --- |
| Previous full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.429.30905.zip` |
| Previous SHA-256 | `3a6229d7f6ac5a7a147ea4fe34102874a0ad1a8789347de0ceae797bd863dc97` |
| Latest full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.429.61741.zip` |
| Latest SHA-256 | `c325741ec38a801889518d62ad756db7d6df1035d755db90a046373c96fb5198` |
| EdDSA signature verification | Not provided in this evidence package; this version is a historical backfill, so the comparison instead directly uses two official full packages |
| Previous codesign / spctl notarization result | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest codesign / spctl notarization result | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Boundaries and disclaimer

This report is not affiliated with OpenAI and does not represent official statements or official release notes.

This article does not redistribute any official binaries; it only publishes diff conclusions derived from officially signed distribution artifacts.

All [Signal] entries only mean “pointing in a certain direction, but unable to reconstruct source-level intent from the artifact”; in this release’s evidence package, there is not enough frontend module-name or binary-size evidence to form any standalone [Signal] changes.

If readers find any factual inference or evidence labeling errors, corrections based on the same evidence package are welcome.
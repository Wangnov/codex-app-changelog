---
title: "Codex Desktop 26.217.1959"
version: "26.217.1959"
build: 669
previous_version: "26.212.1823"
previous_build: 661
platform: "macOS arm64"
released: "2026-02-18"
compared_from: "2026-02-13"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This release is primarily a version/build update. The total number of files at the bundle level is unchanged, with only a small number of content changes; no new user-facing feature flags or expanded dependency capabilities appear in the readable evidence."
official_release_notes: false
---

# Codex Desktop `26.217.1959` (build 669)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

Based on the readable evidence, this release looks more like a small build-level update: the main executable grew slightly, the application package became slightly smaller, but there are no confirmed new feature entry points.  
English one-liner: a small build-level update with no confirmed user-facing feature additions in the readable diff.

## About This Report

This is not an official changelog, but a **reverse-engineered diff report based on officially distributed artifacts**. This update is a historical backfill: both versions are outside the official Sparkle incremental update window, so the fact package uses a direct comparison of two official full packages rather than reconstructing from an incremental package.

The comparison range is from `26.212.1823` build `661`, released on `2026-02-13`, to `26.217.1959` build `669`, released on `2026-02-18`.

In terms of confidence, the fact package provides SHA-256 hashes for both official full packages as well as notarization verification results. Both the previous and latest versions show `CSSMERR_TP_CERT_REVOKED`. The fact package notes that signing certificates for these early versions may have been revoked later, which can affect system security installation checks, but does not affect diff analysis of the bundle contents.

This report uses two evidence levels:

- **[Confirmed]**: Direct human-readable evidence, such as `Info.plist`, `package.json`, signing, or verification results.
- **[Signal]**: Indirect evidence, such as frontend module names or binary size changes; this type of information points in a direction but cannot reconstruct source-level intent from the artifact.

## ✨ Key Changes

### 1. Version and build number advanced to `26.217.1959` build `669` [Confirmed]

This is the clearest change: the app short version was updated from `26.212.1823` to `26.217.1959`, and the build number from `661` to `669`.

This means it is a newly packaged official macOS arm64 distribution artifact, not merely a resource re-signing or file timestamp change.

Key evidence:

```diff
-  "version": "26.212.1823",
+  "version": "26.217.1959",
```

```diff
-  "codexBuildNumber": "661",
+  "codexBuildNumber": "669",
```

`Info.plist` records the same change:

```text
CFBundleShortVersionString: 26.212.1823 → 26.217.1959
CFBundleVersion: 661 → 669
```

### 2. Total bundle file count unchanged, with only a small number of content changes [Confirmed]

The bundle file count remains `410 → 410`, with no added files, no removed files, and `42` files with content changes.

This suggests that, from the outer application package level, this release did not introduce new standalone components, plugins, or large new resource blocks; the changes are mainly concentrated in updates to existing file contents.

Key evidence:

```text
文件数: 410 → 410
新增 0 / 移除 0 / 内容改动 42
```

### 3. Main executable size increased, while frontend application package size decreased [Signal]

`Contents/Resources/codex` increased from `72.1 MB` to `73.7 MB`, an increase of `1.67 MB`; `Contents/Resources/app.asar` decreased from `35.2 MB` to `34.3 MB`, a decrease of `0.89 MB`.

This points to a reorganization of the main program or packaged contents, but the artifact cannot be used to reconstruct source-level intent, nor can it confirm any specific feature addition or removal.

Key evidence:

| Component | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/codex` | 72.1 MB | 73.7 MB | +1.67 MB |
| `Contents/Resources/app.asar` | 35.2 MB | 34.3 MB | -0.89 MB |

### 4. Significant frontend internal file churn, mainly affected by build hash noise [Signal]

The internal file count of `app.asar` changed from `701` to `611`, and machine statistics show `107` added, `197` removed, and `10` changed files. However, the fact package explicitly states that a large amount of churn under `webview/assets` comes from Vite build hash renaming and is noise.

Therefore, this should not be interpreted as hundreds of real features being added or removed; it only indicates that the frontend packaged artifact was regenerated.

Key evidence:

```text
内部文件: 701 → 611 (新增 107 / 移除 197 / 改动 10)
注:webview/assets 下大量增减是 Vite 构建 hash 重命名,属噪音,不要据此判断新功能。
```

### 5. New cached lint command added for development [Confirmed]

`package.json` adds a new `lint:cached` command, which uses ESLint caching and places the cache at `node_modules/.cache/eslintcache`.

This is a development workflow improvement and may be used to speed up code checks in local or build environments; it is not a directly user-visible feature change.

Key evidence:

```diff
+    "lint:cached": "eslint src --max-warnings 0 --cache --cache-strategy content --cache-location node_modules/.cache/eslintcache",
```

### 6. No newly added `cua_node` dependency packages found [Confirmed]

The fact package shows `0` newly added dependencies for `cua_node`.

This means the readable dependency list contains no evidence that new third-party capability packages were added on the `cua_node` side in this release.

Key evidence:

```text
新增 0 个包
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Value |
| --- | ---: |
| File count | 410 → 410 |
| Added files | 0 |
| Removed files | 0 |
| Content changes | 42 |

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/codex` | 72.1 MB | 73.7 MB | +1.67 MB |
| `Contents/Resources/app.asar` | 35.2 MB | 34.3 MB | -0.89 MB |
| `Contents/Resources/THIRD_PARTY_NOTICES.txt` | 1.5 MB | 1.5 MB | +0.00 MB |

### Added Dependency List

| Scope | Added dependencies |
| --- | ---: |
| `cua_node` | 0 packages |

## 🔁 Reproducibility Information

| Artifact | SHA-256 | Size | Verification result |
| --- | --- | --- | --- |
| Previous full package `Codex-darwin-arm64-26.212.1823.zip` | `3a5c042a59d0e4c2c2e76dbbcc41ddaf4942ed943b18193dfc8f3c593cbedfbb` | Not provided in fact package | Notarization verification shows `CSSMERR_TP_CERT_REVOKED` |
| Latest full package `Codex-darwin-arm64-26.217.1959.zip` | `d0dc7466cb5f062cea96dd7371751184cf9fd8eaa8bacb0245b13ede917d29d5` | Not provided in fact package | Notarization verification shows `CSSMERR_TP_CERT_REVOKED` |

The fact package states that this comparison was performed directly between two official full packages, and does not provide EdDSA signature verification results.

## ⚖️ Boundaries and Disclaimer

This report is not affiliated with OpenAI and is not an official set of release notes.

This report does not redistribute any official binary files; it only publishes diff conclusions derived from officially distributed artifacts.

All **[Signal]** items indicate only directional signs at the artifact level. They do not constitute source-level facts and are not equivalent to official product intent.

If anything is missing or misread, corrections based on the same fact package or reproducible evidence are welcome.

---
title: "Codex Desktop 26.519.41501"
version: "26.519.41501"
build: 3044
mac_version: "26.519.41501"
mac_build: 3044
win_version: "26.519.5221.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-05-22"
method: "官方包逆向 diff: macOS Sparkle, Windows MSIX"
summary: "This batch shows shared frontend movement around context usage, Markdown media/citation accessibility, and persistent Computer Use app-control approval, while Windows also carries larger side-panel, CI/PR, browser-device, debug, and settings reshuffle signals."
official_release_notes: false
---

# Codex Desktop 26.519.41501 macOS / Windows Comparison

> Third-party cross-platform changelog · unofficial

This comparison shows that macOS 26.519.41501 and Windows 26.519.5221.0 come from a nearby code batch, but their packaging cadence and frontend module state are not identical.

One-liner: this batch adds shared UI signals around context usage, Markdown media/citations, and Computer Use approval persistence, while Windows carries additional side-panel, CI/PR, browser-device, debug, and settings churn.

## About This Report

This report uses only the machine-extracted facts in the supplied evidence packs. The macOS sample compares 26.519.31651 build 3017 → 26.519.41501 build 3044; the Windows sample compares 26.519.3891.0 → 26.519.5221.0. Platform versions and builds are cited separately and are not inferred from each other.

Evidence is split into two levels:  
[Confirmed] covers Info.plist, plugin manifests, signing/notarization results, SHA-256 values, file-level statistics, package or dependency lists, service entries, skill documentation, and configuration diffs.  
[Signal] covers readable strings, style class names, module names, and size direction in same-name frontend modules. It indicates that related UI, state, or interaction changes entered the bundle diff, but it is not source-level proof and does not guarantee visibility to every user.

Platform scope is marked as `[macOS+Windows]`, `[macOS only]`, or `[Windows only]`. The input did not provide content security policy changes, Computer Use runtime totals, AppxManifest changes, newly added bundled plugin details, or largest-new-file details, so those areas are not extrapolated.

## ✨ Highlights

### [Confirmed] [macOS+Windows] Browser-related bundled plugin versions moved to 26.519.41501

On both platforms, the `browser` and `chrome` bundled plugin manifests only show a version field update: `26.519.31651` → `26.519.41501`.

The macOS paths are under `Contents/Resources/plugins/openai-bundled/plugins/.../.codex-plugin/plugin.json`; the Windows paths are under `resources/plugins/openai-bundled/plugins/.../.codex-plugin/plugin.json`. No functional description rewrite is shown.

Platform difference: the macOS app itself also moved to `26.519.41501` build `3044`; the Windows package version is `26.519.5221.0`, while these two plugin manifests still report `26.519.41501`.

### [Signal] [macOS+Windows] New `computer-use-plugin-icon` frontend module

After removing Vite hash noise, both platforms show the same single real newly added frontend module: `computer-use-plugin-icon`.

This suggests frontend bundle work around a Computer Use plugin visual entry or plugin identity. The facts do not include a matching new manifest, skill document, or service entry, so this should not be described as a complete new capability; it is a frontend signal.

Platform difference: the module name is identical on both platforms. macOS app.asar internal files changed `1576 → 1577`, while Windows changed `1530 → 1531`.

### [Signal] [macOS+Windows] Computer Use app-control approval adds an “always allow” persistence option

On both platforms, `pending-request-item-panel` adds the strings `Checkbox label for always allowing Computer Use to control an app` and `composer.computerUseAppApproval.persist.always`.

This points to a possible persistent approval option when Computer Use controls an app. Because the evidence comes from same-name frontend module change clues, it is a UI/state signal rather than confirmation that every user can already see it.

Platform difference: the module grows by `+3548 bytes` on both platforms.

### [Signal] [macOS+Windows] Thread status area adds context usage and speed information

On both platforms, `local-conversation-thread` adds strings for context usage, token totals, percentage, unavailable state, tokens per second, a compact action entry, and a disabled-while-task-running message.

Readable strings include `Context usage unavailable`, `Tokens per second in the thread summary status section`, `Menu item that compacts the current thread context from the status section`, `Compact is disabled while a task is in progress`, and `codex.localConversation.status.compactOption`.

Platform difference: the module grows by `+3928 bytes` on both platforms, with the same added class names: `hover:before:bg-token-list-hover-background`, `mx-4`, and `w-45`.

### [Signal] [macOS+Windows] Composer status entry shifts from session-id copy toward context-usage toggling

On both platforms, `composer` adds `Toggle context usage` and removes `Copy session id`, `Copied session id`, `Copy ID`, `Copied ID`, and context-window metadata description strings.

This suggests the status UI near the composer may have shifted away from a session-id copy entry and toward context-usage display or toggling. Since the evidence is same-name frontend module string churn, it remains a signal.

Platform difference: the module shrinks by `-3522 bytes` on both platforms, with the same removed layout class set.

### [Signal] [macOS+Windows] Markdown rendering adds accessibility clues for media, citation chips, and adding selected text to chat

The `markdown` module grows by `+60010 bytes` on both platforms and adds strings covering images, videos, file citation chips, extensionless file citations, line information, image loading placeholders, failure fallbacks, and `Add to chat`.

This signal points to accessibility-oriented improvements around Markdown media, file citations, and a selected-text overlay. Class names such as `data-markdown-image-grid`, `data-selected-text-overlay-target`, and `cursor-zoom-in` also support an image-grid, selected-text overlay, or zoom interaction direction.

Platform difference: the added strings and style class names are identical across both platforms.

### [Signal] [macOS+Windows] Home announcements and connection-related banners appear to be removed or consolidated

On both platforms, `home-announcements` removes strings for Fast mode, backend-driven banners, multi-agent composer banners, remote connections banners, `Add Connections`, and a connected apps home banner.

This means several home announcement strings no longer appear in the same-name module diff, or have moved elsewhere. The evidence is not sufficient to say that the underlying features were removed.

Platform difference: the module shrinks by `-29765 bytes` on both platforms.

### [Signal] [macOS only] Worktree init v2 page removes failure, cancel, retry, and environment-editing copy

On macOS, `worktree-init-v2-page` removes strings including `Creating a worktree and running setup.`, `Creating a worktree to fork this conversation.`, `Edit environment`, `Retry button`, `Cancel button for worktree creation`, and `Error creating local task from worktree`.

This indicates changes to the worktree initialization page’s flow copy or error-handling UI in the macOS sample. The Windows facts do not list this module, so the same conclusion cannot be applied to Windows.

### [Signal] [macOS only] browser-use settings removes domain allow/block and history-access copy

On macOS, `browser-use-settings` removes `Access history without asking`, `Add allowed domain`, `Add allowed download domain`, `Add allowed upload domain`, and `Add blocked domain`.

This points to consolidation or migration in browser-use settings around history access, upload/download allow lists, and blocked-domain configuration. The Windows facts do not list this module.

### [Signal] [Windows only] Thread side panel adds major CI/PR, edit-acceptance, browser device-toolbar, and event-state clues

On Windows, `thread-side-panel-tabs` grows from about `0.0 MB` to `0.5 MB` and adds many strings: CI log diagnosis workflow text, permission/credential/MCP or tool-access checks, `A pull request already exists for this branch.`, `Accept edits to stage them`, `Accepted event`, `Accepting event`, and accessibility labels for browser device-toolbar width and height inputs.

This is a high-visibility signal spanning CI repair flow, PR state, edit staging, browser device size controls, and event acceptance state. The macOS same-name module shows many removals in this input, so the Windows direction must not be applied to macOS.

### [Signal] [Windows only] Debug modal shrinks heavily and removes app/session, ambient suggestion, and unpackaged-build copy

On Windows, `debug-modal` shrinks from about `0.2 MB` to `0.0 MB` and removes `(command unavailable)`, `Account ID`, `Allow automatic worktree cleanup in this unpackaged build.`, `Already current`, `Ambient suggestion chats`, `App Actions`, `App Server`, and `App session ID`.

This indicates that account, session, app service, ambient suggestion, and unpackaged-build cleanup copy disappeared from this module or moved elsewhere. The macOS facts do not list this module.

### [Signal] [Windows only] General settings removes font-size, work-mode, default-permission, and dictation-hotkey copy

On Windows, `general-settings` shrinks from about `0.1 MB` to `0.0 MB` and removes `Adjust the base size used for code across chats and diffs`, `Adjust the base size used for the Codex UI`, `Accessible label for work mode settings options`, `Accessible label for disabled default permissions toggle`, `Agent environment`, and `Aria label for hold-to-dictate hotkey capture input`.

This suggests module-level reshuffling or migration in Windows settings around font sizing, work mode, default permissions, agent environment, and dictation hotkey capture. It does not prove that the settings were officially removed.

## 🔎 Low-Level Signal Index

### [Signal] [macOS+Windows] Markdown and accessibility

Shared strings cluster around image, video, file citation chips, extensionless file citations, line numbers, fallbacks, placeholders, and `Add to chat`. Boundary: these come from same-name frontend module changes and only show that related UI copy and states entered the diff.

### [Signal] [macOS+Windows] Thread status and compact

Shared strings cluster around context usage donut, tokens per second, compact option, compact disabled during active tasks, and unavailable context. Boundary: this supports a status-area direction, but does not prove rollout to all accounts.

### [Signal] [macOS+Windows] Home banner consolidation

Both platforms remove copy around Fast mode, backend-driven banners, multi-agent composer, remote connections, and connected apps. Boundary: this only means these strings were removed from the same-name module diff, not that server-side announcement strategy changed.

### [Signal] [macOS only] browser-use settings

macOS-only removals cluster around history access without asking, allowed/blocked domains, and download/upload domains. Boundary: the facts do not provide CSP, permission configuration, or plugin manifest changes confirming a policy change.

### [Signal] [Windows only] Side panel and browser device toolbar

Windows-only additions include CI diagnosis prompts, existing-PR state, accepting edits to stage them, Accepted/Accepting event, and browser device-toolbar width/height inputs. Boundary: these are same-name frontend module clues and should be treated as high-visibility UI signals.

### [Signal] [Windows only] Debug and general settings consolidation

Windows-only removals cover debug modal and general settings. Boundary: large module shrinkage may come from code splitting or migration and should not be directly described as feature removal.

## 📊 Raw Data

### macOS arm64

[Confirmed] Version: `26.519.31651` build `3017` → `26.519.41501` build `3044`.  
[Confirmed] Full-package SHA-256: previous `80c4e7c3073dbfd1107e323d7de11d657837ddb9831b2d99b010b3372bce9e4a`; latest `7e46af58373b9acf799296b83e7c3cf19a5255d263b30cb437b495c35e9628bd`.  
[Confirmed] Notarization: both previous and latest are `accepted`, source `Notarized Developer ID`, origin `OpenAI OpCo, LLC (2DC432GLL2)`.  
[Confirmed] Bundle file count: `1335 → 1335`; added `0` / removed `0` / content-changed `59`.  
[Confirmed] app.asar internal files: `1576 → 1577`; added `252` / removed `251` / changed `9`.  
[Confirmed] Real added frontend module after hash-noise removal: `computer-use-plugin-icon`.  
[Confirmed] Added cua_node dependencies: `0` packages.  
[Confirmed] Major size changes: `better_sqlite3.node` `2.0 MB → 1.9 MB`, change `-0.03 MB`; `bare-modifier-monitor` grows by `+0.02 MB`; `sparkle.node`, `sky.node`, `remote-control-device-key.node`, and `browser-use-peer-authorization.node` grow slightly.  
[Confirmed] Info.plist: `CFBundleShortVersionString` `26.519.31651 → 26.519.41501`; `CFBundleVersion` `3017 → 3044`; `ElectronAsarIntegrity/Resources/app.asar/hash` updated.  
[Confirmed] Key manifests: `browser` and `chrome` plugin versions both changed `26.519.31651 → 26.519.41501`.

### Windows x64

[Confirmed] Version: `26.519.3891.0` → `26.519.5221.0`.  
[Confirmed] Full-package SHA-256: previous `461e9e796fa5c8bb662fb4177ddef17ca38386ea3e34e611a7d4d12ea7a82069`; latest `4c6e9fa6e931dbd87bbc34a04b39c6e8f1b26b98a343f83c7505d1d5f036841d`.  
[Confirmed] Bundle file count: `832 → 832`; added `0` / removed `0` / content-changed `12`.  
[Confirmed] app.asar internal files: `1530 → 1531`; added `252` / removed `251` / changed `10`.  
[Confirmed] Real added frontend module after hash-noise removal: `computer-use-plugin-icon`.  
[Confirmed] Added cua_node dependencies: `0` packages.  
[Confirmed] Major size change: `resources/app.asar` `159.0 MB → 159.0 MB`, change `+0.00 MB`.  
[Confirmed] Key manifests: `browser` and `chrome` plugin versions both changed `26.519.31651 → 26.519.41501`.

## ⚖️ Boundaries and Disclaimer

This is a third-party unofficial reverse-diff report and does not represent OpenAI official release notes. It does not redistribute binaries; it only cites hashes, signing data, manifests, file statistics, and bundle string diffs from the supplied evidence packs.

[Signal] means the related module, string, style, or size change entered the package, not that it is source-level fact or visible to every user. Windows and macOS versions, builds, and module directions are cited separately; a change observed on one platform is not automatically applied to the other. Corrections with official notes or more complete samples are welcome.

---
title: "Codex Desktop 26.519.41501"
version: "26.519.41501"
build: 3044
previous_version: "26.519.31651"
previous_build: 3017
platform: "macOS arm64"
released: "2026-05-23"
compared_from: "2026-05-22"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "This release adds no new dependencies and shows no obvious file-tree expansion; the main changes are version alignment, a computer-use plugin icon signal in the frontend, and a handful of native support component updates."
official_release_notes: false
---

# Codex Desktop `26.519.41501`(build 3044)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

This is a very small macOS arm64 desktop update: the total number of packaged files is unchanged, new dependencies are 0, and the clearest changes are version-number alignment plus metadata synchronization for the built-in Browser / Chrome plugins; a frontend module named `computer-use-plugin-icon` also appears as a signal of a possible feature direction.  
English one-liner: a small maintenance-style build with plugin version alignment and an early computer-use UI signal.

## About This Report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. This entry is a historical backfill: both versions are already outside the official Sparkle delta-update window, so the evidence package was produced by downloading both official full packages directly, comparing them, and generating a structured bundle diff.

In terms of trustworthiness, both versions of `Codex.app` passed Apple notarization and OpenAI Developer ID verification. The `spctl` result for both the previous and latest versions is `accepted`; both sources are `Notarized Developer ID`, and both signing origins are `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

This report uses two evidence levels:

- **[Confirmed]**: Based on direct, human-readable evidence, such as `Info.plist`, plugin `plugin.json`, readable configuration, or documentation diffs.
- **[Signal]**: Based on indirect evidence, such as frontend module names or binary size changes. These entries only indicate that something points in a certain direction, but the artifact cannot be used to reconstruct source-level intent.

Comparison scope: `26.519.31651` build `3017`, released on `2026-05-22`; compared against `26.519.41501` build `3044`, released on `2026-05-23`.

## ✨ Highlights

### 1. App version advanced to `26.519.41501`, with build moving from `3017` to `3044` [Confirmed]

The version fields in `Info.plist` changed clearly: `CFBundleShortVersionString` changed from `26.519.31651` to `26.519.41501`, and `CFBundleVersion` changed from `3017` to `3044`.

This means this is a formally signed and notarized desktop build update, not merely a resource-file replacement. Supporting evidence also includes the `app.asar` hash in `ElectronAsarIntegrity`, which changed from `72b2210d1bd4dc5d59a99f1c677e304418cbf1ae60db818d75d6225a43c2d6c1` to `91bd615ccbed8b9e02a9ecd5c142687d021c6aac67edd9eb5a50f0ba29a9c5c8`.

### 2. Built-in Browser / Chrome plugin metadata synchronized to the new version [Confirmed]

The `plugin.json` version numbers for two built-in OpenAI plugins were updated in sync: both `browser` and `chrome` changed from `26.519.31651` to `26.519.41501`.

This shows that these built-in automation capabilities were repackaged and released together with the app version. The evidence package does not show changes to their descriptions, capability boundaries, or invocation instructions, so this should not be used to infer any specific new Browser or Chrome plugin functionality.

Key evidence:

```diff
-  "version": "26.519.31651",
+  "version": "26.519.41501",
```

### 3. Frontend module `computer-use-plugin-icon` appears [Signal]

After filtering out hash-based filename noise under `webview/assets` and `.vite/build`, the only visibly real new frontend module in the evidence package is `computer-use-plugin-icon`.

The module name points toward a computer-use plugin icon or entry-point display direction, but this is only [Signal]-level evidence: it only shows that a related frontend module name appears in the artifact. It cannot reconstruct source-level intent from the artifact, nor does it prove that the feature has been made available to users.

### 4. Minor binary changes in native support components [Signal]

The size-change table shows small changes across several native components, including `bare-modifier-monitor`, `sparkle.node`, `sky.node`, `remote-control-device-key.node`, `browser-use-peer-authorization.node`, and others.

These changes indicate maintenance activity in the app’s lower-level support layer, touching areas such as updates, device keys, browser authorization, or input monitoring. However, because the evidence comes only from binary size changes, the artifact cannot be used to reconstruct source-level intent or determine specific behavioral changes.

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| bundle file count | 1335 | 1335 | 0 |
| added files | 0 | 0 | 0 |
| removed files | 0 | 0 | 0 |
| content-changed files | - | - | 59 |

Note: Internal `app.asar` file statistics are heavily affected by Vite hash-based renaming. This report does not include its added / removed counts in the table, to avoid misreading build-artifact renames as real feature additions or removals.

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar.unpacked/node_modules/better-sqlite3/build/Release/better_sqlite3.node` | 2.0 MB | 1.9 MB | -0.03 MB |
| `Contents/Resources/native/bare-modifier-monitor` | 0.2 MB | 0.2 MB | +0.02 MB |
| `Contents/Resources/native/sparkle.node` | 0.1 MB | 0.2 MB | +0.01 MB |
| `Contents/Resources/native/sky.node` | 0.2 MB | 0.2 MB | +0.01 MB |
| `Contents/Resources/native/remote-control-device-key.node` | 0.1 MB | 0.1 MB | +0.01 MB |
| `Contents/Resources/native/browser-use-peer-authorization.node` | 0.1 MB | 0.1 MB | +0.01 MB |
| `Contents/Resources/native/devicecheck.node` | 0.1 MB | 0.1 MB | +0.00 MB |
| `Contents/Resources/app.asar.unpacked/node_modules/node-pty/build/Release/pty.node` | 0.1 MB | 0.1 MB | +0.00 MB |
| `Contents/Resources/app.asar.unpacked/node_modules/node-pty/build/Release/pty.node.dSYM/Contents/Resources/DWARF/pty.node` | 0.0 MB | 0.0 MB | +0.00 MB |
| `Contents/Resources/app.asar` | 159.0 MB | 159.0 MB | +0.00 MB |
| `Contents/Resources/native/launch-services-helper` | 0.1 MB | 0.1 MB | +0.00 MB |

### New Dependency List

New `cua_node` dependencies: 0.

## 🔁 Reproducibility Information

| Artifact | URL | SHA-256 | Verification result |
| --- | --- | --- | --- |
| Previous full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.519.31651.zip` | `80c4e7c3073dbfd1107e323d7de11d657837ddb9831b2d99b010b3372bce9e4a` | `spctl`: accepted；source=`Notarized Developer ID`；origin=`Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.519.41501.zip` | `7e46af58373b9acf799296b83e7c3cf19a5255d263b30cb437b495c35e9628bd` | `spctl`: accepted；source=`Notarized Developer ID`；origin=`Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

This comparison directly compares two official full packages; the evidence package does not provide EdDSA delta-package signature verification records. The trust roots are Apple notarization and OpenAI Developer ID code signing.

## ⚖️ Boundaries and Disclaimer

This report is not affiliated with OpenAI, does not represent the official position, and is not official release notes.

This report does not redistribute any official binaries. It only publishes diff conclusions derived from officially signed distribution artifacts.

All [Signal] entries are not source-level facts. They only indicate that corresponding signs appear at the artifact level. Corrections based on reproducible evidence are welcome.
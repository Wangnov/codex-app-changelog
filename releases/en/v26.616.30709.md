
---
title: "Codex Desktop 26.616.30709"
version: "26.616.30709"
build: 4108
mac_version: "26.616.30709"
mac_build: 4108
win_version: "26.616.3309.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-18"
method: "Official package reverse diff(macOS Sparkle / Windows MSIX)"
summary: "No file-level change was observed in the macOS package; Windows moved from 26.611.8604.0 to 26.616.3309.0 with app-layer, browser-control, realtime voice, download-setting, and packaging cleanup changes."
official_release_notes: false
---

# Codex Desktop 26.616.30709(macOS / Windows comparison)

> Third-party cross-platform changelog · Unofficial

The key takeaway for this batch is that the macOS comparison is identical, while the Windows package carries the observable version bump, app-layer changes, and packaging cleanup.

One-liner: macOS shows an identical-package diff, while Windows carries the observable update in this batch.

## About This Report

This report is based on reverse diffs of official distribution artifacts: macOS via official full packages from the Sparkle update channel, and Windows via MSIX packages. The macOS side is a historical backfill; the fact pack shows the same URL, SHA-256, version, and build for both previous and latest packages. Windows is compared from `26.611.8604.0` to `26.616.3309.0`.

Evidence is split into two levels: [Confirmed] means directly verifiable evidence such as plist, manifest, skill docs, type declarations, dependency lists, readable strings, file inventories, or signature checks; [Signal] means directional evidence from module names, size deltas, or compressed bundles, useful for spotting likely areas of change but not for reconstructing full source intent.

Platform labels mean: [macOS+Windows] is supported by both fact packs; [macOS only] appears only in the macOS fact pack; [Windows only] appears only in the Windows fact pack. This batch has no observed common functional change that can be labeled [macOS+Windows].

Comparison scope: macOS arm64 is `26.616.30709` build `4108` against `26.616.30709` build `4108`; Windows x64 is `26.611.8604.0` against `26.616.3309.0`. The release date provided for this batch is `2026-06-18`.

## ✨ Highlights

### [Confirmed][Windows only] Windows advanced to a newer package; macOS had no content diff

Windows moved from `26.611.8604.0` to `26.616.3309.0`. Its file tree changed from `11943` files to `9216`, with `2` added, `2729` removed, and `36` modified. On macOS, both previous and latest are `26.616.30709` build `4108`, with file count `8416 → 8416` and `0` added, removed, or modified files.

That means the observable changes in this batch are concentrated on Windows. The macOS fact pack only proves that this specific comparison sample did not change; it does not prove macOS never received similar changes in another batch.

### [Confirmed][Windows only] The app.asar application layer changed materially

On Windows, `resources/app.asar` grew from `157.6 MB` to `159.6 MB`, a `+1.94 MB` increase. Its internal file count changed from `3024` to `3078`, with `1682` added, `1628` removed, and `27` modified. The fact pack also notes that large churn under `webview/assets` is Vite build hash noise and should not be interpreted as feature churn.

On macOS, `app.asar` internal files stayed at `3115 → 3115`, with `0` added, removed, or modified. The app-layer change is therefore confirmed only on Windows.

### [Confirmed][Windows only] Browser control and multi-tab capabilities continued to expand

The Windows package includes or modifies browser-related files and strings such as `resources/plugins/openai-bundled/plugins/browser/.codex-plugin/api-multi-tab.md`, `resources/plugins/openai-bundled/plugins/browser/docs/api.md`, `resources/plugins/openai-bundled/plugins/chrome/docs/capabilities/browser/viewport.md`, plus readable strings including `Open link in new tab`, `Copy link address`, `Locators must belong to the same tab`, and `Target.attachToTarget did not return a sessionId`.

This points to adjustments around the in-app browser, Chrome plugin, multi-tab API, viewport capability, and Playwright locator checks. A Playwright locator is a page-element targeting abstraction. The macOS side shows no corresponding file or content change.

### [Confirmed][Windows only] Download folder and browser download settings appeared in new strings

Windows added readable strings including `Choose Downloads Folder`, `Folder where files downloaded by the in-app browser are saved`, `Whether manual browser downloads ask where to save each file`, along with `Failed to publish downloads state to renderer`, `allowDownload requires a non-empty url`, and `allowDownload requires an integer tabId`.

This evidence indicates new or changed paths around the in-app browser download directory, manual download save behavior, and publishing download state to the renderer process. The macOS fact pack has no corresponding change.

### [Confirmed][Windows only] Realtime voice and wake-word paths appeared

New Windows strings include `codex.command.realtimeVoice`, `codex.commandDescription.realtimeVoice`, `Failed to apply realtime voice hotkey`, `Failed to start realtime voice wake word`, `Realtime voice wake word detected`, `Realtime voice wake word needs microphone access`, and `Realtime voice wake word helper ready`.

These strings confirm the presence of package paths related to realtime voice commands, hotkeys, wake-word helper behavior, and microphone permission handling. The fact pack does not provide full source semantics, and it does not show a synchronized macOS change.

### [Confirmed][Windows only] Custom file handlers and external session import paths appeared

Windows added strings including `Custom file handlers for opening files from Codex App`, `Dropping invalid custom file handler`, `Dropping invalid custom file handlers config`, `External agent session import failed`, `Invalid Library file`, `Library file is unavailable`, and `Selected CSV file is unavailable`.

This indicates package paths for custom file handling, external agent session import, Library file validation, and CSV file availability checks. There is no matching file or string-level evidence on macOS.

### [Confirmed][Windows only] Native pet / avatar overlay recovery and error paths increased

Windows added strings including `Failed to detach native pet material`, `Failed to dump native pet hierarchy`, `Failed to prewarm avatar overlay`, `Native pet composition recovery attempt`, `Native pet composition recovery failed`, and `Native pet composition recovery succeeded`.

These are readable error and recovery-state strings, confirming package paths for native pet composition, material handling, hierarchy dumping, and avatar overlay prewarming. The macOS diff has no counterpart.

### [Confirmed][Windows only] Packaging cleanup removed many node-headers / OpenSSL headers

Windows removed `2729` files. The removal list includes many entries under `node-headers/include/node/openssl/archs/...`, plus `node-headers/Release/node.lib` and the older `resources/icon.ico`. A new `resources/codex-tray.ico` also appears.

This looks more like Windows packaging cleanup and icon resource adjustment than a direct runtime capability change. The macOS package has no corresponding file-tree change.

### [Confirmed][Windows only] Major executable components grew

On Windows, `resources/codex.exe` grew from `307.7 MB` to `318.0 MB`, a `+10.29 MB` increase. `resources/codex` grew from `271.2 MB` to `278.4 MB`, a `+7.17 MB` increase. `resources/app.asar` also increased by `1.94 MB`.

The Computer Use runtime total stayed flat on both platforms: macOS remained `365.9 MB → 365.9 MB`, and Windows remained `285.6 MB → 285.6 MB`. Both platforms report `0` newly added `cua_node` dependency packages. Therefore, this batch does not support attributing the Windows executable size increase to new `cua_node` dependencies.

## 📊 Raw Data

| Item | macOS arm64 | Windows x64 |
| --- | --- | --- |
| Compared versions | `26.616.30709` build `4108` → `26.616.30709` build `4108` | `26.611.8604.0` → `26.616.3309.0` |
| File count | `8416 → 8416` | `11943 → 9216` |
| Added / removed / modified | `0 / 0 / 0` | `2 / 2729 / 36` |
| app.asar internal files | `3115 → 3115` | `3024 → 3078` |
| app.asar internal added / removed / modified | `0 / 0 / 0` | `1682 / 1628 / 27` |
| Computer Use runtime total | `365.9 MB → 365.9 MB (+0 MB, +0%)` | `285.6 MB → 285.6 MB (+0 MB, +0%)` |
| New cua_node dependencies | `0` | `0` |

Key Windows size changes:

| Component | Old | New | Delta |
| --- | --- | --- | --- |
| `resources/codex.exe` | `307.7 MB` | `318.0 MB` | `+10.29 MB` |
| `resources/codex` | `271.2 MB` | `278.4 MB` | `+7.17 MB` |
| `resources/app.asar` | `157.6 MB` | `159.6 MB` | `+1.94 MB` |
| `chrome.dll` | `307.8 MB` | `307.7 MB` | `-0.17 MB` |
| `resources/codex-windows-sandbox-setup.exe` | `8.8 MB` | `8.8 MB` | `+0.01 MB` |

The macOS key-size-change table is empty: after excluding zero-byte changes and hash noise, the fact pack lists no components with size deltas. The macOS previous and latest full packages both have SHA-256 `7b2c16517bcaa4e5a8acf56e5ae9885497449b864ecd52df3bd9995a93e5ba0b`; both notarization checks are `accepted`, with source `Notarized Developer ID` and origin `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

Windows full-package SHA-256 values: previous `37f4d4e5ec263797b2b0d05a26e900f21aa1942e40ada380c19e8982dc7018c1`; latest `a73f689a23c1d03df934262c193a313f7beec2510965398dbcb442bc95b3525b`.

## ⚖️ Boundaries and Disclaimer

This is a third-party, unofficial changelog based on reverse diffs of public official distribution packages. It is not an OpenAI release note.

This report does not redistribute binaries and does not claim to reconstruct the full source code. All conclusions are limited to the verifiable evidence provided in the fact packs.

Content labeled [Signal] is not source-level fact. Even [Confirmed] readable strings only prove that those strings and related packaged content exist; they do not replace official feature documentation.

Corrections are welcome when based on the same batch artifacts and reproducible diff results.

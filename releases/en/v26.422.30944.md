---
title: "Codex Desktop 26.422.30944"
version: "26.422.30944"
build: 2080
previous_version: "26.422.21637"
previous_build: 2056
platform: "macOS arm64"
released: "2026-04-24"
compared_from: "2026-04-24"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This release is primarily a minor low-level update: the version and build number were bumped, the core executable component grew by about 2.00 MB, while the file structure, dependencies, and readable app-layer evidence changed very little."
official_release_notes: false
---

# Codex Desktop `26.422.30944` (build 2080)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

This is a release with very few externally readable changes: the version increased from `26.422.21637` to `26.422.30944`, the build increased from `2056` to `2080`, and the main visible change is a small increase in the size of the core binary.  
English one-liner: a low-surface-area build bump with a small native binary growth signal.

## About this report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. This release is a historical backfill: both versions fall outside the official Sparkle delta window, so this analysis did not use the delta-package reconstruction flow. Instead, it directly downloaded the two official full packages and performed a structured bundle diff.

In terms of trust, the root of trust for this analysis comes from Apple notarization and the OpenAI Developer ID signature. Both the previous release and the latest release passed `spctl` notarization verification with the result `accepted`; both sources were `Notarized Developer ID`, and both signing identities were `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

This report uses two evidence levels:

- **[Confirmed]**: Direct human-readable evidence, such as `Info.plist` key values, signature and notarization verification results, dependency lists, or structured diffs.
- **[Signal]**: Indirect evidence, such as frontend module names or binary size changes. These changes point in a general direction, but the artifact alone cannot reconstruct source-level intent.

Comparison scope: from `26.422.21637` build `2056`, released on `2026-04-24`, to `26.422.30944` build `2080`, released on `2026-04-24`. Platform: `macOS arm64`.

## ✨ Key changes

### 1. Version and build number bump [Confirmed]

The app version was updated from `26.422.21637` to `26.422.30944`, and the build number was updated from `2056` to `2080`.

This means users are seeing a later build from the same release date, but the factual package does not provide official release notes or any readable feature description.

Key evidence:

```diff
CFBundleShortVersionString: 26.422.21637 → 26.422.30944
CFBundleVersion: 2056 → 2080
```

### 2. Core `codex` component grew by about 2.00 MB [Signal]

`Contents/Resources/codex` increased from `178.9 MB` to `180.9 MB`, a growth of about `2.00 MB`.

This points to substantive changes inside the core executable component, but the only evidence currently available is the indirect signal of binary size growth. The artifact cannot reconstruct source-level intent, nor can it confirm which specific feature, if any, was added.

Key evidence:

```text
Contents/Resources/codex: 178.9 MB → 180.9 MB (+2.00 MB)
```

### 3. App-layer structure remained largely stable [Confirmed]

The total number of files in the bundle remained `507 → 507`; no files were added or removed, and `46` files had content changes.

This means this release was not a large-scale app bundle restructuring. For developers, the more reasonable interpretation is that the changes mainly occurred inside existing files rather than through new components, plugins, or resource directories.

Key evidence:

```text
File count: 507 → 507
Added 0 / Removed 0 / Content changed 46
```

### 4. No new `cua_node` dependencies [Confirmed]

The factual package shows that the number of newly added `cua_node` dependency packages was `0`.

This means that, at the dependency-list level, this release did not introduce new `cua_node` packages. This does not prove that runtime capabilities were completely unchanged, but there is at least no readable evidence of newly added dependencies.

Key evidence:

```text
cua_node added 0 packages
```

### 5. `app.asar` content changed slightly, but this should not be read as broad feature change [Confirmed]

Inside the app-layer `app.asar`, the file count remained `1031 → 1031`; the structured result shows `4` added, `4` removed, and `5` changed. The factual package also explicitly notes that filename additions and removals under `webview/assets` and `.vite/build` involving hashes are Vite build-artifact renaming noise.

Therefore, this report does not interpret these hashed filename changes as added or removed features; it only confirms that the integrity hash of `app.asar` changed.

Key evidence:

```diff
ElectronAsarIntegrity/Resources/app.asar/hash:
50aa3dbd3b33c995917fdc534bf978285ad7f01a32204cc044986db63ba02e1c
→
c7accc89468b457278fcc8a3b713df521337da788cd421c8b318d795b30d85b9
```

## 📊 Raw data

### Bundle file-level statistics

| Item | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 507 | 507 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Content-changed files | - | - | 46 |

### Key component size changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/codex` | 178.9 MB | 180.9 MB | +2.00 MB |
| `Contents/Resources/app.asar` | 119.9 MB | 119.9 MB | +0.00 MB |

### Newly added dependency list

| Scope | Number of newly added dependencies |
| --- | ---: |
| `cua_node` | 0 |

## 🔁 Reproducibility information

| Artifact | URL | SHA-256 | Verification result |
| --- | --- | --- | --- |
| Previous full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.422.21637.zip` | `6e733818af68baa0e1c30afbac877a4f18401b094766f1acba0e1c30afbac877a4f18401b094766f1acba0cc10d1e33270c` | `spctl`: accepted; source=`Notarized Developer ID`; origin=`Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.422.30944.zip` | `5705b030cbb557c84fd2fdbd7c64693867d17c9c5ff56a3060891ca05d55f852` | `spctl`: accepted; source=`Notarized Developer ID`; origin=`Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

| Item | Conclusion |
| --- | --- |
| EdDSA signature verification | This is a historical backfill; the factual package did not provide an EdDSA delta-package signature verification result |
| codesign / spctl notarization | Both the previous release and the latest release were `accepted` |
| Distribution source | Official full-package URL |
| Comparison method | Direct comparison of two official full packages |

## ⚖️ Boundaries and disclaimer

This report is not affiliated with OpenAI, does not represent the official position, and is not official release notes.

This report does not redistribute any official binaries. It only publishes diff conclusions derived from officially signed distribution artifacts.

All [Signal] entries represent only indirect indications at the artifact level. They are not source-level facts and should not be treated as official feature statements.

If any interpretation of the factual package is incorrect, corrections based on reproducible evidence are welcome.

---
title: "Codex Desktop 26.422.30944"
version: "26.422.30944"
build: 2080
previous_version: "26.422.21637"
previous_build: 2056
platform: "macOS arm64"
released: "2026-04-24"
compared_from: "2026-04-24"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This historical backfill mainly confirms a version/build bump and a small increase in the main codex resource, with no observed bundle-tree or cua_node dependency expansion."
official_release_notes: false
---

# Codex Desktop `26.422.30944` build 2080

> Third-party changelog · Unofficial · Based on reverse diffing official update artifacts

This release moves from `26.422.21637` build 2056 to `26.422.30944` build 2080, comparing two builds published on the same date.  
One-liner: a narrow package-level update with no confirmed new dependency surface.

## About This Report

This is not an official changelog. It is a third-party reverse-diff report based on official distribution artifacts. The comparison covers macOS arm64 builds `26.422.21637` build 2056 and `26.422.30944` build 2080, both released on 2026-04-24.

This entry is a historical backfill: the official appcast did not provide a delta package for this version pair, so the comparison was performed by downloading both official full packages and diffing their reconstructed bundle structure.

Evidence is labeled in two tiers:

- **[Confirmed]**: Direct human-readable evidence from Info.plist, signing/notarization output, file-tree statistics, dependency lists, or structured readable diffs.
- **[Signal]**: Indirect evidence only, such as frontend module names or binary size movement. It points in a direction but cannot be reconstructed into source-level intent.

This report intentionally ignores hash-based build noise under `webview/assets` and `.vite/build`; those filename changes are not treated as feature additions or removals.

## ✨ Highlights

### 1. Version and build metadata updated [Confirmed]

The application metadata changed from `26.422.21637` build `2056` to `26.422.30944` build `2080`.

This confirms a desktop version advance at the macOS bundle metadata level, but the fact pack does not include official release notes or user-facing feature descriptions for this build.

Evidence:

```diff
/CFBundleShortVersionString: '26.422.21637' → '26.422.30944'
/CFBundleVersion: '2056' → '2080'
```

### 2. Bundle file tree stayed stable, with content-only changes [Confirmed]

The top-level bundle file count stayed at `507 → 507`. There were no added or removed files, and `46` files had content changes.

This suggests updates inside existing components rather than newly introduced visible subcomponents, plugins, or resource-tree structure. The fact pack does not show readable evidence for new built-in plugins, service entries, skill docs, default prompts, commands, settings, permissions, or error paths.

Evidence:

```text
Files: 507 → 507
Added 0 / Removed 0 / Modified 46
```

### 3. Main `codex` resource grew slightly [Signal]

`Contents/Resources/codex` increased from `178.9 MB` to `180.9 MB`, a `+2.00 MB` change.

This is an indirect binary/resource-size signal. It points to internal changes in the main `codex` component, but it cannot be reconstructed into source-level intent and does not confirm any specific feature change.

### 4. Application dependency surface did not expand [Confirmed]

The number of newly added `cua_node` packages is `0`, and the application-layer internal file count stayed at `1031 → 1031`. Although app.asar internally reports `added 4 / removed 4 / modified 5`, the fact pack explicitly states that many changes under `webview/assets` are Vite hash-renaming noise and should not be interpreted as real feature changes.

This means that, at the dependency-list level visible in the fact pack, this build does not add third-party packages that would support a new capability area such as PDF reading, OCR, image processing, or browser automation.

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 507 | 507 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Modified files | 0 | 46 | +46 |

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/codex` | 178.9 MB | 180.9 MB | +2.00 MB |
| `Contents/Resources/app.asar` | 119.9 MB | 119.9 MB | +0.00 MB |

### Newly Added Dependencies

| Dependency area | New packages | Notes |
| --- | ---: | --- |
| `cua_node` | 0 | No newly added dependency packages observed |

## 🔁 Reproducibility

| Item | Previous | Latest |
| --- | --- | --- |
| Download artifact | `Codex-darwin-arm64-26.422.21637.zip` | `Codex-darwin-arm64-26.422.30944.zip` |
| SHA-256 | `6e733818af68baa0e1c30afbac877a4f18401b094766f1acba0cc10d1e33270c` | `5705b030cbb557c84fd2fdbd7c64693867d17c9c5ff56a3060891ca05d55f852` |
| Size | Not provided | Not provided |
| EdDSA verification | Not provided | Not provided |
| spctl notarization result | `accepted` | `accepted` |
| Notarization source | `Notarized Developer ID` | `Notarized Developer ID` |
| Signing origin | `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` | `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Boundaries and Disclaimer

This report is not affiliated with OpenAI and is not an official changelog.

It does not redistribute any official binaries; it only publishes conclusions derived from diffing official signed distribution artifacts.

[Signal] items represent indirect artifact-level clues only. They are not source-level facts and should not be treated as official feature documentation.

Corrections based on the same artifacts, hashes, and verification procedure are welcome.

---
title: "Codex Desktop 26.506.31004"
version: "26.506.31004"
build: 2604
previous_version: "26.506.21252"
previous_build: 2575
platform: "macOS arm64"
released: "2026-05-08"
compared_from: "2026-05-07"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "This release is mainly a modest app-layer update: newly added frontend modules point to external Agent import flow, settings state, plugins, and in-app browser availability checks, but the artifact layer shows no new low-level dependencies or bundle file additions/removals."
official_release_notes: false
---

# Codex Desktop `26.506.31004` (build 2604)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

The changes from `26.506.21252` to `26.506.31004` are fairly restrained: the bundle file count is unchanged, and the main differences are concentrated in a small number of frontend modules and version metadata inside `app.asar`.  
English one-liner: a small app-layer update with signals around external Agent import, settings state, plugins, and in-app browser availability.

## About This Report

This is not an official changelog, but a **reverse-engineered diff report based on officially distributed artifacts**. This release is a historical backfill: both versions fall outside the official Sparkle incremental update window, so the comparison was performed by downloading two official full packages directly and generating a structured bundle diff.

In terms of confidence, both comparison targets come from official full packages and were verified through Apple notarization and the OpenAI Developer ID signature. The `spctl` results for both the previous and latest versions are `accepted`; both have `Notarized Developer ID` as the source, and both are signed by `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

This report uses two evidence levels:

- **[Confirmed]**: Direct, human-readable evidence, such as `Info.plist` key values, dependency lists, and signing/notarization results.
- **[Signal]**: Indirect evidence from frontend module names or size changes. It can only indicate that something points in a certain direction, and cannot reconstruct source-level intent from the artifact.

Comparison scope: from `26.506.21252` build `2575`, released on `2026-05-07`, to `26.506.31004` build `2604`, released on `2026-05-08`. Platform: `macOS arm64`.

## ✨ Highlights

### 1. Version and build updated to `26.506.31004` / build `2604` [Confirmed]

The app metadata in this release has been updated from `26.506.21252` to `26.506.31004`, and the build number from `2575` to `2604`.

This means it is a later desktop package released on `2026-05-08`, rather than a re-signed or repackaged copy of the same build.

Key evidence:

```diff
CFBundleShortVersionString: 26.506.21252 -> 26.506.31004
CFBundleVersion: 2575 -> 2604
```

### 2. External Agent import flow shows signs of being split into steps [Signal]

Among the real frontend modules, the old `external-agent-import` was removed, while `external-agent-import-step` was added. This suggests the external Agent import flow may have been split into a more granular step-based implementation, but the artifact cannot reconstruct source-level intent, nor does it prove that the capability is already available to users.

In terms of user-visible impact, if this path is already used by the interface, users may see a more step-by-step and explicit external Agent import experience. The current evidence, however, only reaches the module-name level.

Key evidence:

```text
新增模块: external-agent-import-step
移除模块: external-agent-import
```

### 3. New modules related to settings-page state and model logic [Signal]

The newly added modules `hooks-settings-model` and `settings-route-state` point toward changes in the settings page, settings route state, and model-related settings logic, but the artifact cannot reconstruct source-level intent.

In terms of user-visible impact, changes of this kind are usually closer to internal experience details around how settings-page state is saved, switched, or linked, rather than a complete new visible feature. The fact package provides no more specific UI copy or behavioral evidence, so no further inference is made.

Key evidence:

```text
新增模块: hooks-settings-model, settings-route-state
```

### 4. New signals around plugin and in-app browser availability checks [Signal]

The newly added modules `use-plugins` and `use-in-app-browser-use-availability` point toward plugin usage state and in-app browser availability checks, but the artifact cannot reconstruct source-level intent.

In terms of user-visible impact, this may relate to logic that determines whether plugin capabilities are available, or whether browser-related entry points are shown or executable. However, the fact package provides no direct evidence at the UI, permission, or configuration layer, so this can only be treated as a directional signal.

Key evidence:

```text
新增模块: use-plugins, use-in-app-browser-use-availability
```

### 5. New modules related to build flavor and shield checks [Signal]

The newly added modules `get-build-flavor` and `shield-check` point toward build flavor identification and protection or check-related logic, but the artifact cannot reconstruct source-level intent.

In terms of user-visible impact, this kind of module is more likely to affect behavioral branches across different build channels, runtime environments, or security-check paths. But the fact package only provides module names, not policy details or trigger conditions.

Key evidence:

```text
新增模块: get-build-flavor, shield-check
```

### 6. No new `cua_node` dependencies; no observed expansion of low-level capabilities [Confirmed]

The `cua_node` dependency diff shows `0` newly added packages. This means that, at the dependency-list level, this release did not expand low-level capabilities by adding new `cua_node` packages.

In terms of user-visible impact, this release looks more like a small app-layer adjustment than the addition of a new batch of low-level dependencies for PDF, OCR, image processing, browser automation, or similar capabilities.

Key evidence:

```text
cua_node 新增 0 个包
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 1238 | 1238 | 0 |
| Added files |  |  | 0 |
| Removed files |  |  | 0 |
| Files with content changes |  |  | 54 |

Note: the internal file count inside `app.asar` changed from `1377 -> 1382`, with `487` added, `482` removed, and `10` changed. However, the fact package explicitly states that many additions/removals under `webview/assets` are Vite build hash renaming noise. Therefore, these internal added/removed counts are not included in the table and are not used to infer feature additions or removals.

### Key Component Size Changes

| Component Path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 136.7 MB | 136.8 MB | +0.04 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/browser-use/scripts/browser-client.mjs` | 0.7 MB | 0.7 MB | +0.00 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/chrome/scripts/browser-client.mjs` | 0.7 MB | 0.7 MB | +0.00 MB |

### Added Dependency List

The number of newly added `cua_node` dependency packages is `0`.

## 🔁 Reproducibility Information

| Artifact | URL | SHA-256 | Verification Result |
| --- | --- | --- | --- |
| Previous full package `26.506.21252` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.506.21252.zip` | `4bf84cc3111233afaa1465623303e9f1296ec170f70b28e4cea29b3e62b8cc16` | `spctl`: `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest full package `26.506.31004` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.506.31004.zip` | `9a09529d4937d1f24c78f78c7ef7f4b501c594c55b4beb4fecc076d03524dad5` | `spctl`: `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

This fact package does not provide zip file sizes, nor does it provide EdDSA signature verification conclusions. It only provides SHA-256 and Apple notarization / OpenAI Developer ID signature verification results.

## ⚖️ Boundaries and Disclaimer

This document is not affiliated with OpenAI, does not represent an official position, and is not official release notes.

This document does not redistribute any official binary files. It only publishes diff conclusions derived from officially distributed artifacts.

All [Signal] items only mean that “artifact-level module names or size changes point in a certain direction.” They do not constitute source-level facts and do not guarantee that the corresponding capability has already been made available to users.

If you find an error in the interpretation of the facts, corrections are welcome based on the same version, the same artifact, and the same verification criteria.
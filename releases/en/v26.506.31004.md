
---
title: "Codex Desktop 26.506.31004"
version: "26.506.31004"
build: 2604
mac_version: "26.506.31004"
mac_build: 2604
win_version: "26.506.3484.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-05-08"
method: "Official package reverse diff: macOS Sparkle, Windows MSIX"
summary: "The macOS package shows a settings, Browser Use, Hooks, worktree setup, and welcome-page update; the Windows sample is byte-identical in this input."
official_release_notes: false
---

# Codex Desktop 26.506.31004 macOS / Windows Comparison

> Third-party cross-platform changelog · Unofficial

This batch shows a focused macOS-side iteration around settings, Browser Use, Hooks, worktree setup, and the welcome page; the Windows-side input sample is unchanged.

One-liner: macOS shows a settings-heavy UI and workflow update, while the Windows sample is byte-identical in this diff.

## About This Report

This report uses only machine-extracted facts from reverse-reconstructed official signed distribution packages. The macOS comparison range is 26.506.21252 build 2575 → 26.506.31004 build 2604; the Windows comparison range is 26.506.3484.0 → 26.506.3484.0.

Evidence is split into two levels: [Confirmed] comes from Info.plist, file listings, dependency listings, app.asar statistics, signature verification, SHA-256 values, and readable facts outside the “same-name frontend module change clues” section; [Signal] comes from same-name frontend module names, size changes, readable strings, and CSS/state class diffs, and does not prove that a feature is visible to all users.

Platform markers are: [macOS+Windows], [macOS only], and [Windows only]. In the Windows fact package, the previous and latest version, build, SHA-256, file count, and app.asar internal file count are identical, so this report only says that this Windows input sample has no content changes.

## ✨ Highlights

### [Confirmed] [macOS only] Version and signature verification moved to 26.506.31004 / build 2604

macOS moved from 26.506.21252 build 2575 to 26.506.31004 build 2604. In Info.plist, `CFBundleShortVersionString`, `CFBundleVersion`, and `ElectronAsarIntegrity/Resources/app.asar/hash` all changed.

Both macOS full packages passed notarization verification, with source shown as Notarized Developer ID and origin as OpenAI OpCo, LLC. The fact package states this was a historical backfill: no Sparkle delta was available for this pair, so two official full packages were compared directly.

### [Confirmed] [Windows only] The Windows input sample has no content changes

In the Windows comparison, both previous and latest versions are 26.506.3484.0. The two full packages have the same SHA-256: `554ab8b534305bb26338c8b89dd745210625d52f78850ec7996c56772821e4ee`.

The file tree stayed 815 → 815, with 0 added, 0 removed, and 0 changed files. app.asar internal files stayed 1336 → 1336, with 0 added, 0 removed, and 0 changed files. This report does not apply macOS findings to Windows.

### [Confirmed] [macOS only] app.asar changed slightly while the outer bundle tree stayed stable

The macOS outer bundle file count stayed 1238 → 1238, with 0 added, 0 removed, and 54 content-changed files. `Contents/Resources/app.asar` moved from 136.7 MB to 136.8 MB, up 0.04 MB.

Inside app.asar, the file count moved from 1377 to 1382, with 487 added, 482 removed, and 10 changed files. The fact package explicitly warns that many webview/assets changes are Vite build hash renames, so this report treats only de-hashed module changes as feature-direction evidence.

### [Signal] [macOS only] Settings expanded noticeably: general settings, route state, hooks model, and build flavor

After removing hash noise, newly added frontend modules include `get-build-flavor`, `hooks-settings-model`, `settings-route-state`, and `use-plugins`; the same-name `general-settings` frontend module grew by 98182 bytes.

New `general-settings` strings cover UI font sizing, code font sizing, Agent environment, default permissions toggle, work mode options, dictation hotkey capture input, hotkey window hotkey capture input, ambient suggestions toggle, and dictionary entry labels. New state/style classes include multiple `aria-*` entries, input backgrounds, list hover backgrounds, borders, and transparent backgrounds, suggesting concentrated settings-page work around accessibility, inputs, and state representation.

Platform difference: these settings-side signals appear only in the macOS fact package; the Windows input sample has no corresponding content changes.

### [Signal] [macOS only] Browser Use settings gained clues for history, cache, screenshot annotations, and site origins

`browser-use-settings` grew by 46103 bytes. New strings include `Access history without asking`, `Ask before accessing history`, `Always allow`, `Always ask`, `Always include`, `Annotation screenshots`, `Browser cache cleared`, `Aria label for browser use domain input`, and labels for removing individual website origins.

These clues point to Browser Use settings around permission policy, history access policy, cache-clearing feedback, screenshot annotations, and per-origin website management. The evidence boundary is a same-name frontend module diff, so it does not prove that all settings are stably exposed.

Platform difference: this appears only in the macOS sample; the Windows sample has no content changes.

### [Signal] [macOS only] Hooks settings were reorganized around tool execution and conversation compaction events

`hooks-settings` added strings including `Admin config`, `Before a tool executes`, `After a tool executes`, `Before Codex compacts the conversation`, `After Codex compacts the conversation`, plus descriptions for pre/post tool use, pre/post compact, and permission request hook events.

The button wording changed from refresh hooks to reload hooks, and a trust a hook button label was added. This suggests finer-grained UI signals around event types, admin config, trust actions, and reload actions in Hooks settings.

Platform difference: this appears only in the macOS sample; the Windows sample has no content changes.

### [Signal] [macOS only] Worktree setup gained failure recovery, cancel, and status feedback text

`worktree-init-v2-page` added strings covering worktree creation in progress, creating a worktree to fork a conversation, creation success, creation failure, the worktree being created but the renderer failing to start the conversation, local task creation failure, Retry, Cancel, Edit environment, and opening local environment settings after setup failure.

This text shows clear signals for worktree setup changes around error recovery, status display, environment-setting entry points, and canceling into a local conversation.

Platform difference: this appears only in the macOS sample; the Windows sample has no content changes.

### [Signal] [macOS only] The welcome page shows a “coding / everyday work” mode split

`welcome-page` added strings including `For coding`, `For everyday work`, `More technical responses and code detail`, `Same powerful agent, less technical detail`, plus task-oriented choices such as `Build software`, `Analyze data`, `Design products`, `Manage inbox`, `Manage projects`, and `Organize calendar`.

This indicates that welcome page v2 text and task categories for coding mode versus everyday work are present in the package.

Platform difference: this appears only in the macOS sample; the Windows sample has no content changes.

### [Signal] [macOS only] External agent import moved from a monolithic module toward a step module

After removing hash noise, `external-agent-import-step` was added and `external-agent-import` was removed. Based on module names alone, the external agent import flow may have been split into a step-based implementation.

Evidence boundary: this is module-level signal without supporting readable strings or service entry facts, so it is not treated as a confirmed capability surface.

### [Signal] [macOS only] Shield check and in-app Browser Use availability checks entered the frontend module set

After removing hash noise, `shield-check` and `use-in-app-browser-use-availability` were added. The former suggests a security or blocking-check direction, while the latter suggests an in-app Browser Use availability check.

Evidence boundary: the fact package only provides module names, with no related readable strings, permissions, or main-process entry points, so these remain low-to-medium-strength directional signals.

### [Signal] [macOS only] Markdown rendering or syntax-highlighting assets shrank and were replaced

The `markdown` module shrank from 0.3 MB to 0.1 MB, down 270477 bytes. New strings mostly look like Markdown/HTML scope names, such as `heading.1.markdown` through `heading.6.markdown`, `comment.frontmatter`, `entity.name.tag.html`, and `constant.other.reference.link.markdown`; removed strings include multiple older HTML/XHTML DTD fragments and a partial DOM serialization fragment.

This looks more like a package replacement around Markdown parsing, rendering, or syntax highlighting than a directly nameable user-facing feature.

### [Signal] [macOS only] Plugin detail page sharing / enablement UI clues were removed or refactored

`plugin-detail-page` shrank by 128040 bytes. Removed strings include workspace user search and removal, plugin share dialog, MCP server enablement toggle, section navigation, `Add people`, `Add to Codex`, `Adding to Codex`, and `Anyone at workspace`.

This indicates a substantial frontend change around sharing, adding, enablement toggles, or navigation in the plugin detail page. Because the evidence is removal-oriented, it does not prove that the feature was removed; it may have moved or been refactored.

## 🔎 Low-Level Signal Index

### Settings and accessibility

`general-settings`, `browser-use-settings`, and `hooks-settings` all show many `aria-*` labels, button descriptions, and input-control descriptions. Evidence boundary: frontend module strings and style/state class names show that UI accessibility text and state classes entered or changed in the package.

### Browser actions and comment popup

`src` added the style/state class name `pt.BROWSER_COMMENT_POPUP:return`, while `browser-use-settings` also includes text for history access, cache clearing, screenshot annotations, and website origin management. Evidence boundary: the former is only a style/state class signal; the latter is readable-string signal from a same-name frontend module.

### Local state key cleanup or migration

`src` removed names such as `active-remote-project-id`, `active-workspace-roots`, `mac-menu-bar-enabled`, `persisted-atom-state`, `pinned-project-ids`, `pinned-thread-ids`, `primary-runtime-update-jitter-ms`, `project-order`, `projectless-thread-ids`, `selected-remote-host-id`, `windows-wsl-reminder-dismissed-date`, and `worktree-auto-cleanup-enabled`. Evidence boundary: these are bundle-diff name clues, not proof that the persisted storage format was deleted.

### Skills and local image inlining

`use-skills` shrank by 9746 bytes and removed `Failed to inline local image`, `plugin marketplace request requires a marketplace source`, `plugin marketplace request requires one marketplace source`, and `react.memo_cache_sentinel`. Evidence boundary: these are removed strings from a same-name frontend module, showing related error paths or implementation locations changed, not that skills functionality was removed.

### Menu trigger and icon noise

`more-menu-trigger` grew by 1215 bytes, added `http://www.w3.org/2000/svg`, and removed `aria-label`. This is an isolated weak signal with limited reading value, likely related to icon or rendering implementation changes.

## 📊 Raw Data

macOS arm64:

| Item | Previous | Latest | Change |
| --- | --- | --- | --- |
| Version | 26.506.21252 | 26.506.31004 | Updated |
| build | 2575 | 2604 | +29 |
| Bundle file count | 1238 | 1238 | 0 |
| Bundle file changes | - | - | 0 added / 0 removed / 54 content-changed |
| app.asar | 136.7 MB | 136.8 MB | +0.04 MB |
| app.asar internal files | 1377 | 1382 | +5 |
| app.asar internal changes | - | - | 487 added / 482 removed / 10 changed |
| New cua_node dependencies | - | - | 0 |

macOS real frontend module additions/removals after removing hash noise:

| Type | Modules |
| --- | --- |
| Added | `external-agent-import-step`, `get-build-flavor`, `hooks-settings-model`, `settings-route-state`, `shield-check`, `use-in-app-browser-use-availability`, `use-plugins` |
| Removed | `chats`, `external-agent-import` |

macOS key same-name frontend module size changes:

| Module | Change |
| --- | --- |
| `general-settings` | +98182 bytes |
| `browser-use-settings` | +46103 bytes |
| `markdown` | -270477 bytes |
| `hooks-settings` | +3650 bytes |
| `worktree-init-v2-page` | +2102 bytes |
| `welcome-page` | +667 bytes |
| `plugin-detail-page` | -128040 bytes |
| `use-skills` | -9746 bytes |
| `src` | +605 bytes |
| `more-menu-trigger` | +1215 bytes |

Windows x64:

| Item | Previous | Latest | Change |
| --- | --- | --- | --- |
| Version | 26.506.3484.0 | 26.506.3484.0 | 0 |
| build | 26.506.3484.0 | 26.506.3484.0 | 0 |
| SHA-256 | `554ab8b534305bb26338c8b89dd745210625d52f78850ec7996c56772821e4ee` | `554ab8b534305bb26338c8b89dd745210625d52f78850ec7996c56772821e4ee` | 0 |
| Bundle file count | 815 | 815 | 0 |
| Bundle file changes | - | - | 0 added / 0 removed / 0 content-changed |
| app.asar internal files | 1336 | 1336 | 0 |
| app.asar internal changes | - | - | 0 added / 0 removed / 0 changed |
| New cua_node dependencies | - | - | 0 |

## ⚖️ Boundaries and Disclaimer

This is an unofficial third-party changelog and does not represent official OpenAI release notes.

This report does not redistribute binaries. It only references machine-extracted diff facts, signature verification results, SHA-256 values, file statistics, strings, and module names.

[Signal] does not constitute source-level fact and does not guarantee that a feature is visible to all users. It only means the relevant module, text, or style clue entered the corresponding platform package. Corrections using more complete samples, official notes, or runtime verification are welcome.

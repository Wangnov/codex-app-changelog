
---
title: "Codex Desktop 26.506.31004"
version: "26.506.31004"
build: 2604
mac_version: "26.506.31004"
mac_build: 2604
win_version: "26.506.3484.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-05-08"
method: "Official package reverse diff(macOS Sparkle / Windows MSIX)"
summary: "This batch updates the macOS package with frontend signals around settings, plugins, browser availability, and import flow, while the Windows comparison package remains unchanged."
official_release_notes: false
---

# Codex Desktop 26.506.31004(macOS / Windows Comparison)

> Third-party cross-platform changelog · Unofficial

The main takeaway: the macOS build contains real observable updates, while the Windows comparison package shows no file-level or app-layer changes in this batch.  
One-liner: macOS moved forward with app-layer signals around settings, plugins, browser availability, and import flow, while the Windows package stayed unchanged.

## About This Report

This report is based on reverse diffing official signed distribution packages: the macOS side uses full Sparkle update packages, and the Windows side uses MSIX comparison packages. The macOS comparison is from `26.506.21252` build `2575` to `26.506.31004` build `2604`; the Windows comparison is from `26.506.3484.0` to `26.506.3484.0`.

Evidence is split into two levels: [Confirmed] means the claim comes from readable package metadata such as plist, AppxManifest, dependency lists, type declarations, skill documents, or package metadata; [Signal] means only frontend module names or size changes are visible, which can indicate a direction but cannot reconstruct source intent or confirm user-facing availability.

Platform tags mean the following: `[macOS+Windows]` means both platforms show the corresponding change; `[macOS only]` means it is only observed in the macOS package; `[Windows only]` means it is only observed in the Windows package. The key platform difference in this batch is that macOS moved forward, while the Windows package did not show matching content changes.

## ✨ Highlights

### macOS Version Bump, Windows Comparison Package Unchanged [Confirmed] [macOS only]

On macOS, `CFBundleShortVersionString` changed from `26.506.21252` to `26.506.31004`, `CFBundleVersion` changed from `2575` to `2604`, and the integrity hash for `app.asar`, the Electron app code archive, changed as well.

The Windows comparison package stayed at version `26.506.3484.0`. The two package SHA-256 fingerprints are identical, and the file count, content-change count, and internal `app.asar` file count all remained unchanged. In practical terms, the observable new content in this batch is concentrated in the macOS package, with no corresponding Windows delta.

### New Frontend Modules Around Settings, Plugins, and Runtime Availability [Signal] [macOS only]

After excluding hash-renamed asset noise, the macOS app layer added these modules: `external-agent-import-step`, `get-build-flavor`, `hooks-settings-model`, `settings-route-state`, `shield-check`, `use-in-app-browser-use-availability`, and `use-plugins`.

Based on names alone, these signals point toward external agent import steps, build flavor detection, settings route state, plugin usage, in-app browser availability checks, and shield checks. These are module-name signals only, so they do not prove that any specific feature is user-visible. On Windows, `app.asar` stayed at `1336 → 1336` internal files, with `0` added, `0` removed, and `0` changed, so no matching module movement is visible there.

### Older Import and Chat-Related Modules Removed or Replaced [Signal] [macOS only]

After excluding hash noise, the macOS package removed the frontend modules `chats` and `external-agent-import`. Together with the newly added `external-agent-import-step`, this suggests a structural adjustment around the external agent import frontend path. Since the evidence is limited to module names, it does not confirm the exact interaction or business logic change.

No app-layer file changes were observed on Windows, so the same frontend module replacement is not visible there.

### Tiny Browser Plugin Script Size Changes [Signal] [macOS only]

In the macOS package, both `plugins/openai-bundled/plugins/chrome/scripts/browser-client.mjs` and `plugins/openai-bundled/plugins/browser-use/scripts/browser-client.mjs` show tiny size changes recorded as `+0.00 MB`. Because the evidence only includes paths and size deltas, this only proves that browser-related scripts changed; it does not establish a specific feature change.

The Windows file tree has `0` content changes, so there is no corresponding Windows-side change.

### No New cua_node Dependencies [Confirmed] [macOS+Windows]

Both platform fact packs report `0` newly added `cua_node` packages. This means no new dependency-level additions were observed for `cua_node` in this batch.

## 📊 Raw Data

| Item | macOS arm64 | Windows x64 |
| --- | --- | --- |
| Compared versions | `26.506.21252` build `2575` → `26.506.31004` build `2604` | `26.506.3484.0` → `26.506.3484.0` |
| Package-level file count | `1238 → 1238` | `815 → 815` |
| Package-level added / removed / changed | `0 / 0 / 54` | `0 / 0 / 0` |
| Internal `app.asar` files | `1377 → 1382` | `1336 → 1336` |
| `app.asar` added / removed / changed | `487 / 482 / 10` | `0 / 0 / 0` |
| New `cua_node` dependencies | `0` | `0` |

Key macOS size changes:

| Component Path | Old | New | Change |
| --- | --- | --- | --- |
| `Contents/Resources/app.asar` | `136.7 MB` | `136.8 MB` | `+0.04 MB` |
| `Contents/Resources/plugins/openai-bundled/plugins/chrome/scripts/browser-client.mjs` | `0.7 MB` | `0.7 MB` | `+0.00 MB` |
| `Contents/Resources/plugins/openai-bundled/plugins/browser-use/scripts/browser-client.mjs` | `0.7 MB` | `0.7 MB` | `+0.00 MB` |

Key Windows size changes: the fact pack lists no non-zero changed components.

macOS reproducibility evidence:

| Item | SHA-256 | Verification |
| --- | --- | --- |
| Previous full package | `4bf84cc3111233afaa1465623303e9f1296ec170f70b28e4cea29b3e62b8cc16` | accepted, Notarized Developer ID, OpenAI OpCo, LLC |
| Latest full package | `9a09529d4937d1f24c78f78c7ef7f4b501c594c55b4beb4fecc076d03524dad5` | accepted, Notarized Developer ID, OpenAI OpCo, LLC |

Windows reproducibility evidence:

| Item | SHA-256 |
| --- | --- |
| Previous full package | `554ab8b534305bb26338c8b89dd745210625d52f78850ec7996c56772821e4ee` |
| Latest full package | `554ab8b534305bb26338c8b89dd745210625d52f78850ec7996c56772821e4ee` |

## ⚖️ Limits and Notice

This is a third-party unofficial changelog and does not represent official OpenAI release notes.

This report does not redistribute binaries. It only summarizes machine-extracted differences from official signed distribution packages.

Items marked [Signal] only indicate that package-internal module names or size deltas point in a certain direction. They are not source-code facts and do not prove that a feature is available to users.

Corrections are welcome when backed by the same fact pack or newer official package evidence.

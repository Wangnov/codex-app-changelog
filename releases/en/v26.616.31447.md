
---
title: "Codex Desktop 26.616.31447"
version: "26.616.31447"
build: 4133
mac_version: "26.616.31447"
mac_build: 4133
win_version: "26.616.3309.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-18"
method: "官方包逆向 diff(macOS Sparkle / Windows MSIX)"
summary: "macOS moves from 26.616.30709 to 26.616.31447 with only version and asar integrity updates confirmed; Windows 26.616.3309.0 is unchanged across the compared packages."
official_release_notes: false
---

# Codex Desktop 26.616.31447(macOS / Windows comparison)

> Third-party cross-platform changelog · Unofficial

This batch is intentionally quiet at the readable-diff level: macOS advances its package version, while Windows shows no content change in the compared artifacts.  
One-liner: macOS advances to 26.616.31447, while the compared Windows 26.616.3309.0 package is byte-level unchanged in extracted contents.

## About This Report

This report is based on reverse-engineered diffs of officially signed distribution packages. The macOS comparison uses official full packages from Sparkle, a common macOS app update framework. The Windows comparison uses MSIX, the Windows app package format. No official release notes are used.

Evidence is split into two levels:

- [Confirmed]: readable evidence from files such as Info.plist, AppxManifest, plugin.json, dependency manifests, type declarations, or skill documentation, plus explicit file statistics.
- [Signal]: evidence only from frontend module names, size movement, or build artifacts. It can suggest direction, but cannot reconstruct source intent.

Platform labels mean:

- `[macOS+Windows]`: the same change is supported by both platform fact packages.
- `[macOS only]`: observed only in the macOS package.
- `[Windows only]`: observed only in the Windows package.

Comparison range:

- macOS arm64: `26.616.30709` build `4108` → `26.616.31447` build `4133`.
- Windows x64: `26.616.3309.0` build `26.616.3309.0` → `26.616.3309.0` build `26.616.3309.0`.
- The release date field for this batch is recorded as `2026-06-18`.

## ✨ Highlights

### [Confirmed] [macOS only] App version advances to 26.616.31447

On macOS, `Info.plist` shows `CFBundleShortVersionString` changing from `26.616.30709` to `26.616.31447`, and `CFBundleVersion` changing from `4108` to `4133`. The `ElectronAsarIntegrity/Resources/app.asar/hash` also changes from `3d86cae28e6657443c87174307b745c843e74f1154694e4d87ede9b121fcf697` to `df68ece60ec92bf946437dbd262d64426af0efb514b9b94d2c738916482f7d50`.

The platform difference is clear: Windows remains `26.616.3309.0` → `26.616.3309.0`, with `0` content changes, so the corresponding version advance is not observed there.

### [Confirmed] [macOS only] Bundled browser and chrome plugin versions are updated

In the macOS package, two bundled plugin manifests update their `.codex-plugin/plugin.json` version from `26.616.30709` to `26.616.31447`:

- `openai-bundled/plugins/chrome/.codex-plugin/plugin.json`
- `openai-bundled/plugins/browser/.codex-plugin/plugin.json`

This confirms that the plugin manifest layer tracks the new macOS app version. The Windows fact package reports no plugin.json or app-layer changes, so this is classified as present on macOS and not observed on Windows.

### [Confirmed] [macOS only] app.asar changes slightly, but readable evidence only confirms version and integrity metadata

On macOS, `app.asar`, the Electron application archive, remains at `3115 → 3115` internal files, while the diff reports `463 added / 463 removed / 14 changed`. The fact package also notes that many changes under `webview/assets` are Vite build hash renames, where Vite is the frontend build tool; those changes are noise and should not be interpreted as feature additions or removals.

The readable diffs only confirm plugin version updates and the asar integrity hash change. There is not enough evidence to claim new features, UI behavior changes, or business logic changes. On Windows, `app.asar` remains `3078 → 3078`, with `0 added / 0 removed / 0 changed`.

### [Confirmed] [macOS+Windows] No new Computer Use runtime dependencies observed

The Computer Use runtime `cua_node` is unchanged on both platforms, and both report `0` newly added dependency packages:

- macOS: `365.9 MB → 365.9 MB (+0 MB, +0%)`, `0` new packages.
- Windows: `285.6 MB → 285.6 MB (+0 MB, +0%)`, `0` new packages.

This means there is no evidence in this batch of an expanded Computer Use runtime dependency set. The two platforms have different total sizes, but each remains unchanged within its own comparison.

### [Confirmed] [Windows only] Windows package is fully unchanged in this comparison

The Windows file tree remains `9216 → 9216`, with `0` added, `0` removed, and `0` content changes. The previous and latest full package SHA-256 values are also identical: `a73f689a23c1d03df934262c193a313f7beec2510965398dbcb442bc95b3525b`.

So, within this cross-platform batch, Windows looks like a reused or backfilled identical package: there are no reportable app-layer, runtime, or manifest changes.

## 📊 Raw Data

### macOS arm64

| Item | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| Version | `26.616.30709` | `26.616.31447` | Updated |
| Build | `4108` | `4133` | +25 |
| Bundle files | `8416` | `8416` | 0 |
| Bundle added/removed/changed | - | - | `0 / 0 / 69` |
| app.asar internal files | `3115` | `3115` | 0 |
| app.asar added/removed/changed | - | - | `463 / 463 / 14` |
| cua_node total size | `365.9 MB` | `365.9 MB` | `+0 MB, +0%` |
| New cua_node dependency packages | - | - | `0` |

Key size changes:

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | `155.8 MB` | `155.8 MB` | `+0.00 MB` |
| `Contents/PlugIns/CodexDockTilePlugin.plugin/Contents/MacOS/CodexDockTilePlugin` | `0.1 MB` | `0.1 MB` | `-0.00 MB` |
| `Contents/Resources/plugins/openai-bundled/plugins/chrome/extension-host/macos/arm64/extension-host` | `1.3 MB` | `1.3 MB` | `-0.00 MB` |

Verification:

- Previous SHA-256: `7b2c16517bcaa4e5a8acf56e5ae9885497449b864ecd52df3bd9995a93e5ba0b`
- Latest SHA-256: `7c24196e7577ff246adfc8b96b2ba788656bfd97c8bd0bdbdce6a9e0b34bba01`
- Both versions pass notarization verification as `accepted`, with source `Notarized Developer ID` and origin `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

### Windows x64

| Item | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| Version | `26.616.3309.0` | `26.616.3309.0` | 0 |
| Build | `26.616.3309.0` | `26.616.3309.0` | 0 |
| Bundle files | `9216` | `9216` | 0 |
| Bundle added/removed/changed | - | - | `0 / 0 / 0` |
| app.asar internal files | `3078` | `3078` | 0 |
| app.asar added/removed/changed | - | - | `0 / 0 / 0` |
| cua_node total size | `285.6 MB` | `285.6 MB` | `+0 MB, +0%` |
| New cua_node dependency packages | - | - | `0` |

Key size changes: the fact package lists no components with non-zero byte changes.

Verification:

- Previous SHA-256: `a73f689a23c1d03df934262c193a313f7beec2510965398dbcb442bc95b3525b`
- Latest SHA-256: `a73f689a23c1d03df934262c193a313f7beec2510965398dbcb442bc95b3525b`

## ⚖️ Boundaries and Disclaimer

This is a third-party, unofficial cross-platform changelog. It is not an OpenAI release note.

It is based only on reverse-engineered diffs of officially signed distribution packages and does not redistribute binaries.

Items marked [Signal] only indicate directional clues from build artifacts and do not constitute source-level facts. The main conclusions in this batch are based on [Confirmed] evidence.

If official release notes, additional platform packages, or more complete readable diffs become available later, the conclusions here should be revised against that newer evidence.


---
title: "Codex Desktop 26.406.40811"
version: "26.406.40811"
build: 1457
previous_version: "26.406.31014"
previous_build: 1395
platform: "macOS arm64"
released: "2026-04-11"
compared_from: "2026-04-08"
method: "Official full-package comparison + structured bundle diff"
summary: "This appears to be a narrowly scoped maintenance build: version metadata and app.asar integrity changed, while the only frontend signal is a tiny layout-related diff."
official_release_notes: false
---

# Codex Desktop `26.406.40811` build 1457

> Third-party changelog · Unofficial · Based on reverse diffing official update artifacts

This is a very small macOS arm64 release by bundle structure and size: no new dependencies, plugins, or obvious capability surface are visible in the extracted facts.  
English one-liner: a tightly scoped maintenance build with only minimal frontend layout signals visible from the bundle diff.

## About This Report

This is not an official changelog. It is a **reverse diff report based on official distribution artifacts**. The comparison covers macOS arm64 from `26.406.31014` build 1395 to `26.406.40811` build 1457, released on 2026-04-08 and 2026-04-11 respectively.

This entry is a historical backfill. The fact pack states that the official appcast did not provide a delta package for this version pair, so the comparison was performed directly between two official full packages, then summarized from the structured bundle diff. Vite hash filename churn is not treated as feature addition or removal.

Evidence levels used here:

- **[Confirmed]**: directly readable evidence such as Info.plist values, signing/notarization results, dependency lists, and bundle file statistics.
- **[Signal]**: indirect evidence from same-name frontend module diffs or size changes. These point in a direction, but cannot be recovered into source-level intent from the artifact alone.

## ✨ Highlights

### 1. Version and build advanced to `26.406.40811` build 1457 [Confirmed]

The `Info.plist` changes `CFBundleShortVersionString` from `26.406.31014` to `26.406.40811`, and `CFBundleVersion` from `1395` to `1457`.

This confirms a newly packaged macOS arm64 build, rather than a change that is only explained by file hashes or external metadata.

Key evidence:

```diff
CFBundleShortVersionString: 26.406.31014 -> 26.406.40811
CFBundleVersion: 1395 -> 1457
```

### 2. No observed dependency, plugin, or file-level capability expansion [Confirmed]

The bundle file count remains `416 → 416`, with `0` added files, `0` removed files, and `39` content-modified files. The `cua_node` dependency delta shows `0` newly added packages.

At the artifact-structure level, this release does not introduce a visible new component or dependency surface. The fact pack also does not show new built-in plugins, service entries, skill documents, permissions, or user-visible new commands.

Key evidence:

| Item | Change |
| --- | --- |
| Bundle file count | `416 → 416` |
| Added / removed / content-modified | `0 / 0 / 39` |
| New `cua_node` dependencies | `0` |

### 3. `app.asar` integrity changed while size stayed effectively flat [Confirmed]

The main application archive `Contents/Resources/app.asar` remains `43.0 MB → 43.0 MB`, with a reported delta of `+0.00 MB`, while the `ElectronAsarIntegrity` hash changed.

This means application-layer content did change, but the size and file-tree evidence indicate a very small change. It is not enough to infer a new feature.

Key evidence:

```diff
app.asar: 43.0 MB -> 43.0 MB (+0.00 MB)
ElectronAsarIntegrity hash:
09a16a5f2833f1dcebcd6daa5cc0fd81fd08c0a8780d50cb66352d38e42121aa
-> c6e625133b42bfe77bfb8ae2468e0ce27fa16d2e89c1c48fd57a16be0757e946
```

### 4. The main frontend entry shows a tiny layout-related signal [Signal]

The same-name frontend `index` module changed from `webview/assets/index-C6yWjN3W.js` to `webview/assets/index-I0ioI9QD.js`. Its size remains `3.6 MB → 3.6 MB`, with only `72 bytes` added, and the style/state class `min-w-80` was removed.

This points toward a possible adjustment to a minimum-width or layout constraint, but it cannot be recovered into source-level intent from the artifact, nor does it identify the exact screen or user impact.

Key evidence:

```diff
index js: 3.6 MB -> 3.6 MB (+72 bytes)
removed class/state: min-w-80
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 416 | 416 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Content-modified files | 39 | 39 | 39 |

### Key Component Size Changes

| Component path | Old | New | Delta |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 43.0 MB | 43.0 MB | +0.00 MB |

### Newly Added Dependencies

| Dependency area | New package count | Notes |
| --- | ---: | --- |
| `cua_node` | 0 | No newly added dependencies are shown in the fact pack |

## 🔁 Reproducibility

| Item | Previous `26.406.31014` | Latest `26.406.40811` |
| --- | --- | --- |
| Artifact URL | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.406.31014.zip` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.406.40811.zip` |
| SHA-256 | `cd87fba4179ef4c096f5e85a8e4b16452a54b5bce75097b65e139951367f23b8` | `33a66113d6fb9023c1451f448a63ece12255cc2d84bd56106fb02a8835cbea77` |
| Size | Not provided in the fact pack | Not provided in the fact pack |
| EdDSA verification | Not provided in the fact pack | Not provided in the fact pack |
| codesign/spctl notarization result | `CSSMERR_TP_CERT_REVOKED` | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

The previous version reports `CSSMERR_TP_CERT_REVOKED`. The fact pack notes that certificate revocation on some earlier versions can affect whether the system considers that version safe to install, but it does not prevent bundle-content diff analysis.

## ⚖️ Boundaries and Disclaimer

This report is independent of OpenAI and does not represent an official position or official release notes.

It does not redistribute any official binary. It only publishes conclusions derived from differences between official distribution artifacts.

All [Signal] items are indirect artifact-level observations. They are not source-level facts or feature commitments. Corrections backed by reproducible evidence are welcome.

---
title: "Codex Desktop 26.406.40811"
version: "26.406.40811"
build: 1457
previous_version: "26.406.31014"
previous_build: 1395
platform: "macOS arm64"
released: "2026-04-11"
compared_from: "2026-04-08"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "This release is primarily a small app-layer update: the version number and app.asar contents changed, but no new dependencies or new bundle-level components were observed."
official_release_notes: false
---

# Codex Desktop `26.406.40811`(build 1457)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

This is a very restrained minor update in both size and file tree changes, with the main changes concentrated in the app-layer `app.asar` contents and version metadata.  
One-liner: a small app-layer revision with no new bundle files or dependency additions observed.

## About this report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. This is a historical backfill: both versions are already outside the official Sparkle incremental update window, so the comparison uses two official full packages directly rather than reconstructing from incremental packages.

The comparison range is from `26.406.31014` build `1395`, released on `2026-04-08`, to `26.406.40811` build `1457`, released on `2026-04-11`. The notarization validation result for the latest version is `accepted`, with `Notarized Developer ID` as the source and `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` as the signing subject. Validation for the previous version shows `CSSMERR_TP_CERT_REVOKED`; the fact package explains that this was caused by certificates for some earlier versions being revoked later, which affects the system’s security assessment during installation but does not affect analysis of bundle content differences.

This report uses two evidence levels:

- **[Confirmed]**: Direct human-readable evidence, such as Info.plist key values, dependency manifests, signing, and notarization validation results.
- **[Signal]**: Indirect evidence, such as frontend module names or binary size changes; this type of evidence only indicates a direction, but cannot reconstruct source-level intent from the artifact.

## ✨ Key changes

### 1. Version upgraded from `26.406.31014` to `26.406.40811` [Confirmed]

Both the public app version and the internal build number were updated: `CFBundleShortVersionString` changed from `26.406.31014` to `26.406.40811`, and `CFBundleVersion` changed from `1395` to `1457`.

This means it is a clearly released new desktop build, not merely a re-signed or repackaged build.

Evidence:

```diff
CFBundleShortVersionString: 26.406.31014 -> 26.406.40811
CFBundleVersion: 1395 -> 1457
```

### 2. Minor app-layer content updates, but no new bundle components observed [Confirmed]

The total bundle file count remained `416 → 416`, with no files added or removed and `39` files changed. The internal file count inside the app-layer `app.asar` also remained `815 → 815`, with `2` added, `2` removed, and `7` changed.

The perceptible takeaway is that this looks more like a localized app logic or frontend artifact update than the addition of standalone components, plugins, or large-scale resource changes. Because the fact package explicitly notes that filename additions and removals under `webview/assets` and `.vite/build` with hashes are build artifact renames, those additions/removals are not interpreted here as feature additions or removals.

Evidence:

```text
Bundle file count: 416 -> 416
Added 0 / Removed 0 / Content changed 39
app.asar internal files: 815 -> 815
```

### 3. No new `cua_node` dependency packages added [Confirmed]

The fact package shows that the number of newly added `cua_node` dependencies is `0`. This indicates that no new lower-level Node dependency packages were observed joining this dependency set in this diff.

For developers, this usually means this version did not expand obvious new local capabilities by adding packages at that dependency layer; however, whether existing code paths changed cannot be determined from the dependency manifest alone.

Evidence:

```text
cua_node added 0 packages
```

### 4. `app.asar` integrity hash changed [Confirmed]

The `ElectronAsarIntegrity/Resources/app.asar/hash` value in Info.plist changed from the old value to the new value, indicating that the packaged app-layer contents did indeed change.

This change alone does not equal a specific feature update, but together with the internal `app.asar` changes, it confirms that this version is not a version-number-only change.

Evidence:

```diff
09a16a5f2833f1dcebcd6daa5cc0fd81fd08c0a8780d50cb66352d38e42121aa
->
c6e625133b42bfe77bfb8ae2468e0ce27fa16d2e89c1c48fd57a16be0757e946
```

## 📊 Raw data

### Bundle file-level statistics

| Metric | Previous version | Latest version | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 416 | 416 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Files with content changes |  |  | 39 |

### Key component size changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 43.0 MB | 43.0 MB | +0.00 MB |

### Added dependency manifest

| Dependency set | Added count |
| --- | ---: |
| `cua_node` | 0 |

## 🔁 Reproducibility information

| Artifact | URL | SHA-256 | Validation result |
| --- | --- | --- | --- |
| Previous full package `26.406.31014` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.406.31014.zip` | `cd87fba4179ef4c096f5e85a8e4b16452a54b5bce75097b65e139951367f23b8` | Notarization validation shows `CSSMERR_TP_CERT_REVOKED` |
| Latest full package `26.406.40811` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.406.40811.zip` | `33a66113d6fb9023c1451f448a63ece12255cc2d84bd56106fb02a8835cbea77` | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Boundaries and disclaimer

This report is independent of OpenAI. It is a third-party, unofficial reverse-engineered diff summary.

This report does not redistribute any official binaries; it only publishes diff conclusions derived from official distribution artifacts.

All [Signal] entries only represent directional clues at the artifact level and do not constitute source-level facts. The fact package for this version does not contain enough material to support specific feature-level inferences, so the main text primarily retains [Confirmed] conclusions.

Reviews and corrections based on the same artifacts and verification information are welcome.
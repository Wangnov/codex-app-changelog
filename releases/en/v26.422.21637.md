---
title: "Codex Desktop 26.422.21637"
version: "26.422.21637"
build: 2056
previous_version: "26.422.21459"
previous_build: 2049
platform: "macOS arm64"
released: "2026-04-24"
compared_from: "2026-04-24"
method: "官方全量包直接对比 + 结构化 bundle 差异"
summary: "This release is mainly a minor update to the version number and packaged contents, with no observed new dependencies, file tree expansion, or clearly attributable new features."
official_release_notes: false
---

# Codex Desktop `26.422.21637`(build 2056)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

This is a very restrained minor release: judging from the visible artifacts, the main changes are concentrated in the version number, the integrity hash of `app.asar`, and a small amount of content modification. There are no new dependencies or new outer-layer components.  
English one-liner: a small packaging-level update with no clear user-facing feature evidence in the extracted facts.

## About this report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. This is a historical backfill: both versions are already outside the official Sparkle delta window, so the comparison did not use the “previous full package + delta package reconstruction” path. Instead, two official full packages were downloaded and compared directly through a structured bundle diff.

In terms of trustworthiness, both `Codex.app` builds passed Apple notarization and OpenAI Developer ID signature verification:

- Previous version `26.422.21459`: `accepted`, `source=Notarized Developer ID`
- Latest version `26.422.21637`: `accepted`, `source=Notarized Developer ID`
- The signature source for both is `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`

Evidence is divided into two levels:

- **[Confirmed]**: Human-readable evidence from Info.plist, dependency manifests, signature / notarization output, explicit file statistics, and similar sources.
- **[Signal]**: Indirect clues from frontend module names, binary size changes, and similar sources. These can only point in a direction and cannot be reconstructed into source-level intent from the artifacts.

Comparison scope: `26.422.21459` build `2049`, released on `2026-04-24`, to `26.422.21637` build `2056`, also released on `2026-04-24`.

## ✨ Key changes

### 1. Version and build numbers advanced [Confirmed]

The application metadata was updated from `26.422.21459` / build `2049` to `26.422.21637` / build `2056`.

This indicates that this was a formally repackaged release, rather than a re-signing of the same build or a simple download URL change. The most direct evidence comes from `Info.plist`:

```diff
CFBundleShortVersionString: 26.422.21459 -> 26.422.21637
CFBundleVersion: 2049 -> 2056
```

### 2. The application resource archive changed slightly, but no clearly nameable new feature is visible [Confirmed]

The integrity hash of `Contents/Resources/app.asar` changed, which means the Electron application-layer contents were indeed modified. At the same time, the total number of files inside app.asar remained `1031 -> 1031`, with statistics showing `18` additions, `18` removals, and `5` modifications.

It should be noted that the fact package explicitly marks many additions and removals under `webview/assets` as Vite build hash renaming noise, so these cannot be described as added or removed functionality. What can be confirmed is that “application-layer contents changed,” but the fact package does not support further attribution to a specific user-facing feature.

Key evidence:

```diff
ElectronAsarIntegrity/Resources/app.asar/hash:
ad43823456452d17fbb4208b0b9e8299dcaefa90b77dda5f9797384d7036f40a
-> 50aa3dbd3b33c995917fdc534bf978285ad7f01a32204cc044986db63ba02e1c
```

### 3. The outer bundle file tree remained stable [Confirmed]

The outer application bundle file count remained `507 -> 507`, with `0` additions, `0` removals, and `47` content modifications.

This means this release did not add any new outer-layer plugins, framework directories, or resource directories. From a developer’s perspective, it looks more like a replacement-style update to existing contents than a feature expansion that introduces new component boundaries.

### 4. No new packages were added to the dependency manifest [Confirmed]

In the `cua_node` dependency changes, the number of newly added packages is `0`.

Therefore, this release should not be interpreted as adding a new class of underlying capability. The fact package does not provide evidence for new dependencies, API changes, permission allowances, or similar signals, so it cannot support conclusions such as “new PDF, OCR, image processing, browser automation, or other capabilities were added.”

## 📊 Raw data

### Bundle file-level statistics

| Metric | Previous version | Latest version | Change |
| --- | ---: | ---: | ---: |
| Outer bundle file count | 507 | 507 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Content-modified files | 47 | 47 | 47 files changed content |

### Key component size changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 119.9 MB | 119.9 MB | +0.00 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/browser-use/scripts/browser-client.mjs` | 0.8 MB | 0.8 MB | +0.00 MB |

### Newly added dependency manifest

| Scope | Number of new dependencies | Notes |
| --- | ---: | --- |
| `cua_node` | 0 | No new packages observed |

## 🔁 Reproducibility information

| Artifact | URL | SHA-256 | Verification conclusion |
| --- | --- | --- | --- |
| Previous full package `26.422.21459` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.422.21459.zip` | `5ce4b5b8427b9e0d66d2c3b286fec7b54b25cd7b0e3f29d90f46ca529d9902f7` | `spctl`: accepted; source is Notarized Developer ID |
| Latest full package `26.422.21637` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.422.21637.zip` | `6e733818af68baa0e1c30afbac877a4f18401b094766f1acba0cc10d1e33270c` | `spctl`: accepted; source is Notarized Developer ID |

| Verification item | Conclusion |
| --- | --- |
| EdDSA delta package signature verification | Not applicable: the fact package states that both versions are outside the official Sparkle delta window, so the comparison used two official full packages directly |
| Apple notarization | Both versions are `accepted` |
| Developer ID signature source | `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Boundaries and statement

This report is not affiliated with OpenAI and does not represent official statements or the official release cadence.

This article does not redistribute any official binaries. Its conclusions are based only on structured diffs of officially signed distribution artifacts.

Items marked as [Signal] do not constitute source-level facts. The fact package for this release does not contain enough material to support a conclusion that new user-facing features were added.

Reviews and corrections based on the same artifacts, hashes, and verification path are welcome.
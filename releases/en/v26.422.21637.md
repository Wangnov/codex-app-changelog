
---
title: "Codex Desktop 26.422.21637"
version: "26.422.21637"
build: 2056
previous_version: "26.422.21459"
previous_build: 2049
platform: "macOS arm64"
released: "2026-04-24"
compared_from: "2026-04-24"
method: "Official full-package comparison + structured bundle diff"
summary: "This is a same-day maintenance build: version and app.asar integrity changed, the only frontend signal is a tiny FREE state class-name delta, and no new dependencies or bundle files were observed."
official_release_notes: false
---

# Codex Desktop `26.422.21637` build 2056

> Third-party changelog · Unofficial · Based on reverse diffing official update artifacts

This report covers the macOS arm64 artifact diff from `26.422.21459` build 2049 to `26.422.21637` build 2056.  
A same-day maintenance build with very small observable surface change.

## About This Report

This is not an official changelog. It is a **reverse diff report based on official distribution artifacts**. For this historical version pair, no Sparkle delta package was available, so the comparison was performed directly between the two official full packages.

The comparison scope is macOS arm64: previous version `26.422.21459` build 2049 and latest version `26.422.21637` build 2056, both released on `2026-04-24`.

Evidence is classified into two levels:

- **[Confirmed]**: Human-readable structured evidence such as Info.plist values, signing and notarization results, bundle file tree data, dependency lists, and integrity hashes.
- **[Signal]**: Indirect evidence from same-name frontend module changes or binary size deltas. These can point in a direction, but cannot reconstruct source-level intent from the artifact.

## ✨ Highlights

### 1. Version and build advanced to `26.422.21637` build 2056 [Confirmed]

This is a same-day minor build update. `CFBundleShortVersionString` changed from `26.422.21459` to `26.422.21637`, and `CFBundleVersion` changed from `2049` to `2056`.

For users and developers, this means the app identity and visible version metadata were updated, but the fact pack does not include official release notes or feature-level explanations.

Evidence:

```diff
CFBundleShortVersionString: 26.422.21459 -> 26.422.21637
CFBundleVersion: 2049 -> 2056
```

### 2. app.asar changed, while size stayed effectively flat [Confirmed]

The integrity hash for `Contents/Resources/app.asar` changed, confirming that application-layer content did change. At the bundle level, however, the file count stayed at `507 → 507`, with `0` added files, `0` removed files, and `47` content-modified files.

The observable shape looks more like a small revision than a broad capability expansion. The fact pack also shows no new dependencies or new bundle files.

Evidence:

```diff
ElectronAsarIntegrity/Resources/app.asar/hash:
ad43823456452d17fbb4208b0b9e8299dcaefa90b77dda5f9797384d7036f40a
-> 50aa3dbd3b33c995917fdc534bf978285ad7f01a32204cc044986db63ba02e1c
```

### 3. Tiny FREE state class-name delta in the frontend `index` module [Signal]

The same-name frontend module `index` changed from `webview/assets/index-n5_rScsP.js` to `webview/assets/index-gATb9Tvd.js`. Its size remained `2.9 MB → 2.9 MB`, with only `108 bytes` added. The readable diff adds `mv.FREE:case` and removes `pv.FREE:case`.

This points toward a very small adjustment around a FREE state, style, or branch identifier, but it cannot be mapped back to source-level intent and should not be read as a feature addition by itself.

Evidence:

```diff
+ mv.FREE:case
- pv.FREE:case
```

### 4. No new cua_node dependencies observed [Confirmed]

The `cua_node` dependency diff reports `0` newly added packages. This means the artifact evidence does not support a conclusion that new capability packages were introduced at that dependency layer.

The practical takeaway is that the fact pack does not show a new lower-level dependency surface in this build.

Evidence:

```text
0 added packages
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 507 | 507 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Content-modified files | 0 | 47 | +47 |

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 119.9 MB | 119.9 MB | +0.00 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/browser-use/scripts/browser-client.mjs` | 0.8 MB | 0.8 MB | +0.00 MB |

### Newly Added Dependencies

| Scope | Added count | Details |
| --- | ---: | --- |
| `cua_node` | 0 | None |

## 🔁 Reproducibility

| Item | Previous | Latest |
| --- | --- | --- |
| Download artifact | `Codex-darwin-arm64-26.422.21459.zip` | `Codex-darwin-arm64-26.422.21637.zip` |
| SHA-256 | `5ce4b5b8427b9e0d66d2c3b286fec7b54b25cd7b0e3f29d90f46ca529d9902f7` | `6e733818af68baa0e1c30afbac877a4f18401b094766f1acba0cc10d1e33270c` |
| Size | Not provided in fact pack | Not provided in fact pack |
| EdDSA verification result | Not provided in fact pack | Not provided in fact pack |
| codesign/spctl notarization result | `accepted` / `Notarized Developer ID` / `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` | `accepted` / `Notarized Developer ID` / `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Boundaries and Disclosure

This report is independent of OpenAI and is an unofficial third-party reverse diff.

It does not redistribute any official binary. It only publishes conclusions derived from official signed distribution artifacts.

[Signal] entries are artifact-level directional observations. They are not source-level facts and should not be interpreted as official feature notes.

Corrections are welcome when based on the same fact pack or reproducible verification results.

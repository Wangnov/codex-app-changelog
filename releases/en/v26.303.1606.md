---
title: "Codex Desktop 26.303.1606"
version: "26.303.1606"
build: 806
previous_version: "26.228.1430"
previous_build: 760
platform: "macOS arm64"
released: "2026-03-04"
compared_from: "2026-03-01"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This release mainly reflects version and build number updates, developer/debugging script changes, and a signal that frontend modules related to remote connection settings were removed."
official_release_notes: false
---

# Codex Desktop `26.303.1606` (build 806)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

This is a small release with limited size changes and a largely stable file tree; the readable evidence is concentrated mainly in version numbers, build numbers, and developer scripts.  
English one-liner: a small desktop build update with developer-script changes and a weak signal around remote connection settings.

## About this report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. This entry is a historical backfill: both versions fall outside the official Sparkle delta update window, so the comparison uses two official full packages directly rather than reconstructing a delta package.

The comparison scope is `26.228.1430` (build 760, 2026-03-01) → `26.303.1606` (build 806, 2026-03-04), for the macOS arm64 platform.

In terms of confidence, both official full packages have recorded SHA-256 hashes; notarization verification for both returned `CSSMERR_TP_CERT_REVOKED`. The fact package explains that this is due to signing certificates for some early versions being revoked later. This affects the system’s security/install assessment, but does not affect diff analysis of the bundle contents.

Evidence is classified into two levels:

- **[Confirmed]**: Direct evidence from Info.plist, package.json, readable diffs, signatures, verification results, and similar sources.
- **[Signal]**: Indirect evidence from frontend module names, binary size changes, and similar artifacts; this type of information points in a direction but cannot reconstruct source-level intent from the artifact.

## ✨ Key changes

### 1. Version and build number updated to `26.303.1606` / build 806 [Confirmed]

The app version was updated from `26.228.1430` to `26.303.1606`, and the build number from `760` to `806`. This indicates a new macOS arm64 build released on 2026-03-04.

Key evidence:

```diff
- "version": "26.228.1430"
+ "version": "26.303.1606"

- "codexBuildNumber": "760"
+ "codexBuildNumber": "806"
```

Info.plist was updated accordingly:

```diff
/CFBundleShortVersionString: 26.228.1430 → 26.303.1606
/CFBundleVersion: 760 → 806
```

### 2. Added developer metadata probing script entries [Confirmed]

`package.json` added two scripts: `metadata-path` and `metadata-probe`. Based on their names and commands, both point to `scripts/dev-metadata.ts` and are used to query or probe developer metadata paths.

This may not be visible to regular users, but it is a clear change for development, debugging, or internal diagnostics workflows.

Key evidence:

```diff
+ "metadata-path": "pnpm exec tsx ./scripts/dev-metadata.ts path",
+ "metadata-probe": "pnpm exec tsx ./scripts/dev-metadata.ts probe",
```

### 3. Playwright agent debugging script now runs with `tsx` preloaded [Confirmed]

The `playwright:agent:repl` script changed from running a Node.js script directly to running it with `node --import tsx`. The Playwright-related script still exists, but its launch method changed.

This typically affects the runtime path used when developers debug the Electron agent or CDP connection; the fact package cannot further prove any user-facing behavior change.

Key evidence:

```diff
- "playwright:agent:repl": "pnpm run rebuild:sqlite && node ./scripts/playwright-electron-agent-cdp.mjs",
+ "playwright:agent:repl": "pnpm run rebuild:sqlite && node --import tsx ./scripts/playwright-electron-agent-cdp.mjs",
```

### 4. Frontend module related to remote connection settings removed [Signal]

After removing Vite hash noise, the fact package shows that the actual removed frontend module is `remote-connections-settings`.

This points to a contraction, migration, or shutdown of UI or logic related to “remote connection settings” in this release. However, the artifact cannot reconstruct source-level intent, nor does it prove that the corresponding feature was removed for users.

Key evidence:

```text
Removed module: remote-connections-settings
```

### 5. Main program binary size dropped significantly, while the app layer package grew slightly [Signal]

`Contents/Resources/codex` decreased from 84.4 MB to 77.1 MB, a change of -7.37 MB; `Contents/Resources/app.asar` increased from 36.4 MB to 36.5 MB, a change of +0.12 MB.

This points to adjustments in the native main program or packaged contents, but size changes only indicate artifact-level changes and cannot reconstruct source-level intent.

## 📊 Raw data

### Bundle file-level statistics

| Item | Previous version | Latest version |
| --- | ---: | ---: |
| File count | 410 | 410 |
| Added files | 0 | 0 |
| Removed files | 0 | 0 |
| Content changes | 40 | 40 |

Note: app.asar internal file statistics are heavily affected by Vite hash renaming and are therefore not listed here as statistics for real functional changes.

### Key component size changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/codex` | 84.4 MB | 77.1 MB | -7.37 MB |
| `Contents/Resources/app.asar` | 36.4 MB | 36.5 MB | +0.12 MB |
| `Contents/CodeResources` | 0.0 MB | 0.0 MB | -0.00 MB |

### Added dependency list

| Scope | Number of added dependencies |
| --- | ---: |
| `cua_node` | 0 |

The fact package does not provide any other added dependency lists.

## 🔁 Reproducibility information

| Item | Previous version | Latest version |
| --- | --- | --- |
| Download artifact | `Codex-darwin-arm64-26.228.1430.zip` | `Codex-darwin-arm64-26.303.1606.zip` |
| SHA-256 | `52e3911c4738d48cd842eeab1b08de6210513e7e4a6823fdb6b860678499d69f` | `0bc586588888aa17b9169b37fe3de450e9a3e97ad56867b08dc1c303d3883880` |
| Size | Not provided in fact package | Not provided in fact package |
| EdDSA signature verification | Not provided in fact package | Not provided in fact package |
| codesign / spctl notarization result | `CSSMERR_TP_CERT_REVOKED` | `CSSMERR_TP_CERT_REVOKED` |

Download URLs:

```text
https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.228.1430.zip
https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.303.1606.zip
```

## ⚖️ Boundaries and disclaimer

This report is unrelated to OpenAI and is an unofficial third-party analysis.

This article does not redistribute any official binaries; it only publishes diff conclusions derived from official distribution artifacts.

All **[Signal]** entries represent only indirect artifact-level indications. They do not constitute source-level facts and are not equivalent to official feature documentation.

If anything is missing or misread, corrections based on the same fact package are welcome.
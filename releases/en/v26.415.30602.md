---
title: "Codex Desktop 26.415.30602"
version: "26.415.30602"
build: 1773
previous_version: "26.415.21839"
previous_build: 1763
platform: "macOS arm64"
released: "2026-04-17"
compared_from: "2026-04-17"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This release is primarily a minor build update: the version number and app.asar integrity hash changed, but there is no evidence of new files, dependencies, or clearly identifiable new capabilities."
official_release_notes: false
---

# Codex Desktop `26.415.30602` (build 1773)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

The changes from `26.415.21839` to `26.415.30602` are very restrained: confirmed changes are limited to the version number, build number, and application package content hash, with no new dependencies or new outer bundle files observed.  
English one-liner: a small build refresh with no extracted evidence of new user-facing capabilities.

## About this report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. This comparison is a historical backfill: both versions are already outside the official Sparkle delta-update window, so no delta package reconstruction was used. Instead, both official full packages were downloaded and compared directly using a structured bundle diff.

In terms of trustworthiness, both versions of `Codex.app` passed Apple notarization verification, with the source shown as `Notarized Developer ID` and the signing identity as `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`. This indicates that the artifacts used for comparison came from officially signed and notarized distribution packages.

This report uses two evidence levels:

- **[Confirmed]**: Direct human-readable evidence, such as `Info.plist` key values, dependency lists, signing results, and notarization results.
- **[Signal]**: Indirect evidence, such as frontend module names or binary size changes; this type of evidence only indicates “a possible direction” and cannot be reconstructed from the artifact into source-level intent.

Comparison scope: `26.415.21839` build `1763`, released on `2026-04-17`; to `26.415.30602` build `1773`, released on `2026-04-17`.

## ✨ Highlights

### 1. Version and build number updated [Confirmed]

The version number in `Info.plist` was updated from `26.415.21839` to `26.415.30602`, and the build number from `1763` to `1773`.

This means it is a newly packaged official release build, but the artifact does not contain configuration, dependency, or file-level evidence that can be directly mapped to new functionality.

Key evidence:

```diff
/CFBundleShortVersionString: '26.415.21839' → '26.415.30602'
/CFBundleVersion: '1763' → '1773'
```

### 2. The main application content changed, but only slightly [Confirmed]

The integrity hash of `Contents/Resources/app.asar` changed, indicating that the main application content is indeed different; however, the number of outer bundle files remained `498 → 498`, with `0` additions, `0` removals, and only `40` files changed in content.

This looks more like a small build refresh or internal content update. Since the artifact does not provide a readable feature diff, it cannot be interpreted as a specific feature addition.

Key evidence:

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
'21600e64c2c0b63f5f167117a0e65047c5cde8727b776d5a9671f63a742b0798'
→
'4060f6ecc7fa407ee3522af3024e504701401f6745cf5df1b523b88f0d644f97'
```

### 3. No new cua_node dependencies [Confirmed]

The artifact shows that the number of newly added `cua_node` dependencies is `0`.

This means the diff contains no evidence of capability expansion through newly added native dependencies, such as new document-processing, image-processing, or automation packages. This only means “no new dependencies were added to the dependency list”; it does not prove that there were no internal functional changes at all.

Key evidence:

```text
新增 0 个包
```

### 4. app.asar internal file additions and removals should mostly be treated as build noise [Signal]

The internal file count inside `app.asar` remained `1000 → 1000`, with `65` additions, `65` removals, and `5` modifications. The artifact also explicitly notes that many additions and removals under `webview/assets` are Vite build hash renames, which should be treated as noise.

This points to the frontend build artifacts being repackaged, but these hashed filenames cannot be reverse-mapped to source-level intent, nor should they be described as “features added or removed.”

## 📊 Raw data

### Bundle file-level statistics

| Metric | Value |
| --- | ---: |
| File count | `498 → 498` |
| Added files | `0` |
| Removed files | `0` |
| Content changes | `40` |

### Key component size changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | `111.5 MB` | `111.5 MB` | `+0.00 MB` |

### Newly added dependency list

| Scope | Number of newly added dependencies |
| --- | ---: |
| `cua_node` | `0` |

## 🔁 Reproducibility information

| Artifact | SHA-256 | Signature / notarization result |
| --- | --- | --- |
| Previous full package `Codex-darwin-arm64-26.415.21839.zip` | `504aad9b52d0144e2efa1cfdfb1017b479ffd2feb1f34c4c348d9e335e4a1e02` | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest full package `Codex-darwin-arm64-26.415.30602.zip` | `fc9a2b142c67b44edcaf54fad15cf66d1fbfa8baa30527b0e64862baf3747a64` | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Scope and disclaimer

This report is not affiliated with OpenAI, does not represent any official position, and is not official release notes.

This report does not redistribute any official binaries; it only publishes diff conclusions derived from officially signed distribution artifacts.

All [Signal] entries represent only indirect artifact-level indications. They are not source-level facts and should not be interpreted as official feature intent.

If there are extraction errors or improper interpretations in the artifact analysis, corrections based on reproducible evidence are welcome.
---
title: "Codex Desktop 26.422.71525"
version: "26.422.71525"
build: 2210
previous_version: "26.422.62136"
previous_build: 2180
platform: "macOS arm64"
released: "2026-04-29"
compared_from: "2026-04-28"
method: "Official full-package comparison + structured bundle diff"
summary: "A small update: version and asar integrity metadata changed, while frontend signals point to minor onboarding intent and permissions-mode presentation adjustments."
official_release_notes: false
---

# Codex Desktop `26.422.71525` build 2210

> Third-party changelog · Unofficial · Based on reverse-diffing official full packages

This is a small macOS arm64 update with no top-level bundle file count change; the most visible signals are in onboarding and permissions-mode related frontend modules.  
A small desktop update with limited bundle-level movement and a few UI-facing frontend signals.

## About This Report

This is not an official changelog. It is a **reverse-diff report based on officially signed distribution artifacts**. This entry is a historical backfill: the official appcast did not provide a delta package for `26.422.62136` → `26.422.71525`, so the comparison was made directly between the two official full packages.

Evidence is split into two levels:

- **[Confirmed]**: Direct human-readable evidence from Info.plist, signing/notarization checks, bundle file trees, asar integrity hashes, and reproducible distribution artifacts.
- **[Signal]**: Indirect evidence from same-name frontend module diffs, readable strings, or style/class-name changes. These can only point toward UI, state, or interaction changes and cannot recover source-level intent from the artifact.

The comparison covers macOS arm64 `26.422.62136` build `2180`, released on `2026-04-28`, to `26.422.71525` build `2210`, released on `2026-04-29`.

## ✨ Highlights

### 1. Version and build updated to `26.422.71525` / `2210` [Confirmed]

Application metadata changed from `26.422.62136` build `2180` to `26.422.71525` build `2210`.

This confirms a real macOS arm64 desktop update released on 2026-04-29, rather than a filename-only artifact change.

Key evidence:

```diff
CFBundleShortVersionString: 26.422.62136 → 26.422.71525
CFBundleVersion: 2180 → 2210
```

### 2. Onboarding now contains a “Build software” intent signal [Signal]

The `index` frontend module added readable strings including `Build software`, `Welcome v2 intent option for building software`, and `electron.onboarding.welcomeV2.intent.buildSoftware`.

This points toward a “build software” intent option in the welcome/onboarding flow, but the evidence comes from a same-name frontend module diff and cannot establish source-level intent.

Key evidence:

```text
webview/assets/index-BSTEB47c.js → webview/assets/index-D-3V455n.js
Added string: Build software
Added string: Welcome v2 intent option for building software
Added string: electron.onboarding.welcomeV2.intent.buildSoftware
```

### 3. Permissions-mode UI/state signal adds `workspace-write` [Signal]

The `use-permissions-mode` frontend module added the style/state class name `workspace-write`.

This points toward workspace-write related naming in permissions-mode presentation or state, but the evidence is only a built frontend artifact diff and does not prove a source-level permissions model or runtime behavior change.

Key evidence:

```text
webview/assets/use-permissions-mode-BKwvpT92.js → webview/assets/use-permissions-mode-BuZhkYOo.js
Added style/state class: workspace-write
```

### 4. Settings-related frontend modules changed slightly without clear user semantics [Signal]

Both `use-model-settings` and `font-settings` changed. `font-settings` grew by `2888 bytes`, added `http://www.w3.org/2000/svg`, and removed `data-utility-button`.

These signals point to small settings-related UI or rendering-structure changes, but the readable evidence is not enough to confirm a specific feature change.

Key evidence:

```text
use-model-settings: 2.7 MB → 2.7 MB (-2357 bytes)
font-settings: 0.2 MB → 0.2 MB (+2888 bytes)
```

### 5. Bundle structure is nearly unchanged [Confirmed]

The top-level bundle file count stayed at `507 → 507`, with `0` added files, `0` removed files, and `46` content-modified files. The only listed key component size change is `Contents/Resources/app.asar`, from `119.9 MB` to `119.8 MB`, a `-0.04 MB` change.

This indicates the release was not a large component add/remove update. Internal app.asar added/removed file counts are dominated by Vite hash renaming and should not be interpreted as real feature additions or removals.

Key evidence:

```text
Bundle files: 507 → 507
Added 0 / Removed 0 / Content-modified 46
Contents/Resources/app.asar: 119.9 MB → 119.8 MB (-0.04 MB)
```

## 📊 Raw Data

### Bundle File-Level Stats

| Metric | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 507 | 507 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Content-modified files | 46 | 46 | 46 |

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 119.9 MB | 119.8 MB | -0.04 MB |

### Added Dependencies

| Area | Added dependency count | Details |
| --- | ---: | --- |
| `cua_node` | 0 | None |

## 🔁 Reproducibility

| Item | Previous `26.422.62136` | Latest `26.422.71525` |
| --- | --- | --- |
| Artifact | `Codex-darwin-arm64-26.422.62136.zip` | `Codex-darwin-arm64-26.422.71525.zip` |
| SHA-256 | `a549f217c5d84ac7581c7f762b97ca4e0976797d56b206c4e4ee9d1c33faa635` | `ae2197d28506f6a60df45e71831ae0161fffb1a6bf83a3045673401f86ced2d5` |
| EdDSA verification | Not provided in fact pack | Not provided in fact pack |
| codesign/spctl notarization | `accepted`, `source=Notarized Developer ID`, `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` | `accepted`, `source=Notarized Developer ID`, `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Artifact size | Not provided in fact pack | Not provided in fact pack |

## ⚖️ Boundaries and Disclaimer

This document is not affiliated with OpenAI and does not represent official release notes.

It does not redistribute any official binaries; it only publishes conclusions derived from diffs of officially signed distribution artifacts.

All [Signal] items only indicate what the artifact diff points toward. They are not source-level facts and do not imply the behavior is visible to all users.

Corrections based on the same artifacts and hashes are welcome.

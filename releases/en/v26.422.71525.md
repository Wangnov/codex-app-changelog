---
title: "Codex Desktop 26.422.71525"
version: "26.422.71525"
build: 2210
previous_version: "26.422.62136"
previous_build: 2180
platform: "macOS arm64"
released: "2026-04-29"
compared_from: "2026-04-28"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "This is a very small desktop update: the version and build number advanced, the bundle file count stayed unchanged, and the main visible changes are concentrated in updated app.asar content."
official_release_notes: false
---

# Codex Desktop `26.422.71525` (build 2210)

> Third-party changelog · Unofficial · Based on reverse-engineered differences in official update packages

This release moves from `26.422.62136` to `26.422.71525`. Judging from the artifacts, it is a minor update: the outer bundle file count did not change, while the core application package `app.asar` was updated with almost no change in size.  
One-liner: a small notarized desktop update with app-layer content changes and no new top-level bundle files.

## About This Report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. This comparison is a historical backfill: both versions are already outside the official Sparkle incremental update window, so the incremental package reconstruction flow was not used. Instead, both official full packages were downloaded and compared directly through a structured bundle diff.

In terms of confidence, both versions of `Codex.app` passed Apple notarization validation. Their source is shown as `Notarized Developer ID`, and the signing origin is `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`. This indicates that the compared artifacts are official distribution builds notarized by Apple and signed with OpenAI’s Developer ID.

Evidence is divided into two levels:

- **[Confirmed]**: Direct human-readable evidence, such as `Info.plist` key values, file tree statistics, and signing and notarization validation results.
- **[Signal]**: Indirect evidence, such as frontend module names or binary size changes. It can point in a direction, but cannot reconstruct source-level intent from the artifact.

Comparison scope: from `26.422.62136` build `2180`, released on `2026-04-28`, to `26.422.71525` build `2210`, released on `2026-04-29`.

## ✨ Key Changes

### 1. Version and build number advanced to `26.422.71525` / build `2210` [Confirmed]

This is a formal version advance: `CFBundleShortVersionString` changed from `26.422.62136` to `26.422.71525`, and `CFBundleVersion` changed from `2180` to `2210`.

This means macOS, the updater, and external diagnostic tools will identify it as a new build rather than a repackaging of the same version.

Key evidence:

```diff
CFBundleShortVersionString: 26.422.62136 → 26.422.71525
CFBundleVersion: 2180 → 2210
```

### 2. Outer application bundle structure remained basically stable, with no top-level files added or removed [Confirmed]

The outer bundle file count stayed at `507 → 507`, with `0` added, `0` removed, and `46` content changes.

This indicates that the update did not introduce new top-level components, plugins, or standalone resource packages. The changes mainly occurred inside existing files, with very limited impact on the installation directory structure and outer packaging shape.

Key evidence:

```text
File count: 507 → 507
Added 0 / Removed 0 / Content changed 46
```

### 3. Core application package `app.asar` was updated, but its size stayed nearly unchanged [Confirmed]

`Contents/Resources/app.asar` changed from `119.9 MB` to `119.8 MB`, a decrease of `0.04 MB`. At the same time, the `ElectronAsarIntegrity` hash recorded in `Info.plist` changed.

This means the Electron application-layer content was indeed updated, but judging by size, this was not a large-scale capability expansion. Note that many additions and removals inside `app.asar` come from Vite hashed file renames under `webview/assets`, so they cannot be used on their own to determine whether specific features were added or removed.

Key evidence:

```diff
Contents/Resources/app.asar: 119.9 MB → 119.8 MB (-0.04 MB)
ElectronAsarIntegrity hash: 4b1503...c6bb → fe9c0e...062f
```

### 4. No new `cua_node` dependencies observed [Confirmed]

The number of newly added packages in the `cua_node` dependency changes is `0`.

This means that, at least at the dependency-list level, this version did not add new `cua_node` package-level capabilities. It only shows that the dependency list did not gain new entries; it does not prove that related functionality had no internal adjustments.

Key evidence:

```text
cua_node added 0 packages
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Value |
| --- | ---: |
| File count | `507 → 507` |
| Added files | `0` |
| Removed files | `0` |
| Content changes | `46` |

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | `119.9 MB` | `119.8 MB` | `-0.04 MB` |

### Newly Added Dependency List

| Scope | New dependency count |
| --- | ---: |
| `cua_node` | `0` |

## 🔁 Reproducibility Information

| Artifact | SHA-256 | Verification result |
| --- | --- | --- |
| `Codex-darwin-arm64-26.422.62136.zip` | `a549f217c5d84ac7581c7f762b97ca4e0976797d56b206c4e4ee9d1c33faa635` | `Codex.app: accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| `Codex-darwin-arm64-26.422.71525.zip` | `ae2197d28506f6a60df45e71831ae0161fffb1a6bf83a3045673401f86ced2d5` | `Codex.app: accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

| Item | Conclusion |
| --- | --- |
| Distribution package source | Official full package |
| EdDSA signature verification | Not provided in this fact package; this is a historical backfill, using Apple notarization and Developer ID signing as the trust root |
| codesign / spctl | Both versions were `accepted` |
| Notarization source | `Notarized Developer ID` |

## ⚖️ Boundaries and Disclaimer

This report is not affiliated with OpenAI, does not represent an official position, and is not official release notes.

This article does not redistribute any official binary files. It only publishes diff conclusions derived from official distribution artifacts.

All [Signal] entries indicate only indirect artifact-level signals and do not constitute source-level facts. This fact package does not provide real newly added frontend modules or newly added dependencies suitable for inferring new feature directions, so this article does not describe hashed file changes under `webview/assets` or `.vite/build` as feature changes.

If anything is missing or any factual interpretation is inaccurate, corrections are welcome based on the same set of official artifacts and reproducible verification results.
---
title: "Codex Desktop 26.422.21459"
version: "26.422.21459"
build: 2049
previous_version: "26.422.20832"
previous_build: 2025
platform: "macOS arm64"
released: "2026-04-24"
compared_from: "2026-04-23"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This is a very small maintenance update: the version and build numbers advance, the bundle contents show only minor changes, and no new dependencies or top-level files were observed."
official_release_notes: false
---

# Codex Desktop `26.422.21459` (build 2049)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

This release moves from `26.422.20832` to `26.422.21459`, and overall looks more like a low-risk maintenance release than a capability-expansion release.  
English one-liner: a small maintenance update with no visible new dependency or bundle-file surface.

## About this report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. This is a historical backfill: both versions are already outside the official Sparkle delta window, so the factual package uses a direct comparison of the two official full packages rather than a reconstructed delta package.

Confidence comes from Apple notarization and OpenAI Developer ID code-signing verification: `Codex.app` from both the previous and latest versions passed `spctl` notarization checks, with both sources shown as `Notarized Developer ID`, and both signing identities shown as `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

This report uses two evidence levels:

- **[Confirmed]**: Direct, human-readable evidence, such as `Info.plist` key values, file statistics, signing verification results, and dependency lists.
- **[Signal]**: Indirect evidence, such as frontend module names or binary size changes; these entries only indicate “pointing in a certain direction,” but cannot reconstruct source-level intent from the artifact.

Comparison scope: `26.422.20832` (build 2025, 2026-04-23) → `26.422.21459` (build 2049, 2026-04-24).

## ✨ Highlights

### 1. Version and build numbers advanced [Confirmed]

The latest version updates `CFBundleShortVersionString` from `26.422.20832` to `26.422.21459`, and `CFBundleVersion` from `2025` to `2049`.

This means macOS can clearly identify it as a new build, but the factual package does not provide any official release notes.

Key evidence:

```diff
/CFBundleShortVersionString: 26.422.20832 → 26.422.21459
/CFBundleVersion: 2025 → 2049
```

### 2. No top-level files were added or removed in the bundle file tree [Confirmed]

The application bundle file count remains `507 → 507`, with `0` added, `0` removed, and `46` files changed.

This indicates that the update did not introduce a new top-level component or plugin form; from the installation package structure visible to users and developers, the scope of change is small.

### 3. No new `cua_node` dependencies [Confirmed]

The factual package shows `0` newly added `cua_node` dependency packages.

This means this version did not expand this layer of capability through newly added dependency packages; if there are behavioral changes, they are not reflected in the added dependency list.

### 4. `app.asar` has slight content changes, but its size is almost unchanged [Confirmed]

`Contents/Resources/app.asar` went from `119.9 MB` to `119.9 MB`, a change of `+0.01 MB`; the internal file count remains `1031 → 1031`, but there are internal diffs of `73` added, `73` removed, and `6` changed files.

The factual package specifically notes that filename additions and removals with hashes under `webview/assets` and `.vite/build` are Vite build artifact renaming noise, and cannot be used to infer added or removed features. Therefore, what can be confirmed here is that “the packaged content changed slightly,” but no specific new feature can be inferred.

## 📊 Raw data

### Bundle file-level statistics

| Metric | Value |
| --- | ---: |
| File count | `507 → 507` |
| Added files | `0` |
| Removed files | `0` |
| Content changes | `46` |

### Key component size changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | `119.9 MB` | `119.9 MB` | `+0.01 MB` |
| `Contents/Resources/codex_chronicle` | `4.4 MB` | `4.4 MB` | `+0.00 MB` |

### Added dependency list

| Scope | Number of added dependencies |
| --- | ---: |
| `cua_node` | `0` |

## 🔁 Reproducibility information

| Artifact | URL | SHA-256 | Size | EdDSA verification result | codesign / spctl result |
| --- | --- | --- | --- | --- | --- |
| Previous full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.422.20832.zip` | `41b7c2c85ed099d62dabe5273cc4a3fe3d6735a48370b41b9bcf7aa84d4b6d18` | Not provided by factual package | Not provided by factual package; this comparison uses full packages directly | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.422.21459.zip` | `5ce4b5b8427b9e0d66d2c3b286fec7b54b25cd7b0e3f29d90f46ca529d9902f7` | Not provided by factual package | Not provided by factual package; this comparison uses full packages directly | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Boundaries and disclaimer

This report is not affiliated with OpenAI, does not represent OpenAI’s official position, and is not an official release note.

This report does not redistribute any official binaries; it only publishes conclusions organized from the diff factual package.

Content marked with [Signal] does not constitute source-level fact; this version’s factual package does not contain any [Signal] entries that can be written up as standalone functional changes.

Reviews and corrections based on the same artifacts and the same verification information are welcome.
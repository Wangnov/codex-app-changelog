---
title: "Codex Desktop 26.324.21641"
version: "26.324.21641"
build: 1228
previous_version: "26.324.21329"
previous_build: 1214
platform: "macOS arm64"
released: "2026-03-26"
compared_from: "2026-03-25"
method: "Direct official full-package comparison + structured bundle diff"
summary: "This build mainly changes the macOS Bluetooth permission declaration and advances the version and app.asar integrity hash, with no bundle file count change or new cua_node dependencies."
official_release_notes: false
---

# Codex Desktop `26.324.21641` build 1228

> Third-party changelog · Unofficial · Based on reverse diffing official update artifacts

From the readable artifacts, this is a very small macOS maintenance update: the clearest change is a Bluetooth permission declaration moving from Always to Peripheral.  
English one-liner: a small macOS maintenance build with a confirmed Bluetooth permission declaration change and no new `cua_node` packages.

## About This Report

This is not an official changelog. It is a **reverse-engineered diff report based on official distribution artifacts**. This entry is a historical backfill: the facts package says the official appcast did not provide a delta package for `26.324.21329` → `26.324.21641`, so the comparison was made directly between two official full packages.

The comparison covers macOS arm64 `26.324.21329` build `1214` and `26.324.21641` build `1228`. Evidence is grouped into two levels:

- **[Confirmed]**: Direct human-readable evidence such as Info.plist values, dependency lists, signing or verification results, and reproducible download metadata.
- **[Signal]**: Indirect evidence from same-name frontend module changes or size deltas. These can point in a direction, but cannot recover source-level intent from the artifact.

Hashed file changes under `webview/assets` and `.vite/build` are treated as build-output rename noise, not as evidence of added or removed features.

## ✨ Highlights

### 1. macOS Bluetooth Permission Declaration Changed from Always to Peripheral [Confirmed]

The new `Info.plist` adds `NSBluetoothPeripheralUsageDescription` and removes the previous `NSBluetoothAlwaysUsageDescription`. Both use the same text: `This app needs access to Bluetooth`.

This means the app’s macOS Bluetooth permission declaration became more specific at the bundle metadata level. The user-visible effect may appear in system permission descriptions or permission request paths, but the facts package does not include runtime behavior, so no specific interaction change is inferred.

Key evidence:

```diff
+ /NSBluetoothPeripheralUsageDescription = "This app needs access to Bluetooth"
- /NSBluetoothAlwaysUsageDescription = "This app needs access to Bluetooth"
```

### 2. Version, Build, and app.asar Integrity Hash Updated [Confirmed]

`CFBundleShortVersionString` changed from `26.324.21329` to `26.324.21641`, and `CFBundleVersion` changed from `1214` to `1228`. The `Resources/app.asar/hash` value under `ElectronAsarIntegrity` also changed.

This confirms that the application-layer payload differs, but the hash itself only proves content changed. It does not explain feature intent.

Key evidence:

```diff
/CFBundleShortVersionString: "26.324.21329" → "26.324.21641"
/CFBundleVersion: "1214" → "1228"
/ElectronAsarIntegrity/Resources/app.asar/hash:
  1f5295b6f6f4570f8bad796cb98e29648c731d08a036ac73dec1f1a1b5c28b75
  → efec82927b30237ee372d382d8b7cec7b3249411f473ae1c6952a630cf0c7218
```

### 3. Frontend Entry Shows Only a Tiny String-Level Change [Signal]

The same-name `index` frontend module changed, with size moving from `4.1 MB` to `4.1 MB`, a decrease of `149 bytes`. The readable string diff only shows internal symbol-like changes from `bd` to `xd`.

This points to a small change in the frontend build output or minified symbols, but it cannot be mapped back to source-level intent and should not be interpreted as evidence of a user-visible feature addition.

Key evidence:

```text
webview/assets/index-CT1EpLpN.js → webview/assets/index-CsW2954H.js
4.1 MB → 4.1 MB (-149 bytes)
Added strings: :for(i in xd(, :for(s in xd(
Removed strings: :for(i in bd(, :for(s in bd(
```

### 4. No New cua_node Dependencies or Bundle File Count Changes [Confirmed]

The facts package reports `0` new `cua_node` dependencies. The bundle file count stayed at `415 → 415`, with `0` added files, `0` removed files, and `40` files changed.

That means this build does not show direct evidence of newly added native capability packages or a broad bundle structure change. The application layer does contain hashed filename churn, but the facts package explicitly marks this as mainly driven by Vite build hash renaming and not as a reliable signal of real feature additions or removals.

## 📊 Raw Data

### Bundle File-Level Stats

| Metric | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 415 | 415 | 0 |
| Added files | 0 | 0 | 0 |
| Removed files | 0 | 0 | 0 |
| Files with content changes | - | 40 | 40 |

### Key Component Size Changes

| Component Path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 42.2 MB | 42.2 MB | -0.00 MB |
| `Contents/Info.plist` | 0.0 MB | 0.0 MB | +0.00 MB |

### Added Dependencies

| Dependency Scope | Added Count | Details |
| --- | ---: | --- |
| `cua_node` | 0 | No new packages |

## 🔁 Reproducibility

| Item | Previous | Latest |
| --- | --- | --- |
| Version | `26.324.21329` build `1214` | `26.324.21641` build `1228` |
| Release date | `2026-03-25` | `2026-03-26` |
| Artifact URL | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.324.21329.zip` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.324.21641.zip` |
| SHA-256 | `54666ac09f5c53d24b7b96508b1acfdc5a0f2b551b5f419a6a231a36d853f16c` | `9570506c7d54cabf15cddf552a767e649d6f92f01b151ffce93ce867a4ff0cb9` |
| Size | Not provided in facts package | Not provided in facts package |
| EdDSA verification | Not provided in facts package | Not provided in facts package |
| codesign/spctl notarization result | `CSSMERR_TP_CERT_REVOKED` | `CSSMERR_TP_CERT_REVOKED` |

Some early-version signing certificates were later revoked. The notarization result in the facts package shows `CSSMERR_TP_CERT_REVOKED`. This affects whether the version can be safely installed by the system, but it does not affect bundle content diff analysis.

## ⚖️ Boundaries and Disclaimer

This report is not affiliated with OpenAI and is not an official release note.

It does not redistribute any official binaries. It only publishes conclusions derived from diffs of official distribution artifacts.

All [Signal] items are directional artifact-level observations, not source-level facts or statements of official intent. Corrections based on the same facts package or stronger evidence are welcome.

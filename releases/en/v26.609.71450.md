
---
title: "Codex Desktop 26.609.71450"
version: "26.609.71450"
build: 3965
mac_version: "26.609.71450"
mac_build: 3965
win_version: "26.609.9530.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-15"
method: "official package reverse diff(macOS Sparkle / Windows MSIX)"
summary: "In this batch, the macOS package is unchanged, while the Windows package moves from 26.609.4994.0 to 26.609.9530.0, adding resources/codex and resources/rg and updating several native components."
official_release_notes: false
---

# Codex Desktop 26.609.71450(macOS / Windows comparison)

> Third-party cross-platform changelog · Unofficial

The clearest platform difference in this batch is that the macOS comparison is fully unchanged, while the Windows package contains real file-level updates.  
One-liner: macOS is byte-level unchanged in this comparison, while Windows adds bundled resources and updates native components.

## About This Report

This report is based on reverse diffs of two official signed distribution artifacts: Sparkle full-package comparison for macOS, and MSIX package comparison for Windows. It only uses file tree data, manifests, size data, signature validation, and app.asar statistics present in the fact packs, with no inference about source intent.

Evidence levels are split into two categories: [Confirmed] means direct evidence from readable manifests, file trees, dependency lists, app.asar statistics, or signature validation; [Signal] means only module names or size changes suggest a direction and cannot establish source-level facts. The main changes in this batch are all [Confirmed].

Platform tags mean: [macOS+Windows] appears on both platforms; [macOS only] appears only in the macOS diff; [Windows only] appears only in the Windows diff.

Comparison scope: macOS arm64 compares 26.609.71450 build 3965 against 26.609.71450 build 3965; Windows x64 compares 26.609.4994.0 against 26.609.9530.0. The release date is the one provided by the fact pack: 2026-06-15.

## ✨ Highlights

### [Confirmed] [Windows only] Windows Adds Two Top-Level Resource Files

The Windows file tree adds 2 files. The largest newly added files are:

- `resources/codex`: 235.0 MB
- `resources/rg`: 5.4 MB

The corresponding macOS diff has 0 added files, so this is not a shared cross-platform change in this batch. It is a Windows-side change that is not present in the macOS comparison.

### [Confirmed] [Windows only] Several Windows Native Components Changed Size

Several native binaries in the Windows package changed content and increased in size, concentrated around `node-pty`, `windows-updater.node`, and `better-sqlite3.node`:

- `conpty.node`: 1.5 MB → 1.8 MB
- `pty.node`: 1.5 MB → 1.8 MB
- `winpty-agent.exe`: 1.2 MB → 1.4 MB
- `winpty.dll`: 1.1 MB → 1.3 MB
- `resources/native/windows-updater.node`: 1.1 MB → 1.2 MB
- `better_sqlite3.node`: 3.0 MB → 3.1 MB

The macOS file tree reports 0 content changes, so these native component changes can only be confirmed for the Windows package.

### [Confirmed] [Windows only] Windows app.asar Has Minor File-Level Changes

The Windows `app.asar` internal file count goes from 2815 to 2818, with 14 added, 11 removed, and 11 changed files. Here, `app.asar` refers to the Electron application archive.

The fact pack also notes that many changes under `webview/assets` are Vite build hash rename noise and should not be used to infer new features. This report therefore only confirms that the Windows application archive changed at the file level, without inferring functionality.

The macOS `app.asar` internal file count is 2870 → 2870, with 0 added, 0 removed, and 0 changed files.

### [Confirmed] [macOS only] The macOS Comparison Has No Content Changes

Both macOS sides are 26.609.71450 build 3965, and the full-package URL and SHA-256 are identical:

`e773c824d5ad0d8567bbabb1d39f32ce730dd4699e4a9de5b838ea6955e268b7`

The macOS bundle file count is 8298 → 8298, with 0 added, 0 removed, and 0 content changes. `app.asar` internal files are 2870 → 2870. The Computer Use runtime `cua_node` total is 365.8 MB → 365.8 MB. Newly added dependencies: 0. No Info.plist changes are listed.

### [Confirmed] [macOS+Windows] Computer Use Runtime Size Is Unchanged

The `cua_node` total is unchanged in each platform’s own comparison:

- macOS: 365.8 MB → 365.8 MB, +0 MB, +0%
- Windows: 285.5 MB → 285.5 MB, +0 MB, +0%

Both platforms also report 0 newly added `cua_node` dependencies. Computer Use runtime here refers to the `cua_node` runtime file set named in the fact pack.

### [Confirmed] [macOS+Windows] Distribution Verification Is Reproducible, With Different Evidence Forms

Both macOS full packages pass notarization validation with `accepted`, source `Notarized Developer ID`, and origin `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

The Windows fact pack provides full-package URLs and SHA-256 hashes for the previous and latest versions:

- 26.609.4994.0: `547618a744149221078a27febdfff65c924b46ff85ab2fe1595180e128be8d85`
- 26.609.9530.0: `2601c0a15797b9dba27bd0b1fe466c1829f91b3d8e3ec71854610b6ef732f7d9`

## 📊 Raw Data

### macOS arm64

- Version: 26.609.71450 build 3965 → 26.609.71450 build 3965
- Bundle file count: 8298 → 8298
- Added / removed / content changed: 0 / 0 / 0
- `app.asar` internal files: 2870 → 2870
- `app.asar` added / removed / changed: 0 / 0 / 0
- `cua_node` total: 365.8 MB → 365.8 MB, +0 MB, +0%
- Newly added `cua_node` dependencies: 0
- Previous and latest SHA-256: `e773c824d5ad0d8567bbabb1d39f32ce730dd4699e4a9de5b838ea6955e268b7`
- Notarization validation: `accepted` on both sides

### Windows x64

- Version: 26.609.4994.0 → 26.609.9530.0
- Bundle file count: 11972 → 11974
- Added / removed / content changed: 2 / 0 / 16
- `app.asar` internal files: 2815 → 2818
- `app.asar` added / removed / changed: 14 / 11 / 11
- `cua_node` total: 285.5 MB → 285.5 MB, +0 MB, +0%
- Newly added `cua_node` dependencies: 0
- Large added files: `resources/codex` 235.0 MB, `resources/rg` 5.4 MB
- Key size changes: `node-pty` binaries, `windows-updater.node`, and `better_sqlite3.node` all increased
- Previous SHA-256: `547618a744149221078a27febdfff65c924b46ff85ab2fe1595180e128be8d85`
- Latest SHA-256: `2601c0a15797b9dba27bd0b1fe466c1829f91b3d8e3ec71854610b6ef732f7d9`

## ⚖️ Boundaries and Disclaimer

This is a third-party reverse-diff changelog, not an official OpenAI release note.

It does not redistribute binaries. It only summarizes machine-extracted facts from official signed distribution packages.

[Signal] items do not establish source-level facts. The main conclusions in this batch come from [Confirmed] file trees, sizes, signature validation, and archive statistics. Corrections are welcome if the fact pack or the summary is inaccurate.

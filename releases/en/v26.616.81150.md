
---
title: "Codex Desktop 26.616.81150"
version: "26.616.81150"
build: 4306
mac_version: "26.616.81150"
mac_build: 4306
win_version: "26.616.10790.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-23"
method: "official package reverse diff: macOS Sparkle, Windows MSIX"
summary: "This batch is mainly about cross-platform version and bundled-plugin version alignment; macOS confirms browser/chrome plugin updates, while Windows additionally confirms the computer-use plugin update to the same bundled-plugin version."
official_release_notes: false
---

# Codex Desktop 26.616.81150 macOS / Windows Comparison

> Third-party cross-platform changelog · Unofficial

This batch does not show new runtime dependencies, CSP changes, resource-tree expansion, or confirmed frontend feature additions; the strongest evidence is package metadata and bundled-plugin manifest version alignment.

The strongest confirmed change in this batch is cross-platform package and bundled-plugin version alignment, with Windows additionally showing the computer-use plugin manifest update.

## About This Report

This report is based on reverse-reconstructed diffs of official signed distribution packages. The macOS comparison is `26.616.71553 build 4265` → `26.616.81150 build 4306`; the Windows comparison is `26.616.9593.0` → `26.616.10790.0`.

Evidence is split into two levels. `[Confirmed]` comes from plist, manifest, dependency lists, plugin lists, service entries, skill docs, readable config, or non-frontend facts in the package. `[Signal]` comes from frontend module names, compressed bundle direction, style class names, or size-change clues. This batch does not provide “same-name frontend module change clues” or “new readable strings” sections, so no frontend UI or copy changes are stated as confirmed facts.

Platform ownership is marked as `[macOS+Windows]`, `[macOS only]`, or `[Windows only]`. The two platforms come from different packages of the same Codex codebase, but version numbers and file statistics are cited separately from each platform’s facts.

## ✨ Highlights

### [Confirmed][macOS+Windows] Application Version Metadata Moved to the New Batch

On macOS, `Info.plist` shows `CFBundleShortVersionString` changing from `26.616.71553` to `26.616.81150`, `CFBundleVersion` changing from `4265` to `4306`, and `ElectronAsarIntegrity/Resources/app.asar/hash` changing with the package.

The Windows package comparison is `26.616.9593.0` → `26.616.10790.0`. The fact package does not include a concrete AppxManifest diff, so Windows can only be described at the package-version comparison level here.

### [Confirmed][macOS+Windows] Bundled browser Plugin Version Synchronized to 26.616.81150

On both platforms, the `browser` plugin manifest shows its version changing from `26.616.71553` to `26.616.81150`. The plugin description still covers the Codex in-app browser for opening, inspecting, navigating, testing, clicking, typing, or screenshotting local targets including `localhost`, `127.0.0.1`, `::1`, `file://` URLs, and the current in-app browser tab.

This is a bundled-plugin manifest level confirmed change. The fact package only shows the version field changing; it does not show new service entries, skill docs, or default prompts.

### [Confirmed][macOS+Windows] Bundled chrome Plugin Version Synchronized to 26.616.81150

On both platforms, the `chrome` plugin manifest shows its version changing from `26.616.71553` to `26.616.81150`. Its description continues to cover automation that depends on the user’s existing Chrome state, including tabs, logged-in sessions, cookies, extensions, and Codex Chrome Extension setup.

This is also a plugin manifest level confirmed change. The fact package does not show new chrome plugin capability surface, only package version synchronization.

### [Confirmed][Windows only] Bundled computer-use Plugin Version Synchronized to 26.616.81150

On Windows, the `computer-use` plugin manifest shows its version changing from `26.616.71553` to `26.616.81150`. Its description is controlling Windows desktop apps from Codex through Computer Use.

The macOS fact package does not include a corresponding `computer-use` plugin manifest diff, so the same manifest change cannot be claimed for macOS in this input sample.

### [Confirmed][macOS+Windows] Computer Use Runtime Size and cua_node Dependencies Did Not Change

The macOS Computer Use runtime total remains `377.4 MB → 377.4 MB`, with `+0 MB, +0%`. The Windows total remains `298.9 MB → 298.9 MB`, also `+0 MB, +0%`. Both platforms list `0` new `cua_node` dependency packages.

This means the fact package does not show Computer Use runtime expansion or dependency additions. Even though Windows has a `computer-use` plugin manifest version update, runtime size and dependency lists did not grow with it.

### [Confirmed][macOS+Windows] Bundle File Trees Stayed Stable, with Changes Concentrated in a Small Number of Modified Files

The macOS bundle file count remains `8827 → 8827`, with `0` added, `0` removed, and `68` content changes. The Windows file count remains `9697 → 9697`, with `0` added, `0` removed, and `15` content changes.

The largest `app.asar` component shows `+0.00 MB` on both platforms: `Contents/Resources/app.asar` is `155.8 MB → 155.8 MB` on macOS, and `resources/app.asar` is `159.6 MB → 159.6 MB` on Windows. This supports the view that there was no large resource or runtime reshuffle, but it does not mean there were no code changes.

### [Signal][macOS+Windows] app.asar Has Build-Artifact Additions and Removals, but the Fact Package Warns Against Treating Hash Noise as Functionality

Inside `app.asar`, macOS stays at `3115 → 3115` files, with `10` added, `10` removed, and `10` modified. Windows stays at `3078 → 3078` files, with `10` added, `10` removed, and `11` modified.

The fact package explicitly states that many additions and removals under `webview/assets` are Vite build hash renames and should be treated as noise. Therefore these changes are only build-artifact signals, not evidence of added frontend features.

## 🔎 Low-Level Signal Index

### Frontend Modules and UI Clues

This batch does not provide detailed “real frontend code module additions/removals” or “same-name frontend module change clues.” Therefore there is no frontend evidence that can be expanded into UI, state, layout, or interaction changes.

### Readable Strings, Commands, and Settings

This batch does not provide a “new readable strings in main process and frontend bundle” section. Apart from plugin manifest descriptions shown in the diff, there are no new copy strings, error paths, commands, settings, or status strings to group.

### CSP, Permissions, and Platform Manifests

macOS only shows `Info.plist` version fields and the `app.asar` integrity hash changing. The Windows fact package does not provide an AppxManifest change section. Neither platform provides a content security policy change section, so CSP or permission policy changes cannot be claimed.

### New Components and Resources

Both platform bundle trees show `0` added and `0` removed files. The additions and removals inside `app.asar` are bounded by the fact package’s Vite hash-noise warning. No new component, service entry, skill doc, default prompt, or largest-new-file addition is confirmed.

## 📊 Raw Data

macOS arm64:

| Item | Value |
| --- | --- |
| Previous version | `26.616.71553 build 4265` |
| Latest version | `26.616.81150 build 4306` |
| Bundle file count | `8827 → 8827` |
| Bundle added / removed / modified | `0 / 0 / 68` |
| app.asar | `155.8 MB → 155.8 MB (+0.00 MB)` |
| app.asar internal files | `3115 → 3115` |
| app.asar internal added / removed / modified | `10 / 10 / 10` |
| Computer Use runtime | `377.4 MB → 377.4 MB (+0 MB, +0%)` |
| New cua_node dependencies | `0` |
| Confirmed changed files | `browser` plugin manifest, `chrome` plugin manifest, `Info.plist` |

Windows x64:

| Item | Value |
| --- | --- |
| Previous version | `26.616.9593.0` |
| Latest version | `26.616.10790.0` |
| Bundle file count | `9697 → 9697` |
| Bundle added / removed / modified | `0 / 0 / 15` |
| app.asar | `159.6 MB → 159.6 MB (+0.00 MB)` |
| app.asar internal files | `3078 → 3078` |
| app.asar internal added / removed / modified | `10 / 10 / 11` |
| Computer Use runtime | `298.9 MB → 298.9 MB (+0 MB, +0%)` |
| New cua_node dependencies | `0` |
| Confirmed changed files | `computer-use` plugin manifest, `browser` plugin manifest, `chrome` plugin manifest |

Verification and source summary:

| Platform | Previous SHA-256 | Latest SHA-256 |
| --- | --- | --- |
| macOS | `a2b2eee5d8b18f3da9cd8c4065dc9000ec938e055d52f7b2e9dc5be5e80f6570` | `7325382f657b6f8221f5bdd5421e7f898a4d26cff7c9c2376e50f1000d35b6ef` |
| Windows | `d0d57caccde4e95b6326c8ab8f5ebb610cbbaff4f80197d34e586432d57ad84d` | `321d0908e1847afff38b354cb39fabb7712374d32c0947eccf6a5fed0e484ffe` |

Both macOS previous and latest packages were notarization-verified as `accepted`, with source `Notarized Developer ID` and signer `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

## ⚖️ Boundaries and Disclaimer

This is a third-party reverse-diff changelog, not official OpenAI release notes, and it does not redistribute binaries.

`[Confirmed]` only means directly verifiable plist, manifest, dependency, size, or file-level differences in the fact package. `[Signal]` only means build-artifact, module, or size-direction clues; it is not source-level fact and does not guarantee user-visible availability.

No information outside the provided fact packages was used. Future official notes, additional platform samples, or finer-grained source-level evidence should be used to revise these conclusions where applicable.

---
title: "Codex Desktop 26.616.81150"
version: "26.616.81150"
build: 4306
mac_version: "26.616.81150"
mac_build: 4306
win_version: "26.616.10790.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-23"
method: "official package reverse diff: macOS Sparkle, Windows MSIX"
summary: "This batch is mainly a cross-platform bundled automation plugin version sync; Windows additionally syncs the computer-use plugin, while app-layer and Computer Use runtime sizes stay effectively unchanged."
official_release_notes: false
---

# Codex Desktop 26.616.81150 macOS / Windows Comparison

> Third-party cross-platform changelog · Unofficial

Based on package facts, this batch looks more like bundled plugin metadata and packaging version synchronization than a broad capability expansion.

One-liner: a small cross-platform packaging/plugin metadata sync, with Windows additionally updating the bundled computer-use plugin manifest.

## About This Report

This report uses only machine-extracted facts from reverse-reconstructed official signed distribution packages: macOS arm64 compares `26.616.71553` build `4265` → `26.616.81150` build `4306`; Windows x64 MSIX compares `26.616.9593.0` → `26.616.10790.0`.

Evidence has two levels: [Confirmed] comes from plist, manifest, plugin lists, service entrypoints, dependency lists, skill docs, readable strings, or key file diffs; [Signal] comes from frontend module names, size movement, or compressed bundle direction clues. Platform tags are `[macOS+Windows]`, `[macOS only]`, and `[Windows only]`.

This input sample does not provide concrete entries for newly bundled plugin details, real frontend module additions/removals, same-name frontend module change clues, newly readable strings, content security policy changes, AppxManifest changes, or new components/resources, so those categories are not expanded into feature conclusions.

## ✨ Highlights

### [Confirmed][macOS+Windows] Bundled Browser Plugin Synced To The App Version

The `browser` plugin manifest on both platforms updates from `26.616.71553` to `26.616.81150`. Its description still points to Codex in-app browser automation scenarios, including opening, inspecting, navigating, testing, clicking, typing, and screenshotting local web targets, as well as handling `localhost`, `127.0.0.1`, `::1`, `file://`, and the current in-app browser tab.

Platform difference: the macOS path is `Contents/Resources/plugins/openai-bundled/plugins/browser/.codex-plugin/plugin.json`; the Windows path is `resources/plugins/openai-bundled/plugins/browser/.codex-plugin/plugin.json`. The input only shows the version field changing; it does not show a browser plugin capability text change.

### [Confirmed][macOS+Windows] Bundled Chrome Plugin Synced To The App Version

The `chrome` plugin manifest on both platforms updates from `26.616.71553` to `26.616.81150`. The packaged description still points to automation that depends on the user's existing Chrome state, including tabs, logged-in sessions, cookies, extensions, and Codex Chrome Extension setup. It also retains the guidance to prefer purpose-built connectors, APIs, or CLIs, and to ask the user to reauthenticate or explicitly approve Chrome as a fallback when authentication is missing or expired.

Platform difference: both macOS and Windows have this manifest version sync. The input does not show changes to the Chrome plugin description or entrypoint capabilities.

### [Confirmed][Windows only] Windows Also Syncs The Computer Use Plugin Manifest

In the Windows package, `resources/plugins/openai-bundled/plugins/computer-use/.codex-plugin/plugin.json` updates from `26.616.71553` to `26.616.81150`. Its description says it controls desktop apps on Windows from Codex through Computer Use.

Platform difference: the macOS fact package does not list a `computer-use` plugin manifest diff in this batch. Therefore this can only be described as a Windows-sample-only manifest change, not as proof that macOS lacks the corresponding capability or sync.

### [Confirmed][macOS only] macOS App Version And asar Integrity Hash Updated

The macOS `Info.plist` shows `/CFBundleShortVersionString` changing from `26.616.71553` to `26.616.81150`, `/CFBundleVersion` changing from `4265` to `4306`, and `/ElectronAsarIntegrity/Resources/app.asar/hash` changing with the `app.asar` contents.

Platform difference: the Windows input does not provide AppxManifest changes, so this report does not claim Windows manifest field changes.

### [Confirmed][macOS+Windows] App-Layer app.asar Has Small Content Changes, With File Counts Preserved

macOS `app.asar` internal files stay at `3115 → 3115`, with `10` added, `10` removed, and `10` changed. Windows `app.asar` internal files stay at `3078 → 3078`, with `10` added, `10` removed, and `11` changed.

Platform difference: both platforms have small internal file replacement or modification, but the total internal file counts and changed-file counts differ between macOS and Windows. The input explicitly warns that many `webview/assets` additions/removals are Vite build hash-renaming noise and must not be treated as new functionality.

### [Confirmed][macOS+Windows] Computer Use Runtime Size Is Unchanged, With No New cua_node Dependencies

The macOS Computer Use runtime total is `377.4 MB → 377.4 MB`, a `+0 MB`, `+0%` change. Windows is `298.9 MB → 298.9 MB`, also `+0 MB`, `+0%`. Both platforms report `0` newly added `cua_node` packages.

Platform difference: absolute runtime size differs, with the macOS sample larger; within each platform, however, this batch shows no total-size growth and no newly added `cua_node` packages.

### [Confirmed][macOS+Windows] Package-Level File Trees Add And Remove No Files, Only Modify Contents

The macOS bundle file count is `8827 → 8827`, with `0` added, `0` removed, and `68` content changes. The Windows file count is `9697 → 9697`, with `0` added, `0` removed, and `15` content changes.

Platform difference: both platforms have no package-level file additions/removals, but macOS has many more content-changed files than Windows. On both platforms, the largest changed component points to `app.asar`, with a table-reported delta of `+0.00 MB`.

## 🔎 Low-Level Signal Index

### Packaging And Version Sync

[Confirmed][macOS+Windows] The `browser`, `chrome`, and Windows `computer-use` manifest version fields sync to `26.616.81150`. Evidence boundary: this proves packaged plugin metadata version updates; it does not by itself prove plugin runtime logic changed.

### Frontend And Resource Layer

[Signal][macOS+Windows] Both platforms show `10` added and `10` removed internal files in `app.asar`, while the input also states that hash renames under `webview/assets` are noise. Evidence boundary: this is a build artifact change signal, not evidence of new frontend functionality.

### Platform Manifests And Security Policy

[Confirmed][macOS only] `Info.plist` version, build, and asar integrity hash changed. Evidence boundary: Windows AppxManifest changes are not provided, and neither fact package provides content security policy change entries, so this cannot be expanded into permission or security policy changes.

### Runtime And Dependencies

[Confirmed][macOS+Windows] Computer Use runtime totals are unchanged, and the number of newly added `cua_node` dependencies is `0`. Evidence boundary: this is a size-total and package-dependency fact; it does not rule out fine-grained content edits inside existing files, but the input provides no such runtime file diff.

## 📊 Raw Data

macOS arm64:

| Item | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| App version | `26.616.71553` build `4265` | `26.616.81150` build `4306` | version update |
| Bundle file count | `8827` | `8827` | added `0` / removed `0` / changed `68` |
| `Contents/Resources/app.asar` | `155.8 MB` | `155.8 MB` | `+0.00 MB` |
| app.asar internal files | `3115` | `3115` | added `10` / removed `10` / changed `10` |
| Computer Use runtime | `377.4 MB` | `377.4 MB` | `+0 MB` / `+0%` |
| New `cua_node` dependencies | `0` | `0` | none added |

macOS verification facts: the previous full package SHA-256 is `a2b2eee5d8b18f3da9cd8c4065dc9000ec938e055d52f7b2e9dc5be5e80f6570`; the latest full package SHA-256 is `7325382f657b6f8221f5bdd5421e7f898a4d26cff7c9c2376e50f1000d35b6ef`. Both notarization checks are `accepted`, with source `Notarized Developer ID` and origin `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

Windows x64:

| Item | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| App version | `26.616.9593.0` | `26.616.10790.0` | version update |
| Bundle file count | `9697` | `9697` | added `0` / removed `0` / changed `15` |
| `resources/app.asar` | `159.6 MB` | `159.6 MB` | `+0.00 MB` |
| app.asar internal files | `3078` | `3078` | added `10` / removed `10` / changed `11` |
| Computer Use runtime | `298.9 MB` | `298.9 MB` | `+0 MB` / `+0%` |
| New `cua_node` dependencies | `0` | `0` | none added |

Windows verification facts: the previous full package SHA-256 is `d0d57caccde4e95b6326c8ab8f5ebb610cbbaff4f80197d34e586432d57ad84d`; the latest full package SHA-256 is `321d0908e1847afff38b354cb39fabb7712374d32c0947eccf6a5fed0e484ffe`. The input sample does not provide detailed Windows signature verification output.

## ⚖️ Boundaries And Disclaimer

This is a third-party unofficial changelog and does not represent OpenAI official release notes.

This report does not redistribute binaries. It summarizes facts from reverse-reconstructed diffs of official signed distribution artifacts.

[Signal] only indicates package-artifact direction clues. It is not source-code fact and does not mean the feature is visible to all users.

Corrections are welcome if new official notes, more platform samples, or fuller machine-extracted facts become available.

---
title: "Codex Desktop 26.527.31326"
version: "26.527.31326"
build: 3390
previous_version: "26.527.30818"
previous_build: 3370
platform: "macOS arm64"
released: "2026-05-30"
compared_from: "2026-05-29"
method: "Direct comparison of two official full packages + structured bundle diff"
summary: "This release has almost no visible feature-level changes, mainly a version bump and minor packaging-content updates."
official_release_notes: false
---

# Codex Desktop `26.527.31326` (build 3390)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

The changes from `26.527.30818` to `26.527.31326` are very restrained: the file count is unchanged, dependencies are unchanged, and the human-readable diffs are mainly concentrated in version numbers and packaging integrity hash updates.  
English one-liner: a minimal maintenance-style build with no directly evidenced user-facing feature additions.

## About This Report

This is not an official OpenAI changelog, but a **reverse-engineered diff report based on official distribution artifacts**. This entry is a historical backfill: both versions are already outside the Sparkle delta window, so the comparison was performed by downloading the two official full packages and comparing them directly, rather than reconstructing from a delta package.

The reproducible chain is: obtain the previous and latest official full packages → use Apple notarization and the OpenAI Developer ID as the trust root → perform a structured bundle diff on `Codex.app`. The evidence package shows that both versions passed `codesign` / `spctl` notarization verification, both have source `Notarized Developer ID`, and the signing subject is `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

This report uses two evidence levels:

- **[Confirmed]**: Direct, human-readable evidence, such as `Info.plist` key values, plugin configuration, dependency lists, and signing/notarization results.
- **[Signal]**: Indirect evidence only, such as frontend module names or binary size changes; this type of information can only indicate “points in a certain direction,” but cannot be reconstructed from the artifact into source-level intent.

Comparison scope: from `26.527.30818` build `3370`, released on `2026-05-29`, to `26.527.31326` build `3390`, released on `2026-05-30`. Platform: macOS arm64.

## ✨ Highlights

### 1. App Version and Build Number Bumped [Confirmed]

This is a version-bump build: `CFBundleShortVersionString` was updated from `26.527.30818` to `26.527.31326`, and `CFBundleVersion` was updated from `3370` to `3390`.

The visible effect is that users and developers will see the new version number in system information, app bundle metadata, and plugin version declarations. However, the evidence package provides no direct evidence of new features, permissions, or dependency-backed capabilities.

Key evidence:

```diff
/CFBundleShortVersionString: '26.527.30818' → '26.527.31326'
/CFBundleVersion: '3370' → '3390'
```

### 2. Browser and Chrome Plugins Updated Alongside the Main App Version [Confirmed]

In the configuration files for the built-in Browser and Chrome plugins, the plugin version number was likewise updated from `26.527.30818` to `26.527.31326`.

This means the plugin packages were repackaged and released together with the main app. However, apart from the version fields, the evidence package shows no changes to plugin descriptions, capability declarations, or interface behavior.

Key evidence:

```diff
"version": "26.527.30818"
→
"version": "26.527.31326"
```

### 3. Minor Changes in app.asar, but No Evidence Supporting a New Capability Conclusion [Confirmed]

The integrity hash of `app.asar` changed, indicating that the application-layer archive content was indeed updated. At the same time, the bundle file tree shows that the total file count stayed at `1982 → 1982`, with `0` additions, `0` removals, and `51` content changes.

The visible takeaway is that this was a real repackaging or minor content update, not merely an outer version-number change. However, the readable diff in the evidence package only supports “version and packaging-content updates,” and does not support a conclusion that features were added.

Key evidence:

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
'efd27b4e1ae7bb61d8d058c92edaaf3b3a809cb5dbdc1cfd321bbd192dec50ee'
→
'7b24b4099e5ac419d3b02d8928ab1422f8d1b650cb51a1af754e8bc2f4ff6e85'
```

### 4. No New cua_node Dependencies [Confirmed]

The evidence package shows `0` newly added `cua_node` dependencies. Therefore, this release provides no direct evidence that lower-level capability packages expanded at the dependency layer.

For developers, this means that, at least from the dependency list, this update does not look like it introduced a new PDF, OCR, image-processing, or browser-automation capability stack. If behavioral changes exist, they are not reflected as newly added dependencies.

Key evidence:

```text
新增 0 个包
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Value |
| --- | ---: |
| File count | `1982 → 1982` |
| Added files | `0` |
| Removed files | `0` |
| Content changes | `51` |

Note: files inside `app.asar` changed from `2774 → 2774`, including `289` additions, `289` removals, and `12` changes. The evidence package explicitly states that the large number of additions and removals under `webview/assets` are Vite build hash renaming noise, so they are not listed here as real feature additions or removals.

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | `140.3 MB` | `140.3 MB` | `+0.00 MB` |

### Newly Added Dependency List

| Area | Number of newly added dependencies |
| --- | ---: |
| `cua_node` | `0` |

## 🔁 Reproducibility Information

| Artifact | SHA-256 | Size | Verification result |
| --- | --- | --- | --- |
| Previous full package `Codex-darwin-arm64-26.527.30818.zip` | `99a589d8512a2820d7a43a8a68a9e4b9fb91b93c21be0faa4bbcb0d5aab8298e` | Not provided | `spctl`: accepted; source=`Notarized Developer ID`; origin=`Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest full package `Codex-darwin-arm64-26.527.31326.zip` | `cfaa14d6a8a8a8de403028f927f6b64ba6692c3c9759eafec0fd33764124b9d2` | Not provided | `spctl`: accepted; source=`Notarized Developer ID`; origin=`Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

Note: the evidence package does not provide download artifact sizes, nor does it provide EdDSA signature verification conclusions. This historical backfill uses a direct comparison of two official full packages, with Apple notarization and OpenAI Developer ID code signing as the trust root.

## ⚖️ Boundaries and Disclaimer

This report is not affiliated with OpenAI and does not represent official documentation or official release notes.

This report does not redistribute any official binary files; it only publishes diff conclusions derived from official distribution artifacts.

No [Signal] item constitutes a source-level fact, and this report does not use any [Signal]-level change as the basis for concluding that a feature was added.

Corrections are welcome if there are issues with evidence-package parsing, summarization, or evidence labeling.
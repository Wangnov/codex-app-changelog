---
title: "Codex Desktop 26.417.41555"
version: "26.417.41555"
build: 1858
previous_version: "26.417.40842"
previous_build: 1851
platform: "macOS arm64"
released: "2026-04-21"
compared_from: "2026-04-20"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This release mainly updates the version and main application package contents; no new dependencies, file tree expansion, or confirmed new feature entry points were observed."
official_release_notes: false
---

# Codex Desktop `26.417.41555` (build 1858)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

This is a very small desktop update: the version number and build number were bumped, and the main application package contents changed, but the factual package evidence does not show new dependencies, permission expansion, or clear new user-facing functionality.  
One-liner: a small signed build update with app.asar content changes, but no confirmed feature-level additions.

## About This Report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. This is a historical backfill: both versions are outside the official Sparkle incremental update window, so the method was not incremental package reconstruction, but downloading both official full packages and performing a structured bundle diff.

In terms of trustworthiness, both versions passed Apple notarization and OpenAI Developer ID verification. `codesign` / `spctl` results both show accepted, with the origin listed as Notarized Developer ID.

This report uses two evidence levels:

- **[Confirmed]**: Human-readable direct evidence from Info.plist, signature verification, file tree statistics, dependency lists, and similar sources.
- **[Signal]**: Indirect evidence from frontend module names, binary size changes, and similar sources; this type of information points in a direction, but cannot reconstruct source-level intent from the artifact.

Comparison scope: from `26.417.40842` build `1851`, released on `2026-04-20`; to `26.417.41555` build `1858`, released on `2026-04-21`.

## ✨ Key Changes

### 1. Version Number and Build Number Updated [Confirmed]

The short version number in the application metadata was updated from `26.417.40842` to `26.417.41555`, and the build number was updated from `1851` to `1858`.

This indicates that this is a newly signed and officially distributed build, not a repackaging of the same version.

Key evidence:

```diff
/CFBundleShortVersionString: 26.417.40842 → 26.417.41555
/CFBundleVersion: 1851 → 1858
```

### 2. Main Application Package Contents Changed, but No Confirmed New Feature Entry Point [Confirmed]

The content hash of `Contents/Resources/app.asar` changed, indicating that the Electron main application package was indeed updated.

However, the total number of bundle files remained `497 → 497`, with `0` added files, `0` removed files, and no new dependencies in the factual package. Therefore, we can only confirm that “the main application contents changed”; this cannot be further stated as the addition of any specific user-facing feature.

Key evidence:

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
503840ccd1090c7859f7d666c2de706170823dcd8b783714522357b1a58588d3
→ eda8b73ad668dd304d3a68504c3c2882429418e6f9af71c4191bcffaf18bdbc6
```

### 3. No Dependency-Level Capability Expansion Observed [Confirmed]

The number of newly added `cua_node` dependencies is `0`. This means that, at the dependency list level, this release did not add new low-level capability packages.

The practical takeaway is that this diff does not look like the introduction of a major new capability; it is closer to an update to existing application code.

Key evidence:

```text
cua_node added 0 packages
```

### 4. Minor Changes Inside app.asar, but They Do Not Establish Functional Changes [Signal]

The number of internal application-layer files remained `1000 → 1000`, with internal statistics showing `5` added, `5` removed, and `5` modified. The factual package also explicitly notes that additions and removals of hash-bearing filenames under `webview/assets` and `.vite/build` are Vite build artifact renaming noise.

Therefore, this only indicates that “frontend build artifacts changed”; it cannot reconstruct source-level intent from the artifact, nor can it be described as the addition or removal of any feature.

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Previous Version | Latest Version |
| --- | ---: | ---: |
| File count | 497 | 497 |
| Added files | 0 | 0 |
| Removed files | 0 | 0 |
| Content changes | 43 | 43 |

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 111.2 MB | 111.2 MB | +0.00 MB |

### Newly Added Dependency List

| Scope | Number of newly added dependencies |
| --- | ---: |
| `cua_node` | 0 |

## 🔁 Reproducibility Information

| Artifact | SHA-256 | Verification result |
| --- | --- | --- |
| Previous full package `Codex-darwin-arm64-26.417.40842.zip` | `93afc61a2fe4099a113d5c4351439231e43f6598a6d9f4942c611c44461c7629` | Notarization verification accepted; origin Notarized Developer ID; signing subject Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2) |
| Latest full package `Codex-darwin-arm64-26.417.41555.zip` | `5cd157c770584f743bd2855e3e4ebf5558b82a4d4130ea4928b701aa1d4513a5` | Notarization verification accepted; origin Notarized Developer ID; signing subject Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2) |

| Item | Conclusion |
| --- | --- |
| EdDSA signature verification | Not provided in this factual package; this release was compared directly from full packages |
| `codesign` / `spctl` notarization conclusion | Both versions accepted |
| Distribution trust root | Apple notarization + OpenAI Developer ID code signing |

## ⚖️ Boundaries and Disclaimer

This report is not affiliated with OpenAI and does not represent official statements or official release notes.

This report does not redistribute any official binaries; it only publishes diff conclusions derived from officially signed distribution artifacts.

All [Signal] items only represent directional clues at the artifact level. They do not constitute source-level facts and do not represent confirmed product intent.

Reviews and corrections based on the same artifacts, the same hashes, and the same verification chain are welcome.
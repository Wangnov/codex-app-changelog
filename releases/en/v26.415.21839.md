---
title: "Codex Desktop 26.415.21839"
version: "26.415.21839"
build: 1763
previous_version: "26.415.20818"
previous_build: 1727
platform: "macOS arm64"
released: "2026-04-17"
compared_from: "2026-04-16"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This release mainly adds a native launch-services-helper build and distribution component, while also updating the version and app.asar integrity hash."
official_release_notes: false
---

# Codex Desktop `26.415.21839` (build 1763)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

This is a very small desktop update: the confirmed user- and developer-visible changes are centered on the addition of a native `launch-services-helper` helper component, and the build process now explicitly generates it.  
English one-liner: a small macOS arm64 update centered on a new native Launch Services helper.

## About this report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. This comparison is a historical backfill: both versions have already fallen outside the official Sparkle delta-update window, so the fact package uses a direct comparison of two official full packages rather than reconstructing a delta package.

The comparison range is from `26.415.20818` build 1727, released on 2026-04-16, to `26.415.21839` build 1763, released on 2026-04-17. The platform is macOS arm64.

In terms of trustworthiness, both app bundles passed Apple notarization and OpenAI Developer ID verification. The `spctl` result for both the previous and latest versions is `accepted`, the origin is `Notarized Developer ID`, and the signing source is `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

This report uses two evidence levels:

- **[Confirmed]**: Direct human-readable evidence, such as `Info.plist`, `package.json`, signing / notarization results, file paths, and hashes.
- **[Signal]**: Indirect evidence, such as frontend module names or binary size changes; this type of information can only indicate “pointing in a certain direction, but not enough to recover source-level intent from the artifact.”

## ✨ Highlights

### 1. Added a native Launch Services helper [Confirmed]

A new file was added under `Contents/Resources/native/`: `launch-services-helper`, with a size of about 0.1 MB. At the same time, both the development and `owl` startup scripts in `package.json` added a `pnpm exec node ./scripts/build-launch-services-helper.mjs` step.

This means Codex Desktop has started including a native helper related to Launch Services in its build and distribution pipeline. The fact package does not include the helper’s source code or call sites, so it is not possible to further assert whether it is specifically responsible for opening files, registering protocols, launching the app, or other system integration behavior.

Key evidence:

```diff
+    "dev": "pnpm run rebuild:sqlite && pnpm exec node ./scripts/build-launch-services-helper.mjs && pnpm --dir .. run generate:third-party-notices && cross-env NODE_ENV=development electron-forge start",
+    "owl": "pnpm exec node ./scripts/build-launch-services-helper.mjs && pnpm --dir .. run generate:third-party-notices && cross-env NODE_ENV=development node ./scripts/owl-shell.mjs run",
```

New file:

```text
Contents/Resources/native/launch-services-helper (0.1 MB)
```

### 2. Version and build number advanced [Confirmed]

The app version was updated from `26.415.20818` to `26.415.21839`, and the build number from `1727` to `1763`. This appears in both `Info.plist` and `package.json`.

This is a standard release progression, but it also shows that this update is not merely a signature or metadata replacement: the bundle file tree does contain 1 new file and 39 content changes.

Key evidence:

```diff
-  "version": "26.415.20818",
+  "version": "26.415.21839",
```

```diff
-  "codexBuildNumber": "1727",
+  "codexBuildNumber": "1763",
```

`Info.plist` changes:

```text
CFBundleShortVersionString: 26.415.20818 → 26.415.21839
CFBundleVersion: 1727 → 1763
```

### 3. app.asar integrity hash updated [Confirmed]

`ElectronAsarIntegrity/Resources/app.asar/hash` in `Info.plist` changed, indicating that the Electron ASAR content integrity record was regenerated for this release.

This is usually the result of changes to packaged application-layer content. The fact package only shows 6 file content changes inside app.asar, while a large number of Vite hash filename additions and removals are noise; therefore, this report does not interpret hash-file changes under `webview/assets` and `.vite/build` as new features.

Key evidence:

```text
ElectronAsarIntegrity/Resources/app.asar/hash:
5e8423d4df65bc7af56701e76fc28c6431d5dcaf63c54cc60708675e315e7d8d
→
21600e64c2c0b63f5f167117a0e65047c5cde8727b776d5a9671f63a742b0798
```

### 4. No new cua_node packages in the dependency layer [Confirmed]

The fact package shows 0 newly added `cua_node` dependencies. This means the visible changes in this release did not expand capabilities by adding new `cua_node` dependency packages.

Combined with the newly added `launch-services-helper`, this release looks more like the addition of a very small native helper distribution unit than the introduction of a new set of large dependency-based capabilities.

## 📊 Raw data

### Bundle file-level statistics

| Metric | Previous version | Latest version | Change |
| --- | ---: | ---: | ---: |
| File count | 497 | 498 | +1 |
| Added files | 0 | 1 | +1 |
| Removed files | 0 | 0 | 0 |
| Content changes | 0 | 39 | +39 |

Note: app.asar internal files are 1000 → 1000, with 68 internal additions / 68 removals / 6 changes; many additions and removals under `webview/assets` are Vite build hash rename noise, and this report does not count them as real feature additions or removals.

### Key component size changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 111.5 MB | 111.5 MB | +0.00 MB |
| `Contents/_CodeSignature/CodeResources` | 0.1 MB | 0.1 MB | +0.00 MB |
| `Contents/CodeResources` | 0.0 MB | 0.0 MB | +0.00 MB |

### Added dependency list

| Scope | Added count |
| --- | ---: |
| `cua_node` | 0 |

Excerpt of added files:

| Path | Size |
| --- | ---: |
| `Contents/Resources/native/launch-services-helper` | 0.1 MB |

## 🔁 Reproducibility information

| Artifact | URL | SHA-256 |
| --- | --- | --- |
| Previous full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.415.20818.zip` | `35081c8d5791f400abd998a7b164a547c193b1a93aceebf542ff8ec8f20cb548` |
| Latest full package | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.415.21839.zip` | `504aad9b52d0144e2efa1cfdfb1017b479ffd2feb1f34c4c348d9e335e4a1e02` |

| Verification item | Previous version | Latest version |
| --- | --- | --- |
| Notarization verification | `accepted` | `accepted` |
| Origin | `Notarized Developer ID` | `Notarized Developer ID` |
| Signing source | `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` | `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

This fact package does not provide the download artifact size, nor does it provide an EdDSA delta package signature verification result; the reason is that this release is a historical backfill based on direct comparison of two official full packages.

## ⚖️ Boundaries and statement

This report is not affiliated with OpenAI, does not represent the official position, and is not official release notes.

This report does not redistribute any official binary files; it only publishes diff conclusions derived from officially signed distribution artifacts.

All [Confirmed] items come from readable configuration, file trees, hashes, or signing / notarization information in the fact package. All [Signal] items only mean “pointing in a certain direction, but not enough to recover source-level intent from the artifact.” This article does not use [Signal] as the basis for any feature conclusion.

Corrections are welcome if there are any fact package parsing errors, improper summaries, or missing details.
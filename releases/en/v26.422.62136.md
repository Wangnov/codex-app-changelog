---
title: "Codex Desktop 26.422.62136"
version: "26.422.62136"
build: 2180
previous_version: "26.422.30944"
previous_build: 2080
platform: "macOS arm64"
released: "2026-04-28"
compared_from: "2026-04-24"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This release is mainly a version and main application content update: the bundle file tree remains stable, while the core codex executable component increased in size by about 3.48 MB."
official_release_notes: false
---

# Codex Desktop `26.422.62136` (build 2180)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

This is a backfilled diff report for the macOS arm64 desktop release from `26.422.30944` to `26.422.62136`.  
One-liner: stable bundle shape, updated signed build, and a larger core `codex` component.

## About This Report

This is not an official OpenAI changelog, but a **reverse-engineered diff report based on official distribution artifacts**.

This release is a historical backfill: both versions are outside the official Sparkle delta update window, so the method was not delta package reconstruction. Instead, both official full packages were downloaded, directly compared, and used to generate a structured bundle diff.

In terms of trustworthiness, both versions of `Codex.app` passed Apple notarization and Developer ID verification:

- Previous version: `accepted`, `source=Notarized Developer ID`, `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`
- Latest version: `accepted`, `source=Notarized Developer ID`, `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`

This report uses two evidence levels:

- **[Confirmed]**: Direct human-readable evidence from Info.plist, dependency manifests, signature verification, file-level statistics, and similar sources.
- **[Signal]**: Indirect evidence such as component size changes; this kind of information points in a direction but cannot reconstruct source-level intent from the artifact.

Comparison scope: `26.422.30944` build `2080`, released on `2026-04-24` → `26.422.62136` build `2180`, released on `2026-04-28`.

## ✨ Highlights

### 1. Version and build officially advanced to `26.422.62136` / `2180` [Confirmed]

Application metadata shows that `CFBundleShortVersionString` was updated from `26.422.30944` to `26.422.62136`, and `CFBundleVersion` was updated from `2080` to `2180`.

This means the release is not merely a repackaging or signing change, but a macOS arm64 release with an explicit version number advancement.

Key evidence:

```diff
CFBundleShortVersionString: 26.422.30944 → 26.422.62136
CFBundleVersion: 2080 → 2180
```

### 2. The bundle file tree remained stable, with no top-level files added or removed [Confirmed]

Both bundle versions contain `507` files, with `0` added, `0` removed, and `47` content changes.

This indicates that, from the outer application bundle structure, this release did not introduce new standalone components, plugins, or resource directories. The changes are concentrated in the contents of existing files. For developers, this looks more like an update to existing components than an expansion of the application shape or packaging structure.

Key evidence:

```text
File count: 507 → 507
Added 0 / Removed 0 / Content changed 47
```

### 3. The core `codex` component increased by about 3.48 MB [Signal]

`Contents/Resources/codex` increased from `180.9 MB` to `184.4 MB`, a change of `+3.48 MB`.

This points to a substantive content update in the core runtime component. However, binary size changes alone cannot reconstruct source-level intent from the artifact, nor can they determine exactly what capabilities were added or what issues were fixed.

Key evidence:

```text
Contents/Resources/codex: 180.9 MB → 184.4 MB (+3.48 MB)
```

### 4. The number of application-layer internal files did not change; hashed filename changes should not be interpreted as new features [Confirmed]

The internal file count in `app.asar` remained `1031 → 1031`, while the machine diff shows `73` added, `73` removed, and `7` changed. The fact package explicitly states that many additions and removals under `webview/assets` are Vite build hash renames, which are noise.

Therefore, this should not be described as “73 new feature files added” or “73 feature files removed.” The readable conclusion is only that the application-layer archive was updated, while filename-level additions and removals are mainly affected by build artifact renaming.

Key evidence:

```text
Internal files: 1031 → 1031
Added 73 / Removed 73 / Changed 7
Note: many additions/removals under webview/assets are Vite build hash renames and are noise
```

### 5. No new packages were added to `cua_node` dependencies [Confirmed]

In the `cua_node` dependency changes, the number of added packages is `0`.

This means that, based on this dependency manifest, this release did not extend related capabilities by adding new `cua_node` packages. At the observable level, this report has no evidence supporting “a new set of dependency-driven features was added.”

Key evidence:

```text
cua_node added 0 packages
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Previous version | Latest version | Change |
| --- | ---: | ---: | ---: |
| bundle file count | 507 | 507 | 0 |
| added files | 0 | 0 | 0 |
| removed files | 0 | 0 | 0 |
| content-changed files | - | 47 | 47 |

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/codex` | 180.9 MB | 184.4 MB | +3.48 MB |
| `Contents/Resources/app.asar` | 119.9 MB | 119.9 MB | -0.00 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/browser-use/scripts/browser-client.mjs` | 0.8 MB | 0.8 MB | +0.00 MB |

### Added Dependency Manifest

| Dependency area | Added count | Conclusion |
| --- | ---: | --- |
| `cua_node` | 0 | No new packages added |

## 🔁 Reproducibility Information

| Artifact | URL | SHA-256 | Verification result |
| --- | --- | --- | --- |
| Previous full package `26.422.30944` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.422.30944.zip` | `5705b030cbb557c84fd2fdbd7c64693867d17c9c5ff56a3060891ca05d55f852` | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest full package `26.422.62136` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.422.62136.zip` | `a549f217c5d84ac7581c7f762b97ca4e0976797d56b206c4e4ee9d1c33faa635` | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Boundaries and Disclaimer

This report is not affiliated with OpenAI, does not represent an official position, and is not official release notes.

This report does not redistribute any official binaries. It only publishes diff conclusions derived from officially signed distribution artifacts.

All [Signal] entries indicate only indirect artifact-level signs. They do not constitute source-level facts and should not be interpreted as official feature intent.

Corrections or additional evidence are welcome, provided they are based on the same version, the same artifacts, and the same verification chain.
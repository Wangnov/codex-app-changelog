
---
title: "Codex Desktop 26.422.62136"
version: "26.422.62136"
build: 2180
previous_version: "26.422.30944"
previous_build: 2080
platform: "macOS arm64"
released: "2026-04-28"
compared_from: "2026-04-24"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This build mainly updates the lower-level codex component and adds a remote authentication failure path, with only limited UI-level signals."
official_release_notes: false
---

# Codex Desktop `26.422.62136` build 2180

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update artifacts

This build moves from `26.422.30944` build 2080 to `26.422.62136` build 2180. The most visible artifact-level changes are growth in the bundled `codex` component and a new remote-authentication error string in the main bundle.  
English one-liner: this build points to remote-auth handling changes and a larger bundled Codex runtime, with only limited UI-level signals.

## About This Report

This is not an official changelog. It is a third-party reverse-diff report based on official distribution artifacts. This entry is a historical backfill: the official appcast did not provide a delta package for this version pair, so the comparison uses two official full packages.

The comparison covers macOS arm64 from `26.422.30944` build 2080 to `26.422.62136` build 2180. The bundle file count remains `507 → 507`, with `47` files changed in content.

Evidence levels:

- **[Confirmed]**: Directly observable facts from Info.plist, newly added readable strings in the main bundle, component sizes, signing, and notarization results.
- **[Signal]**: Indirect evidence from same-name frontend module diffs or binary size changes. These point in a direction but cannot be reconstructed into source-level intent.

## ✨ Highlights

### 1. New failure text for unauthenticated remote Codex 【Confirmed】

The main process / frontend bundle adds this readable string:

```text
Remote codex is not authenticated
```

This shows that the new artifact contains an explicit failure path for an unauthenticated remote Codex state. For users or developers, the visible effect may be a more specific failure reason in remote-related flows. However, because this comes from a compressed bundle, it does not reveal the full source semantics, trigger conditions, or exact UI placement.

The comparison also shows this older string removed:

```text
Remote SSH failed without an exit code or stderr
```

That points to a change in the wording focus for remote failures, but it does not establish implementation details.

### 2. The lower-level `codex` component grew noticeably 【Signal】

`Contents/Resources/codex` increased from `180.9 MB` to `184.4 MB`, a `+3.48 MB` change.

This points to a substantive update in the lower-level runtime or bundled capability surface. The fact package does not include new commands, configuration, dependencies, or readable feature documentation, so this remains a [Signal]: it shows that the component changed materially, but cannot be reconstructed into source-level intent.

### 3. Branch-mismatch confirmation dialog strings were removed 【Signal】

In the same-name frontend module `index`, the diff shows the removal of branch-related dialog strings that existed in the previous version:

```text
Continue on a different branch?
composer.branchMismatchDialog.title
composer.branchMismatchDialog.subtitle
composer.branchMismatchDialog.skipFuture
```

These signals point to a possible adjustment or removal of the frontend interaction around a checked-out branch differing from a thread branch. Because this comes from same-name frontend module diff clues, it remains a UI, state, or interaction [Signal], not a source-level fact.

### 4. Editor drag-and-drop state styling changed 【Signal】

The same-name frontend module `index` adds several editor group drag-and-drop related class or state names:

```text
bg-token-editor-group-drop-background/60
bg-token-editor-group-drop-into-prompt-background
border-token-editor-group-drop-into-prompt-foreground/40
text-token-editor-group-drop-into-prompt-foreground
z-40
```

These class names point to possible changes in editor-area drag-and-drop visual states, especially around drop-into-prompt states. They are still only class-name diffs from a frontend bundle and cannot establish the concrete interaction or source implementation.

### 5. Version metadata and asar integrity hash updated 【Confirmed】

`Info.plist` shows the version and build update directly:

```text
CFBundleShortVersionString: 26.422.30944 → 26.422.62136
CFBundleVersion: 2080 → 2180
```

The `ElectronAsarIntegrity/Resources/app.asar/hash` changed from:

```text
c7accc89468b457278fcc8a3b713df521337da788cd421c8b318d795b30d85b9
```

to:

```text
4b15031128e180394a033dec86a00526477691cf63ac1f5dca7f78d24e83c6bb
```

This is version and integrity metadata, not a standalone feature change, but it helps verify that the bundle diff corresponds to the new artifact.

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| Bundle file count | 507 | 507 | 0 |
| Added files |  |  | 0 |
| Removed files |  |  | 0 |
| Files with content changes |  |  | 47 |

### Key Component Size Changes

| Component path | Previous | Latest | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/codex` | 180.9 MB | 184.4 MB | +3.48 MB |
| `Contents/Resources/app.asar` | 119.9 MB | 119.9 MB | -0.00 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/browser-use/scripts/browser-client.mjs` | 0.8 MB | 0.8 MB | +0.00 MB |

### Newly Added Dependencies

| Area | Newly added dependencies |
| --- | ---: |
| `cua_node` | 0 packages |

## 🔁 Reproducibility

| Item | Previous `26.422.30944` | Latest `26.422.62136` |
| --- | --- | --- |
| Download URL | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.422.30944.zip` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.422.62136.zip` |
| SHA-256 | `5705b030cbb557c84fd2fdbd7c64693867d17c9c5ff56a3060891ca05d55f852` | `a549f217c5d84ac7581c7f762b97ca4e0976797d56b206c4e4ee9d1c33faa635` |
| Download artifact size | Not provided | Not provided |
| EdDSA verification result | Not provided | Not provided |
| codesign / spctl notarization result | `accepted` | `accepted` |
| Notarization source | `Notarized Developer ID` | `Notarized Developer ID` |
| Signing origin | `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` | `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ Boundaries And Disclaimer

This report is independent from OpenAI and does not represent official release notes or an official position.

It does not redistribute any official binaries. It only publishes conclusions derived from diffs of official signed distribution artifacts.

All [Signal] items only indicate a direction suggested by artifact differences. They are not source-level facts and should not be treated as officially confirmed feature changes.

Corrections based on reproducible evidence are welcome.

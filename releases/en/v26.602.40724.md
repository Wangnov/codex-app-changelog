---
title: "Codex Desktop 26.602.40724"
version: "26.602.40724"
build: 3593
previous_version: "26.602.30954"
previous_build: 3575
platform: "macOS arm64"
released: "2026-06-05"
compared_from: "2026-06-04"
method: "Direct comparison of official full packages + structured bundle diff"
summary: "This release is primarily a minor version bump: the app itself and the bundled Browser/Chrome plugins were updated in sync, with no visible new capabilities at the file tree, dependency, or readable feature-configuration level."
official_release_notes: false
---

# Codex Desktop `26.602.40724` (build 3593)

> Third-party changelog · Unofficial · Based on reverse-engineered diffs of official update packages

This is a very restrained minor release: based on the artifacts, the main changes are the version number, the integrity hash of `app.asar`, and synchronized version updates for the bundled Browser/Chrome plugins.  
English one-liner: a small maintenance-style build with no visible new dependency or bundle-file expansion.

## About This Report

This is not an official changelog, but a **reverse-engineered diff report based on official distribution artifacts**. This entry is a historical backfill: both versions fall outside the official Sparkle delta-update window, so the comparison uses two official full packages directly, followed by structured bundle diff analysis.

In terms of confidence, both compared artifacts come from official download URLs. Both the previous version and the latest version passed Apple notarization verification, with the source shown as `Notarized Developer ID` and the signing identity as `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`.

This report uses two evidence levels:

- **[Confirmed]**: Direct human-readable evidence, such as `Info.plist`, plugin configuration, hashes, signing, and notarization results.
- **[Signal]**: Indirect evidence only, such as frontend module names or binary size changes; this kind of evidence can only indicate a direction, but cannot reconstruct source-level intent from the artifact.

Comparison scope: from `26.602.30954` (build 3575, 2026-06-04) to `26.602.40724` (build 3593, 2026-06-05), on macOS arm64.

## ✨ Highlights

### 1. App Version Advanced to `26.602.40724` [Confirmed]

`Info.plist` shows that the app short version was updated from `26.602.30954` to `26.602.40724`, and the build number from `3575` to `3593`.

This means this is a newly packaged official desktop build, but based on readable configuration, it does not appear to include new permissions, entry components, or file-tree scale changes.

Key evidence:

```diff
/CFBundleShortVersionString: '26.602.30954' → '26.602.40724'
/CFBundleVersion: '3575' → '3593'
```

### 2. Bundled Browser and Chrome Plugin Versions Updated in Sync [Confirmed]

The `plugin.json` versions for the bundled `browser` and `chrome` plugins were both updated from `26.602.30954` to `26.602.40724`.

The observable point is that these two automation plugins were republished together with the desktop app, but the factual package does not show changes to their descriptions, capability declarations, or configuration content.

Key evidence:

```diff
"version": "26.602.30954" → "26.602.40724"
```

Affected files:

- `Contents/Resources/plugins/openai-bundled/plugins/browser/.codex-plugin/plugin.json`
- `Contents/Resources/plugins/openai-bundled/plugins/chrome/.codex-plugin/plugin.json`

### 3. App Resource Package Changed Slightly, but Exposes No Clear New Feature [Signal]

`Contents/Resources/app.asar` changed from `143.1 MB` to `143.1 MB`, a delta of `+0.01 MB`. Its internal file count remained `2830 → 2830`, with `17` additions, `17` removals, and `12` modifications.

This points to a rebuild or small content adjustment in app-layer resources, but source-level intent cannot be reconstructed from the artifact. The factual package also explicitly notes that filename changes under `webview/assets` and `.vite/build` involving hashes are build-artifact renaming noise and should not be used to infer feature additions or removals.

Key evidence:

```text
Contents/Resources/app.asar: 143.1 MB → 143.1 MB (+0.01 MB)
Internal files: 2830 → 2830 (added 17 / removed 17 / modified 12)
```

### 4. No New `cua_node` Dependencies [Confirmed]

The factual package shows `0` newly added `cua_node` dependencies.

This means there is no evidence in this diff of lower-level capability expansion through newly added dependencies. Combined with the total bundle file count of `2075 → 2075`, this looks more like a small-scope maintenance build than a capability-expansion release.

Key evidence:

```text
0 new packages
File count: 2075 → 2075
```

## 📊 Raw Data

### Bundle File-Level Statistics

| Metric | Previous Version | Latest Version | Change |
| --- | ---: | ---: | ---: |
| File count | 2075 | 2075 | 0 |
| Added files | - | - | 0 |
| Removed files | - | - | 0 |
| Files with content changes | - | - | 53 |

Note: Added/removed file counts inside `app.asar` are not included in this table because they contain Vite hash filename-renaming noise, which can easily be misread as real feature additions or removals.

### Key Component Size Changes

| Component path | Old | New | Change |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 143.1 MB | 143.1 MB | +0.01 MB |

### Newly Added Dependency List

| Scope | Added count | Notes |
| --- | ---: | --- |
| `cua_node` | 0 | No new packages found |

## 🔁 Reproducibility Information

| Artifact | SHA-256 | Size | EdDSA Signature Verification | codesign/spctl Notarization |
| --- | --- | --- | --- | --- |
| Previous full package `Codex-darwin-arm64-26.602.30954.zip` | `730f8f07dd4031b30de1bee11e770a956835930af2334df200abaf5645103e01` | Not provided in factual package | Not applicable: this comparison uses full packages directly | accepted; source=`Notarized Developer ID`; origin=`Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| Latest full package `Codex-darwin-arm64-26.602.40724.zip` | `be1a6ae44613c3f440d4e3d342040ebe08f273f975a372e812082967dc0dc30d` | Not provided in factual package | Not applicable: this comparison uses full packages directly | accepted; source=`Notarized Developer ID`; origin=`Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

Download URLs:

- Previous version: `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.602.30954.zip`
- Latest version: `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.602.40724.zip`

## ⚖️ Boundaries and Disclaimer

This report is not affiliated with OpenAI, is not official release notes, and does not represent an official interpretation.

This report does not redistribute any official binaries; it only publishes diff conclusions derived from officially signed distribution artifacts.

All [Signal] entries indicate only directional clues at the artifact level. They do not constitute source-level facts and should not be treated as statements of product intent.

If you find any errors in factual package parsing, synthesis, or wording, corrections based on reproducible evidence are welcome.
---
title: "Codex 桌面版 26.422.21459"
version: "26.422.21459"
build: 2049
previous_version: "26.422.20832"
previous_build: 2025
platform: "macOS arm64"
released: "2026-04-24"
compared_from: "2026-04-23"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "这是一次非常小的维护更新：版本号与构建号前进，bundle 内容仅有少量改动，未观察到新增依赖或新增顶层文件。"
official_release_notes: false
---

# Codex(代码代理桌面应用) 桌面版 `26.422.21459`(build 2049)

> 第三方变更日志 · 非官方 · 基于官方更新包逆向差异

本版从 `26.422.20832` 更新到 `26.422.21459`，整体更像一次低风险维护发布，而不是能力扩展发布。  
English one-liner: a small maintenance update with no visible new dependency or bundle-file surface.

## 关于这份报告

这不是官方变更日志，而是一份**基于官方分发产物的逆向差异报告**。本次为历史回填：两个版本均已超出官方 Sparkle(应用自动更新框架) 增量窗口，因此事实包采用两个官方全量包直接比较，而不是增量包重建。

可信度来自 Apple(苹果公司) 公证与 OpenAI Developer ID(开发者身份签名) 代码签名验证：上一版与最新版的 `Codex.app` 均通过 `spctl` 公证检查，来源均显示为 `Notarized Developer ID`，签名来源均为 `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`。

本文使用两档证据等级：

- **【实证】**：来自人类可读的直接证据，例如 `Info.plist` 键值、文件统计、签名验证结论、依赖清单。
- **【信号】**：来自间接证据，例如前端模块名或二进制体积变化；这类条目只能说明“指向某方向，但无法从产物还原到源码级意图”。

对比范围：`26.422.20832`(build 2025，2026-04-23) → `26.422.21459`(build 2049，2026-04-24)。

## ✨ 重点变化

### 1. 版本与构建号前进 【实证】

最新版将 `CFBundleShortVersionString` 从 `26.422.20832` 更新到 `26.422.21459`，`CFBundleVersion` 从 `2025` 更新到 `2049`。

这意味着 macOS(苹果桌面操作系统) 层面能明确识别这是一个新构建，但事实包没有给出任何官方 release notes(发布说明)。

关键证据：

```diff
/CFBundleShortVersionString: 26.422.20832 → 26.422.21459
/CFBundleVersion: 2025 → 2049
```

### 2. Bundle 文件树没有新增或移除顶层文件 【实证】

应用 bundle(应用包目录) 文件数保持 `507 → 507`，新增 `0`、移除 `0`，内容改动 `46` 个文件。

这说明本次更新没有引入新的顶层组件或插件形态；从用户和开发者可见的安装包结构看，变化范围较小。

### 3. 未新增 `cua_node` 依赖 【实证】

事实包显示 `cua_node` 新增依赖为 `0` 个包。

这意味着本版没有通过新增依赖包来扩展这一层能力；若有行为变化，也没有体现在新增依赖清单上。

### 4. `app.asar` 有轻微内容变化，但体积几乎不变 【实证】

`Contents/Resources/app.asar` 从 `119.9 MB` 到 `119.9 MB`，变化为 `+0.01 MB`；内部文件数量保持 `1031 → 1031`，但存在新增 `73`、移除 `73`、改动 `6` 的内部差异。

事实包特别标注：`webview/assets` 与 `.vite/build` 下带 hash(内容指纹) 的文件名增减是 Vite(前端构建工具) 构建产物重命名噪音，不能据此判断新增或删除功能。因此这里能确认的是“打包内容发生了小幅变化”，不能推出具体功能新增。

## 📊 原始数据

### Bundle 文件级统计

| 指标 | 数值 |
| --- | ---: |
| 文件数 | `507 → 507` |
| 新增文件 | `0` |
| 移除文件 | `0` |
| 内容改动 | `46` |

### 关键组件体积变化

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | `119.9 MB` | `119.9 MB` | `+0.01 MB` |
| `Contents/Resources/codex_chronicle` | `4.4 MB` | `4.4 MB` | `+0.00 MB` |

### 新增依赖清单

| 范围 | 新增依赖数量 |
| --- | ---: |
| `cua_node` | `0` |

## 🔁 可复现信息

| 制品 | URL | SHA-256 | 大小 | EdDSA(爱德华曲线数字签名算法) 验签结论 | codesign / spctl 结论 |
| --- | --- | --- | --- | --- | --- |
| 上一版全量包 | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.422.20832.zip` | `41b7c2c85ed099d62dabe5273cc4a3fe3d6735a48370b41b9bcf7aa84d4b6d18` | 事实包未提供 | 事实包未提供；本次为全量包直接比较 | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| 最新版全量包 | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.422.21459.zip` | `5ce4b5b8427b9e0d66d2c3b286fec7b54b25cd7b0e3f29d90f46ca529d9902f7` | 事实包未提供 | 事实包未提供；本次为全量包直接比较 | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ 边界与声明

这份报告与 OpenAI 无关，不代表官方立场，也不是官方发布说明。

本文不重分发任何官方二进制，只发布基于差异事实包整理出的结论。

带有【信号】标记的内容不构成源码级事实；本版事实包中没有可写成独立功能变化的【信号】条目。

欢迎基于同一制品与同一校验信息复核并指正。
---
title: "Codex 桌面版 26.611.62324"
version: "26.611.62324"
build: 4028
mac_version: "26.611.62324"
mac_build: 4028
win_version: "26.611.8604.0"
platforms: ["macOS arm64", "Windows x64"]
released: "2026-06-15"
method: "官方包逆向 diff(macOS Sparkle / Windows MSIX)"
summary: "本批次 macOS 包与对比基线完全一致；Windows 版出现少量 app.asar 内容变化，并新增临时 app-server 状态 reconciliation 字符串，但两端 Computer Use 运行时与 cua_node 依赖均未变化。"
official_release_notes: false
---

# Codex 桌面版 26.611.62324(macOS / Windows 对照)

> 第三方跨平台变更日志 · 非官方

本批次的核心差异是：macOS 侧没有任何文件级变化，Windows 侧有极小范围的应用层变更，主要信号指向临时 app-server 状态对账/修复逻辑。  
One-liner: macOS is byte-identical to its baseline, while Windows adds a small app-layer change around temporary app-server state reconciliation.

## 关于这份报告

本报告基于官方签名分发包的逆向 diff：macOS 取 Sparkle 全量包对比，Windows 取 MSIX 分发包对比。macOS 对比版本为 `26.611.62324` build `4028` → `26.611.62324` build `4028`；Windows 对比版本为 `26.611.8273.0` → `26.611.8604.0`。发布日期按本批次输入记录为 `2026-06-15`。

证据等级分两档：【实证】表示来自可读清单、包结构、依赖清单、字符串字面量或文件统计等可直接核验的材料；【信号】表示只能从前端/主进程 bundle、体积或模块变化推断方向，不能还原源码意图。

平台归属标记含义：[macOS+Windows] 表示两平台都有同类事实；[仅 macOS] 表示只在 macOS 事实包出现；[仅 Windows] 表示只在 Windows 事实包出现。

## ✨ 重点变化

### 应用包内容变化

【实证】[仅 macOS] macOS 本批次没有实际内容变更。文件数 `8267 → 8267`，新增 `0`、移除 `0`、内容改动 `0`；`app.asar` 内部文件 `3074 → 3074`，新增 `0`、移除 `0`、改动 `0`。上一版与最新版全量包 SHA-256 相同：`aa5ffa86f0f629e8b02a7ae08f43cdaa87fca7e176b5b9b7e672c1c21a9c1963`。

【实证】[仅 Windows] Windows 版存在小范围内容变化。文件数 `11943 → 11943`，新增 `0`、移除 `0`、内容改动 `16`；`app.asar` 内部文件 `3024 → 3024`，新增 `313`、移除 `313`、改动 `12`。事实包明确提示 `webview/assets` 下大量增减属于 Vite 构建 hash 重命名噪音，不能据此判断新功能。

### 临时 app-server 状态对账

【实证】[仅 Windows] Windows 新版主进程/前端 bundle 中新增两个可读字符串：`Failed to reconcile temporary app-server state` 与 `Reconciled temporary app-server state`。这能确认新版包里出现了“临时 app-server 状态 reconciliation（对账/协调）”相关文案或日志字面量。

【信号】[仅 Windows] 这些字符串指向某种临时 app-server 状态修复、对账或迁移路径，但事实包只提供压缩 bundle 中的字符串，不能确认触发条件、用户可见性、完整流程或源码实现。macOS 对应批次没有任何内容变化，因此没有同步出现这组信号。

### Computer Use 运行时与 cua_node 依赖

【实证】[macOS+Windows] 两平台的 Computer Use 运行时总量均未变化。macOS 为 `365.9 MB → 365.9 MB`，变化 `+0 MB, +0%`；Windows 为 `285.6 MB → 285.6 MB`，变化 `+0 MB, +0%`。

【实证】[macOS+Windows] 两平台 `cua_node` 依赖均未新增包，新增数量都是 `0`。这说明本批次事实包中没有 Computer Use 运行时依赖扩展的证据。

### 签名与来源验证

【实证】[仅 macOS] macOS 上一版与最新版均通过公证验证，结果为 `accepted`，来源为 `Notarized Developer ID`，签名主体为 `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`。

【实证】[仅 Windows] Windows 事实包提供了上一版与最新版分发包 URL 及 SHA-256。上一版 SHA-256 为 `32cf55c6d9a656eb18066890bac655b77e277bb7be84b942b223d27e86631d21`，最新版 SHA-256 为 `37f4d4e5ec263797b2b0d05a26e900f21aa1942e40ada380c19e8982dc7018c1`。

## 📊 原始数据

### macOS arm64

- 版本：`26.611.62324` build `4028` → `26.611.62324` build `4028`
- Bundle 文件数：`8267 → 8267`
- Bundle 变化：新增 `0` / 移除 `0` / 内容改动 `0`
- `app.asar` 内部文件：`3074 → 3074`
- `app.asar` 变化：新增 `0` / 移除 `0` / 改动 `0`
- Computer Use 运行时：`365.9 MB → 365.9 MB`，`+0 MB, +0%`
- `cua_node` 新增依赖包：`0`
- 上一版与最新版 SHA-256：`aa5ffa86f0f629e8b02a7ae08f43cdaa87fca7e176b5b9b7e672c1c21a9c1963`

### Windows x64

- 版本：`26.611.8273.0` → `26.611.8604.0`
- Bundle 文件数：`11943 → 11943`
- Bundle 变化：新增 `0` / 移除 `0` / 内容改动 `16`
- `resources/app.asar`：`157.6 MB → 157.6 MB`，变化 `+0.00 MB`
- `app.asar` 内部文件：`3024 → 3024`
- `app.asar` 变化：新增 `313` / 移除 `313` / 改动 `12`
- Computer Use 运行时：`285.6 MB → 285.6 MB`，`+0 MB, +0%`
- `cua_node` 新增依赖包：`0`
- 上一版 SHA-256：`32cf55c6d9a656eb18066890bac655b77e277bb7be84b942b223d27e86631d21`
- 最新版 SHA-256：`37f4d4e5ec263797b2b0d05a26e900f21aa1942e40ada380c19e8982dc7018c1`

## ⚖️ 边界与声明

这是一份第三方逆向变更日志，不代表 OpenAI 官方发布说明。

本报告不重分发二进制文件，只基于官方签名分发产物的文件级、清单级与字符串级差异进行归纳。

标为【信号】的内容只表示“包内出现了相关方向的迹象”，不构成源码事实，也不保证一定对应用户可见功能。欢迎基于可复现证据指正。


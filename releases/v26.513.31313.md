---
title: "Codex 桌面版 26.513.31313"
version: "26.513.31313"
build: 2867
previous_version: "26.513.20950"
previous_build: 2816
platform: "macOS arm64"
released: "2026-05-16"
compared_from: "2026-05-14"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "本版主要是 Computer Use 组件的小幅更新，同时前端出现置顶 / 持久化状态相关的新模块信号。"
official_release_notes: false
---

# Codex 桌面版 `26.513.31313`(build 2867)

> 第三方变更日志 · 非官方 · 基于官方更新包逆向差异

这个版本从产物层面看是一次较小的更新：核心变化集中在 Computer Use(让模型操作桌面 / 浏览器的能力) 插件版本提升，以及若干前端模块释放出的置顶与持久化状态方向信号。  
English one-liner: a small desktop update centered on Computer Use component bumps and early UI-state signals.

## 关于这份报告

这不是官方变更日志，而是一份**基于官方分发产物的逆向差异报告**。本次是历史版本回填：两个版本均已超出官方 Sparkle(应用自动更新框架) 增量窗口，因此采用上一版与最新版两个官方全量包直接比较，而不是增量包重建。

对比范围是 `26.513.20950` build `2816`，发布于 `2026-05-14`，到 `26.513.31313` build `2867`，发布于 `2026-05-16`，平台为 macOS arm64(苹果芯片架构)。

可信度方面，两个制品的信任根来自 Apple(苹果公司) 公证与 OpenAI Developer ID(开发者身份签名)。上一版与最新版的公证验证结果均为 `accepted`，来源均为 `Notarized Developer ID`，签名来源均显示为 `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`。

本文使用两档证据等级：

- **【实证】**：来自人类可读的直接证据，例如 Info.plist(应用元信息文件)、插件配置 diff(差异)、签名与公证结论。
- **【信号】**：来自间接证据，例如前端模块名或二进制体积变化；这类证据只说明“指向某方向”，但无法从产物还原到源码级意图。

## ✨ 重点变化

### 1. Computer Use 插件版本从 `1.0.791` 提升到 `1.0.793` 【实证】

`computer-use` 插件的 `.codex-plugin/plugin.json` 中，版本号从 `1.0.791` 更新为 `1.0.793`。

这意味着桌面控制相关插件确实被更新过；从事实包可确认的是插件版本提升，但不能进一步断定具体行为、能力或交互发生了哪些源码级变化。

关键证据：

```diff
-  "version": "1.0.791",
+  "version": "1.0.793",
```

### 2. Computer Use 相关二进制体积小幅增加 【信号】

体积变化最大的几个文件都集中在 Computer Use 相关组件：`CUALockScreenGuardian` 增加 `+0.17 MB`，`SkyComputerUseService` 增加 `+0.14 MB`，`SkyComputerUseClient` 增加 `+0.08 MB`。

这与插件版本提升方向一致，指向 Computer Use 底层服务、客户端或锁屏守护相关逻辑有更新；但体积变化只能说明二进制内容变了，无法从产物还原到源码级意图，也不能直接推导出新增用户功能。

关键证据：

| 组件 | 变化 |
| --- | ---: |
| `CUALockScreenGuardian` | `+0.17 MB` |
| `SkyComputerUseService` | `+0.14 MB` |
| `SkyComputerUseClient` | `+0.08 MB` |

### 3. 前端出现置顶相关模块 `pin` / `pin-filled` 【信号】

去掉 Vite(前端构建工具) hash(哈希) 文件名噪音后，前端真实新增模块包含 `pin` 与 `pin-filled`。

这指向界面中可能存在“置顶 / 固定”一类视觉或交互元素的准备，但模块出现不等于功能已经对用户开放，也无法从产物还原到源码级意图。

关键证据：

```text
新增模块: persisted-signal, pin, pin-filled
```

### 4. 前端状态管理出现 `persisted-signal`，并移除 `settings-route-state` 【信号】

前端真实新增模块包含 `persisted-signal`，同时移除了 `settings-route-state`。

这指向前端状态保存或路由状态处理方式可能发生调整；但这只是模块名级别的间接证据，无法确认具体用户路径、设置页行为或源码级设计意图。

关键证据：

```text
新增模块: persisted-signal
移除模块: settings-route-state
```

### 5. 应用版本与 asar 完整性哈希更新 【实证】

Info.plist 中版本号从 `26.513.20950` 更新到 `26.513.31313`，build 从 `2816` 更新到 `2867`，同时 `app.asar` 的完整性 hash 发生变化。

这说明应用主包内容确实更新，但 `app.asar` 内部大量新增 / 移除主要来自 `webview/assets` 下的构建 hash 重命名，不应解读为上千个真实功能改动。

关键证据：

```text
CFBundleShortVersionString: 26.513.20950 → 26.513.31313
CFBundleVersion: 2816 → 2867
ElectronAsarIntegrity hash: fd8467...e84e → 8242...509
```

## 📊 原始数据

### Bundle 文件级统计

| 指标 | 数值 |
| --- | ---: |
| 文件数 | `1331 → 1331` |
| 新增 | `0` |
| 移除 | `0` |
| 内容改动 | `96` |

### 关键组件体积变化

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/plugins/openai-bundled/plugins/computer-use/Codex Computer Use.app/Contents/SharedSupport/CUALockScreenGuardian.app/Contents/MacOS/CUALockScreenGuardian` | 14.7 MB | 14.9 MB | +0.17 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/computer-use/Codex Computer Use.app/Contents/MacOS/SkyComputerUseService` | 15.0 MB | 15.1 MB | +0.14 MB |
| `Contents/Resources/plugins/openai-bundled/plugins/computer-use/Codex Computer Use.app/Contents/SharedSupport/SkyComputerUseClient.app/Contents/MacOS/SkyComputerUseClient` | 10.1 MB | 10.2 MB | +0.08 MB |
| `Contents/Resources/app.asar.unpacked/node_modules/better-sqlite3/build/Release/better_sqlite3.node` | 1.9 MB | 2.0 MB | +0.03 MB |
| `Contents/Resources/node_repl` | 15.0 MB | 15.1 MB | +0.01 MB |
| `Contents/Resources/app.asar` | 147.7 MB | 147.7 MB | +0.01 MB |

### 新增依赖清单

`cua_node` 新增依赖：`0` 个。

## 🔁 可复现信息

| 制品 | SHA-256 | 验证 / 结论 |
| --- | --- | --- |
| 上一版全量包 `Codex-darwin-arm64-26.513.20950.zip` | `cd29516a850973879115b7b4f2a4bfa32a9a05b7d6d84a638f78656c4980e82c` | 公证验证 `accepted`；`source=Notarized Developer ID`；`origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| 最新版全量包 `Codex-darwin-arm64-26.513.31313.zip` | `6001ed876cc8b62e1ae41b7c9e1246c31e7bd13eb11aa5ba7ff163bd9ef88c80` | 公证验证 `accepted`；`source=Notarized Developer ID`；`origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ 边界与声明

本文与 OpenAI 无关，不代表官方说明或官方 release notes(发布说明)。

本文不重分发任何官方二进制，只发布基于官方签名分发产物得到的差异结论。

所有【信号】条目都只表示产物层面的方向性线索，不构成源码级事实，也不等同于确认用户可见功能已经上线。

如有遗漏或误读，欢迎基于同一事实包指正。
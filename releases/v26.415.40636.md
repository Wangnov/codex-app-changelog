---
title: "Codex 桌面版 26.415.40636"
version: "26.415.40636"
build: 1799
previous_version: "26.415.32059"
previous_build: 1789
platform: "macOS arm64"
released: "2026-04-18"
compared_from: "2026-04-18"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "本次历史回填显示版本号与 app.asar 完整性哈希更新，但 bundle 文件树、关键体积和 cua_node 新增依赖面保持稳定。"
official_release_notes: false
---

# Codex 桌面版 `26.415.40636` build 1799

> 第三方变更日志 · 非官方 · 基于官方更新包逆向差异

这版更像是一次小范围应用层刷新：可确认的公开变化集中在版本号、build 号和 `app.asar` 完整性哈希，事实包没有显示新增依赖或新的 bundle 级组件。  
English one-liner: A narrow app-layer refresh with no confirmed expansion in bundle structure or native dependency surface.

## 关于这份报告

这不是官方变更日志，而是一份**基于官方分发产物的逆向差异报告**。本次是历史回填：官方 appcast 未对 `26.415.32059` → `26.415.40636` 这一对版本提供增量包，因此改为下载两个官方全量包并做结构化 bundle 差异。

可信度分为两档：**【实证】**表示来自 Info.plist、依赖清单、签名 / 公证结果等人类可读或结构化直接证据；**【信号】**表示只能从体积、哈希、文件 churn 等间接线索判断，指向某方向，但无法从产物还原到源码级意图。

对比范围为 macOS arm64：上一版 `26.415.32059` build `1789`，最新版 `26.415.40636` build `1799`，两者发布日期均为 `2026-04-18`。

## ✨ 重点变化

### 1. 版本与 build 前进到 `26.415.40636` / `1799` 【实证】

这是一个可安装 bundle 层面的版本更新：`CFBundleShortVersionString` 从 `26.415.32059` 变为 `26.415.40636`，`CFBundleVersion` 从 `1789` 变为 `1799`。

这意味着 macOS、自动更新链路和诊断信息会识别为新的桌面端 build；事实包没有提供官方 release notes。

```diff
/CFBundleShortVersionString: '26.415.32059' → '26.415.40636'
/CFBundleVersion: '1789' → '1799'
```

### 2. Bundle 外层结构没有扩张，未观察到新增本地依赖 【实证】

文件树保持 `498 → 498`，新增 `0`、移除 `0`，内容改动 `40`；`cua_node` 新增依赖为 `0` 个。

这说明在 bundle 外层和本地依赖面上，没有证据显示新增插件、外置组件或新的本地包能力。可感知变化更可能发生在既有应用载荷内部，而不是新增一组可独立识别的 macOS 组件。

关键证据：

```text
文件数: 498 → 498
新增 0 / 移除 0 / 内容改动 40
cua_node 新增 0 个包
```

### 3. `app.asar` 内容确有更新，但语义变化只能作为低层信号 【信号】

`ElectronAsarIntegrity` 中的 `app.asar` 哈希发生变化，说明应用层载荷内容被更新；同时关键体积表显示 `Contents/Resources/app.asar` 仍为 `111.5 MB → 111.5 MB`，变化为 `+0.00 MB`。

这指向一次小体积或等体积的应用层内容刷新，但事实包没有给出新增可读字符串、服务入口、技能文档、默认提示、命令、设置、权限或用户可见文案，因此无法从产物还原到源码级意图。

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
'7aa7db298d9b72c91594737c441e2273f6b057b2f9d15d30a6d2f6121434dd3c'
→
'6b92163fce92a3896545d37a0a06abf4654c9bfbb46ed6ba0fdabac775f423a2'
```

## 📊 原始数据

**Bundle 文件级统计**

| 指标 | 上一版 | 最新版 | 变化 |
| --- | ---: | ---: | ---: |
| Bundle 文件数 | 498 | 498 | 0 |
| 新增文件 | 0 | 0 | 0 |
| 移除文件 | 0 | 0 | 0 |
| 内容改动文件 | 0 | 40 | +40 |

**关键组件体积变化表**

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 111.5 MB | 111.5 MB | +0.00 MB |

**新增依赖清单**

| 范围 | 新增包数量 | 明细 |
| --- | ---: | --- |
| `cua_node` | 0 | 无新增包 |

## 🔁 可复现信息

| 制品 | 下载地址 | SHA-256 | 大小 | EdDSA 验签结论 | codesign / spctl 公证结论 |
| --- | --- | --- | --- | --- | --- |
| 上一版 `26.415.32059` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.415.32059.zip` | `8d2cdf5a922f0820acc1903c2b4006c1cb2ffdeed0d53bdf0607ec4b3183bd97` | 事实包未提供 | 事实包未提供 | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| 最新版 `26.415.40636` | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.415.40636.zip` | `d382ab435b981d508f488167859dedab70601366ffa9c85050e81943908b5e9e` | 事实包未提供 | 事实包未提供 | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |

## ⚖️ 边界与声明

本报告与 OpenAI 无关，非官方，不代表 OpenAI 的发布说明或产品承诺。

本文不重分发任何官方二进制，只发布基于官方签名分发产物得到的差异结论。

所有【信号】条目只表示产物层线索指向某方向，不构成源码级事实。若后续出现官方 release notes 或更完整的可复验材料，应以新的直接证据为准。欢迎基于可复现材料指正。


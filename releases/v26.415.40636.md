---
title: "Codex 桌面版 26.415.40636"
version: "26.415.40636"
build: 1799
previous_version: "26.415.32059"
previous_build: 1789
platform: "macOS arm64"
released: "2026-04-18"
compared_from: "2026-04-18"
method: "官方全量包直接比较 + 结构化 bundle 差异"
summary: "这是一个非常小的同日构建更新，除版本号、构建号和 app.asar 完整性哈希外，未观察到可归纳为用户或开发者可感知功能变化的直接证据。"
official_release_notes: false
---

# Codex 桌面版 `26.415.40636`(build 1799)

> 第三方变更日志 · 非官方 · 基于官方更新包逆向差异

本版是从 `26.415.32059` 到 `26.415.40636` 的同日小幅构建更新：文件数量保持不变，未发现新增依赖、权限面或可明确归纳的新功能证据。  
English one-liner: a small same-day build update with no clear user-facing feature delta in the extracted facts.

## 关于这份报告

这不是官方变更日志，而是一份**基于官方分发产物的逆向差异报告**。本次事实包明确说明：由于两个版本均超出官方 Sparkle(应用自动更新框架) 增量窗口，本次不是通过增量包重建，而是下载两个官方全量包后直接比较，并生成结构化 bundle(应用包目录) 差异。

可信度方面，本次信任根来自 Apple(苹果公司) 公证与 OpenAI Developer ID(开发者身份签名) 代码签名验证。上一版与最新版的 `Codex.app` 均通过 `spctl`(macOS 安全评估工具) 公证验证，结果为 `accepted`，来源为 `Notarized Developer ID`，签名来源为 `Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)`。

证据等级分为两档：

- **【实证】**：来自人类可读的直接证据，例如 `Info.plist`(应用元信息文件) 键值、签名验证结果、文件级统计、依赖清单。
- **【信号】**：仅来自间接证据，例如前端模块名或二进制体积变化；这类证据只能说明“指向某方向，但无法从产物还原到源码级意图”。

对比范围：`26.415.32059` build `1789`，发布于 `2026-04-18`；对比到 `26.415.40636` build `1799`，发布于 `2026-04-18`。平台为 `macOS arm64`。

## ✨ 重点变化

### 1. 版本号与构建号推进到 `26.415.40636` / build `1799` 【实证】

最新版将 `CFBundleShortVersionString` 从 `26.415.32059` 更新到 `26.415.40636`，将 `CFBundleVersion` 从 `1789` 更新到 `1799`。

这说明这是一个新的官方签名构建，但仅凭事实包中可读证据，无法进一步归纳出新的用户功能、权限变化或开发者接口变化。

关键证据：

```diff
/CFBundleShortVersionString: '26.415.32059' → '26.415.40636'
/CFBundleVersion: '1789' → '1799'
```

### 2. 应用主体内容有少量改动，但没有可识别的新能力证据 【实证】

bundle 文件总数保持 `498 → 498`，没有新增或移除文件，内容改动为 `40` 个文件。应用层 `app.asar`(Electron 应用资源包) 内部文件数保持 `1000 → 1000`，机器统计显示新增 `1`、移除 `1`、改动 `5`。

这类变化说明应用主体确实发生了内容更新；但事实包没有提供新增模块名、依赖、接口、权限或文档层面的直接证据，因此不应把它写成某个具体功能上线。

关键证据：

```text
Bundle 文件数: 498 → 498
新增 0 / 移除 0 / 内容改动 40

app.asar 内部文件: 1000 → 1000
新增 1 / 移除 1 / 改动 5
```

### 3. `app.asar` 完整性哈希更新，符合内容变更后的校验结果 【实证】

`ElectronAsarIntegrity`(Electron 资源完整性校验) 中记录的 `app.asar` 哈希发生变化。

这意味着应用资源包内容与上一版不同；但哈希本身只证明内容不同，不说明变化的产品意图或用户可见行为。

关键证据：

```diff
/ElectronAsarIntegrity/Resources/app.asar/hash:
'7aa7db298d9b72c91594737c441e2273f6b057b2f9d15d30a6d2f6121434dd3c'
→
'6b92163fce92a3896545d37a0a06abf4654c9bfbb46ed6ba0fdabac775f423a2'
```

### 4. 未发现新增依赖或能力面扩展 【实证】

`cua_node` 依赖变化显示新增包数量为 `0`。事实包也没有列出体积最大的新增文件。

这意味着至少从本次结构化依赖清单看，没有新增可直接指向 PDF(可移植文档格式)、OCR(光学字符识别)、图像处理、浏览器自动化等能力扩展的依赖证据。

关键证据：

```text
cua_node 新增 0 个包
体积最大的新增文件: 无节选内容
```

## 📊 原始数据

### Bundle 文件级统计

| 指标 | 上一版 | 最新版 | 变化 |
| --- | ---: | ---: | ---: |
| 文件数 | 498 | 498 | 0 |
| 新增文件 | 0 | 0 | 0 |
| 移除文件 | 0 | 0 | 0 |
| 内容改动 | - | - | 40 |

### 关键组件体积变化

| 组件路径 | 旧 | 新 | 变化 |
| --- | ---: | ---: | ---: |
| `Contents/Resources/app.asar` | 111.5 MB | 111.5 MB | +0.00 MB |

### 新增依赖清单

| 依赖来源 | 新增数量 | 说明 |
| --- | ---: | --- |
| `cua_node` | 0 | 未发现新增包 |

## 🔁 可复现信息

| 制品 / 验证项 | 结果 |
| --- | --- |
| 上一版全量包 | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.415.32059.zip` |
| 上一版 SHA-256 | `8d2cdf5a922f0820acc1903c2b4006c1cb2ffdeed0d53bdf0607ec4b3183bd97` |
| 最新版全量包 | `https://persistent.oaistatic.com/codex-app-prod/Codex-darwin-arm64-26.415.40636.zip` |
| 最新版 SHA-256 | `d382ab435b981d508f488167859dedab70601366ffa9c85050e81943908b5e9e` |
| EdDSA(爱德华兹曲线数字签名算法) 验签 | 不适用；事实包说明本次为历史回填，改为两个官方全量包直接比较 |
| 上一版公证验证 | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| 最新版公证验证 | `accepted`; `source=Notarized Developer ID`; `origin=Developer ID Application: OpenAI OpCo, LLC (2DC432GLL2)` |
| codesign(代码签名工具) 结论 | 信任根为 OpenAI Developer ID 代码签名 |
| spctl 公证结论 | 上一版与最新版均通过，结果为 `accepted` |

## ⚖️ 边界与声明

本报告与 OpenAI 无关，不代表官方立场，也不是官方 release notes(发布说明)。

本报告不重分发任何官方二进制文件，只发布基于官方签名分发产物得到的差异结论。

事实包中已明确提示：`webview/assets` 与 `.vite/build` 下带 hash(哈希值) 的文件名增减属于构建产物重命名噪音，本报告没有把这些变化写成新增或删除功能。

所有【信号】条目都不构成源码级事实；本次报告中未使用【信号】级条目归纳具体功能变化。欢迎基于同一事实包或更完整的可读证据指正。